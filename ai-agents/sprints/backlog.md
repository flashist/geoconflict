# Backlog — the default home for unsprinted task briefs

This is **not a sprint**. It is the board every task brief lands on when no sprint was named for it.

The filename is deliberately `backlog.md` and **not** `sprint-backlog.md`: `/fkit-status` finds the
active sprint by globbing `sprint-*.md`, and a board of explicitly unscheduled work must stay outside
that glob or it can be reported as the active sprint.

> ⚠️ **A second, older board exists** at [`sprint-backlog.md`](sprint-backlog.md) — ~23 entries of
> unsprinted work, and its filename *is* inside the glob. Task `0001` consolidates the two boards and
> retires it. Until that ships, **both files must be read** to see all unsprinted work.

The **Priority** column reads `—` for every row: this board is unranked by design. Needing a rank is
the signal to pull the task into a sprint.

## Status

| Status | Priority | Task | Brief |
|---|---|---|---|
| ✅ Done (agent-closed — not owner-verified) | — | Migrate `tasks/backlog/` to the fkit task-folder convention | [`0002-migrate-backlog-tasks-to-folders`](../tasks/done/0002-migrate-backlog-tasks-to-folders/brief.md) |
| ✅ Done (agent-closed — not owner-verified) | — | Migrate `tasks/done/` and `tasks/cancelled/` to the fkit task-folder convention | [`0003-migrate-done-cancelled-tasks-to-folders`](../tasks/done/0003-migrate-done-cancelled-tasks-to-folders/brief.md) |
| 🔲 Backlog | — | Consolidate unsprinted work onto `backlog.md` and retire `sprint-backlog.md` | [`0001-consolidate-unsprinted-work-onto-backlog-board`](../tasks/backlog/0001-consolidate-unsprinted-work-onto-backlog-board/brief.md) |
| 🔲 Backlog | — | Reconcile legacy status markers in the sprint plans to the canonical vocabulary | [`0004-reconcile-legacy-status-markers-in-sprint-plans`](../tasks/backlog/0004-reconcile-legacy-status-markers-in-sprint-plans/brief.md) |
| 🔲 Backlog | — | `ADMIN_TOKEN`: fail closed on a missing secret, and compare in constant time *(prod verified clean 2026-08-10 — normal-priority hardening, not an incident)* | [`0005-admin-token-fail-closed-and-constant-time-compare`](../tasks/backlog/0005-admin-token-fail-closed-and-constant-time-compare/brief.md) |
| 🔲 Backlog | — | Investigation: `SLOW_TURN_THRESHOLD_MS` vs the 66.7 ms turn interval | [`0006-investigate-slow-turn-threshold-vs-turn-interval`](../tasks/backlog/0006-investigate-slow-turn-threshold-vs-turn-interval/brief.md) |
| 🔲 Backlog | — | Investigation: blast radius of the `src/core` → `src/client` imports | [`0007-investigate-core-to-client-import-coupling`](../tasks/backlog/0007-investigate-core-to-client-import-coupling/brief.md) |
| 🚧 Blocked — needs 0009 findings **and** the first paid entitlement (by design) | — | Migrate `PrivilegeRefresher` to fail-closed | [`0008-privilege-refresher-fail-closed-migration`](../tasks/backlog/0008-privilege-refresher-fail-closed-migration/brief.md) |
| 🔲 Backlog | — | Self-host the upstream OpenFront API dependency (findings phase) — **pull ahead of any cosmetics monetization work** | [`0009-self-host-upstream-openfront-api-dependency`](../tasks/backlog/0009-self-host-upstream-openfront-api-dependency/brief.md) |
| 🚧 Blocked — 0009 findings + payment infra + owner decision on the design set | — | Re-enable flags as a paid non-country cosmetic (Task 9) | [`0010-re-enable-flags-paid-non-country-cosmetic`](../tasks/backlog/0010-re-enable-flags-paid-non-country-cosmetic/brief.md) |
| 🚧 Blocked — 0009 findings + payment infra + owner rulings (pattern set, ad coupling) | — | Re-enable territory patterns (Task 9a) | [`0011-re-enable-territory-patterns`](../tasks/backlog/0011-re-enable-territory-patterns/brief.md) |
| 🔲 Backlog | — | Reconcile `## Sprint` field values in task briefs to a parseable form | [`0050-reconcile-sprint-field-values`](../tasks/backlog/0050-reconcile-sprint-field-values/brief.md) |
| 🔲 Backlog | — | Sweep the bare legacy task identities left behind by the `0002` migration | [`0051-sweep-bare-legacy-task-identities`](../tasks/backlog/0051-sweep-bare-legacy-task-identities/brief.md) |
| 🔲 Backlog | — | Wiki vault follow-up: legacy task filenames left stale by the folder migrations | [`0052-wiki-vault-legacy-filename-follow-up`](../tasks/backlog/0052-wiki-vault-legacy-filename-follow-up/brief.md) |
| 🔲 Backlog | — | Track the upstream `PLAN_SPRINT` resolution defect in fkit's `dashboard.sh` until a release fixes it *(handed to the fkit maintainer 2026-08-10 — not our fix to make)* | [`0053-fix-plan-sprint-name-resolution-in-dashboard`](../tasks/backlog/0053-fix-plan-sprint-name-resolution-in-dashboard/brief.md) |
| ➡️ Moved | — | Investigation: public-game routing can send games to a dead or unready worker → **Sprint 4** *(promoted 2026-08-22, hours after filing, when the owner ruled it runs before `0056`; tracked on [`plan-sprint-4.md`](plan-sprint-4.md) from here on)* | [`0057-investigate-worker-routing-to-dead-or-unready-workers`](../tasks/done/0057-investigate-worker-routing-to-dead-or-unready-workers/brief.md) |
| 🔲 Backlog | — | `Worker.ts`: a failed port bind leaves a silently hung worker — add `server.on("error")` *(latent defect; explicitly refuted as the 2026-08-22 cause)* | [`0058-worker-server-on-error-handler`](../tasks/backlog/0058-worker-server-on-error-handler/brief.md) |
| 🔲 Backlog | — | Precompile the server at image-build time instead of running `ts-node/esm` in production *(leading — unproven — hypothesis for **why** the worker died on 2026-08-22. **Owner-ruled 2026-08-23: stays here; the outage-track pause covers it.** Reasoning and a "re-raise only if" condition are recorded in the brief)* | [`0059-precompile-server-for-prod-instead-of-ts-node`](../tasks/backlog/0059-precompile-server-for-prod-instead-of-ts-node/brief.md) |
| 🔲 Backlog | — | Investigation: prod Telegram feedback delivery fails with `TypeError: fetch failed` *(§9 follow-up — the player feedback channel is **dead in production and fails silently**. ⚠️ The recorded "likely needs `TELEGRAM_PROXY_URL`" diagnosis is **disproven**: the proxy is wired (`Master.ts:217-218,319`) and forwarded (`deploy.sh:308`), so this is an investigation, not a known fix. Strongest promotion candidate on this board)* | [`0061-investigate-prod-telegram-feedback-delivery-failure`](../tasks/backlog/0061-investigate-prod-telegram-feedback-delivery-failure/brief.md) |
| ➡️ Moved | — | `PROFILE_INTERNAL_TOKEN` is never forwarded to production — the profile client silently no-ops → **Sprint 4** *(filed here 2026-08-23 with the blocking claim flagged unverified; **verified the same day — it blocks `0017` and `0018`** — and promoted. Tracked on [`plan-sprint-4.md`](plan-sprint-4.md) from here on)* | [`0062-forward-profile-internal-token-in-deploy`](../tasks/backlog/0062-forward-profile-internal-token-in-deploy/brief.md) |
| 🔲 Backlog | — | Product decision: auth strategy — JWT/Discord auth service vs Yandex-only identity *(from `0063`'s reframe: no auth service exists on this origin — no login routes, no JWT signing, JWKS URL serves the SPA HTML — so the Discord/token login UI is dead code. Decide: build an auth service someday, or commit to Yandex-only and remove the surface. Decision task → ADR)* | [`0069-auth-strategy-jwt-discord-vs-yandex-only`](../tasks/backlog/0069-auth-strategy-jwt-discord-vs-yandex-only/brief.md) |
| 🔲 Backlog | — | `TokenLoginModal` silent failure — restore user-facing error or remove the dead login UI *(from `0063`'s reframe; `TokenLoginModal.ts:73` alert commented out, failures silent. Restore-vs-remove follows the `0069` ruling)* | [`0070-token-login-modal-silent-failure`](../tasks/backlog/0070-token-login-modal-silent-failure/brief.md) |
| 🔲 Backlog | — | Map label density tuning at mid-zoom *(owner judged `0041`'s shipped labels too cluttered at mid-zoom — the tuning pass `0041`'s clutter flag anticipated; `src/client/` only, owner eyeball is the gate)* | [`0071-map-label-density-tuning-mid-zoom`](../tasks/backlog/0071-map-label-density-tuning-mid-zoom/brief.md) |
| ⛔ Cancelled (agent-closed — not owner-verified) (2026-08-24) — duplicate of 0064 (pre-existing, owner-ruled 2026-08-23, better-scoped); useful specifics merged into 0064 — owner-ruled 2026-08-24 | — | Deploy-time config guard — required env vars forwarded and well-formed | [`0072-deploy-time-config-guard`](../tasks/cancelled/0072-deploy-time-config-guard/brief.md) |
| 🔲 Backlog | — | Remove inert upstream HTML leftovers *(audit §H3 — commented `og:`/googletag/Publift fragments + dead Steam link; deletion-only; footer mentions stay per the `0066` ruling)* | [`0073-remove-inert-upstream-html-leftovers`](../tasks/backlog/0073-remove-inert-upstream-html-leftovers/brief.md) |
| 🔲 Backlog | — | Product decision: a private-lobby invite shared from the Yandex build takes the recipient OUTSIDE the portal iframe — portal-relative vs standalone invite *(from `0198`, which fixed the **path** on that same line and deliberately left the **host** question open. Research what Yandex supports for portal deep-linking + measure the off-portal session, then owner ruling → ADR. Also settles the `location.search` residual. Decision task; producer's rank, not an owner ruling)* | [`0199-yandex-invite-link-leaves-portal-iframe`](../tasks/backlog/0199-yandex-invite-link-leaves-portal-iframe/brief.md) |
| ➡️ Moved to [Sprint 4](plan-sprint-4.md) | — | Confirm and fix the `supertest` profile-server flake — 9 failures in 170 runs → **Sprint 4** *(filed here 2026-08-29 as the default home for an unsprinted brief and **promoted the same day, owner-ruled** via `AskUserQuestion` in the lead session, on the producer's recommendation — priority **Medium** was the producer's rank and the owner **confirmed rather than disturbed** it. Split out of `0197` under owner amendment **A4**. Promoted on the grounds that `0197`'s segfault is an **upstream V8 bug with no repository-side fix** while this one is in **our own test code at roughly ten times the rate**. Tracked on [`plan-sprint-4.md`](plan-sprint-4.md) from here on)* | [`0200-supertest-profile-server-flake-confirm-and-fix`](../tasks/done/0200-supertest-profile-server-flake-confirm-and-fix/brief.md) |
| ➡️ Moved to [Sprint 4](plan-sprint-4.md) | — | Nothing runs the shell test harnesses — decide how to gate them so they cannot rot unnoticed *(➡️ **PROMOTED INTO SPRINT 4 on 2026-09-04, on an owner ruling given live in session — this row is KEPT as a pointer, not deleted, and is NOT flipped to Done.** The ruling: **all phases P0–P7 of the profile backend rebuild go into Sprint 4**, and this task **is P7** — Phase 2 of epic [`0213`](../tasks/backlog/0213-profile-backend-clean-slate-rebuild/brief.md). ⚠️ **The promotion changed the BOARD PLACEMENT AND NOTHING ELSE** — the brief's status stays `🔄 In progress` (started 2026-09-02), the already-ruled Phase 1 decision stands, the open step-6 question stays open, and **rank was NOT ruled**: it remains the producer's `Medium`, and this board's `—` is the unranked-board convention, not an owner rank. 🚨 **On merit this is the highest leverage per hour on the whole profile list — 2–4 h, and startable today**: it needs no box, no CI, no Docker daemon and no DB, while P0 and P1–P6 wait on an inspection of the existing box. 📌 **Corrected 2026-09-04 on the same day's material reframe:** an earlier version of this row called it *"the ONLY one of the eight phases startable today"* — that was written when the epic was scoped as a procurement. **A profile VPS and S3 bucket DO exist and are reused in place**, so `0216` (P1-spike) is **also** startable now. **Two, not one.** 📌 **CORRECTED 2026-09-04: there are FOUR ungated harnesses, not two** — `tests/profile-backup-dryrun.sh` and `tests/profile-backup-redeploy.sh` join the two already listed; re-verified by the producer (`grep` over `package.json` and `jest.config.ts` returns nothing for either). **Step 6's in-or-out call now covers four files.** 📌 **`0202` was NOT promoted and stays on this board**; the `0201` → `0202` sequencing ruled 2026-09-02 is unaffected. **Full record on the [Sprint 4](plan-sprint-4.md) row.** Original filing follows.* — *(from `0195`: `tests/scripts/profile-deploy-hardening.test.sh` was broken for ~2 months because no npm script, no `npm test` reach (`jest.config.ts:47` matches TS only), no git hook and **no CI at all** ever runs it. 🔄 **Started 2026-09-02**, driven from the lead session. ✅ **THE PLAN-GATE DECISION IS RULED — owner ruling 2026-09-02, given live in session: FOLD THE SHELL HARNESSES INTO `npm test`** (the brief's candidate 2). The other three candidates are **rejected**: own npm script, pre-push hook, and **CI explicitly — the owner is not introducing a CI platform for this**. Phase 1 is therefore closed before the task starts; go straight to Phase 2 and implement only what was ruled. ⚠️ Candidate 2's cost still applies and is not waived: the harness runtime must be **measured** first (verification step 1), and the in-or-out call on `scripts/test-check-docker-secret-boundary.sh` — which requires Docker — is still an open item in the brief. **Sequencing: `0201` goes FIRST** — see the `0202` row. Priority remains the producer's rank, not an owner ruling)* | [`0201-gate-the-shell-test-harnesses-so-they-cannot-rot-unrun`](../tasks/backlog/0201-gate-the-shell-test-harnesses-so-they-cannot-rot-unrun/brief.md) |
| 🔲 Backlog | — | Deploy harness prints ✅ for assertions an early abort satisfies vacuously — close the false-green class *(13 vacuous ✅ lines across T1/T2/T3/T5/T6/T7/T9, verified on a clean worktree at `f2b9422`. ⚠️ **The filing claim did not fully hold** — T3/T6/T7/T9 already carry distinguishing-message assertions and those are what caught the breakage; no block was fully false-green, so the defect is **latent, not realized**, and the `0196` parallel is weaker than filed. Priority is **`Low` — an owner ruling of 2026-09-01**, superseding the producer's earlier Low–Medium rank; the task **stands and was NOT cancelled**, and this Backlog board placement is **owner-confirmed** the same day. 📌 **Ordering RAISED AND DECIDED 2026-09-02, owner ruling given live in session: `0201` goes FIRST, `0202` second, and `0202` stays `Low`.** Both briefs record a *preference* for `0202` first (a gate is worth more over assertions that mean what they say); the owner ruled against it and the `Low` rank stands. **Recorded so neither point is re-raised** — do not re-litigate either)* | [`0202-shell-harness-absence-assertions-pass-vacuously`](../tasks/backlog/0202-shell-harness-absence-assertions-pass-vacuously/brief.md) |
| 🔲 Backlog | — | Streamer Program: Verified Streamer Status & In-Game Customization *(**row added 2026-09-02 on an owner ruling given live in session** — Ruling A, taking the producer's recommendation. The brief existed since filing but appeared in **no sprint file at all** — board-invisible, the same class as `0022`. This row makes it visible; it changes nothing about the brief. Its own `## Sprint` field already reads *"Unscheduled — parking lot. Return to this when citizenship + name rendering (Task 8) are live"* (`brief.md:6-7`), which is exactly this board, so no promotion question arises. Origin: one Twitch streamer asked for recognition; the owner declined for now and wanted the idea captured. Brief priority `Low`, which this unranked board does not carry — the rank lives in the brief. **Row appended, not inserted** (ADR-035))* | [`0023-streamer-program`](../tasks/backlog/0023-streamer-program/brief.md) |
| 🔲 Backlog | — | UX: Faster Access to Quick Messages *(**row added 2026-09-02 on an owner ruling given live in session** — Ruling A, taking the producer's recommendation. The brief existed since filing but appeared in **no sprint file at all** — board-invisible, the same class as `0022`. Player-reported friction: reaching a quick message during combat takes four steps — tap player → chat icon → category → phrase — so a call for troops or gold arrives after the player has already been captured. 📌 **The brief had NO `## Sprint` section at all** (it went `## ID` → `## Priority`, with the "no sprint assigned" text buried inside the Priority line); **the heading was created in the same pass on owner Ruling B, with the value `Backlog — unscheduled`.** That is the only brief change; Priority, Status and Owner are untouched. Brief priority `Low / Future`, which this unranked board does not carry — the rank lives in the brief. **Row appended, not inserted** (ADR-035))* | [`0024-ux-quick-message-access`](../tasks/backlog/0024-ux-quick-message-access/brief.md) |
| 🔲 Backlog | — | Hint Videos: Content Production (Social Media + In-Game Assets) *(**row added 2026-09-02 on an owner ruling given live in session** — Ruling A, taking the producer's recommendation. The brief existed since filing but appeared in **no sprint file at all** — board-invisible, the same class as `0022`. Its own `## Sprint` field already reads *"Backlog — content production task (Mark), no engineering dependency. Can start any time"* (`brief.md:6-7`), which is exactly this board. Feeds `0029-hint-videos-in-game`, which cannot begin until 3–4 clean assets exist here — a Blocks fact, not a Depends on fact; this task depends on nothing. 🔴 **LIVE THIS WEEK, but deliberately NOT marked `🔄 In progress`** — owner ruling 2026-09-02: this is **content production the owner still owns entirely for Version A** (Russian captions + voice-over), running alongside an agent-assisted capture track, so an in-progress marker on an engineering board would misreport who is doing what. ✅ **Four production decisions ruled 2026-09-02 and recorded in the brief:** (1) **format is MP4, not GIF** — the brief left this open ("decide during production"); **GIF cannot meet both the 1000px width and the under-2MB budget, measured**, so the open choice is closed; (2) **the HUD stays in frame**; (3) **first batch is topics #2, #3, #4, #5** (conquering territories, empty territory, bots vs nations vs players, terrain types); (4) topics **#1, #7, #8** — boats, trade routes, capturing enemy trading ships — are **explicitly DEFERRED to their own spike**, not dropped. A feasibility spike confirmed unattended agent capture works. ⚠️ **Topic #6** (construction price progression) carries no ruling either way — it is in neither the first batch nor the deferred set. Brief priority `Medium`, which this unranked board does not carry — the rank lives in the brief. **Row appended, not inserted** (ADR-035))* | [`0028-content-hint-videos-production`](../tasks/backlog/0028-content-hint-videos-production/brief.md) |
| 🔲 Backlog | — | Defense Post Range: Show Faint Area for Already-Built Posts *(**row added 2026-09-02 on an owner ruling given live in session** — Ruling A, taking the producer's recommendation. The brief existed since filing but appeared in **no sprint file at all** — board-invisible, the same class as `0022`. This row makes it visible; it changes nothing about the brief. Its own `## Sprint` field already reads *"Backlog (unsprinted) — independent client-side feature, no dependency on the citizenship/payments track. Ships to all players"* (`brief.md:6-7`), which is exactly this board. Map-readability polish: a built Defense Post's coverage radius is drawn only while placing the build ghost and vanishes once built. Scope is `src/client/` only, rendering-only, desync-safe — no `src/core/` change. Brief priority `Low–Medium`, which this unranked board does not carry — the rank lives in the brief. **Row appended, not inserted** (ADR-035))* | [`0043-defense-post-range-always-visible`](../tasks/backlog/0043-defense-post-range-always-visible/brief.md) |
| 🔲 Backlog | — | Pre-arming gate for the config parity guard — the ten items that must land before `--enforce` is wired *(**filed 2026-09-02 on an owner ruling given live in session**: the pre-arming gate becomes its own follow-up brief rather than staying inside [`0064`](../tasks/backlog/0064-deploy-time-config-parity-guard/brief.md), because it is bigger than `0064`'s remaining scope implies and keeping it there would turn a shippable unit into a long-running container. Origin: `0064`'s **two-round stateful review, round 2 CONVERGED — verdict ship report-only**. ⚠️ **The reportable outcome was scheduling, not correctness: arming `--enforce` is now 10 gate items, not 2** — R1, R4 and the eight new round-2 findings R12–R21. **Hard sequencing: all ten land BEFORE `--enforce` is wired**; the wiring itself stays in `0064` (ruling R3's second half). 📌 **Two items already carry owner rulings — do not re-decide:** **R1 (HIGH, the client blind spot — `src/core/configuration/**` maps to the game pipeline only, yet the browser bundle reads it, so a broken client supply channel prints green; reproduced by deleting the `STRIPE_PUBLISHABLE_KEY` DefinePlugin entry → still `REQUIRED 0`, `--enforce` exit 0) has its FIX METHOD ruled by disposition D2** — classify those reads against **both** the deploy heredoc and webpack `DefinePlugin` — and D1/Q1 ruled **when** (before arming, not before the report-only ship). 🚩 **R4 is explicitly UNDISPOSITIONED — the owner declined to rule (D7)** and it needs a decision before arming: should a new unmapped `src/` directory hard-fail a deploy, and should a missing input fail closed while a missing checker silently skips? 📌 **Lead item among the eight new ones is R12 (medium): `export KEY=` matches neither heredoc read pattern** (column 0 or indented, lowercase too) — reproduced independently twice as **total silence, `REQUIRED 0 / INFO 0`**, suppressing a `0195`-shaped B2 finding inside the guard built to catch `0195`. **No live instance today.** ⚠️ **Carries an unresolved open question the reviewer explicitly did NOT verify — whether Docker Compose `env_file` tolerates an `export` prefix** — which decides whether the silent case is a *working* deploy the guard cannot see (then R12 is worse than medium) or a broken one. **Nothing is broken today and report-only is safe**, so this is not urgent — but it blocks a capability the owner has said they want. Brief priority **Medium–low, the producer's rank, not an owner ruling**, which this unranked board does not carry. **Board chosen honestly:** no owner ruling scheduled it into a sprint and it cannot start until `0064`'s weekend report-only run has happened, so filing it on Sprint 4 would assert a commitment nobody made. **Row appended, not inserted** (ADR-035))* | [`0203-config-parity-guard-pre-arming-gate`](../tasks/backlog/0203-config-parity-guard-pre-arming-gate/brief.md) |
| 🔲 Backlog | — | Teams multiplayer: the Bot team can lead and make the match unwinnable — decide the resolution policy *(**filed 2026-09-02 on an owner ruling given live in session** — ruling **R4**: split risk 2 out of [`0022`](../tasks/done/0022-win-check-multiplayer-regression-investigation/brief.md) so that task's risks 1 and 3 can ship without waiting on a decision only risk 2 needs. **REAL and PRE-EXISTING — not a PR #77 regression**: the `if (max[0] === ColoredTeams.Bot) return;` guard predates `0140-solo-win-condition-fix`, which only appended `&& gameType !== GameType.Singleplayer`. The `return` sits **above** `this.active = false`, so the win check is never deactivated and simply retries — ✅ producer-verified in `src/core/execution/WinCheckExecution.ts` (guard `:109-114`, `setWinner` `:115`, `active = false` `:117`; ⚠️ **these numbers drift — a coder is editing that file right now for `0022`; locate by symbol**). **Not a hang and not a crash: the match runs normally, it is just unwinnable while the Bot team leads.** ⚠️ **The trigger is CORRECTED from `0022`'s brief, which is part of why this was split.** `0022` emphasises the 95% territory route; in Team mode that threshold really is **95%** (✅ verified `DefaultConfig.ts:713-718`), which means the humans are already wiped out — **rare, and not the realistic shape.** The realistic shape is **private Team + timer**: at expiry the aggregate 400-bot single team (`MapPlaylist.ts:169` `bots: 400`; `GameImpl.maybeAssignTeam` `:463-472` routes every `PlayerType.Bot` to `ColoredTeams.Bot`) plausibly outsizes any one human team, the guard fires, and **the timer expires with no winner — permanently**. Public lobbies ship `maxTimerValue: undefined` (✅ `MapPlaylist.ts:162`) so the timer route is **private/custom only**; the timer is host-settable (✅ `HostLobbyModal.ts:773-774`). ⚠️ **Explicitly UNVERIFIED and flagged in the brief:** how often the Bot team actually out-tiles the leading human team at a typical expiry — the reasoning is structural, not measured, with **no production observation and no player report on file**. Do not present it as a confirmed field incident. ~~🚧 **BLOCKED, and the status says so honestly — this cannot start.** The gate is carried **verbatim** from `0022`'s brief: *"Decide the resolution policy with Mark before implementing."* Three options recorded, no recommendation attached, because the choice is the owner's: (1) the Bot team wins, (2) the next-highest **human** team wins, (3) no winner after a timeout.~~ ✅ **UNBLOCKED 2026-09-02 — owner ruling given live in session: the next-highest HUMAN team wins (option 2).** Owner's reasoning: the timer exists to end the match, and a human team gets a real result; **match-end XP still credits normally**, which was the decisive point. ⛔ **Options 1 and 3 are REJECTED and must not be re-opened** — option 1 ends a multiplayer match with "Bot team has won", exactly what the guard exists to prevent; option 3 is not free — a match ending with no winner sends no `winner` message, and `creditMatchXp` (`GameServer.ts:1253`, **sole** call site `:1199`) runs only from `handleWinner`, so it would **silently drop the whole match's match-end XP** — the same failure `0022`'s risk 1 is fixing for FFA, and the owner picked the option that avoids repeating that defect class. ~~⚠️ **One implementation question is OPEN and deliberately unanswered:** "next-highest human team" is not yet precisely defined — by territory (tile count)? by troops? how are ties broken? The producer did not answer it; it must be settled in the plan before code is written.~~ ✅ **THE MEASURE IS RULED — owner ruling 2026-09-02, given live in session: TERRITORY (tile count).** Owner's reasoning: it matches how the win condition already works everywhere else — the 80%/95% thresholds are territory-based (`DefaultConfig.ts:713-718`) — and **players already read territory as the score**. ⛔ It is decided because the owner ruled it, **not** because it is the existing `checkWinnerTeam()` ranking; that was explicitly not the argument. 🚩 **TWO SUB-QUESTIONS REMAIN OPEN and are NOT answered by this ruling — the producer did not answer them:** **(a) tie-breaking** when two human teams are level on tile count, and **(b) whether "human team" means any team that is not `ColoredTeams.Bot`, or specifically one with at least one real client** (relevant if Nations land on a named team). Both must be settled in the plan and approved before code is written. ~~Also still open from `0022` and never ruled on: whether the fallback should apply **only on the timer branch** rather than every tick — ⚠️ **that one has an FFA twin on [`0206`](../tasks/done/0206-ffa-timer-expiry-award-to-top-client-player/brief.md); consider ruling both at once**, since a split answer leaves `checkWinnerTeam()` and `checkWinnerFFA()` with inconsistent policies.~~ ✅ **THE BRANCH SCOPE IS RULED — owner ruling 2026-09-02, given live in session: BOTH BRANCHES.** The fallback applies to the **timer branch AND the territory-threshold branch**, and it was ruled **once for both `0205` and `0206`**, deliberately, so `checkWinnerTeam()` and `checkWinnerFFA()` stay on **one consistent policy** — the split `0022`'s notes warn against now cannot arise. Owner's reasoning: both-branches is the only option that actually closes the **public-FFA XP loss**, the main mode and the original defect. ⚠️ **That reasoning is FFA-shaped; the Team half follows from CONSISTENCY, not from a Team-side measurement** — in Team mode the threshold route is the **95%** one this row already calls rare (`DefaultConfig.ts:713-718`), so the practical Team-side effect is **unmeasured**. ⚠️ **Two conditions ride with the ruling and are part of it:** (1) it is a **materially larger behaviour change** than the deferred timer-only option and must be planned as such; (2) it **must be re-checked against the tutorial first-place-for-losing bug before shipping** — recorded as a **hard verification step on `0206`**, where it bites; it does not bite here because a tutorial is `gameMode: FFA` (`Main.ts:822`) and `tick()` calls `checkWinnerTeam()` only when `gameMode !== FFA`, so that function never runs in a tutorial *(traced in `0022`'s review; **not re-verified by the producer this turn — `unverified` by me**)*. **Verification gains a branch: the threshold route must be tested separately from the timer route.** ~~**None of the three gates the task; nobody is building it.**~~ ✅ **TWO open sub-questions remain — (a) tie-breaking and (b) what "human team" means — neither gates the task, and nobody is building it.** **Struck, not deleted.** ⛔ **Do not "fix" this by deleting the `gameType !== GameType.Singleplayer` clause** — that reintroduces the Singleplayer Team stall PR #77 fixed, and it is marked ⛔ in `0022`'s brief for the same reason. Brief priority **Low–Medium, the producer's rank, not an owner ruling**, which this unranked board does not carry. ✅ **Re-checked 2026-09-02 after the blocker cleared and held at Low–Medium** — clearing a gate makes a task *ready*, not more *important*, and only that axis changed; the trigger is still private/custom Team lobbies with a timer, and the frequency is still unmeasured. **Board chosen honestly:** ruling R4 said only "split it out" — it scheduled nothing ~~— and the task is blocked on an unmade decision, so it could not be worked in Sprint 4 even if listed there~~ (✅ that second reason is spent; the first stands alone); filing it on the sprint board would assert a commitment nobody made. Same reasoning as `0203`. ✅ **PLACEMENT REVIEWED AND DELIBERATELY LEFT — owner ruling 2026-09-02, given live in session.** The owner looked at this board placement after the resolution-policy ruling unblocked the task and **agreed it stays here, unscheduled**, and **agreed with the producer's Low–Medium rank and its reasoning** (pre-existing, unmeasured frequency, realistic trigger is private lobbies with a timer set). **This is a decision, not an oversight** — recorded so nobody later reads an unblocked task sitting on the backlog board as a scheduling miss and promotes it. The rank remains the producer's, owner-confirmed rather than owner-originated. **Row appended, not inserted** (ADR-035). ℹ️ **RENUMBERED `0204` → `0205` on 2026-09-02, on an owner ruling given live in session.** `0204` was allocated correctly by the rule (max across all three boards was `0203`) but had been **reserved invisibly**: the plan-carry-check hook task is referenced as `0204` in `.claude/skills/fkit-sprint-ship-loop/SKILL.md`, which enumerates five honesty markers `0204` must delete when it lands, and was never filed as a brief so no board could see it. Renumbering one new folder beat editing five load-bearing markers. ⛔ Those skill-file `0204` references are correct and were **not** touched. 🔴 **2026-09-03 — THE PREMISE IS CONFIRMED BY MEASUREMENT, and three things landed the same day.** **(1) Empirical findings.** A headless deterministic simulation, run at the owner's explicit request, on the real **World** map (**651,609 land tiles**), **400 bots**, **`DefaultConfig` not `TestConfig`**: with a timer, the bot team tops the ranking **12/12**, `setWinner` is called **0/12**, the win check is still `active` **12/12** (bot team ≈595k tiles vs best human team ≈5k). **The guard is proven CAUSAL** — the identical board with `gameType: Singleplayer` sets the winner **3/3**, and only the `gameType !== Singleplayer` clause differs. 🚩 **Two claims in this row are CORRECTED by that evidence.** **(i) "The territory route is rare" is NOT supported as written** — under idle humans the bot team crosses **95 %** at **ticks 6180–9480 ≈ 7–10 minutes of real play**, **12/12**, on the shipped **public** config (`maxTimerValue: undefined`). It is rare **because humans play**, not because 95 % is hard to reach; **Verification step 1b was amended accordingly** (prove it on the public config, not only a synthetic forced board). **(ii) The defect is PASSIVITY-DEPENDENT, not unconditional** — activity sweep over 40 team slots: **0–20 % active → bot team leads 3/3 each; 40 % → 1/3; 60–100 % → 0/3**, matches resolve normally. **Crossover is between 20 % and 60 %.** ⚠️ **Caveat that rides with that number:** "active" was `FakeHumanExecution` at **Medium**, which plays better than a casual human, so **the real crossover is probably HIGHER than 40 %** — do not quote 40 % as the human threshold. ~~✅ **`HumansVsNations` is EXEMPT**~~ 🚩 **"EXEMPT" IS SUPERSEDED — see the 2026-09-03 *"Make it deliberate"* ruling at the end of this row. Struck, not deleted; the measurement is unchanged, only the label was wrong.** — the only public Team config that resolves, and only **incidentally** (`disableNPCs` is false there, `MapPlaylist.ts:165`, so 61 Nations on a named team take ≈619k tiles and win, 4/4); stated so nobody reads it as a counter-example — **and now, so nobody reads it as "needs no coverage" either.** ⚠️ **Not literally permanent, and the alternative ending is WORSE:** `GameServer.ts:56` caps a match at 3 hours → `GamePhase.Finished`, and a private game also ends when all clients disconnect (`:876-882`) — but **neither path runs `handleWinner`**, so the match ends with **no winner and no match-end XP for anyone**, which is **exactly the outcome rejected option 3 was rejected for**. ⚠️ **Limits, recorded honestly:** single map (`world`), humans idle by construction (the owner's requested method), and **production frequency STILL UNMEASURED** — no telemetry, no player report. **It is a simulator result, not a field observation.** ℹ️ **Mechanism correction from a separate architect pass:** this row's `maybeAssignTeam` claim about Nations has the **right outcome but the wrong mechanism** — Nations get their team from **`assignTeams()`** (`GameImpl.ts:170` → `TeamAssignment.ts`) and **never reach `maybeAssignTeam()`**; bots reach it via `SpawnExecution`, which passes no team. ✅ **And the tutorial claim this row flagged `unverified` is NOW VERIFIED:** `Main.ts:823` sets `gameMode: GameMode.FFA`, `:835` `isTutorial: true`, and FFA routes to `checkWinnerFFA()` — **the tutorial cannot reach `checkWinnerTeam()`**. **(2) ✅ PRIORITY HELD AT LOW–MEDIUM — owner ruling 2026-09-03, given live in session, WEIGHED AGAINST THIS EVIDENCE.** The producer flagged that the passivity dependence arguably lowers real-world likelihood and put the rank to the owner rather than moving it. Owner's reasoning, recorded because it is **not** the obvious reading of "premise confirmed": the passivity dependence **supports** the rank rather than undermining it — **busy public lobbies are safe (60 %+ → 0/3 stalls)**, so the realistic trigger remains **private and quiet lobbies**, which is what the brief already said. ⚠️ **Record it as weighed and held, NOT as unchanged because nobody looked** — it is now owner-confirmed against measured evidence, not merely inherited from 2026-09-02. **(3) ✅ SUB-QUESTION (b) IS PARTLY RULED — owner ruling 2026-09-03, given live in session: ADR-110 is ACCEPTED, and it is ONE POLICY ACROSS BOTH MODES** — an AI player (`PlayerType.AiPlayer`, which has a real `clientID`) **may** be declared winner, in Team mode (`0205`) as well as FFA (`0206`). **The winner predicate stays `clientID() !== null` with NO `PlayerType.AiPlayer` exclusion** — cite `ai-agents/knowledge-base/decisions/adr-110-ai-player-may-be-declared-winner.md`, which carries a **pre-committed revisit trigger**; read the ADR there rather than this summary. 🚩 **What remains of (b): the ALL-NATIONS TEAM case.** Nations are **clientless** (`clientID === null`), so the predicate excludes them, and a private Team lobby really can hand a **whole named team** to Nations (`HostLobbyModal.ts:42` defaults). Two candidate answers are recorded in the brief — award to the top team regardless (a Nations team can win a real match), or Nations may never win (**recreates the no-winner/no-XP wedge** in exactly the private-lobby case this task exists to close). 📌 **The owner DELIBERATELY DEFERRED this to plan time on 2026-09-03** — *"settle it in `0205`'s plan, with the code in front of whoever plans it, not by a blind ruling now"*, because it is the same shape as the FFA no-eligible-winner hole and deserves the same care. ⚠️ **It is a deferred decision, NOT an oversight — do not re-raise it as one.** 🚩 **STILL OPEN and still not gating: (a) tie-breaking** between human teams level on tile count, untouched by anything ruled so far. **Verification gained steps 7 and 8** (no `PlayerType.AiPlayer` exclusion, per ADR-110; and ~~`HumansVsNations` still resolves as it does today~~ — **step 8 is since AMENDED into 8a/8b/8c; see immediately below**). 🔴 **(4) 2026-09-03 — A SCOPE CONSTRAINT ON THE FIX, owner ruling given live in session: *"Make it deliberate."*** **`0205`'s fix must resolve EVERY Team config BY DESIGN — `HumansVsNations` included — not leave one working by coincidence.** **The finding:** `HumansVsNations` is the **only public Team config that resolves correctly, and only by accident** — `disableNPCs` is **false** there (`src/server/MapPlaylist.ts:165`), so 61 **actively-playing** Nations sit on a named team, take ≈619k tiles and win (measured **4/4**); **every other public Team config stalls.** **Owner's stated concern, recorded because it is the whole reason for the ruling:** a **config change elsewhere could silently remove the accident, and nobody would connect the two** — the Nations are on that team only because one boolean happens to be false for one playlist entry, nothing in code records that a win-condition property depends on it, and flipping it would make the config stall with **no test failing**. ⚠️ **This is a SCOPE CONSTRAINT, not a new open question and not a gate** — it does not block starting; it changes what "done" means, and **the plan must not treat `HumansVsNations` as already-solved.** 🔴 **A future reader must not mistake "it currently works" for "it needs no coverage."** ⛔ **Do not scope the fix to "the configs that currently stall"**, and **do not let the fix depend on `disableNPCs` being false there**, on Nations being present, or on their tile count. **Verification step 8 is amended accordingly: 8a** resolves **through the fix path, by design**, not the accident; **8b** with `disableNPCs` forced **true** for `HumansVsNations` it **must still resolve** — the step that catches the exact failure the ruling exists to prevent; **8c** no Team config left resolving only by coincidence. 🚩 **It is ENTANGLED with the deferred all-Nations question and the two must be settled as ONE decision** — `HumansVsNations`'s current accidental resolution **is** a Nations team winning, so a *"Nations may never win"* answer would **remove the accident and regress this config from working to stalling.** The `EXEMPT` label earlier in this row is **struck** accordingly. 📊 **Also 2026-09-03: the still-unmeasured production frequency now has its own brief — [`0208`](../tasks/backlog/0208-measure-clientless-leader-at-win-condition-in-production/brief.md)**, filed unscheduled on this board on the owner ruling *"File a brief, don't schedule."* 📌 **HISTORICAL — this records what happened at `0208`'s filing on 2026-09-03, and it is accurate as that. It is NOT a current-state claim. Superseded 2026-09-04: `0208` is now SCHEDULED INTO SPRINT 4 — see its row on this board (status `➡️ Moved`) and [`plan-sprint-4.md`](plan-sprint-4.md).** ⛔ **The ruling quoted above is left EXACTLY as recorded and is deliberately NOT restated, reinterpreted or struck here.** **This task's Low–Medium rank rests on "busy lobbies are safe", which is a claim about a never-measured lobby-activity distribution** — `0208` is what would confirm or overturn it, and it also closes this task's investigation-step-2 residual. ⚠️ **Neither task gates the other**)* | [`0205-teams-bot-team-win-stall-resolution-policy`](../tasks/backlog/0205-teams-bot-team-win-stall-resolution-policy/brief.md) |
| ➡️ Moved to [Sprint 4](plan-sprint-4.md) | — | 🔴 **REVERTED 2026-09-04 — `0206`'s BEHAVIOUR IS NOT IN THE GAME AND WAS NEVER DEPLOYED.** Owner ruling given live in session. It is `✅ Done` on [Sprint 4](plan-sprint-4.md) — **correctly**, the work was done — **but the effect was reverted before it reached a player.** The plan's **premise** was disproved by measurement; the code was not defective and did not cause the stall. 🔴 **The XP loss is STILL OPEN AND STILL LIVE.** Replacement: [`0211`](../tasks/backlog/0211-credit-participation-xp-at-elimination-or-match-end/brief.md), ~~on this board, unscheduled~~ — ✅ **CORRECTED 2026-09-04: `0211` was PROMOTED INTO SPRINT 4** (owner ruling, live in session, the same day) and is **no longer on this board**; its row here is kept as `➡️ Moved`. **Struck, not deleted — spent, not wrong.** Status `🔲 Backlog`, **not started**; rank **Medium–High, the producer's.** ⛔ **`0211` must not SHIP until [`0208`](../tasks/backlog/0208-measure-clientless-leader-at-win-condition-in-production/brief.md) is DEPLOYED AND COLLECTING DATA** — ✅ planning and building in parallel is explicitly allowed; ⚠️ neither task is `🚧 Blocked`. **Full record: the `0206` row on [`plan-sprint-4.md`](plan-sprint-4.md) and the STOP box at the top of the brief.** — FFA: award the win to the top player *with* a `clientID` instead of suppressing it — close the stall the `0022` guard leaves behind *(**filed 2026-09-02**. Two origins, both recorded. **Origin 1:** the coder's **option (b)** for `0022`'s risk 1 — guard **plus** a timer-expiry award — which the owner **DECLINED** in ruling **R2** ("a behaviour change, not a defect fix"), asking that it be recorded as a candidate follow-up. This is that follow-up, now filed. **Origin 2:** `0022`'s review, finding **R1**, gave it a second and larger reason to exist. ✅ **Owner ruling 2026-09-02: R1 is ACCEPTED AS A RESIDUAL of the guard-only shape and carried into this brief — it is NOT a defect to fix inside `0022`.** **What R1 costs, in full:** with guard-only, an FFA match where a bot or Nation leads at 80% or at timer expiry emits **no `Win` update at all**, so `ClientGameRunner`'s `gameEnded` path (`:516`, block `:530-536`) never runs. Consequences the reviewer traced: (1) **`saveGame()`** (`:532`) stops firing — a **`localStorage`-only** record (`LocalPersistantStats.ts:46`), **cosmetic**; (2) **`reportPlacements()`** (`:535`) stops firing — ⚠️ **this is the one that matters: top-3 humans now get no leaderboard placement points where they previously did.** The trade is genuinely two-sided — **better** in the "a human eventually wins" case (points land on the real win, not an arbitrary mid-match moment, because the guard returns above `this.active = false` and the check stays alive) and **worse** in the "nobody ever wins" case (**no points at all**); (3) ⚠️ **the useful half, recorded so nobody restores the old behaviour by accident — for the TUTORIAL, removing this is a FIX, not a regression.** Previously a bot winning a tutorial ran `reportPlacements()`, which ranks **only Humans** (`ClientGameRunner.ts:409-412`) — a tutorial has exactly one — so `myIndex === 0` and the player received **first-place leaderboard points for LOSING a tutorial to a bot**, via `LeaderboardReporter.reportPlacement` (`:44-60`) which has **no game-type guard** and writes to the **real platform leaderboard**. ✅ Producer-verified this turn; (4) 🔴 **server-side, the largest one: no `winner` message ⇒ `handleWinner` (`GameServer.ts:1144`, invoked `:366`) never runs ⇒ `creditMatchXp` (`:1253`, sole call site `:1199`) never runs — the whole match's match-end XP is silently lost, for every player.** ✅ Producer-verified. That is risk 1's original defect, and the award in this task is what closes it. ~~🚩 **ONE SCOPE QUESTION IS OPEN AND NEEDS AN OWNER DECISION BEFORE IMPLEMENTATION — the producer did not settle it.**~~ ✅ **RULED 2026-09-02 — see below. Struck, not deleted.** The finding that drove it stands: **a timer-only award closes NOTHING in public FFA.** ✅ Verified this turn: public lobbies of every mode ship `maxTimerValue: undefined` (`MapPlaylist.ts:162`), so the **timer branch never fires in a public lobby**; FFA's threshold is **80%** not Team's 95% (`DefaultConfig.ts:713-718`); and public FFA carries `bots: 400` (`:169`) **and keeps Nations** — `disableNPCs` is Team-only (`:165`). So in public FFA the only reachable route is the **80% threshold** branch. ~~**Timer-only (the literal deferred option (b), automatically tutorial-safe) or both branches (closes the public-FFA XP loss, much larger behaviour change)?**~~ ✅ **RULED 2026-09-02, owner ruling given live in session: BOTH BRANCHES — timer AND the 80% territory threshold.** Owner's reasoning: it is **the only option that actually closes the public-FFA XP loss**, the main mode and the original defect; timer-only would have left every public FFA match's silent XP loss exactly where it is, because public lobbies ship `maxTimerValue: undefined`. ⚠️ **Two conditions ride with the ruling and are part of it, not caveats:** (1) it is a **materially larger behaviour change than the deferred option (b)** and must be **planned as such** — not as the deferred option simply un-deferred; (2) 🔴 **it MUST be re-checked against the tutorial first-place-for-losing bug before shipping, recorded as a HARD VERIFICATION STEP in the brief.** The widening puts the **threshold branch — the only route a tutorial can reach** — in scope, so the automatic tutorial safety a timer-only award carried is **gone by design**; a bot crossing 80% in a tutorial must not hand the single Human first-place points via `reportPlacements()` (`ClientGameRunner.ts:409-412`, `:418-419`) and `LeaderboardReporter.reportPlacement` (`:44-60`, **no game-type guard, real platform leaderboard**). **Verification now requires BOTH branches tested separately** — a green timer test does not cover the threshold branch, and the threshold branch is the one that matters in public FFA. ⚠️ This was the **FFA mirror of an identical question on [`0205`](../tasks/backlog/0205-teams-bot-team-win-stall-resolution-policy/brief.md)** (timer-branch-only vs every tick) — ✅ **both were ruled together, exactly as this brief recommended**, so FFA and Team do **not** end up with inconsistent policies; `0022`'s notes flagged that split as something to avoid, and it is now closed by construction. ⚠️ **`0205` still carries two OPEN sub-questions this ruling does not touch** (tie-breaking; what "human team" means). **Struck, not deleted.** Also open and deliberately unanswered: the ranking measure ("top player with a `clientID`" — tile count is the existing FFA ranking but that is **not** decided by default), tie-breaking, and whether the match should then end on the threshold branch — 🚩 **that last one is made LIVE and material by the branch-scope ruling** (the threshold branch is now in scope, and ending there removes the "a human could still win later" property the `0022` guard was placed to preserve); **it must be answered in the plan and approved, and the producer is not answering it.** **Investigation-first** — the reachability claim (does a clientless leader actually reach 80% in a real public FFA lobby?) is **UNMEASURED, with no production observation and no player report on file**; measuring it is phase 1 and it decides whether the rank holds. **Depends on `0022` shipping** (this modifies the guard `0022` introduces) — sequencing, not a decision gate, so the status is `🔲 Backlog`, not `🚧 Blocked`. Brief priority **Medium — the producer's rank, not an owner ruling**, which this unranked board does not carry: ranked **above `0205`** (Low–Medium) because public FFA is the main mode and always has clientless players, and held at Medium rather than higher because the frequency is unmeasured and the owner already deliberately declined this once. ~~**Board chosen honestly:** ruling R2 said only "record it as a candidate follow-up" — that defers work, it does not commit a sprint; filing it on Sprint 4 would assert a commitment nobody made. **Same "no ruling scheduled it" reason as `0203` and `0205`.**~~ ➡️ **PROMOTED TO [Sprint 4](plan-sprint-4.md) on 2026-09-03, on an owner ruling given live in session** (via `AskUserQuestion`, on the producer's recommendation). **The struck sentence above is now spent, not wrong** — it was true until a ruling scheduled this, and a ruling now has. Owner's stated reasoning, recorded verbatim from the option they chose: *"Schedule it now — Put 0206 into the current sprint scope. It needs no deploy slot to plan or build — only to ship. Two design questions are still open (does the match end on the threshold branch, and what the ranking/tie-break measure is), so I'd spawn a plan first and bring it to you."* ⚠️ **The owner ruled SCHEDULING ONLY — not rank, not scope, not the open design questions.** The brief's `Medium` priority is **still the producer's rank**, unchanged and not owner-originated; the technical scope and both open design questions are **untouched**. ⚠️ **Scheduled is not started** — the task's status stays `🔲 Backlog` on the Sprint 4 board; planning is the next step and the owner said so in the ruling itself. Tracked on [`plan-sprint-4.md`](plan-sprint-4.md) from here on; **this row is kept as `➡️ Moved`, not deleted**, so the trail from "filed here" to "tracked there" survives — same treatment as `0057`, `0062` and `0200`. ⛔ **`0204` was NOT taken** — it stays reserved for the plan-carry-check hook task. **Row appended, not inserted** (ADR-035); this promotion **edited the row in place and re-ordered nothing**)* | [`0206-ffa-timer-expiry-award-to-top-client-player`](../tasks/done/0206-ffa-timer-expiry-award-to-top-client-player/brief.md) |
| 🔲 Backlog | — | `WinModal.buildPlayerParticipation`: the doc comment says AI players are skipped — they are not *(**filed 2026-09-03 on an owner ruling given live in session** — *"File a small brief."* Spotted while recording **ADR-110**'s Teams-parity ruling onto [`0205`](../tasks/backlog/0205-teams-bot-team-win-stall-resolution-policy/brief.md) and [`0206`](../tasks/done/0206-ffa-timer-expiry-award-to-top-client-player/brief.md). **The defect:** `src/client/graphics/layers/WinModal.ts:487-492` documents the function as *"only human players have one (AI players return null and are skipped)"* — **wrong.** The skip is on `clientID === null` (`:498-499`), which excludes **Bots and Nations** (`PlayerType.FakeHuman`) and **INCLUDES** AI players, since `PlayerType.AiPlayer` carries a real `clientID`. ✅ Producer-verified 2026-09-03; ⚠️ locate by symbol, the line numbers drift. ✅ **HARMLESS TODAY, and that is verified end to end rather than assumed:** the server bounds crediting to the frozen start roster — `GameServer.ts:1276-1278` builds `eligibleRoster` from `gameStartInfo.players`, which is `activeClients` (`:480-487`) and **does not include AI players** (they sit in a separate `aiPlayers` field, `:488-491`), and `MatchQualification.ts:83` skips any entry not in that roster. **So an AI player cannot be credited XP today and the comment's error changes nothing at runtime.** ⚠️ **But it is a LIVE TRAP, not dead paper:** it describes the exact predicate `0206` is about to change and the exact predicate **ADR-110 just ruled on** (accepted 2026-09-03: an AI player **may** win, one policy across FFA and Team, predicate stays `clientID() !== null` with **no `PlayerType.AiPlayer` exclusion**). A planner trusting this comment could conclude ADR-110 needs no code change where it does, or "restore consistency" by adding the exclusion the ADR forbids. ⛔ **Scope is COMMENT-ONLY, no behaviour change** — do **not** add a `PlayerType.AiPlayer` filter to `buildPlayerParticipation`, and do **not** touch `WinCheckExecution`; the win predicate belongs to `0205`/`0206`. `src/client/` only, so the "all `src/core/` changes must be tested" rule does not bite. ⚠️ **The "harmless today" claim is verified against today's code and is NOT permanent** — it rests on the roster gate; if that gate changes, these entries stop being inert. **Not blocking and not blocked:** it does not depend on `0206` and does not gate it, but doing it first removes a trap from `0206`'s planning path. Brief priority **Low — the producer's rank, not an owner ruling**, which this unranked board does not carry. **Board chosen honestly:** the ruling was *"file a small brief"* — that authorises a brief, it does not schedule a sprint; same reasoning as `0203` and `0205`. ⛔ **`0204` was NOT taken** — it stays reserved for the plan-carry-check hook task, which lives only in `.claude/skills/fkit-sprint-ship-loop/SKILL.md` prose. **Row appended, not inserted** (ADR-035))* | [`0207-winmodal-participation-comment-ai-player-correction`](../tasks/backlog/0207-winmodal-participation-comment-ai-player-correction/brief.md) |
| ➡️ Moved to [Sprint 4](plan-sprint-4.md) | — | Measure how often a clientless leader is in front when the win condition fires — in production, **AND how often Singleplayer awards platform-leaderboard points** *(➡️ **PROMOTED INTO SPRINT 4 on 2026-09-04, on an owner ruling given live in session — this row is KEPT as a pointer, not deleted, and is NOT flipped to Done.** ⚠️ **This REVERSES the 2026-09-03 ruling recorded below** (*"File a brief, don't schedule"*) — **struck-don't-delete applies: the original ruling text stays readable and was correct for the day it was given.** 🔴 **What changed: this task is now LOAD-BEARING FOR THREE SEPARATE DECISIONS, not one** — **(1)** it is now **ADR-110's re-raise trigger** (the architect corrected that pointer on 2026-09-04 from `0206`'s never-run phase-1 measurement to this task); **(2)** it scopes [`0211`](../tasks/backlog/0211-credit-participation-xp-at-elimination-or-match-end/brief.md) by answering **whether stalled-match survivors are a real population**, now that survivors are in `0211`'s scope; **(3)** it caps [`0205`](../tasks/backlog/0205-teams-bot-team-win-stall-resolution-policy/brief.md)'s rank, which has always been held down by unmeasured frequency. **Plus: Part B's measurement window CLOSES when `0210`'s guard ships** — once Singleplayer stops awarding leaderboard points, how often it was happening becomes **permanently unobservable** — and **planning it alongside `0211` avoids two plans reasoning about the same crediting path.** ~~⚠️ **Scheduled is NOT started** — the brief's status stays `🔲 Backlog`; nobody is building it. ⚠️ **The owner ruled SCHEDULING ONLY, not rank** — rank stays **`Medium`, the producer's**.~~ 📌 **BOTH CLAUSES SPENT 2026-09-04 — struck, not deleted; each was TRUE WHEN WRITTEN and each was overtaken the SAME DAY.** ⛔ **Status: the task WAS picked up — Part A and Part B are BUILT AND REVIEWED, and the brief now reads `🚧 Blocked` — built and reviewed, UNCOMMITTED, UNDEPLOYED, NO DATA COLLECTED.** ⛔ **BUILT AND REVIEWED IS NOT DONE**, and 🔴 **[`0211`](../tasks/backlog/0211-credit-participation-xp-at-elimination-or-match-end/brief.md)'s gate is STILL UNSATISFIED — it requires this task DEPLOYED AND COLLECTING DATA, not merely built.** ⛔ **Rank: no longer `Medium` — a SECOND owner ruling the same day raised it; the value `High` is the PRODUCER'S judgement, not the owner's.** ⚠️ **This pointer row's own `➡️ Moved` status is unchanged and correct.** **Full record on the [Sprint 4](plan-sprint-4.md) row.** Original filing follows. — **filed 2026-09-03 on an owner ruling given live in session** — *"File a brief, don't schedule."* 📌 **The owner's reasoning is recorded because it IS the task:** filing puts the question **on a board** instead of leaving it only in a conversation, and **stops "we never measured it" being rediscovered in three months.** 📌 **SCOPE WIDENED the same day, 2026-09-03, by a SECOND owner ruling given live in session — *"Add it — measure both."*** The task now has **two halves that are different questions on different code in different tiers, and must NOT be merged into one event**: **Part A** = multiplayer clientless-leader incidence (`src/core/`, as originally filed, unchanged and described below); **Part B** = **Singleplayer platform-leaderboard award incidence** (`src/client/`) — how often the client awards platform-leaderboard points from **non-tutorial Singleplayer**, via **both** `reportPlacements()` **and** `reportParticipation()`. ⚠️ **The folder name was deliberately NOT changed** and now under-describes the task; renaming would break every inbound link, including the ones `0206`'s close re-pointed minutes earlier. **What Part A measures:** how often, in live production, a **clientless leader** (`clientID() === null` — a **Bot** or a **Nation**/`PlayerType.FakeHuman`) is in front when an **FFA** match reaches its win condition, and how often the **bot team** (`ColoredTeams.Bot`) leads at the decision tick in a **Team** match — **on BOTH branches, kept distinguishable: the territory threshold AND timer expiry.** ⛔ **Not AI players** — `PlayerType.AiPlayer` carries a real `clientID` and per **ADR-110** may legitimately win; an AI-player win is a normal win, not a stall. 🔴 **Why it exists — the gap, stated exactly:** today's evidence for both [`0205`](../tasks/backlog/0205-teams-bot-team-win-stall-resolution-policy/brief.md) and [`0206`](../tasks/done/0206-ffa-timer-expiry-award-to-top-client-player/brief.md) is a **headless simulator result, not a field observation** — real **World** map, **400 bots**, **`DefaultConfig` not `TestConfig`**, **12/12** stalls with `setWinner` called **0/12** — but with **humans idle by construction**, which was the owner's requested method. The activity sweep showed the Team-mode defect is **passivity-dependent** (**0–20 % active → stalls; 60–100 % → resolves**), and ⚠️ the "active" players were `FakeHumanExecution` at **Medium**, which plays better than a casual human, so **the real crossover is probably HIGHER than 40 %** — do not quote 40 % as the human threshold. **Nobody knows the production rate.** **Three consumers named in the brief:** (1) **`0205`'s Priority is Low–Medium, held by the owner on 2026-09-03 on the reasoning that busy lobbies are safe** — that is a claim about the real lobby-activity distribution, which has never been measured, so real numbers are what would confirm or overturn it; (2) **`0206` is scheduled in Sprint 4 and its brief carries an unmeasured-frequency flag** (whether a clientless leader actually reaches the 80 % FFA threshold in a real public lobby is recorded as UNMEASURED, no production observation, no player report on file); (3) **`0205`'s own investigation step 2 carries an unmeasured-in-production residual**, marked done by simulation with the residual left open in the same breath. **Instrumentation only — this task ships NO gameplay change and does NOT fix the stall** (that is `0205` and `0206`). 🔴 **The load-bearing design instruction: instrument the DECISION POINT, not the guard's early return** — placed inside the `return` path, the metric **silently goes to zero the day `0205`/`0206` replace that return with a fallback award**, while still reading as a healthy green line. ⚠️ **Two over-count hazards, both large, and a naive counter hits both at once:** (A) the guard `return`s **above** `this.active = false`, so the check re-fires every 10 ticks at ~66.7 ms/tick ≈ **90 events/minute** for up to the **3-hour** cap (`GameServer.ts:56`) — order **10⁴ events from one match**; (B) **the server is a turn relay and never simulates**, so **every connected client** runs this code and emits its own copy — the two hazards **multiply**. **A once-per-match latch is required**, and 🚩 **it only half-solves (B): the denominator becomes *client-matches*, not matches** — either de-duplicate to one emitter or **write the denominator into `analytics-event-reference.md`**; ⛔ an unlabelled inflated count is worse than no count, because it looks authoritative. ⚠️ **The emission seam is a design decision the brief deliberately does NOT make** — the instrumentation point is in `src/core/` and the analytics enum is in `src/client/flashist/FlashistFacade.ts`; `src/core/` **already** imports `src/client/` in seven places (✅ verified: `GameRunner.ts`, `Schemas.ts`, `GameImpl.ts`, `PlayerImpl.ts`, `AttackExecution.ts`, `TradeShipExecution.ts`, `validations/username.ts`) so the line is not bright — **but that coupling is the explicit subject of [`0007`](../tasks/backlog/0007-investigate-core-to-client-import-coupling/brief.md)**, and ⚠️ **there is no existing seam to reuse: `WinCheckExecution.ts:12` declares a `WinEvent` class that is ✅ verified to be referenced NOWHERE else in `src/` — it is dead code, not a hook.** 📌 **An `fkit-architect` consult is expected at plan time to approve the seam.** ⚠️ **Verification cannot be completed locally:** `GameAnalytics` initialises only when `DEPLOY_ENV === "prod"` (`FlashistFacade.ts:397`) — correct for this task's purpose, but the dashboard check is a **separate post-deploy step**; ⛔ do not weaken that gate for local convenience. ⛔ **Explicitly out of scope:** no server-side OTEL counter (the server **cannot see this at all** — it never simulates, and when the guard fires **no `winner` message is sent**, which is the defect; a server metric would need a new client→server message — a separate brief), no behaviour change to `WinCheckExecution`, no deleting the `gameType !== GameType.Singleplayer` clause (PR #77's Singleplayer fix), and no dashboard build. **All changes are in `src/core/`, so the "must be tested" rule bites in full**, and **determinism must be verified** — the change lives in the deterministic simulation with state hashing every 10 ticks. **`analytics-event-reference.md` must be updated** (project rule; the producer did not write it — `ai-agents/knowledge-base/` is outside the producer's lane). ⛔ **No identifiers in the event or its dimensions** — no player, Yandex, lobby or client IDs; the question is a rate and a rate needs no identity. ⛔ **No secrets, endpoints or connection strings** anywhere in this chain — variable and event **names** only. 📌 **PART B — the Singleplayer half, added by the 2026-09-03 widening.** 🔴 **It is `points`, not `placement`** — ✅ verified, `reportPlacement()` in `LeaderboardReporter.ts` passes **only `params.points`** to `increaseCurPlayerLeaderboardScore(...)`; **`placement` reaches nothing but a `console.debug` and never leaves the browser**, so counting it answers nobody's question. ⚠️ **That distinction was conflated repeatedly on 2026-09-03 and the brief is written to make re-conflating it hard.** **Both award paths are in scope, and the unguarded one is the farmable one:** `reportParticipation()` is **completely unguarded** and fires **+1 per Singleplayer match *started*** — no win, no loss, no opponent needed, so **a measurement that only counts `reportPlacements()` misses the farm entirely**; `reportPlacements()` awards `awardTable[myIndex]` over `[10, 5, 2]`, and Singleplayer has exactly **one** `PlayerType.Human` so `myIndex === 0` unconditionally → **10 points for LOSING to a bot**. 🟢 **The over-count hazard does NOT apply to Part B — ✅ VERIFIED this turn by reading the code, not assumed, and it is the biggest structural difference between the halves.** **Hazard A (per-tick re-fire) — no:** `ClientGameRunner` already carries `hasReportedParticipation` and `hasProcessedWin` as fields and sets each `true` immediately before its call, so each path fires **at most once per runner instance** — **pre-existing production latches, not something this task must add**. **Hazard B (per-client multiplication) — no:** ✅ `Transport.ts` sets `isLocal = gameRecord !== undefined \|\| gameType === GameType.Singleplayer`, so a Singleplayer match runs against the in-browser `LocalServer` with **exactly one client**, and the path only ever reports the local player (`PlayerType.Human` re-checked in both `LeaderboardReporter` functions). 🔴 **So Part B's denominator is MATCHES, not client-matches** — ⛔ **do not copy Part A's client-match caveat onto Part B's events**, that would be its own kind of lie. ✅ **Replays already excluded** (both call sites are `gameRecord === undefined`-guarded). ⚠️ **ONE RESIDUAL LEFT UNVERIFIED, deliberately:** a **mid-match page reload** builds a fresh `ClientGameRunner` and resets both latches — whether Singleplayer can be resumed at all was **NOT tested**; the evidence points at *no* (`saveReconnectSession` is called only `if (!this.transport.isLocal)`, and `isLocal` is true for Singleplayer) **but that is an inference from one call site, not a test** — it is a verification step, not a settled fact. 🟡 **Part of Part B may already be answerable with NO CODE — check before building:** `Game:Mode:Solo` **already ships in production** (`MatchStartAnalytics.gameModeAnalyticsEvent()`, fired right after `Game:Start`), ⚠️ **but it includes the tutorial** (per `analytics-event-reference.md`: *"solo mode, missions, and tutorial matches"*) so it **over-states** the non-tutorial scope, and its trigger differs from `reportParticipation()`'s; and `Match:Loss:OpponentWon` **already ships** and `WinModal.isSoloOpponentWin()` carries **exactly the right predicate** (non-tutorial Singleplayer + all three tagged-tuple winner shapes) 🔴 **but it also requires `myPlayer.isAlive()` and `reportPlacements()` does not — an ELIMINATED human still gets the 10 points and fires no such event — so it is a LOWER BOUND and cross-check, never the answer.** ✅ **The carried fact holds with exact scope:** `grep -n "Singleplayer\|gameType\|isTutorial"` is **zero hits** across `ClientGameRunner.ts`, `src/client/leaderboard/` and `src/client/flashist-game/` — **the measurement must ADD a discriminator, not read an existing one** — 📌 **but a ready-made predicate exists two files over** in `MatchStartAnalytics.ts` and `WinModal.ts`, and `ClientGameRunner` already imports the former, so a plan writing a fourth game-type predicate from scratch should say why. **Part B dimensions:** path (participation vs placement — **merging them destroys the farmability answer**), 🔴 **tutorial vs non-tutorial** (load-bearing — `0210`'s scope is non-tutorial, and it is exactly where the existing `Game:Mode:Solo` count fails), points awarded, and human-won vs human-lost. ⛔ **No identifiers, and specifically NO per-player "how many Singleplayer matches did they start" dimension** — that is behavioural tracking of people, and this task measures a rate; farm detection would be a separate brief with its own privacy review. ⛔ **Part B ships NO guard** — that is `0210`, and 🔴 **adding it here would destroy this task's own measurement.** ⛔ **Part B does not touch `placement`/`_winUpdate` (that is `0209`), `awardTable`, or `WinCheckExecution`.** 📌 **Part B needs NO architect consult and carries NO determinism risk** — it is entirely `src/client/`; ⚠️ **Part A still carries both in full.** 🚩 **SEQUENCING TENSION, flagged and deliberately NOT resolved:** **this task's value decays, and the decay is caused by a task that is already scheduled.** `0206` is in **Sprint 4**; instrumented **before** it ships, this answers *"how often does the stall happen today?"* — the question `0205`'s rank and `0206`'s flag are actually waiting on. Instrumented **after**, it answers *"how often does the new fallback award fire?"* — useful, but a **different question**, and the original becomes **permanently unanswerable**. 📌 **The SAME decay now applies to [`0210`](../tasks/backlog/0210-singleplayer-platform-leaderboard-reporting-policy/brief.md), and that is the owner's stated reason for widening this task** — `0210` is **ruled, unblocked and ready to plan**, and when its guard ships, how often Singleplayer was minting points becomes **permanently unobservable**. 🔴 **Part B decays HARDER than Part A, and the difference is real:** Part A survives `0206` as a *different* question (*"how often does the fallback award fire?"*) because the decision point still exists after the fix, whereas **Part B has no successor question at all** — `0210`'s ruling is *report nothing*, so afterwards the counter reads **zero forever by design**. **Part B is a snapshot with an expiry date, not an ongoing metric**, and the brief says so rather than pretending otherwise. 🔴 **AND YET IT DOES NOT GATE `0210` — stated twice in the brief on purpose, because it is the point most likely to be misread.** The owner's `0210` ruling was **explicitly NOT conditioned on incidence** (option C was rejected on **farmability**, with the reasoning that unmeasured incidence does not rescue it), so **the guard is right either way**; ⛔ **nobody may add a "blocked by `0208`" marker to `0210`, hold its plan waiting for a number, or re-file it as dependent** — **if the two collide, `0210` wins and Part B loses its window**, a trade the owner accepted in advance. ⚠️ **`0206` is now CLOSED (2026-09-03) and sits in `ai-agents/tasks/done/`** — 🔴 **closed is NOT deployed, and the decay is caused by DEPLOY**; ⚠️ **its production state was NOT verified when this row was written**, so check it at plan time. ⛔ **The owner ruled "don't schedule" and that ruling is NOT overridden here** — this is recorded so the trade-off is visible when someone next looks at the board, and it is why the decision-point instruction above matters. ~~⚠️ **All three of `0208`, `0209` and `0210` remain UNSCHEDULED as of 2026-09-03 by owner ruling** — neither `0209` nor `0210` is urgent and Sprint 4 has a deploy to get through;~~ **the widening is NOT a scheduling signal.** ✅ **CORRECTED 2026-09-04 — the struck clause is stale in its `0208` third ONLY. Struck, not deleted; it was true on the 2026-09-03 it names, and is spent, not wrong.** **`0208` was PROMOTED INTO SPRINT 4** by a separate owner ruling on 2026-09-04 (this row's status column already reads `➡️ Moved`), status still `🔲 Backlog` — **scheduled is not started** — ~~rank still **`Medium`, the producer's**~~ 🔴 **RANK CORRECTED 2026-09-04: the struck rank was WRONG WHEN WRITTEN (same session, by the producer) — wrong on the value AND on whose call it was. `0208`'s rank is `High`.** ⚠️ **SPLIT PROVENANCE: the RAISE is an OWNER RULING (2026-09-04); the VALUE `High` is the PRODUCER'S judgement — the owner named no value.** ⛔ **Do not collapse to "the owner ranked it `High`".** ⚠️ **`0209` and `0210` are UNAFFECTED: both verified 2026-09-04 as still `🔲 Backlog`, unscheduled, on this board; their "stay unscheduled" ruling stands.** ⛔ **The struck clause's point survives: the scope widening still was NOT the scheduling signal — a separate ruling was.** Brief priority ~~**Medium–low**~~ 📌 **RE-RANKED 2026-09-03 to Medium on the scope widening — the PRODUCER'S rank, NOT the owner's**, which this unranked board does not carry. ⚠️ **The owner has ruled *file*, *do not schedule* and *measure both*; they have NEVER ranked it.** **Why it moved one notch:** **two decaying windows now instead of one** (`0206` and, newly, `0210`, which is ready to plan today), and **Part B is cheaper per answer than Part A — part of it may cost nothing at all**, since `Game:Mode:Solo` already ships. **Why not higher:** **measuring never outranks fixing** — `0210` closes a live farmable path that reaches a player-visible platform ranking, and `0206` closed a silent XP loss; that is also why the non-gating rule above is **not** softened by the re-rank. **Unchanged: above `0207` (Low)** because it produces evidence **three** live tasks are reasoning without. **Board chosen honestly — and here it is the owner's explicit instruction, not an inference:** *"don't schedule"*; filing it on Sprint 4 would assert a commitment the owner deliberately withheld. Same board reasoning as `0203`, `0205` and `0207`. ⛔ **`0204` was NOT taken** — it stays reserved for the plan-carry-check hook task, which lives only in `.claude/skills/fkit-sprint-ship-loop/SKILL.md` prose and is invisible to every board. ℹ️ **ID verified by the full four-check procedure** (task folders → all boards → `grep -rn` over `.claude/` → repo-wide grep); the `.claude/` step also surfaced `0241`–`0247`/`0264`/`0265`, which are the **fkit toolkit's own task numbering inside installed upstream skill files, not this project's board** — recorded in the brief so no future allocator re-derives that, and so nobody treats them as free either. **Row appended, not inserted** (ADR-035))* | [`0208-measure-clientless-leader-at-win-condition-in-production`](../tasks/backlog/0208-measure-clientless-leader-at-win-condition-in-production/brief.md) |
| 🔲 Backlog | — | Decide what `placement` means in `reportPlacements()`, then correct it — today it is the literal `1` for everyone *(**filed 2026-09-03 on an owner ruling given live in session** — *"Accept now, brief it alongside."* The owner accepted the consequence for [`0206`](../tasks/done/0206-ffa-timer-expiry-award-to-top-client-player/brief.md)'s current round and asked for the defect to be **tracked separately rather than absorbed silently**. **The defect:** `src/client/ClientGameRunner.ts:426` computes `const placement = +1;` — a **unary plus on the literal `1`**, so every top-3 human is labelled placement 1. ✅ **Producer-verified 2026-09-03, including the instruction to check it: the POINTS are correct** — `const points = awardTable[myIndex];` (`:425`) indexes properly against `[first, second, third]` = `[10, 5, 2]` (`FlashistGameSettings.ts:10-15`). **Only the reported placement value is wrong.** ⚠️ Locate by symbol (`reportPlacements`), the line numbers drift. **Pre-existing and unrelated to `0206`** — commit `84fd4ff`, 2026-04-30. 🔴 **THE OBVIOUS FIX IS WRONG — do not prescribe `myIndex + 1`.** ✅ Verified independently twice on 2026-09-03 (producer while filing; the `fkit-coder` processing `0206`'s review): `reportPlacements()` **ignores its `_winUpdate` parameter entirely** — the underscore is deliberate — and ranks **`PlayerType.Human` only**, by tiles descending (`:409-418`). Bots, Nations (`FakeHuman`) and AI players are absent from that ranking, so `myIndex + 1` is a **human tile-rank, not a placement**; and 📌 **under ADR-110 the declared winner may be an `AiPlayer` who is not in the ranking at all**, so `myIndex + 1` could report `1` for a player who demonstrably did not win. **This is therefore a DESIGN question, not a one-character fix** — the brief scoped three definitions (rank among humans / rank among all players / winner-relative) and routed the choice to the owner. 📌 **RULED 2026-09-03, live in session: `placement` means RANK AMONG HUMANS** (option A). **Owner's reasoning:** it **agrees with the points already awarded**, which are human-relative (`awardTable[myIndex]` over `[10, 5, 2]`) — the reported number would finally match what the points already say. ⛔ **Rank-among-all-players REJECTED** (it disagrees with the points — "placement 7" while receiving first-place points — and reconciling that means changing the award table, a product change). ⛔ **Winner-relative REJECTED** (most truthful and the only option fully respecting ADR-110, but the costliest — three winner tuple shapes, Team mode easy to get wrong — and disproportionate for a value that today reaches only a debug log). **Both recorded in the brief as considered-and-rejected with reasoning, so nobody reopens them as unweighed.** 🔴 **The ruling settles the DEFINITION, NOT the implementation — `myIndex + 1` is still NOT prescribed**, and the prohibition survives the ruling: `myIndex` indexes a human-filtered, tile-sorted array whose **tie handling is undocumented** (no `smallID` tiebreak, unlike `WinCheckExecution`) and which may contain disconnected/eliminated humans — **the plan establishes the expression against the code; the brief does not assert it.** ⛔ A brief prescribing the mechanical fix would repeat the `0022` failure, where the brief's own prescribed fix would have regressed PR #77. ⚠️ **Two accepted consequences of option A the plan must handle, not ignore:** the label means something narrower than a reader assumes (**documenting it at the declaration is mandatory, not optional**), and it can read `1` for a player who lost to an **AI winner** — ✅ **correct under A, not a bug**, and the brief asks that it be exercised once so a later reader does not file it as one. 🔴 **THE R2 / plan-§8.1 FRAMING IS OVERSTATED and is corrected here:** both describe the wrong placement as being **sent to the Yandex platform leaderboard**. ✅ **It is not.** Verified in `src/client/leaderboard/LeaderboardReporter.ts:44-58` — the only platform call is `increaseCurPlayerLeaderboardScore(params.points)`, whose signature is `(increase: number, leaderboardId?: string)` (`FlashistFacade.ts:1372-1375`), so **`placement` is never passed to it**; `params.placement` is consumed **only by the `console.debug` on the next line**, under the comment `// TODO: integrate platform leaderboard API (placement)`. **Today's harm is a wrong number in a browser debug log — nothing reaches the platform.** R2's **severity** (low) ✅ stands; R2's **mechanism** ❌ does not; R2's **frequency-change** observation ✅ still holds but now describes more *debug lines*, not more wrong platform writes. ⛔ **This correction was NOT written into `0206`'s ledger** — an `fkit-coder` is processing that review and `0206`'s folder was not touched; **it needs relaying.** **The `0206` frequency change, stated accurately:** public/private FFA matches with a clientless leader used to stall with no winner, so no `Win` update, so `gameEnded` never true (`ClientGameRunner.ts:516`, `:530-536`) and `reportPlacements()` never ran; after `0206` they end with a declared winner and it runs. **Two consequences, separated:** (1) **points now get awarded** in those matches — **`0206`'s intent, correct, not a defect**; (2) the wrong placement label now gets **logged** there too — this task's defect, blast radius the debug console. ⛔ **Out of scope:** do not change `points`; do not wire placement into the platform call (that is the `TODO`, a separate decision); do not touch `0206`'s files while its review runs. `src/client/` only as scoped, so the "all `src/core/` changes must be tested" rule does not bite — **but if a chosen approach reaches `src/core/`, it applies in full.** 📌 **UNBLOCKED 2026-09-03 by the ruling — ready to plan.** Owner is now **fkit-coder** (the producer/owner decision is done). ⚠️ **No ordering dependency on [`0210`](../tasks/backlog/0210-singleplayer-platform-leaderboard-reporting-policy/brief.md) any more** — the earlier soft-ordering caveat is **moot**, since `0210`'s option B (which presumed a winner-relative definition) was rejected and both were ruled the same day into a coherent pairing; **either order, or in parallel.** 📌 **The SPLIT from `0210` was confirmed by the owner on its merits**, after reading the producer's own weakening of the split argument — orthogonal axes, each answerable "no change" independently, and `0210` carries harm this task does not. **Weighed, not overlooked.** Brief priority **Low — the producer's rank, not an owner ruling**, which this unranked board does not carry: 🔴 **low on IMPACT, not on COST — do not pull it in as a quick win**, and the ruling removed the design question but **not** the implementation caution. **Board chosen honestly:** the ruling *"brief it alongside"* authorises a brief, it does not schedule a sprint; filing it on Sprint 4 next to `0206` would assert a commitment nobody made. Same reasoning as `0203`, `0205`, `0207`, `0208`. ℹ️ **ID verified by the full four-check procedure** (task folders, highest in use `0208` → all boards → `grep -rn "0209" .claude/`, zero hits → repo-wide grep, zero hits). ⛔ **`0204` NOT taken** (invisibly reserved by the plan-carry-check hook task, prose-only in `.claude/skills/fkit-sprint-ship-loop/SKILL.md`); ⛔ **`0241`–`0247`, `0264`, `0265` NOT taken** — the **fkit toolkit's own numbering** under `.claude/skills/fkit-heal/`, not this project's board, and not free either. ⚠️ **This brief was RESCOPED after filing began** — first drafted as the mechanical fix, corrected before this row was written; the mechanical framing is recorded in the brief only so nobody reintroduces it. **Row appended, not inserted** (ADR-035))* | [`0209-define-placement-semantics-and-fix-literal-one`](../tasks/backlog/0209-define-placement-semantics-and-fix-literal-one/brief.md) |
| 🔲 Backlog | — | Decide whether Singleplayer should report to the platform leaderboard at all — today it awards first place for losing to a bot *(**filed 2026-09-03; the ORIGIN is `0206`'s plan §8 item 2, which records "Briefs for both are the producer's to file" — the SPLIT into its own brief is the producer's judgment, NOT an owner ruling.** **The defect:** in **non-tutorial Singleplayer**, a human who **loses to a bot** is awarded **first-place** platform-leaderboard points. Same shape as the tutorial bug [`0022`](../tasks/done/0022-win-check-multiplayer-regression-investigation/brief.md) fixed — **still live, touched by neither `0022` nor `0206`.** ✅ **Producer-verified 2026-09-03 by reading the full path:** (1) `WinCheckExecution.ts:74-92` — when the leader is clientless the outer guard is `gameType !== Singleplayer \|\| isTutorial === true`; for non-tutorial Singleplayer **both disjuncts are false**, so the whole protective block — including `0206`'s new fallback award **and** its `if (gameType === Singleplayer) return;` — is **skipped entirely**, falling through to `setWinner(max, …)` at `:108` with `max` the bot; (2) a `Win` update is emitted via `GameImpl.makeWinner()` (`:662`, `:667-687`, Singleplayer-specific clientless branch at `:678-681`); (3) `ClientGameRunner.ts:516` sets `gameEnded` from the presence of a `Win` update and **never reads who won**, then `:530-536` calls `reportPlacements(winUpdate)`; (4) `reportPlacements()` **ignores `_winUpdate`** and ranks **humans only** (`:409-418`) — Singleplayer has exactly **one** Human, so `myIndex === 0` unconditionally; (5) `points = awardTable[0]` = **10** → `increaseCurPlayerLeaderboardScore(10)` (`LeaderboardReporter.ts:44-49`, `FlashistFacade.ts:1372`). 🔴 **The client's leaderboard path has NO game-type awareness whatsoever** — ✅ grepping `ClientGameRunner.ts` for `Singleplayer`, `gameType` and `isTutorial` → **zero hits**, same grep over `src/client/leaderboard/` and `src/client/flashist-game/` → **zero hits**. **There is no Singleplayer guard to repair; there has never been one.** **The irony is written into the code:** `WinCheckExecution.ts:85-89` justifies `0206`'s Singleplayer `return` precisely because *"awarding its single Human the win for LOSING to a bot would hand them first-place platform-leaderboard points via ClientGameRunner.reportPlacements() — the exact bug 0022 fixed"* — ✅ that reasoning is **correct and `0206`'s guard is right**, but it only protects the **new fallback branch**; the **pre-existing fall-through** at `:108` hands over the same points and always has. ⚠️ **This is NOT a `0206` regression and must not be described as one.** **Second facet, same question:** `reportParticipation()` also has **no game-type guard** (`ClientGameRunner.ts:504-514`), so every Singleplayer match **started** awards **1** point — whatever is decided should cover it. **It was a PRODUCT QUESTION — should Singleplayer report at all?** — routed to the owner with three options. 📌 **RULED 2026-09-03, live in session: SINGLEPLAYER REPORTS NOTHING** (option A — no participation, no placement). **Owner's reasoning:** Singleplayer is **unranked practice against bots** and is **trivially farmable**, so points from it **devalue the leaderboard for real multiplayer play**; it is also the **simplest guard to add and to reason about**. ⛔ **Report-only-on-a-genuine-win REJECTED** — it keeps a solo reward the owner judges shouldn't exist (winning against bots is no more leaderboard-worthy than losing), costs more (three winner tuple shapes), and drags in the separate multiplayer question; ⚠️ **doubly moot, since it presumed a winner-relative `placement` and `0209` was ruled rank-among-humans the same day.** ⛔ **Accept-the-inflation REJECTED** — farmability was decisive **regardless of incidence**, so the unmeasured rate does not rescue it. **Both recorded in the brief as considered-and-rejected with reasoning.** 🔴 **The load-bearing consequence for whoever plans it: the guard must be ADDED, not repaired** — ✅ verified there is **no game-type awareness anywhere** in this path, so the whole guard is new code; and 🔴 **`reportParticipation()` is IN SCOPE, not just `reportPlacements()`** — the ruling says *reports nothing*, and an unguarded `reportParticipation` keeps minting **1 point per Singleplayer match started**, which is the farmable path the ruling exists to close. **The plan must also state explicitly whether the tutorial is included** (it is a Singleplayer game, so a naive predicate already covers it — very likely correct, but it should be a decision, not an accident). 📌 **UNBLOCKED — ready to plan.** Owner is now **fkit-coder**. ⚠️ **What is NOT verified, stated plainly:** nobody has **run** it — the code path says 10 points are credited, but **no live reproduction exists**, so **reproduce before fixing**; and the **incidence is unmeasured** — [`0208`](../tasks/backlog/0208-measure-clientless-leader-at-win-condition-in-production/brief.md) measures the **multiplayer** side only and **does not cover Singleplayer**. 🚩 **STILL OPEN, NOT RULED — deliberately left unresolved: should `0208`'s measurement scope extend to Singleplayer incidence, or stay multiplayer-only?** It has **not been put to the owner**. ⛔ **`0208` is left EXACTLY as filed — nothing about it was changed by the 2026-09-03 rulings.** ⚠️ **It does NOT gate `0210`** — the ruling was explicitly **not** conditioned on incidence (farmability was decisive regardless), so this ships either way. **But it is worth asking BEFORE `0210` ships:** afterwards the guard makes the original rate permanently unobservable — the same value-decay `0208`'s own brief flags against `0206`. Flagged on the board rather than left in a reply. ⛔ **Out of scope:** do not change `awardTable` or the point values (correct, and the part that **does** reach the platform); do not touch `WinCheckExecution` — the defect is downstream of it, in the client. 🚩 **Regression guard that matters most: confirm multiplayer is unaffected** — a loosely written guard could silence the real FFA/Team leaderboard, which is `0206`'s entire point. **Relation to [`0209`](../tasks/backlog/0209-define-placement-semantics-and-fix-literal-one/brief.md):** orthogonal axes — *which modes report* vs *what the number means*. 📌 **The SPLIT was RULED 2026-09-03 and confirmed on its merits:** the owner **read the producer's own honest weakening of the split argument** (that `0209` had been rescoped from a one-expression fix into a design decision too, making the two closer than first argued) **and kept them separate anyway** — the axes remain orthogonal, **either can be answered "no change" independently**, and `0210` carries **real harm `0209` does not** (its points reach the platform; `0209`'s wrong value never leaves the client). ⚠️ **That doubt was weighed and resolved, not overlooked — do not re-propose merging without new information.** ⚠️ **The earlier ordering caveat is MOOT** — option B was rejected here and `0209` was ruled rank-among-humans, a coherent pairing; **no ordering dependency remains, either order or in parallel.** 📌 **ADR-110 does NOT pre-answer this** — it governs who may be *declared winner*, not who *receives leaderboard points*. Brief priority **Medium — the producer's rank, not an owner ruling**, which this unranked board does not carry: **ranked ABOVE `0209`** on one fact — **`0209`'s wrong value never leaves the client; this one's points genuinely reach the Yandex board**, and Singleplayer is **farmable** (start, lose, take 10). ~~**Below High** because it is a decision, not a bug report.~~ 📌 **Medium STANDS after the ruling, but the reason changed:** it is no longer held back by an open question — it is ordinary scheduled work. **Not ranked on incidence — that is unmeasured.** ℹ️ **ID verified by the same four-check procedure**, run once for the `0209`/`0210` pair: zero hits for `0210` across `ai-agents/`, `.claude/` and `src/`; highest ID in use before the pair was `0208`. ⛔ `0204` and `0241`–`0247`/`0264`/`0265` **not free, not considered** (see `0209`'s row). **Row appended, not inserted** (ADR-035))* | [`0210-singleplayer-platform-leaderboard-reporting-policy`](../tasks/backlog/0210-singleplayer-platform-leaderboard-reporting-policy/brief.md) |
| ➡️ Moved to [Sprint 4](plan-sprint-4.md) | — | Credit participation XP at **elimination OR match end**, whichever comes first — without double-crediting *(➡️ **PROMOTED INTO SPRINT 4 on 2026-09-04, on an owner ruling given live in session — this row is KEPT as a pointer, not deleted, and is NOT flipped to Done.** ⛔ **The owner DECLINED the producer's "leave it unscheduled" recommendation**; their reasoning: **the XP loss is measured and live, and the design assessment is already done, so it can start immediately.** ⚠️ **Scheduled is NOT started** — the brief's status stays `🔲 Backlog`; nobody is building it. ⚠️ **They ruled SCHEDULING ONLY and explicitly declined "re-rank first"** — rank held at **`Medium–High`, the producer's**. **Full record on the [Sprint 4](plan-sprint-4.md) row.** Original filing follows. — 📌 **SCOPE WIDENED 2026-09-04 BY TWO FURTHER OWNER RULINGS, given live in session — the title above is now NARROWER than the scope and is kept unchanged on purpose (the folder name is the task's identity and seven files link to it). The real scope: CLOSE THE XP LOSS — eliminated players AND survivors, FFA AND Team mode.** 🔴 **Ruling 1 — SURVIVORS ARE IN SCOPE.** The architect's §7 showed crediting at elimination covers players who **die** and leaves **survivors of a match that never ends with no trigger at all** ⇒ as first scoped this task closed **half** the loss. Owner's reasoning: *"half a fix leaves you rediscovering this in three months."* ⛔ **`GameServer.end()` is NOT the survivor trigger** — the architect verified it would credit **zero** in every match that ends normally (`phase()` requires `noActive`; `selectMatchCredits` excludes anyone absent from `activeClients`). ⛔ **THE RULING STATES A REQUIREMENT, NOT A MECHANISM — the trigger is the PLAN'S to choose.** ⚠️ **A third option was CONSIDERED AND NOT CHOSEN, and the distinction is fiddly:** *fix the stall so survivors reach a normal match end and are credited through the existing path.* ⛔ **A planner must NOT present "just fix the stall" as a fresh unexplored idea — it was raised, weighed and passed over AS THE SCOPE DECISION** (the owner declined to replace `0211` with a stall fix); ⛔ **but it is NOT forbidden as a MECHANISM — if the plan concludes that making the match actually end is the cleanest way to give survivors a trigger, that satisfies the ruling and may legitimately be the chosen design.** ⇒ **Settled: the REQUIREMENT. Open: the MECHANISM. Do not collapse the two in either direction.** 🔴 **Ruling 2 — TEAM MODE IS COVERED.** `checkWinnerTeam()` has the **same guard shape** as `checkWinnerFFA()`, so a bot-team-led multiplayer match **stalls and loses its XP identically** — found **independently by the coder performing the `0206` revert**, and ⚠️ **nobody had connected it before**, across `0022`, `0206` and `0205` *(⛔ reported-not-re-verified by the producer — that file was being edited; confirm by symbol at plan time)*. Owner's reasoning as put and accepted: **the fix lives in the CREDITING path, not the win check**, so covering both is **likely near-free**, and it stops [`0205`](../tasks/backlog/0205-teams-bot-team-win-stall-resolution-policy/brief.md) **being solved twice or forgotten.** ⛔ **THIS DOES NOT MERGE `0211` AND `0205`** — `0205` is a **resolution-policy** question (*who should win a stalled Team match?*), `0211` is **crediting** (*do those players get their XP?*); `0211` **may make part of `0205`'s justification moot without settling `0205`'s own question**, and ⛔ **`0205`'s status, scope and rank are UNCHANGED — the owner has not ruled on them** (a reciprocal cross-reference was added to `0205`'s Notes and nothing else in that brief was edited). ⚠️ **SINGLEPLAYER IS EXPLICITLY NOT RULED** — do not assume it in or out; `0210`'s ruling is about **leaderboard points, not XP**, and must not be read across. ⚠️ **NEITHER RULING CHANGED SCHEDULING OR RANK** — ~~still unscheduled,~~ still **Medium–High, the producer's**, deliberately **held** across the widening because coverage and urgency are different axes and only coverage moved. ✅ **CORRECTED 2026-09-04 — the struck "still unscheduled" is stale; `0211` was SCHEDULED INTO SPRINT 4 later the same day by a SEPARATE owner ruling. Struck, not deleted — spent, not wrong.** ⛔ **The rest of this sentence still stands exactly as written: neither SCOPE-WIDENING ruling changed scheduling or rank, and the rank is still `Medium–High`, the producer's.** **Original filing follows.** — **filed 2026-09-04 on an owner ruling given live in session**, as the **replacement for the reverted [`0206`](../tasks/done/0206-ffa-timer-expiry-award-to-top-client-player/brief.md)**. ~~⚠️ **The owner ruled the FILING and the SCOPE; they did NOT rule on scheduling — this row is unscheduled and the rank is the producer's.**~~ ✅ **CORRECTED 2026-09-04 — struck, not deleted; spent, not wrong. A LATER owner ruling the same day DID rule on scheduling: `0211` is SCHEDULED INTO SPRINT 4**, tracked on [`plan-sprint-4.md`](plan-sprint-4.md), this row kept as `➡️ Moved`. ⚠️ **Status `🔲 Backlog` — scheduled is not started.** ⛔ **The rank half is UNCHANGED and still true: `Medium–High`, the producer's, never an owner ruling.** 🔴 **Why it exists: `0206` was REVERTED 2026-09-04 and its behaviour is NOT in the game and was NEVER DEPLOYED**, so the defect it was scheduled to close is **still open and still live** — a match nobody can win runs to the `maxGameDuration` cap and **every player in it silently loses their match-end XP.** ⛔ **State `0206`'s history precisely — three different claims, only the first true:** ✅ **`0206` did what its approved plan specified and the plan's PREMISE was wrong**; ⛔ **NOT "`0206` was buggy"** (it passed a two-reviewer stateful review, Codex: *"No findings."*, and its play-test gate **passed**); ⛔ **NOT "`0206` caused the stall"** (the stall predates it, `0022`, and survives the revert). **THE LOSS IS MEASURED, NOT REASONED** — observed 2026-09-04, single human, private FFA, eliminated by a Nation, watched to termination: elimination showed **only a defeat modal, no match-end screen, no winner, no stats**; the **server logged NOTHING at elimination**; match end read `private game complete` / `ending game with 11203 turns` / `archiving game` with **no `handleWinner`, no winner vote, no `creditMatchXp`**, and `archiveGame` ran with **no `winner` attribute and no player stats**. 🔴 **THE CENTRAL DESIGN PROBLEM, not a detail: the server does not know when a player is eliminated** — it is computed client-side and the server is a turn relay. **Trust is first-class**: a client-reported elimination is a *claim*, and crediting on an unverified claim is a farming surface (`getCreditableYandexId()` is the existing seam precedent, ADR-103). **`creditMatchXp` has exactly ONE call site, inside `handleWinner` (`src/server/GameServer.ts:1199`) — decoupling it is the substance of the work**; `GameServer.end()` runs on **every** termination and is a candidate seam. ✅ **ARCHITECT'S ASSESSMENT HAS LANDED — `ai-agents/knowledge-base/reports/2026-09-04-elimination-time-xp-crediting-design-assessment.md`. READ IT BEFORE PLANNING.** It landed **while the brief was being written**, so it was **not** available when the brief was drafted; ⚠️ **assume the report is more current than the brief wherever the two differ.** ⛔ **The brief deliberately carries NO design and does not duplicate or pre-empt the report's recommendation** — the technical picture is the architect's. 📌 Headline: **feasible, cheaper than it looks** *(a pointer, not a summary)*. 🔴 **Two of the brief's assumptions were CORRECTED by it, both recorded in place, struck-not-deleted:** **(1)** ~~double-crediting UNVERIFIED~~ → ✅ **VERIFIED: enforced by a primary key at the database layer, with an integration test covering the concurrent case** — the producer had verified only that `ProfileApiClient`'s contract comment (`src/server/ProfileApiClient.ts:32`) *says* `(game_id, yandex_player_id)`, not that the schema enforces it; **(2)** ~~`GameServer.end()` is a candidate seam~~ → ⛔ **WRONG SEAM — crediting there would award ZERO credits in every match that ends the normal way.** 🔴 **AND A GAP THE REPORT SHARPENED (its §7): crediting at elimination covers ELIMINATED players but leaves SURVIVORS of a stalled match with no trigger at all** ⇒ **as scoped, this task does NOT close the whole XP loss** — unruled, and now a *known* gap rather than a speculative one. ⚠️ **The report carries its own open questions for the owner (§11), NOT answered in the brief and not duplicated there — both lists need the owner.** ⛔ **Do NOT reintroduce `0206`'s fallback award** — the owner ruled it wrong: *"if a bot has 80 % and a player has 20 %, it's the problem of the player."* ⚠️ **Losing to a bot is a legitimate outcome; this task credits participation in a match that happened, it does not manufacture a winner** — and it does **not** fix the stall, which stays separately open. **Rank Medium–High, the producer's:** higher than `0206`'s `Medium` because the loss is now **observed** rather than inferred; not higher because **production frequency is still UNMEASURED** ([`0208`](../tasks/backlog/0208-measure-clientless-leader-at-win-condition-in-production/brief.md), ~~also unscheduled~~ — ✅ **CORRECTED 2026-09-04: `0208` is SCHEDULED INTO SPRINT 4, status `🔲 Backlog`, not started; struck, not deleted — spent, not wrong.** ⛔ **This does NOT raise the rank: the frequency is still UNMEASURED, which is what caps it — scheduling the measurement is not the same as having the number**), the design is genuinely open, and `0206`'s own history argues for caution. **ID `0211` verified free by the four checks** — `grep -rn "0211" .claude/ ai-agents/` **zero hits**; max in task folders `0210`; max on all boards `0210`; and the `.claude/` prose sweep returned only **`0202`** and **`0204`**, ⛔ **`0204` still reserved for the plan-carry-check hook task and must not be allocated.** **Row appended, not inserted** (ADR-035))* | [`0211-credit-participation-xp-at-elimination-or-match-end`](../tasks/backlog/0211-credit-participation-xp-at-elimination-or-match-end/brief.md) |
| 🔲 Backlog | — | Investigate tutorial abandonment — segment the 9.8 % completion rate by platform / browser / device before proposing any fix *(**filed 2026-09-04 on an owner request given live in session**, after the finding fell out of an unrelated analytics query. ⚠️ **The owner asked for the investigation to be written and filed; they did NOT rank it and did NOT schedule it.** Rank **Medium–High, the producer's**, recorded in the brief with its reasoning. **The finding, GameAnalytics game `330439`, Design events / Count, 5 Aug – 3 Sep 2026:** `Tutorial:Started` **106.08K**, `Tutorial:Duration` **14.00K**, `Tutorial:Completed` **10.43K**, `Tutorial:Skipped` **3.57K**, `Tutorial:TooltipShown` **116.69K**, `Tutorial:TooltipClosed` **83.80K** ⇒ read at face value, **~87 % of starts reach no end state** and completion is **9.8 %**. The tutorial is **~22.9 % of all match starts** (against `Game:Mode:Solo` 374.95K + `Game:Mode:Multiplayer` 87.61K). **The owner's actual question is SEGMENTATION, not the aggregate** — *"whether some platforms / combinations of platform / browser / device cause the majority of the problem"*, their example being *"mostly skipped on mobile, but played on desktop"*. 🔑 **It may be answerable with NO CODE:** `FlashistFacade.ts:405-406` already sets `setCustomDimension01(mobile/desktop)` and `setCustomDimension02(yandex/web)` — ✅ **verified present in source 2026-09-04** — so the splits may already exist in the dashboard and need only a query. ⚠️ **But that premise is NOT established: `configureAvailableCustomDimensions01/02` is NEVER called anywhere in `src/`** (grep, 2026-09-04 — those two lines are the only `setCustomDimension`/`configure…` hits in the tree), while the method **does** exist in the installed SDK (`node_modules/gameanalytics/dist/GameAnalytics.d.ts:676`). GA generally requires values declared **before** `initialize()` or the set calls are rejected — ⛔ **runtime behaviour of this SDK version NOT verified, so this is a documented-API concern, not a confirmed defect.** **That is why step 1 is "look at the dashboard, not at an editor": if the dimensions are empty the task changes shape into code + deploy + wait, and the owner must be told before anyone proceeds.** 🔴 **SIX CORRECTIONS to the original framing were found by reading the code and are recorded in the brief; two change what the number MEANS:** **(1) THE BIGGEST — repeat attempts inflate `Started`, and only `Started`.** The tutorial is gated on `localStorage` alone (`Main.ts:993`), and `TUTORIAL_COMPLETED_KEY` is written on **complete** (`TutorialLayer.ts:298`, `WinModal.ts:579`) and on **skip** (`TutorialLayer.ts:317`) but on **neither abandonment path** ⇒ completers and skippers contribute **one** start ever, abandoners contribute **another every session**. **The denominator counts attempts and can grow without bound per person; the numerator is capped at one per browser profile** ⇒ **a large share of the 87 % may be a few people bouncing repeatedly, not 92K distinct players walking away.** ✅ **Directly measurable and made the first analytical step:** `Tutorial:Started` carries the **lifetime attempt count as its event value** (`TutorialStorage.ts:5-11`, `Main.ts:778-781`), and `analytics-event-reference.md:313` already names this exact use. **(2) The consistency check is WEAKER than it was framed.** `Completed` + `Skipped` = `Duration` is exact — **but guaranteed by construction**, since all three call sites fire `Duration` beside their end event (`TutorialLayer.ts:299-300`, `:312-316`, `WinModal.ts:580-581`). ✅ It proves the pipeline **delivered without loss or duplication** — real and useful, it rules out a dropped-event explanation — ⛔ **it does NOT corroborate the 9.8 %. Delivery check, not a semantic one.** **(3) `Tutorial:Completed` fuses TWO different outcomes** — *dismissed the last tooltip* (`TutorialLayer.ts:299`) and *won the tutorial match* (`WinModal.ts:580`); **the dashboard cannot tell them apart.** **(4) `TooltipShown` > `Started` is CONFIRMED, not a hypothesis, and it is the most valuable lead** — the string is assembled at runtime as prefix + tooltip number for N=1–7 (`TutorialLayer.ts:215`, enum `FlashistFacade.ts:104`). **If that dashboard row rolls up its seven children, ~1.1 tooltips are shown per start ⇒ the drop-off is at tooltip 1 or 2, not spread across the tutorial** — ⚠️ **the roll-up assumption is UNVERIFIED and must be confirmed in the dashboard**, but if it holds the per-tooltip funnel crossed with platform is **the single highest-value query.** **(5) The reference doc's experiment-gating claim looks STALE** — `analytics-event-reference.md:309` says tutorial events fire *"only for players who see the tutorial experiment"*, but `Main.ts:993` gates on `localStorage` alone with no flag, and cohort events are built at runtime from whatever Yandex returns (`FlashistFacade.ts:823-833`) ⇒ whether an `Experiment:Tutorial:*` flag still exists is a **Yandex-dashboard fact, not a source fact. It decides the DENOMINATOR — resolve it before computing any rate.** **(6) `mobile` is an INPUT-MODALITY proxy, not a device class** — `isMobileDevice()` is `pointer: coarse` **or** an Android/iPhone UA (`Utils.ts:276-281`), so **touch-screen laptops classify as mobile** and iPads arrive via `pointer: coarse`; cross-check against GA's native platform/device dimensions. ⚠️ **CAVEATS THAT MUST SURVIVE INTO ANY WRITE-UP:** **(a)** `Tutorial:Started` fires **before the match starts** — logged at `Main.ts:779-781`, with `await fetchCosmetics()` and the `join-lobby` dispatch only afterwards ⇒ **a tutorial abandoned in that window logs a start with no match**, so 106.08K is an **upper bound** and 9.8 % a **lower bound**; sizing that gap is an explicit step. **(b)** 🔴 **The dashboard showed a persistent "You're viewing data in Demo mode" string in the page markup on EVERY read.** The owner confirmed the figures match their real traffic and the session proceeded on that ruling, **but the banner never cleared** ⇒ **these numbers are owner-confirmed, NOT unconditionally verified**; the brief requires this caveat be carried **verbatim** into the findings report. **(c)** Analytics are **production-only** (`FlashistFacade.ts:397`, the `DEPLOY_ENV === "prod"` guard) — **any verification of any future fix must happen in production**, and no dev signal exists. ⛔ **THIS TASK FIXES NOTHING and scopes NO implementation** — investigation-first per `PROJECT.md`, and this brief documents six unknowns. ⛔ **If the loss turns out to be mobile-concentrated, the fix may be OUT OF SCOPE by an existing rule:** `PROJECT.md:87-88` parks deep mobile rendering optimization until **mobile DAU exceeds 1,500** — quick wins and honest expectation-setting are in, a rewrite is not. **A future planner must NOT assume a mobile fix is automatically in scope; that is an owner call.** **Owner: the OWNER or an agent with browser access to the GameAnalytics dashboard — phase 1 is NOT runnable from the repo alone**; `fkit-coder` only if step 1 shows instrumentation is missing. **Findings go to `ai-agents/knowledge-base/reports/`; ⛔ NOT to `ai-agents/wiki-vault/`** — route an ingest through `fkit-wiki` afterwards. ⚠️ **No secrets: the GameAnalytics game and secret keys sit in `FlashistFacade.ts` beside the dimension calls and are deliberately NOT reproduced in the brief, the board, or any report.** ⚠️ **All line numbers above were read from the WORKING TREE on 2026-09-04 and two cited files were UNCOMMITTED** — `FlashistFacade.ts` (one added line above every citation ⇒ ±1 drift vs `HEAD`) and `analytics-event-reference.md` (in-flight +133/−60 rewrite that **will** move its `:309`/`:313` citations). **Every claim also names its symbol — search by symbol, not line number.** **ID `0212` verified free** — `grep -rn "0212" ai-agents/ .claude/ src/` **zero hits**; highest ID in task folders and on all boards **`0211`**. ⛔ `0204` and `0241`–`0247`/`0264`/`0265` **not free, not considered** (see `0209`'s row). **Row appended, not inserted** (ADR-035))* | [`0212-investigate-tutorial-abandonment-by-platform-segment`](../tasks/backlog/0212-investigate-tutorial-abandonment-by-platform-segment/brief.md) |

**Execution order** for the migration set is `0002 → 0003 → 0001 → 0004`, which is dependency order,
not ID order. `0005`–`0009` are independent of it and of each other.

⚠️ **`0057`–`0059` were filed 2026-08-22 from the production outage that took out all public
multiplayer lobbies.** The outage fix itself is in **Sprint 4** (`0055`, `0056`); these were the
follow-ups *not* needed to restore crash recovery, so they landed here unsprinted rather than
inflating the sprint. Full record:
[`incidents/2026-08-22-prod-public-lobbies-empty-outage.md`](../knowledge-base/incidents/2026-08-22-prod-public-lobbies-empty-outage.md).

**`0057` did not stay here.** Later the same day the owner ruled the readiness-gate quorum
(**18 of 20, 90-second deadline**) and ruled that **`0057` runs before `0056`** — the routing findings
are most useful while that quorum is committed but not yet built. That made `0057` a hard dependency
of a Sprint 4 task, so it was **promoted into Sprint 4**; an unranked, explicitly-unscheduled board is
the wrong place for a blocker on a sprint's critical path. Its row above is kept as `➡️ Moved` rather
than deleted, so the trail from "filed here" to "tracked there" survives.

Note for anyone reading the earlier framing: the misroute estimate was *1 in 20*. Under the ruled
18/20 quorum — which permits **two** missing indices — it is **up to 2 in 20 (~10%)**. `0057` sizes it.

**`0059`** stays here, and remains the only item on the board that might *remove* the 2026-08-22 crash
rather than survive it. Whether it gets pulled into a sprint is an open owner call.

`0008` is **deliberately blocked** on two conditions, not one: `0009`'s findings, **and** the first
paid entitlement going live (paid citizenship, Task 9 flags, or Task 9a patterns). It is the
pre-committed migration from the owner's `adr-102` ruling, filed now so the commitment is a scheduled
task rather than a promise inside a document.

⚠️ **`0009` grew teeth on 2026-08-09.** Cosmetic entitlements (`flares`) come from the **upstream
OpenFront API**, not from Geoconflict's own infrastructure — so it blocks the cosmetics monetization
path, not just the archive task. The board is unranked by design, so there is no rank to raise; the
owner's ruling is that `0009` is **pulled into a sprint ahead of any cosmetics monetization work**,
and that ordering takes effect at pull time.

IDs were allocated when each brief was written; ordering lives in each brief's `**Depends on:**` line.

## The cosmetics monetization chain

`0010` (flags) and `0011` (patterns) are the **root** of a dependency chain that two later sprints
already assume is done:

```
0009 (entitlement origin)  →  0010 flags + 0011 patterns
                                   ↓
                    Sprint 5 Task 8a (nickname styling)
                    Sprint 5 Task 15 (custom uploaded flags/patterns)
                                   ↓
                    Sprint 6 paid map packs (purchase surface)
```

Until 2026-08-09 neither root had a brief, a sprint, or an owner — the chain rested on nothing.
`plan-index.md:87-88` assigns both to Sprint 4, but neither appears in the Sprint 4 plan document.
Both are now briefed and blocked, which is honest; previously they were invisible.

## Provenance

`0001`–`0004` were written during project initiation (2026-08-08). `0005`–`0011` came out of the
open-questions interview and its follow-ups on 2026-08-09, each authorised by an owner ruling
recorded in that session.

`0050`–`0052` surfaced during task `0002` (the `tasks/backlog/` folder migration) and were
owner-approved for briefing on 2026-08-10. They are the migration's three recorded residuals:
`0050` the unparseable `## Sprint` field, `0051` the bare legacy identities in the knowledge-base,
`0052` the wiki-vault references the migration deliberately excluded (plan decision D3).

`0051` and `0052` both **depend on `0003`** and are not pullable until it lands. `0052`'s ordering was
an open question when it was drafted; the owner ruled on **2026-08-10** that it runs **after `0003`**,
in one pass covering both migrations — so it now covers `0003`'s renames too, not just `0002`'s.

`0069`–`0070` were filed 2026-08-24 from `0063`'s owner-accepted reframe (see that task's worklog):
`0069` is the product decision the reframe surfaced (auth service vs Yandex-only identity), `0070` the
split-out silent-failure fix whose direction depends on it. Neither gates anything in Sprint 4 —
`0063`'s in-sprint scope proceeds regardless.

`0071`–`0073` were filed 2026-08-24 from the open-questions interview's owner rulings (relayed via
the lead session): `0071` the mid-zoom label-density tuning the owner asked for after judging
`0041`'s shipped labels too cluttered; `0072` the deploy-time config guard answering the
`0062`/`0063` silent-misconfig pattern — **cancelled the same day, owner-ruled: it duplicated the
pre-existing, board-invisible `0064-deploy-time-config-parity-guard`; its two useful specifics were
merged into `0064`'s brief**; `0073` the `0025` audit's §H3 inert-leftover cleanup. The
same interview also produced a no-task ruling recorded in `0066`'s brief Notes (standalone footer's
upstream mentions KEPT; no new upstream-brand mentions). None of the three gates anything in Sprint 4.

`0209`–`0210` were filed 2026-09-03 from `0206`'s out-of-scope items — **both are pre-existing defects in
the client's platform-leaderboard reporting, neither is a `0206` regression, and neither gates `0206`.**
`0209` (the `placement = +1` literal) was filed on the owner's live ruling *"Accept now, brief it
alongside"*; `0210` (Singleplayer awarding first place for losing to a bot) comes from `0206`'s plan §8
item 2, and **the decision to give it its own brief rather than a section inside `0209` is the
producer's, not an owner ruling** — the two sit on orthogonal axes (*what the reported number means* vs
*which game modes report at all*). ⚠️ **Both turned out to be design decisions, not mechanical fixes**:
`0209` was rescoped mid-filing after the `fkit-coder` on `0206`'s review verified that `myIndex + 1` is a
**human tile-rank, not a placement**, and that under **ADR-110** the declared winner may be an AI player
absent from that ranking entirely. 🔴 **A correction to `0206`'s R2 finding is recorded in `0209`'s brief
and NOT in `0206`'s ledger** (that folder was in active use): R2 says the wrong placement is *sent to the
Yandex leaderboard* — ✅ verified that it is **not**; `reportPlacement()` passes only `points` to the
platform and `placement` reaches nothing but a `console.debug`. **R2's low severity stands; its mechanism
does not.** Neither task gates anything in Sprint 4.

**Three owner rulings on this pair, all given live in session 2026-09-03**, recorded in both briefs
strike-don't-delete: (1) **`0210` → option A, Singleplayer reports nothing** — unranked practice against
bots, trivially farmable, so its points devalue the board for real multiplayer play, and it is the
simplest guard to reason about; ⚠️ the guard must be **added, not repaired** (there is no game-type
awareness in that path at all) and **`reportParticipation()` is in scope**, not just `reportPlacements()`.
(2) **`0209` → `placement` means rank among humans** — it agrees with the points, which are already
human-relative; 🔴 **this rules the definition, not the implementation — `myIndex + 1` remains
un-prescribed** and the `0022`-style prohibition stands. (3) **The split is kept**, confirmed on its
merits after the owner read the producer's own weakening of the argument — the axes stay orthogonal and
`0210` carries harm `0209` does not. **The rejected options are recorded with reasoning in each brief so
nobody reopens them as unweighed**, and the earlier `0209`-before-`0210` ordering caveat is now **moot**.
Both tasks are **unblocked and ready to plan**, owner **fkit-coder**, in either order or in parallel.
~~🚩 **One question is still open and was NOT ruled:** whether `0208`'s measurement scope should extend to
**Singleplayer** incidence. It has not been put to the owner, **`0208` is left exactly as filed**, and it
gates neither task — but it is worth asking before `0210` ships, since the guard makes the original rate
unobservable afterwards.~~

📌 **RULED 2026-09-03, later the same day — *"Add it — measure both."*** The question above **was** put to
the owner and answered: **`0208` is widened** to measure **Singleplayer platform-leaderboard award
incidence** (non-tutorial, via **both** `reportPlacements()` **and** the unguarded `reportParticipation()`)
alongside the multiplayer clientless-leader rate. **The owner's reasoning:** `0210`'s ruling is that
Singleplayer reports **nothing**, so once that guard ships **how often it was happening becomes
permanently unobservable** — measuring first is what tells the owner whether `0210` mattered at all.
🔴 **This changed `0208`'s SCOPE ONLY. It did NOT change `0209`'s or `0210`'s status, scope, owner or
priority, and it does NOT gate `0210`** — that ruling was never conditioned on incidence. **All three
tasks remain unscheduled.** ⚠️ **The struck text is kept, not deleted**, so a later reader can see this
was a real open question that was actually asked and answered — not a gap quietly tidied away.
