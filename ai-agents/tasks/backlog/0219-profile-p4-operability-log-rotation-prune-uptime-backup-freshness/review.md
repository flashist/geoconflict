# Review — 0219

Task: ai-agents/tasks/backlog/0219-profile-p4-operability-log-rotation-prune-uptime-backup-freshness/brief.md
File(s) under review: setup-profile.sh · build-deploy-profile.sh · profile-checks.sh (new) · example.env.profile · tests/profile-checks.sh (new) · tests/scripts/profile-deploy-hardening.test.sh · tests/scripts/ShellHarnesses.test.ts · CLAUDE.md · ai-agents/knowledge-base/container-log-retention.md · ai-agents/knowledge-base/profile-backup-restore-runbook.md (`:23` cell only)
Status: closed-out

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1 | low | profile-checks.sh:119,137,182,205,242 | Threshold overrides are used unvalidated. A non-integer `PROFILE_CHECKS_MAX_*` (e.g. `abc`) makes the `[ -gt ]` tests in checks 1/3/4 error (rc 2 → false) and fall through to **OK** (verified: `[ 5 -gt abc ]` → "integer expression expected", else-branch taken); a non-integer `PROFILE_CHECKS_CERT_MIN_DAYS` aborts check 6's `$((CERT_MIN_DAYS * 86400))` under `set -u` **before `report()` pings** (verified: `abc` → "unbound variable", `1x` → "value too great for base", rc 1). Cron runs carry no overrides, so blast radius = a manual/B7/B9 operator run only. A `case` guard (like `check_renewal_errors` already does for the offset) would close it. Raised by Codex (X1); verified. Defect. |
| R2 | 1 | low | profile-checks.sh:96 (and :50) | A present-but-malformed `backup.env` / `checks.env` is sourced under `set -e` and aborts the run before any check or ping (verified: a syntax-error line → rc 2 at the `.`). The dead-man's switch still alerts on the missing ping, so this is not silent — but the alert carries no body naming the cause. Both files are written by `setup-profile.sh` with `%q`, so it needs a hand edit to occur. Raised by Codex (X2); PARTIALLY CORRECT — alert still arrives, only the diagnostic is lost. Frontier-adjacent (the design explicitly leans on missing-ping for "script broken"); coder's call whether to guard the source in a subshell. |
| R3 | 1 | low | profile-checks.sh:196-209 | Coverage gap: check 4 counts ANY certbot invocation as "attempt", and B1 showed `certbot.timer` (no nginx hooks) also writes `letsencrypt.log` at random times. So a deleted/broken hooked cron (the only path that can actually renew, `setup-profile.sh:1093`) keeps check 4 **OK**, and its port-80 failures do not reach `certbot-renew.log` (accepted R2), so check 5 stays OK too; only check 6 fires, at 20 d before expiry (~10 d after the first due attempt). Not silent — check 6 backstops — but the worklog's "check 4 = the twice-daily renew cron ran" reading is stronger than what the code proves. B1 evidence shows the log records each run's argv (`--pre-hook …`), so matching that line within the window would make check 4 mean what it says. Distinct from residual R2 (that is about error visibility, this is about attempt attribution). Defect (coverage), non-blocking. |
| R4 | 1 | nit | profile-checks.sh:153-156 | Check 2 proves existence only: a `Size` of `0` (or any size) reads OK. The marker carries `size_bytes`; comparing the two would turn "exists" into "exists and is the object the marker verified" at no cost. The upload path already verified size once (`profile-backup.sh:166-167`), so this is defence in depth, not a hole. |
| R5 | 1 | nit | CLAUDE.md:192 | Harness row says `~1 s`; measured 3.9 s under the jest wrapper on this host (59 assertions, each an `env -i bash` spawn). Timeout is 180 s so nothing breaks; the number is just off. |

Disproven / not recorded as rows (so the coder is not asked to chase them):
- Prune can remove the rollback or running image — **INCORRECT**. Keep-list = `docker ps -aq` images ∪ `PROFILE_IMAGE` ∪ `PREV_PROFILE_IMAGE` (`setup-profile.sh:793-802`); ID formats agree (`{{.Image}}`, `{{.Id}}`, `images -q --no-trunc` all `sha256:` full). `docker image prune -f` cannot eat the digest-pinned, untagged previous image: verified on local Docker that an image pulled as `repo@sha256:…` with no tag is NOT listed by `dangling=true`. Section sits after the rollback branch (`:552-583`, which `exit 1`s) and after HTTPS, before systemd (`:776-816`) — the harness asserts the order.
- `set -e` in the prune loops — **INCORRECT** as a risk: `[ -n "$x" ] && …` as a loop's last command returns 1 but `set -e` does not fire on it (verified: `for i in 1; do [ -n "" ] && echo x; done` under `set -e` continues).
- A check aborting before the ping — no path found besides R1/R2 above: every `$( … )` helper ends in `|| true`, `rclone`/`openssl`/`curl` are `command -v`-guarded, the `for f in "$LE_LOG" "$LE_LOG".*` unmatched glob is `[ -f ]`-filtered, GNU `date -d`/`stat -c` are first with BSD fallbacks. `rclone lsjson` on a single object path works (verified locally: file → one entry rc 0; missing → rc 3).
- Vixie `%` — no `%` in the new cron line (`:1057`); logic lives in `checks.sh`.
- Secret hygiene — ping URL travels only via the 0600 staged export (`build-deploy-profile.sh:562`) → `checks.env` 0600 (`setup-profile.sh:1006-1012`) → `curl` argv on the box; never echoed, never in a git-tracked file; T11 asserts it is absent from every deploy argv. `/fail` body: check names, ages, counts, object basename, `notAfter`, certbot-renew tail (certbot text; no creds), marker `error` (only `die` strings from `profile-backup.sh` — no bucket/endpoint reach it). C17 greps every run's log + body for the synthetic key/secret/bucket/endpoint/URL (21 bodies, non-vacuous).
- Harness false-green — none found: `run_deploy` allow-list is the explicit `env -i` set + caller extras; the compose/prune/cron awk extractions fail loudly when empty; T11 drives the real script. Both harnesses + the jest wrapper re-run green here (`RESULT: 59 passed, 0 failed`, `ALL PASS`, 4/4 wrapper tests).
- Docs vs code — accurate except R5: 100m × 10 on both services, 2 GB ceiling, no `daemon.json`, keep-list wording, runbook `:23` semantics all match the code.

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect (low — operator runs only; cron passes no overrides) | Re-verified: `[ 5 -gt abc ]` → false branch (silent OK); `$((abc*86400))` → `unbound variable` rc 1, `1x` → `value too great for base` — both before `report()`. Added `int_or_default` guard (`profile-checks.sh:90-104`): junk → `fail "thresholds" …` + default stands in, run reaches the ping; `10#` normalises leading zeros; `0` stays valid (B9 uses `MAX_WEEKLY_AGE_DAYS=0`). Harness C18 (+7 assertions). Applied under the ADR-032 standing approval — in-plan (plan: "each check … reports FAIL rather than aborting the run before the ping"), mechanical, localized. | ✅ done |
| R2 | PARTIALLY CORRECT | Frontier-move | Re-verified and **stronger than claimed**: a syntax error in a sourced file kills the shell even inside `if ! . file` (rc 2) — the only guard is a subshell pre-parse (`( . file )`), a new mechanism the plan does not carry. The plan's edge-case list already rules the outcome: "Checker crashes … → no ping → dead-man alert." Both files are `%q`-written by `setup-profile.sh` (`:1006-1012`); only a hand edit can trigger it; the missing-ping alert still pages, only the body is lost. Proposed as an accepted residual; **owner ruled it accepted, 2026-09-13** (relayed by `fkit-sprint-ship-loop`) — recorded below. | won't fix (frontier) |
| R3 | CORRECT | Defect (coverage, low — check 6 backstops at 20 d) | Re-verified: `certonly` (`setup-profile.sh:708-713`) passes no hooks, so the renewal conf carries none and `certbot.timer`'s hookless `renew` can never succeed behind nginx, yet it writes `letsencrypt.log` and keeps check 4 OK if the hooked cron dies. Returned as `NEEDS-DECISION` (A: disable `certbot.timer` via `setup-profile.sh` · B: argv-match the cron's `--pre-hook` line · C: residual on check 6's backstop). **Owner ruled (A), 2026-09-13** (relayed by `fkit-sprint-ship-loop`). Applied: `setup-profile.sh` HTTPS section, after `certonly` and before the hooked renew cron — `systemctl disable --now certbot.timer >/dev/null 2>&1 \|\| true` (inside the `PROFILE_DOMAIN` guard, idempotent, never fails the deploy); hardening harness asserts presence with `\|\| true` and the line order certonly < disable < renew-cron; check 4's comment/messages (`profile-checks.sh:216-218`, `:232`/`:234`) now say "the hooked renew cron ran" — true once the timer is gone. Lands on the box at B4; B7(d) confirms `systemctl is-enabled certbot.timer` → `disabled`. | ✅ done |
| R4 | CORRECT | Defect (nit — defence in depth) | Check 2 now compares remote `Size` to the marker's `size_bytes` (`profile-checks.sh:169-181`): mismatch → FAIL naming both sizes; no `size_bytes` in the marker → existence still suffices (both markers carry it — `profile-backup.sh:48-65` writes one shape). Harness C6 (+2 assertions). Applied under the standing approval — in-plan spirit (check 2 = "the object the marker names really exists"), mechanical, localized. | ✅ done |
| R5 | CORRECT | Defect (doc) | `CLAUDE.md:192` `~1 s` → `~5 s (measured 5.0 s under the jest wrapper on this host …)` — re-measured after the +9 assertions from R1/R4 (4.96 s), so the number differs from the reviewer's 3.9 s for that reason. Applied — mechanical. | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)
- **Malformed hand-edited `backup.env` / `checks.env` kills the checker before any check or ping** (R2; owner ruling 2026-09-13, relayed by `fkit-sprint-ship-loop`) — What: the run dies at the `.` source (`profile-checks.sh:50`, `:96`); the dead-man's switch pages on the missing ping, with no body naming the cause. Why (structural): both files are `%q`-written by `setup-profile.sh` (`:1006-1012`), so only a hand edit can produce this; a syntax error in a sourced file kills bash even inside `if ! . file` (verified, rc 2), so the only guard is a subshell pre-parse — a mechanism the approved plan does not carry — and the plan's edge-case list already rules "checker crashes → no ping → dead-man alert"; the alert direction is loud. Rejected: subshell pre-parse (new mechanism for a hand-edit-only failure); `bash -n` pre-check (same). Re-raise only if: a checker run is observed to die this way in production, or the env files gain a writer other than `setup-profile.sh`.
- (the worklog's R1–R9 remain the coder's build-time residuals; promote here only on the owner's say-so)
