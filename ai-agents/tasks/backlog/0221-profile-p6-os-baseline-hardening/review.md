# Review — 0221

Task: ai-agents/tasks/backlog/0221-profile-p6-os-baseline-hardening/brief.md
File(s) under review: src/profile-server/Shutdown.ts (new) · src/profile-server/Server.ts · tests/profile-server/Shutdown.test.ts (new) · Dockerfile.profile · setup-profile.sh (0221 sections only: unattended-upgrades, fail2ban, sshd hardening, compose `restart`/`init`, header + banner — the 0220 `persist_or_reuse_secret` / `report_config_values` hunks are excluded, reviewed under 0220) · build-deploy-profile.sh · example.env.profile · profile-checks.sh · tests/profile-checks.sh · tests/scripts/profile-deploy-hardening.test.sh (0221 block only)
Status: closed-out

Scope base: working tree vs `HEAD` (`6822210`). Round 1 reviewers: own pass + Codex adversarial pass (`codex exec --sandbox read-only`, completed, 5 findings). Gates run by the reviewer this round: `npx jest tests/profile-server/Shutdown` ×3 → 7/7 each (0.31–0.36 s); `npx jest tests/profile-server tests/scripts` → 15 suites / 249 tests pass (33.6 s, includes the shell-harness wrapper); `bash tests/scripts/profile-deploy-hardening.test.sh` → `ALL PASS`; `bash tests/profile-checks.sh` → `RESULT: 73 passed, 0 failed`. Relevant ADRs: none (101–111 are game/product decisions; no ADR touches sshd/fail2ban/shutdown).

## Reviewer findings
| #  | Round | Sev    | file:line | Claim |
|----|-------|--------|-----------|-------|
| R1 | 1 | medium | setup-profile.sh:477-483 | **Lock-out path the guard's own comment claims to close is still open.** Guard 1 proves only that *some* key is in `/root/.ssh/authorized_keys`, not that the *deploying operator* holds it. On the documented first-provision path (`example.env.profile:46-49`: the password fallback "can only work for a brand-new box's first provision") a cloud image typically ships a provider-injected key; if the operator is on `sshpass` and does not hold that key, the section turns password auth off and the operator is locked out once the session ends. The section comment ("never disable password auth on a box with no key path in — the operator may be on the password fallback right now") describes a check the code does not make. Today's box: 1 line in authorized_keys, the deploy key (0215), so the live risk is low; the structural gap is real. Fix shape (coder's call): `build-deploy-profile.sh` already stages `export` vars into the remote shell (`build-deploy-profile.sh:575-588`) — stage the auth mode (`key`/`password`) or the deploy key's fingerprint (`ssh-keygen -lf`), and have the sshd section refuse to harden unless the current deploy authenticated by a key that is in `authorized_keys`. Needs the staged-variable harness allow-list update the coder already noted. Defect. |
| R2 | 1 | medium | src/profile-server/Shutdown.ts:81-103 | **The 8 s deadline bounds the HTTP drain but not `pool.end()`.** Normal path: `clearTimeout(deadline)` at :95 fires before `closePoolOnce()` at :103 settles; a `pool.end()` that never settles (pg-pool waits for checked-out clients to be released) means `exit(0)` never runs and the container sits until Docker's 10 s SIGKILL (exit 137 — the pre-0221 shape). Deadline path has the same hole: `closePoolOnce().finally(() => exit(1))` at :87 also depends on `pool.end()` settling. No request is lost (the drain completed), so this is the stated "SIGKILL never races the drain" promise being narrower than the header implies, not data loss. `Shutdown.test.ts:257` covers a *rejecting* `pool.end`, not a never-settling one. Fix shape: keep the deadline armed until `exit` is called (a `closed` flag instead of `clearTimeout`), or race `pool.end()` against the remaining budget. Raised by both. Defect. |
| R3 | 1 | medium | setup-profile.sh:490, 513-516, 540-545 | **On a redeploy the two rollback paths delete a previously working drop-in; "state exactly as before" holds only for a first deploy.** `cat >` at :490 overwrites `00-geoconflict-hardening.conf` unconditionally; both failure paths `rm -f` it. A redeploy whose gate fails for a reason unrelated to the drop-in's content (e.g. `sshd -T` failing transiently → `? ?` for every key, or a new file sorting before `00-`) leaves the box with root password auth re-opened by `99-qemu.conf` until the next successful deploy. Not a lock-out (key path guarded; abort is loud and says "Drop-in removed"), so medium not high (Codex: high). Fix shape: back the existing file up before the write and restore it (not delete) on both failure paths — today the bytes are identical, so restore == leave in place. Raised by both. Defect (comment/behaviour mismatch on the safety-critical section). |
| R4 | 1 | low | setup-profile.sh:519 | `systemctl reload ssh 2>/dev/null \|\| systemctl reload sshd` under `set -e`: if the real reload fails, its error text is hidden by `2>/dev/null`, the fallback fails with "unit sshd not found", and the script aborts BEFORE the effective-config gate with the drop-in left on disk. It passed `sshd -t` and the key path is guarded, so the next sshd start activates a stricter-only config — no lock-out — but the gate never ran on it and the operator does not see why the reload failed. Fix shape: `if ! { systemctl reload ssh \|\| systemctl reload sshd; }; then rm -f drop-in; abort` with stderr visible. Raised by both. Defect (minor). |
| R5 | 1 | low | setup-profile.sh:443 | `systemctl restart fail2ban` under `set -e` aborts before the explicit `fail2ban-client status sshd` gate if fail2ban fails to restart on the new jail file — the jail file stays and fail2ban is down. Outcome is the same fail-closed abort the gate would produce, only without the gate's diagnostic message; the leftover file is what one would inspect anyway. Not a rollback the plan promised. Codex-only. Informational/defect-minor. |
| R6 | 1 | low | setup-profile.sh:407 | `unattended-upgrades --dry-run --debug 2>&1 \| grep -iA4 … \| head -8 \|\| true`: `--dry-run` still DOWNLOADS pending packages (apt's "simulate, download but do not install"), and `head -8` closing the pipe can SIGPIPE `unattended-upgrades` mid-download once grep has emitted 8 lines (two "allowed origins" hits = 10). Harmless (dry-run, fcntl lock released on death) but it adds deploy time on a box with pending updates and can truncate the very evidence line the step exists to print. Fix shape: drop `head`, or grep the log line only (`Allowed origins are:`). Own pass. Defect (minor). |
| R7 | 1 | low | tests/scripts/profile-deploy-hardening.test.sh:794-798 | Harness gaps in the lock-out-relevant assertions: (a) the fail2ban gate assertion passes if `fail2ban-client status sshd` appears anywhere in the section and ANY `^\s*exit 1$` exists — it would stay green if the status call were `\|\| true`d; (b) the gate-failure rollback (`rm -f` + reload after `SSHD_GATE_FAILED`, :540-545) is not asserted at all — only the `sshd -t` removal is. Both false-green shapes. Raised by both (b own). Defect (test). |

Verified-OK — not findings, recorded so the coder is not asked to chase them:
- `sshd -T -C user=root,host=localhost,addr=127.0.0.1` spec is correct (all three of user/host/addr given; `laddr`/`lport` optional — a `Match LocalPort`/`Match Address <other range>` block would simply not match in test mode, the residual the worklog already records). Plain `sshd -T` skips Match blocks, so the `-C` probe is what makes the gate honest.
- `Match all` in the `00-` file wins over `99-qemu.conf`'s `Match User root`: in the match pass the first matching block to set a keyword wins; include-file Match state is restored per file, so the main `sshd_config` globals after the Include are untouched (coder's Docker probe on `ubuntu:26.04` + `debian:12` agrees).
- `sshd -t` runs before the reload, and `-t` failure removes the drop-in on the spot; reload not restart; the deploy's own session survives.
- IdentitiesOnly: the `build-deploy-profile.sh:377-381` comment slightly overstates — fail2ban's sshd filter (normal mode) marks `Failed publickey for <valid user>` as `<F-NOFAIL>`, so single rejected keys do not count; the real trigger is sshd's `Too many authentication failures` at ≥ `MaxAuthTries` (6) offered keys, which the filter does count. The fix is still correct and the comment's wrong detail changes nothing.
- Multiple deploys inside `findtime`: every deploy's ssh/scp calls are successful auths — nothing counts. Only loopback in `ignoreip` is a settled tradeoff (plan Q3).
- `#clear Unattended-Upgrade::Allowed-Origins;` in `52…` after the package's `50unattended-upgrades` is the right precedence (lexical, later wins; lists append unless cleared). `${distro_id}:${distro_codename}-security` is the Ubuntu pocket name.
- `Server.closeIdleConnections`/`closeAllConnections` exist since Node 18.2; Node 24 in the image. No second SIGTERM/SIGINT handler anywhere in `src/profile-server/` (only `migrate.ts`'s `process.exit`, a different entry point). No test imports `Server.ts`, so `install(process)` never runs inside a jest worker.
- Deadline vs close-callback race: `timedOut` flag + `clearTimeout` make a double `exit` impossible (single-threaded ordering); rejection of `pool.end` is caught in `closePoolOnce` and still exits.
- `Shutdown.test.ts`: real port 0 on 127.0.0.1, 200 ms deadline against a request parked forever (always the deadline wins), 3 runs at ~0.3 s each, no flake observed. ECONNREFUSED after `server.close()` is deterministic (the listen fd is closed synchronously inside `uv_close`).
- `Dockerfile.profile` CMD joins byte-for-byte to `package.json`'s `start:profile-server` (harness `node -e` assertion checks it); `--experimental-specifier-resolution=node` is accepted by node 24.13.0 locally. `init: true` + exec-form node: docker-init is PID 1 and forwards SIGTERM/reaps; correct.
- Section placement: under the flock, after `ufw --force enable`, before Directories and before the stack is touched — an abort in any 0221 section leaves the running stack untouched.
- `profile-checks.sh` check 8 uses `-e` (any inode at the path) — fine; C19 runs before C17's leak guard so it is covered; `tests/profile-checks.sh` is NOT in `ShellHarnesses.test.ts` (0219's residual, not 0221's — the hardening harness that IS gated covers checks.sh structurally).

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect | **Owner ruling: FIX NOW.** `build-deploy-profile.sh` sets `DEPLOY_SSH_AUTH=key\|password` in the branch it takes and stages it as `PROFILE_DEPLOY_SSH_AUTH` through the existing 0600 export block. `setup-profile.sh` sshd section: new **guard 0**, first in the section — `password` → abort with a clear message (install the key, redeploy over the key path). Guard 1's comment now claims only what it checks (SOME key present, not the operator's). Consequence, documented in the password branch + `example.env.profile`: a password-mode deploy now aborts at the sshd section, so the fallback cannot complete a first provision either — bootstrap = `ssh-copy-id` first. Harness **T12** drives the REAL deploy in both modes and asserts the staged value; then RUNS guard 0 (extracted up to `# Guard 1:` so the host's `/root/.ssh` is never read) with `password` → rc≠0 + message, `key` → rc 0. Severity confirmed medium (live risk low: 1 key, the deploy key; structural gap real). | ✅ done |
| R2 | CORRECT | Defect | Deadline now armed until `exit(0)`: `server.close` cb no longer clears it; `closePoolOnce().then(...)` clears + exits 0 only if the deadline has not fired. Deadline path: warn (distinct text when the server had already closed — "pg pool did not close in time"), `closeAllConnections()`, start the pool close best-effort, `exit(1)` **without awaiting the pool** (the budget is spent; postgres reaps the connections). New test: never-settling `pool.end` → exit 1 at the deadline, log sequence asserted. Case 4's "pg pool closed" assertion dropped — it only held because the fake `exit` does not exit. Negative control run: the new test **hangs to jest's 5 s timeout on the pre-fix logic**, passes on the fix. Severity medium confirmed (no data loss; the promise the header made was narrower than stated). | ✅ done |
| R3 | CORRECT | Defect | **Owner ruling: FIX — restore, not delete.** `SSHD_DROPIN_BACKUP=/etc/ssh/geoconflict-hardening.conf.previous` (outside `sshd_config.d/`, so never Included); stale backup removed, then an existing drop-in is `cp -p`'d before the `cat >`; `sshd_rollback()` = `mv` the backup back if one exists, else `rm` the new file; used on `sshd -t` failure, reload failure (R4) and gate failure; backup discarded only after the gate passes. Harness: backup path outside `sshd_config.d/`; `sshd_rollback()` restore-else-remove body; exactly ONE `rm -f "$SSHD_DROPIN"` in the section (inside the function — no failure path deletes); order backup < write; backup discarded after the gate. | ✅ done |
| R4 | CORRECT | Defect (minor) | `sshd_reload()` tries `ssh` then `sshd`, capturing both stderr streams to a temp file that is printed only when BOTH fail — no `2>/dev/null` left. `if ! sshd_reload; then sshd_rollback; sshd_reload \|\| true; abort`. Harness: block shape asserted; `systemctl reload ssh 2>/dev/null` is now a RED. The order awk moved to the CALL sites (`if ! sshd -t`, `if ! sshd_reload`) because the helper definitions now sit above them. | ✅ done |
| R5 | CORRECT | Defect (minor) | `systemctl enable --now fail2ban \|\| echo ⚠️…` and `systemctl restart fail2ban \|\| echo ⚠️…` — the `F2B_JAIL_UP` gate is the single decider and its diagnostic is always reached. Harness asserts both are non-fatal. Verified: the outcome was already fail-closed; only the diagnostic was unreachable. | ✅ done |
| R6 | CORRECT | Defect (minor) | Dry run captured to `$(mktemp)` (`> "$UU_DRYRUN_LOG" 2>&1 \|\| true`), grepped afterwards, `head` dropped, file removed. Verified against `unattended-upgrade`'s own help: `--dry-run` = "simulation, download but do not install" — the download side effect is inherent to the evidence; recorded as a residual (deploy time on a box with pending updates), not fixed. Harness: captured-not-piped assertion (single-pipe regex; `\|\| true` is allowed). | ✅ done |
| R7 | CORRECT | Defect (test) | (a) fail2ban gate: the exact probe line (`if fail2ban-client status sshd … F2B_JAIL_UP=1`) + `exit 1` inside the `if [ "$F2B_JAIL_UP" != 1 ]` block (awk-scoped). (b) gate-failure path: `sshd_rollback` → `sshd_reload \|\| true` → `exit 1` inside the `SSHD_GATE_FAILED` block (awk-scoped). Negative control: the new harness against pre-fix snapshots of the five files it reads → **15 ❌, all in the new/changed 0221 assertions; 206 ✅ elsewhere**; repo → **221 ✅ / 0 ❌ `ALL PASS`** (was 204). | ✅ done |

Round-1 close-out notes: all seven are defects, none re-litigates a settled tradeoff (no prior residuals; no ADR in scope). R1 and R3 were owner-ruled fixes relayed by the sprint driver; R2, R4–R7 were applied under the loop's standing approval as verified-CORRECT, localized, in-plan fixes (ADR-019 discipline; decision log in `worklog.md`). Owner question **Q3** (reboot-required check) was ruled *page daily until rebooted, as built* — recorded below as an accepted residual, no code change. No commit made by this step.

## Accepted residuals (shared, do-not-re-litigate)
- **reboot-required pages daily until rebooted** (owner ruling 2026-09-13, reviewer Q3) — What: `profile-checks.sh` check 8 is FAIL-class; while `/var/run/reboot-required` exists every daily run pings `/fail`, so the dead-man's switch pages each day until the operator reboots. No warn-only class is added. · Why (structural): with `Automatic-Reboot "false"` (Q1) this check is the only signal that a security patch is pending activation; a warn-only class would be a second severity nobody reads, and the daily page is the intended nag. Rejected: a warn-only class; suppressing after N days. · Re-raise only if: the page rate becomes a real operator cost (e.g. more than one such window a month), or a maintenance-window reboot is automated.
- **password fallback cannot complete a provision** (consequence of the owner's R1 ruling) — What: a deploy over `ALLOW_PROFILE_SSH_PASSWORD_FALLBACK` now aborts at the sshd section (guard 0), after ufw/unattended-upgrades/fail2ban and before Directories/the stack. Bootstrapping a fresh password-only box = `ssh-copy-id` the deploy key, then deploy over the key path. · Why (structural): turning password auth off on a session that came in by password is the exact lock-out R1 names; the only safe orders are "key first, then harden". Rejected: skipping the sshd section on a password deploy (ships an unhardened box with a green deploy). · Re-raise only if: a first-provision path that installs the deploy key itself is wanted (that is a new feature, not a fix).
