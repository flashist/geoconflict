# Review — 0056

Task: ai-agents/tasks/done/0056-restore-worker-crash-recovery-and-survivable-scheduling-gate/brief.md
File(s) under review: src/server/WorkerSupervisor.ts (new, 276 lines), src/server/Master.ts (working tree vs HEAD `2d1135c`, numstat `+56/−66`, of which 0193's already-reviewed `+17/−10` is excluded), tests/server/Master.test.ts (`+652/−11`; only the `describe("WorkerSupervisor")` block, `:291-698`, plus the import hunk `:32-44`, is this task's). Every other dirty path ignored.
Status: closed-out

Round 1 — 2026-08-27. Reviewers run: fkit-reviewer own pass + Codex (`codex-cli 0.145.0`, `codex exec --sandbox read-only`, completed, 6 findings, primed with the settled rulings; it re-raised none of them). Checks re-run by the reviewer this round on the working tree: `npm test` 96 suites / 846 tests pass; `npm run lint` exit 0; `npx tsc --noEmit -p tsconfig.json` exit 0; `jest tests/server/Master.test.ts --randomize` ×2 (seeds −61468811, 1990158607) 26/26; `--detectOpenHandles` 26/26 clean.

Verified as claimed (no row needed): fork-loop guard airtight for every path the supervisor *sees* — `scheduleRestart` (`WorkerSupervisor.ts:233-275`) prunes per index (`:240-243`, `issuedAt > now − 600 000`), gives up at `priorRestartsInWindow >= 5` with no timer and no fork (`:246-261`), and every non-give-up call arms exactly one timer → exactly one `forkIndex`; a duplicate `exit` for a replaced clusterId lands on the `=== undefined` branch (`:108-116`, no restart); `exitedAfterDisconnect` returns before `scheduleRestart` (`:124-131`); a synchronous `fork` throw is routed back into the same cap (`:211-220`). Index 0 survives (`:108`, test #3). `started` is checked-then-set in both `maybeStart` (`:182-187`) and `deadline` (`:151-152`), so `onSchedulingStart` runs at most once in any order (test #6 covers quorum → full → restart → deadline). The 90 s timer is armed once, after the fork loop, so a sync fork failure in `start()` cannot skip it (`:82-87`); `Master.ts:39-41` wires it to real `setTimeout` (live run: deadline line at +90 s). `readyWorkers` gone with zero dangling references (`grep -rn readyWorkers src tests` → none). `cluster.on` handlers at `Master.ts:121-137` precede `workerSupervisor.start()` at `:139`. 0193's `setInterval(() => void lobbyPollTick(scheduleLobbies), 100);` is byte-identical inside `startScheduling()` (`:150-157`); `lobbyPollTick`/`publicLobbyIDs` hunks untouched. `workerSupervisor` is a module-scope export (`Master.ts:32-47`) built after `log` (`:27`) with `startScheduling` a hoisted declaration — no TDZ; importing forks nothing (test #18). `readyIndices()` returns sorted `number[]` — the worker sends `parseInt(process.env.WORKER_ID)` (`Worker.ts:36`, `:461-462`), so the set is number-keyed end to end and `missingIndices()` agrees with it (live run 5.3 named `[16]` only). All new diagnostic lines (died / exited-after-disconnect / restarting / restarted / giving up / quorum / deadline / failed-to-fork) carry values in both text and single-object meta. Tests pin the ruled numbers: #7 asserts 17 → no start, 18 → start; #2 and #13 assert `[1000, 2000, 4000, 8000, 16000]` (+ 30 000 ceiling); #12 arms 5 timers then asserts the 6th death adds no timer, no fork (25 total), `abandonedIndices() = [4]`, one error — it would fail for a cap of 4 or 6; #8 asserts `ms === 90_000`; the timer harness refuses to fire a timer twice (`:336-340`). Temp Step 5.3 hacks fully reverted (`git diff HEAD -- DevConfig.ts Worker.ts` empty; no `FORCE_CRASH`/`TEMP 0056` remnants). Worklog: no `persistentID` values, no endpoints/keys/DSNs. 4a (OTEL) correctly recorded NOT PASSED — owner check pending, not a defect.

Round 2 — 2026-08-27 (phase 2: owner rulings recorded, fixes verified; no new review passes per procedure Step 6). Owner rulings (lead session): R1 → (b) scope extension; R4 → (A) plan-literal, producer re-targets brief step 8's grep at close; R2/R3/R5 → apply; 4a → owner checking Uptrace before close (still NOT PASSED). Verified against the working tree (`src/server/WorkerSupervisor.ts` 316 lines; `Master.ts` numstat `+74/−66`; `Master.test.ts` `+733/−11`, hunks only at the import block `:5-43` and the append after `:55` — 0193's describe occupies the same lines `:86-289` as in round 1 and the 0055 tests are unchanged vs HEAD apart from the import list):
- **R1 attach point — the coder's correction to the ruling's wording is right, reproduced independently.** Reviewer's own Node v24.13.0 cluster probe (`cwd: "/nonexistent"` → async `ENOENT`): no listener → `uncaughtException`, 0 `exit` events; `worker.process.once("error")` → still uncaught, listener never ran; `worker.once("error")` → caught, 0 uncaught, listener ran, `pid` undefined, `cluster.workers` empty. Source (`lib/internal/cluster/worker.js`, read from the binary): the `Worker` constructor installs `this.process.on('error', … this.emit('error', …))` as the first `process` listener; with no `Worker` listener that `emit` throws and aborts later `process` listeners. `Master.ts:34-52` attaches `worker.once("error")` inside the `fork` dep → `handleExit({ clusterId, pid, code: null, signal: null, exitedAfterDisconnect: false, spawnError })`. The map entry is set synchronously after `fork` returns (`WorkerSupervisor.ts:258`) and the event is `nextTick`, so the lookup at `:125` finds the index. `handleExit` `spawnError` branch (`:142-151`): error line `Worker N failed to spawn: <msg>` with `{ workerIndex, clusterId, pid, spawnError }`, `ready.delete`, then `scheduleRestart(index, null, null)` — same window, same cap, ends in `giving up`. Comment at `:246-249` corrected. Test #19 drives 6 async failures → timers `[1000, 2000, 4000, 8000, 16000]`, 7 forks, then no timer/no fork, `abandonedIndices() = [1]`, the `giving up` line. `once` is sufficient: the master never `send()`s to or `kill()`s a worker, so spawn failure is the only `Worker 'error'` source (a hypothetical non-spawn `'error'` on a live worker would schedule a restart while the old process lives; its later `exit` then hits the `=== undefined` branch, so no double fork — noted, not a finding). Not unit-testable: the `Master.ts` listener wiring itself (`cluster.fork` is real) — covered by the probe and the live run 6. `✅ done` stands.
- **R2** `markReady(index, clusterId?)` (`:96-107`): with a sender, `indexByClusterId.get(clusterId) !== index` → warn `Ignoring WORKER_READY …` `{ workerIndex, clusterId }` and return; `Master.ts` passes `worker.id`. Covers the post-exit drain (entry deleted) and a mismatched payload (entry maps elsewhere). A live worker always maps (entry set at fork, READY arrives later). Test #20 asserts stale sender ignored, mismatched payload ignored, live sender accepted. Existing sender-less calls unchanged. `✅ done` stands.
- **R3** meta added at `:109-112` (`{ workerIndex, readyCount, numWorkers }`), `:114-117` (`{ readyCount, numWorkers }`), `:260-264` (`{ workerIndex, clusterId, pid }`); message texts byte-identical. `✅ done` stands.
- **R4** no code; accepted residual recorded below. `won't fix (frontier)` stands.
- **R5** test #10 `:543-548`: `markReady(0)` after the deadline → `onSchedulingStart` still 1. `✅ done` stands.
Checks re-run this round: `npm test` 96 suites / 848 tests; `npm run lint` exit 0; `npx tsc --noEmit -p tsconfig.json` exit 0; `jest tests/server/Master.test.ts --randomize` ×2 (seeds 759294149, −948391210) 28/28; `--detectOpenHandles` 28/28 clean. Step 5.3 hacks still reverted (`git diff HEAD -- DevConfig.ts Worker.ts` empty; no `FORCE_CRASH`/`TEMP 0056` remnants). Worklog: `persistentID` appears only as prose (`:96`, `:318`), no values; no endpoints/keys/DSNs. **Final verdict: ✅ Ready to merge (validation-gated)** — no open findings; the one remaining gate is 4a (owner's Uptrace attribute check), plus brief step 8 post-deploy by design. Status `closed-out`.

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1     | medium | src/server/WorkerSupervisor.ts:209-221 (comment :212), src/server/Master.ts:34-37, :565-567 | The fork-failure path covers only a **synchronous** throw, but Node's `ChildProcess.spawn` (`lib/internal/child_process.js`, read from the v24.13.0 binary) emits `EAGAIN` / `EMFILE` / `ENFILE` / `EACCES` / `ENOENT` **asynchronously** as an `'error'` event on `worker.process` (`process.nextTick(onErrorNT)` → `_handle.onexit(err)` → `exitCode < 0` → `emit('error')`, and **no `'exit'`**); only other errnos throw. `cluster`'s primary registers no `'error'` listener on `worker.process`, so the event throws as an unhandled-error exception, which `Master.ts:565` swallows as a generic `uncaught exception: …` line. Net effect: on a resource-level fork failure the index is **silently lost** — no `exit`, no restart, not counted against the cap, never abandoned, no `giving up` line; only the 90 s audit names it and only if it happens inside the first 90 s. The comment "e.g. EAGAIN" (:212) and plan Step 2 item 4 / test #17 describe a path Node does not take for that errno. The loss shape is **pre-existing** (HEAD had the same hole); what is new is the claim that it is covered. Not a loop, not a crash. Options for the coder: (a) correct the comment/plan to "sync throw only" and record the async case as a residual; (b) in `Master.ts`'s `fork` dep attach `worker.process.once("error", …)` and route it into the supervisor as a death (e.g. `handleExit` with a synthesized `code: null, signal: null` + the errno in the message) so it counts against the same cap — small, but a scope addition the owner should rule on. |
| R2 | 1     | low  | src/server/Master.ts:121-125, src/server/WorkerSupervisor.ts:89-90 | `markReady` trusts the payload index and ignores the sending `worker`: no cross-check `indexByClusterId.get(worker.id) === message.workerId`, no `Number.isInteger && 0 ≤ id < n` guard. Two consequences, both small: (i) a `WORKER_READY` drained from the IPC channel *after* that process's `exit` (Node keeps reading the channel after `exit`) re-adds a dead index for at most the backoff delay (1–16 s) until the replacement reports ready — briefly wrong for the quorum count and, once 0192 lands, for placement; (ii) an out-of-range id would count toward quorum yet never appear in `missingIndices()`. Verified: the worker sends `parseInt(WORKER_ID)` (`Worker.ts:36`, `:461-462`) and `WORKER_ID` is set by the master's own fork, so neither happens with today's code; same trust model as HEAD. Raised by Codex as two highs + a medium; **disproven at that severity** — recorded as optional hardening (the `worker` argument is already in scope at `:121`). |
| R3 | 1     | low  | src/server/WorkerSupervisor.ts:92-97, :224 | Three relocated lines carry no meta object: `Worker N is ready. (x/n ready)` (:92-94), `All workers ready` (:96), `Started worker N (PID: …)` (:224). The plan's Step 3a rule ("every log line this task adds or touches … both as meta and in the message") reads as covering them; the plan's Step 1/3 wording "(existing text)" reads as exempting them. Practical cost: `Started worker` lacks `clusterId`, so the initial clusterId↔index pairing is first visible in Uptrace only at that worker's death line (`Restarted worker` does carry it, :226-229). Raised by Codex (three lows). Cosmetic; no action required. |
| R4 | 1     | low  | src/server/WorkerSupervisor.ts:95-97 | **NEEDS-DECISION (owner), not a defect.** `All workers ready` now logs on every return of the ready set to `n` — 3× in live run 1 (boot + two restarts back to 2/2), 1× on a healthy boot (run 3). Plan-literal (Step 3). The once-only marker the brief's verification step 8 actually wants is `starting game scheduling`: it appears on exactly one of the `Quorum reached …` / `Readiness deadline reached …` lines, guarded by `started`. Options: (A) keep as-is — the repeat is a useful "back to full strength" signal; adjust step 8's grep to `starting game scheduling` (docs only). (B) add a first-time-only guard so `All workers ready` is once per boot (two-line change; alters the plan's stated behaviour). Reviewer recommendation: (A). |
| R5 | 1     | low  | tests/server/Master.test.ts:520-539 | Coverage gap, no code defect: the **deadline-then-quorum** order (deadline fires below quorum, later `markReady` calls reach quorum) has no test; #10's `one` harness stops after the deadline. Code is correct by inspection — `maybeStart` returns on `started` before anything else (`WorkerSupervisor.ts:182`). Optional: one extra assertion in #10 (`one.markReady(0)` → `onSchedulingStart` still 1). |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT (severity medium confirmed — silent loss of an index on a resource-level fork failure; pre-existing shape, new in that the code claimed to cover it) | Defect | Owner ruled (b), scope extension. **Verified first on Node v24.13.0** with a cluster probe: `cwd: /nonexistent` → `ENOENT` arrives as an async `'error'` event, **no `'exit'`** on the worker or on `cluster`, `pid` undefined, `cluster.workers` emptied; `uid: 0` as non-root → **synchronous** `EPERM` throw. **One correction to the ruling's attach point:** `worker.process.once("error")` does NOT catch it — `cluster`'s `Worker` constructor forwards `process 'error'` to itself (`lib/internal/cluster/worker.js`: `this.process.on('error', (code, signal) => this.emit('error', …))`), that forwarder is the first listener, its `emit('error')` on the unlistened `Worker` throws, and the throw aborts the remaining `process` listeners (probe: still `UNCAUGHT`, my `process` listener never ran). Listening on the **`Worker`** (`worker.once("error", …)`) catches it cleanly (probe: no uncaught, listener ran). Applied: `Master.ts` fork dep attaches `worker.once("error", …)` → `workerSupervisor.handleExit({ clusterId, pid, code: null, signal: null, exitedAfterDisconnect: false, spawnError: error.message })`; `WorkerExit.spawnError?: string` added; `handleExit` on `spawnError` logs `Worker <i> failed to spawn: <message>` at **error** (meta `workerIndex, clusterId, pid, spawnError`), removes the index from the ready set and routes into `scheduleRestart` — same cap, same backoff, ends in `giving up`. Comment at the sync-throw `catch` corrected to "synchronous throws only (EPERM / invalid args); EAGAIN & co. arrive via `handleExit` with `spawnError`". Test #17 re-described as the sync path (`spawn EPERM`); new test #19 drives 6 async spawn failures → 5 restarts (1/2/4/8/16 s), 7 forks total, then `abandonedIndices() = [1]` + the `giving up` line. The master sends nothing to workers, so a spawn failure is the only source of a `Worker 'error'` (comment says so). The `Master.ts` wiring itself is covered by the Node probe + live Step 5.1 re-run (0 `uncaught exception` lines), not by a unit test — `cluster.fork` is not mockable here. | ✅ done |
| R2 | PARTIALLY CORRECT (mechanism accurate; severity low as the reviewer assigned — cannot happen with today's sender, so hardening, not a live defect) | Defect (latent) | Owner ruled apply. `markReady(index, clusterId?)`: when `clusterId` is given and `indexByClusterId.get(clusterId) !== index`, log at warn `Ignoring WORKER_READY for worker <i> from cluster worker <id>: not the live process for that index` and return. `Master.ts` passes `worker.id`. Covers both stated cases (stale post-exit drain: the map entry is already deleted; mismatched payload: the entry maps elsewhere). Existing tests call `markReady(index)` without a sender and are unchanged. New test #20. Live run 6: 0 `Ignoring` lines on the real messages. | ✅ done |
| R3 | CORRECT (cosmetic; the plan's Step 3a rule does read as covering touched lines) | Defect (diagnostic completeness) | Owner ruled apply. Meta added: `Worker N is ready.` → `{ workerIndex, readyCount, numWorkers }`; `All workers ready` → `{ readyCount, numWorkers }`; `Started worker N (PID: …)` → `{ workerIndex, clusterId, pid }` — the initial clusterId↔index pairing is now visible at fork time. Asserted in tests #3, #10, #20. Message texts unchanged. | ✅ done |
| R4 | CORRECT (not a defect — plan-literal behaviour; the once-only marker is `starting game scheduling`) | Frontier | Owner ruled (A): keep as-is, no code; the producer adjusts brief step 8's grep to `starting game scheduling` at close. Recorded as an accepted residual below. | won't fix (frontier) |
| R5 | CORRECT (coverage gap, code correct by inspection — `maybeStart` returns on `started` first) | Defect (test gap) | Owner ruled apply. Test #10's `one` harness now calls `markReady(0)` after the deadline and asserts `onSchedulingStart` still 1 (and `All workers ready` meta). | ✅ done |

Round 1 coder pass — 2026-08-27, spawned `fkit-coder` (Process-review worker, `fkit-sprint-ship-loop`, standing approval under the owner-approved plan + the owner's per-finding rulings relayed by the driver). Checks after the fixes: `jest tests/server/Master.test.ts --randomize` (seed 1277313514) 28/28; `npm test` 96 suites / 848 tests; `npm run lint` exit 0; `npx tsc --noEmit -p tsconfig.json` exit 0; Step 5.1 live re-run (run 6, 2 workers, `kill -9` worker 1): died → `Restarting … 1000 ms` → `Restarted` at +1.0 s → ready; `Quorum reached` 1, `could not find id` 0, `Ignoring WORKER_READY` 0, `uncaught exception` 0; real lobby served; all processes stopped, ports free. 4a unchanged: NOT PASSED, owner checking Uptrace. Nothing committed. `0193`'s describe untouched.

## Accepted residuals (shared, do-not-re-litigate)
- Quorum 18/20 as `ceil(9n/10)`, 90 s deadline — What: owner-ruled 2026-08-22, confirmed 2026-08-26 · Why (structural): brief decision (a); plan approval record · Re-raise only if: a diff changes the ratio or the deadline.
- Restart cap 5 per index per rolling 10-min window; backoff 1/2/4/8/16 s with `k` = restarts issued in window (D2); 30 s ceiling enforced but unreachable live — What: plan NEEDS-DECISION 2 → (A) · Why (structural): owner ruling at plan approval 2026-08-27 · Re-raise only if: a diff changes the counting semantic or the numbers.
- New file `src/server/WorkerSupervisor.ts` with injected side effects (D1) — What: the brief's "Master.ts plus tests" scope crossed by exactly this file · Why (structural): owner ruling at plan approval · Re-raise only if: a second new file appears.
- Missing indices at quorum time at `info`; the 90 s deadline audit at `error`; the give-up line at `error` — What: plan Step 3 interpretation, on record · Why (structural): an error line at 18/20 would fire on every healthy prod boot · Re-raise only if: the owner overrides the interpretation.
- Worker routing / placement untouched (ADR-109, 0192); a ready-then-wedged worker stays in the ready set (liveness, not health); games on a dead index are lost — What: recorded, owner-accepted tradeoffs · Why (structural): ADR-109 "Residual risks" · Re-raise only if: a diff touches routing or readiness semantics.
- `All workers ready` repeats on every return of the ready set to `n` (R4) — What: plan-literal Step 3 behaviour kept; the once-per-boot marker is `starting game scheduling` (on exactly one of the `Quorum reached …` / `Readiness deadline reached …` lines, guarded by `started`); brief verification step 8's grep is to be adjusted by the producer at close · Why (structural): owner ruled (A) 2026-08-27 — the repeat is a useful "back to full strength" signal, and a once-only guard would alter the approved plan's stated behaviour · Re-raise only if: a diff makes `starting game scheduling` repeatable, or the owner reverses (A).
- 4a (OTEL attribute delivery) recorded NOT PASSED under D3 — What: `OTEL_AUTH_HEADER` was in no readable env file; owner's Uptrace check pending · Why (structural): brief Step 3a/4a require observation, not a code reading; the message-text fallback is already in the code · Re-raise only if: the owner's check shows the attributes absent AND the message text lacks a value.
