# Review — 0193

Task: ai-agents/tasks/done/0193-guard-fetchlobbies-against-overlapping-ticks/brief.md
File(s) under review: src/server/Master.ts, tests/server/Master.test.ts (working tree vs HEAD `282655c`; every other dirty path ignored — other units of this run)
Status: closed-out

Round 1 — 2026-08-26. Reviewers run: fkit-reviewer own pass + Codex (`codex-cli 0.145.0`, `codex exec --sandbox read-only`, completed). Checks re-run by the reviewer this round: `npm test` (96 suites / 828 tests pass), `npm run lint` (clean), `npx tsc --noEmit -p tsconfig.json` (clean), `jest tests/server/Master.test.ts --detectOpenHandles` (clean, no "did not exit" warning), `--randomize` (8/8 pass). Live re-run of 0057 Appendix run 2 by the reviewer (dev, 2 workers, worker 1 `SIGSTOP` 40 s, sampler 120 × 500 ms, loopback only): `Error fetching game` **1 line per stuck ID** (6 IDs; plan expected 4–8); `Too Many Requests` **0**, `Failed to schedule` **0**; **0 flaps** (`X, [], X`), 39/120 raw empties in two stretches with no live lobby anywhere (replacement create hung on the wedged worker — `0192`'s shape); drain on `SIGCONT` = 6 creates in 4 ms, 5 of them IDs the master had already dropped (orphans = 5, vs 4 in the worklog — random draw); `lsof :3002` 14 ESTABLISHED lines ≈ ≤ 7 connections (worklog 12 ≈ ≤ 6). The worklog's Step 3 numbers reproduce in shape; magnitudes vary with the draw as expected.

Verified as claimed: `fetchLobbies` body byte-unchanged (diff hunks at `:89`, `:128-136`, and an insertion before `:433` only); the three 0055 tests unchanged (hunks at `:8-19` mock, `:21` imports, `:57+` append); `finally` releases the flag on resolve, per-ID abort, whole-`fetchLobbies` rejection and a synchronously throwing `onEmpty` (case 5 exercises it); the check-and-set at `Master.ts:430-431` runs synchronously before the first `await`, so two ticks on one event loop cannot both pass it — with `lobbyPollTick` the sole caller of `fetchLobbies`, at most one poll is outstanding and every `publicLobbiesJsonStr` write is the newest poll's; healthy cadence unchanged (`setInterval` 100 ms kept, `lobbies === 0 → scheduleLobbies()` branch intact; `scheduleLobbies` is fire-and-forget so a hung create does not hold the flag); no `persistentID` values in the worklog.

Round 2 — 2026-08-27 (phase 2: owner ruling recorded, R1 fix verified; no new review passes per procedure Step 6). Owner ruling (lead session): **R1 fixed now**. Verified against the working tree: `src/server/Master.ts` unchanged since round 1 (numstat `+17/−10`, same three hunks `:89`, `:128-136`, insertion before `:433`); `tests/server/Master.test.ts` `+235/−11`. Fix lands as described and is unconditional: every fixture that can outlive its test (`abortsOnSignal`, `pendingUntilReleased`) is built via `trackedPromise()` (`:96-107`), which pushes a settle hook into `settleStragglers` at construction, before the executor runs; `afterEach` (`:156-168`) switches to real timers, calls every hook (`resolve` on an already-settled promise is a no-op), drains one real `setImmediate` (the settle → `resp.json()` → `Promise.all` → `finally` chain is microtasks only, so one macrotask turn suffices), clears the set, then runs the reset tick with a `jest.fn()` and asserts it ran exactly once before asserting the body is `{"lobbies":[]}` — a pinned flag now fails by name in `afterEach` rather than silently. Constraint documented ("FIXTURES MUST SETTLE", `:90-94`). The coder's no-cascade proof (case 1 with `pending.release()` removed → only case 1 fails) was **not re-executed by the reviewer** (it requires editing a source file, which the reviewer may not do); it is supported by the mechanism trace above. Checks re-run this round: `jest tests/server/Master.test.ts --detectOpenHandles` 8/8 clean; `--randomize` ×3 (seeds 677724358, 1285158684, −419580500) 8/8 each; `npm test` 96 suites / 828 tests pass; `npm run lint` exit 0; `npx tsc --noEmit -p tsconfig.json` exit 0. Worklog numstat nit corrected (`+17/−10`). No `persistentID` values in the worklog or ledger. Disposition: R1 `✅ done` stands; no accepted-residual entry needed. **Final verdict: ✅ Ready to merge — no open findings.** Status `closed-out` (set by the coder per Step 6; confirmed by the reviewer this round).

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1     | low  | tests/server/Master.test.ts:150-158 | Test hygiene, not production: the `afterEach` reset goes through the guarded API (`await lobbyPollTick(() => {})`). If a case fails before `pending.release()` (`:178`) — or a future case in this file (0056's injected-timer suite) uses a fixture that never settles — `lobbyPollInFlight` stays `true`, the reset tick is a no-op at `Master.ts:430`, `publicLobbiesJsonStr` is not restored, and the rest of the file goes order-dependent. Failure is visible (the `afterEach` assertion trips), not silent, so the blast radius is a cascading second failure under an already-failing run. Raised by both reviewers. Options: leave as-is with a comment stating the constraint (fixtures must settle), or isolate the suite with `jest.isolateModules`/fresh import per test. |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect (test hygiene; severity low — visible cascade under an already-failing run, no production path) | Fixed 2026-08-27 under the owner's ruling (lead session), `tests/server/Master.test.ts` only, `src/server/Master.ts` untouched (numstat still `+17/−10`). Mechanism, the smallest that makes the reset unconditional: every fixture promise that can outlive its test is built through `trackedPromise()`, which registers a settle hook in `settleStragglers`; `afterEach` switches to real timers, settles every straggler (no-op if already settled), drains the chain on a real `setImmediate`, then runs the reset tick with a `jest.fn()` and **asserts it ran** (`expect(resetTick).toHaveBeenCalledTimes(1)`) before asserting the body is `{"lobbies":[]}`. Constraint stated in a block comment above the fixtures ("FIXTURES MUST SETTLE …"). Proof: with case 1's `pending.release()` removed, only case 1 fails (its own await times out) and all 7 other tests pass — no cascade. Checks after the fix: `jest tests/server/Master.test.ts` 8/8; `--randomize` (seed 504260008) 8/8; `npm test` 96 suites / 828 pass; `npm run lint` 0; `npx tsc --noEmit -p tsconfig.json` 0. Not chosen: a reset export from `Master.ts` (touches the file 0056 builds on next), `jest.isolateModules` (re-imports `app` per test — larger restructure for the same guarantee). | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)
- Worker rate limiter counts the master — What: `Worker.ts` 20 req/s per-IP limiter untouched · Why (structural): owner ruling (brief Notes; plan approval record) · Re-raise only if: a diff changes the limiter.
- Orphan public games on worker recovery — What: left for `0192` (create-call timeout) · Why (structural): owner ruling; this task bounds poll concurrency only · Re-raise only if: a diff makes orphans worse.
- Quorum 18/20 and worker routing — What: untouched (ADR-109) · Why (structural): recorded, owner-accepted tradeoffs · Re-raise only if: a diff touches routing or readiness.
- Body frozen up to 5 s while a poll waits on a stuck ID — What: same freeze as before the guard (every pre-change tick also waited on `Promise.all`); per-ID dedupe, re-arm-with-`setTimeout`, abort-old-poll and a generation counter rejected in the approved plan · Why (structural): plan §Step 1 table · Re-raise only if: a diff makes the freeze longer than the pre-existing 5 s abort.
- Uncleared 5 s abort timers on the success path, and the ≤ 1 s empty-body blink behind nginx's 1 s cache — What: pre-existing, out of scope · Why (structural): plan §Step 1 / Risks · Re-raise only if: a diff changes either.
