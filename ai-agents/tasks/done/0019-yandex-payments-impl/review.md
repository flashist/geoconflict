# Review — 0019

Task: ai-agents/tasks/done/0019-yandex-payments-impl/brief.md
File(s) under review: uncommitted working tree vs HEAD 6462e59 — src/core/profile/PaymentsContract.ts, migrations/002_yandex_payments.sql, src/profile-server/{YandexSignature,PaymentsRepository,Routes,Server}.ts, src/client/{PaymentsApiClient,PaymentsReconciliation}.ts, src/client/flashist/FlashistFacade.ts, setup-profile.sh, new tests under tests/
Status: closed-out

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1 | low | migrations/002_yandex_payments.sql:36 | `processed_purchases.intent_id` FK has no `ON DELETE SET NULL`, contradicting its own comment ("keeps the receipt row even if the intent is ever purged"): deleting a player_profiles row (152-ФЗ erasure) cascades into purchase_intents and then FAILS on the receipt FK. Migration is unapplied anywhere — editable in place. |
| R2 | 1 | low | src/profile-server/PaymentsRepository.ts:48-55 | `GRANT_FLAGS_SQL` grants citizenship flags for ANY productId — safe today (enum = ["citizenship"] + route product_mismatch check), but the day a second product joins `PAYMENT_PRODUCT_IDS`, its grant silently sets `is_paid_citizen`. Cheap guard: assert/switch on productId inside `grantPaidPurchase`. |
| R3 | 1 | low | src/profile-server/Routes.ts:421-424 (vs 349-352) | Reconcile grants unprocessed tokens on already-used intents (any-state — plan step 5 specifies this), which undoes `/complete`'s `intent_used` refusal; the comment at Routes.ts:350-351 ("a second real purchase riding a spent intent is refused") overstates a guarantee the system doesn't hold. No entitlement gain (flags idempotent; each token = a real paid purchase). Raised by Codex (as high); verified low. Disposition: align reconcile with the refusal, or keep behavior + fix the comment + record as accepted residual — owner's call. |
| R4 | 1 | low | src/profile-server/PaymentsRepository.ts:149-154 | Intent single-use is not atomic: `SET used_at = coalesce(used_at, now())` with no `WHERE used_at IS NULL`/rowcount check, and `/complete` reads `usedAt` before granting — two concurrent `/complete` calls with DIFFERENT valid tokens on one open intent both succeed. End state identical to the reconcile path (R3); the race only converts a 409 into a 200. Raised by Codex (as high); verified low. Note: fixing R4 alone does NOT restore the single-use guarantee while R3's any-state reconcile stands — fix both or neither. |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect (latent — migration unapplied) | Added `on delete set null` to `processed_purchases.intent_id` FK + extended the comment; erasure cascade no longer fails, receipt row survives as the comment always claimed. Owner ruling 2026-08-14: fix in place. | ✅ done |
| R2 | CORRECT | Defect (latent — enum has one entry today) | Guard at top of `grantPaidPurchase`: non-`citizenship` productId throws before any DB touch; a second product must add its own grant branch. Unit test added (throws, zero queries). Owner ruling 2026-08-14: guard now. | ✅ done |
| R3 | CORRECT | Frontier (any-state reconcile is plan step 5 by design — interrupted purchases must land) | Behavior kept; misleading `/complete` comment (Routes.ts ~350) rewritten to state the refusal is route-local, not a system guarantee, pointing at the residual. Owner ruling 2026-08-14: keep + fix comment + record residual. | won't fix (frontier) — comment fix ✅ |
| R4 | CORRECT | Frontier (coupled to R3: fixing it alone restores nothing while any-state reconcile stands) | None — no code hardening. Same end state as the reconcile path; race only converts a 409 into a 200, no entitlement gain. Owner ruling 2026-08-14: record as residual alongside R3. | won't fix (frontier) |

## Accepted residuals (shared, do-not-re-litigate)
- Client-asserted yandexPlayerId at /intent — What: `/intent` accepts the raw client-asserted id · Why (structural): ADR-103 trust seam; the grant is bound to the Yandex-HMAC-signed payload via developerPayload → intent row, so a forged id only lets an attacker pay real money to gift citizenship to an id of his own choosing · Re-raise only if: a path grants paid state WITHOUT HMAC verification, or the Yandex secret key is issued (ADR-103 exit).
- HMAC dual-construction — What: verifier accepts HMAC over the transmitted base64 payload string OR the decoded JSON text · Why (structural): Yandex docs don't pin the HMAC message; same key either way, no security loss · Re-raise only if: live verification (checklist step 0) lands and the verifier isn't narrowed to the confirmed construction.
- Deferred live verifications — What: real catalog / real test purchase / live reconciliation not run · Why (structural): blocked on 0014 (Yandex catalog approval + secret issuance); written checklist exists in the task folder · Re-raise only if: 0014 unblocks and the checklist isn't executed before enabling payments.
- No intent expiry — What: purchase_intents rows never expire · Why (structural): a reconcile may legitimately arrive days later; single-use + token PK are the replay guards (plan step 2) · Re-raise only if: observed abuse or unbounded table growth in production.
- Integration tests not executed — What: tests/integration/PaymentsRepository.it.test.ts written, gated by RUN_DB_TESTS, not run · Why (structural): Docker Desktop needs a manual interactive start on this machine (project memory); accepted at the build gate · Re-raise only if: the suite still hasn't run by ship/deploy time.
- No visible Buy CTA in 0019 — What: gating primitives + analytics constant only · Why (structural): owner ruling 3; 0018 wires the flow — a dead button is a shipped-and-reverted bug class here · Re-raise only if: 0019 ships UI anyway.
- Post-grant inbox no-op seam — What: afterPaidPurchaseGranted() is a documented no-op · Why (structural): inbox is backlog task 0012; hook fires after commit so a future inbox failure can't roll back a money grant · Re-raise only if: 0012 lands without wiring it.
- Any-state reconcile vs /complete's intent_used refusal (R3, owner-ruled 2026-08-14) — What: `/reconcile` grants unprocessed tokens on already-used intents while `/complete` returns 409 `intent_used`; intent single-use is a per-route refusal, not a system guarantee · Why (structural): reconcile exists precisely to land purchases whose `/complete` never arrived (plan step 5); aligning it with the refusal would strand real paid purchases. No entitlement gain — flags are idempotent and every token is a distinct real payment. Rejected alternative: refusing used intents in reconcile · Re-raise only if: a product joins the catalog whose grant is NOT idempotent (e.g. a consumable/stackable good), or intent single-use becomes a real security boundary (ADR-103 exit / signed-id era).
- Non-atomic intent used_at (R4, owner-ruled 2026-08-14) — What: `used_at = coalesce(used_at, now())` with no `WHERE used_at IS NULL`/rowcount check; two concurrent `/complete` calls with different valid tokens on one open intent can both return 200 · Why (structural): end state is identical to the any-state reconcile path above — the race only converts a 409 into a 200; hardening it alone restores no guarantee while that residual stands (fix both or neither, per R4's own note). Rejected alternative: atomic claim (`WHERE used_at IS NULL` + rowcount) without touching reconcile · Re-raise only if: the any-state-reconcile residual above is re-opened (they move together).
