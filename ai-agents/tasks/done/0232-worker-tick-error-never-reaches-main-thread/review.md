# Review — 0232

Task: ai-agents/tasks/done/0232-worker-tick-error-never-reaches-main-thread/brief.md
File(s) under review: src/core/GameRunner.ts · src/core/worker/WorkerMessages.ts · src/core/worker/Worker.worker.ts · src/core/worker/WorkerClient.ts · tests/core/GameRunner.test.ts · tests/core/worker/WorkerClient.test.ts · tests/core/worker/WorkerWorker.test.ts (working tree vs `HEAD` = `fd2c88e` on `dev`; the 0219/0241 working-tree files and `src/client/` are out of scope and were confirmed untouched by this task — `git status --short src/ tests/`)
Status: closed-out

## Reviewer findings

Round 1 — 2026-09-13. Reviewers run: own pass + Codex (`codex exec --sandbox read-only`, exit 0, full coverage). Verdict: **⚠️ Changes requested — 1 defect (none blocking)** + 1 nit. Checks run by the reviewer: `npm test -- tests/core` 29 suites / 238 tests green; full `npm test` 119 suites / 1245 tests green, none skipped; `npx tsc --noEmit` clean; `eslint` + `prettier --check` clean on the six prettier-clean touched files (`GameRunner.ts` carries pre-existing drift at HEAD outside this hunk — not this task's). "4 fail on old code" verified by reading each assertion against the HEAD versions.

| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1     | low  | src/core/GameRunner.ts:176-177 | D2's "non-Error throws are reported" still has one dark corner: `String(error)` itself throws for a null-prototype object or a throwable whose `toString()` throws (`String(Symbol())`/`String(null)` are safe). That TypeError escapes the `catch`, `isExecuting` stays latched, and it surfaces only as the worker-side `unhandledrejection` log (`Worker.worker.ts:39` → `:182-184`) — no `game_error`, no modal: the old silent freeze, for that throwable class only. Raised by both reviewers. No such throw exists in `src/core` today (grep: zero non-`Error` throws), so this is a completeness gap in the D2 guarantee, not a regression. Defect, low. Recommended shape if acted on: wrap the coercion (`try { String(error) } catch { "non-coercible throw" }`) and add a `throw Object.create(null)` case to `tests/core/GameRunner.test.ts`. |
| R2 | 1     | low  | src/core/worker/WorkerClient.ts:57-61 | Nit, consistency only: `case "game_error"` forwards `message.error` unguarded while `case "game_update"` (`:52`) also checks `message.gameUpdate`. A malformed `game_error` with no `error` would make `ClientGameRunner`'s `"errMsg" in gu` (`ClientGameRunner.ts:526`) throw a TypeError inside the message handler. No producer sends one (`Worker.worker.ts:27` always attaches it), so this is unreachable today. Optional; a `&& message.error` matches the sibling case. |

Verified and **not** findings (so nobody chases them): message contract is structured-clone safe (`ErrorUpdate` = two strings, `stack: undefined` clones) and `WorkerClient.handleWorkerMessage` is the only consumer of the `WorkerMessage` union (grep) · D4 cannot double-fire or fire post-`cleanup()`: pre-init only `initReject` runs (callback unset until `start()`, which requires `isInitialized`), post-init `initReject` is already cleared at `WorkerClient.ts:83`, and `cleanup()` nulls the callback before a second event could land · D4 makes every post-init uncaught *sync* worker exception game-ending — checked for pre-existing non-fatal sources: the only timer in `src/core` reachable in the worker is `FetchGameMapLoader.ts:17` (resolve-only), and the message listener is `async` so its throws are F2, not `error` events · latch vs catch-up: 20 heartbeats per frame all early-return at `GameRunner.ts:155-157`, exactly one `game_error` · hot `game_update` path: same discriminant check, first `case`, no added work · double `transport.leaveGame()` (F5): `killExistingSocket` nulls the socket (`Transport.ts`), so the second remote call returns at the null guard; singleplayer is a repeated `clearInterval` — observed harmless in the worklog · tests: T1 "non-Error throw", T2 "game_error reaches callback", T2 "post-init error event", T3 "ErrorUpdate leaves as game_error" are exactly the four that fail on HEAD; `globalThis.self`/`Worker` stubs cannot leak (jest `testEnvironment: "node"`, per-file globals) · worklog carries no frequency/severity figure; every count in it that could be re-run (119/1245, `.gitignore:18`, `joinLobby` closure `:254-257`) re-checked and matches.

Deferred by owner-approved plan scope, **not** re-litigated here (listed so it is not lost): a crash **before** the 20 s connection-check arm (`ClientGameRunner.ts:501-506`) lets `onConnectionCheck` (`:1153-1166`) reconnect and rejoin the match with a terminated worker — observed by the coder in multiplayer (worklog 4.5). This task makes that path reachable; the plan (step 4.5, owner-approved 2026-09-13) routes it to **0231** (`ClientGameRunner.ts` is 0231's file). The lead should make sure 0231's brief actually receives the hand-off text in the worklog.

## Coder response

Round 1 — 2026-09-13, coder spawned by `fkit-sprint-ship-loop` (Process-review worker) under the declared-approval marker; fixes applied under the standing approval (ADR-019 discipline via ADR-032): both verified CORRECT, mechanical, inside the approved plan's D2 territory. Verification after the fixes: `npm test -- tests/core` 29 suites / 239 tests green; full `npm test` 119 suites / 1246 tests green (was 1245; +1 = the R1 case), none skipped; `npm run lint` 0 problems; `npx tsc --noEmit` clean; prettier clean on `WorkerClient.ts` and `GameRunner.test.ts`; `GameRunner.ts` keeps its pre-existing drift (lines 75–97, 129, 141–144 at HEAD, untouched — worklog decision 4), the changed hunks themselves are prettier-clean (checked by diffing prettier's output). Severity assigned by blast radius, not inherited: R1 low (no non-`Error` throw exists in `src/core` today — grep — so no reachable producer; a completeness gap in the D2 guarantee, not a regression), R2 low (unreachable — `Worker.worker.ts:27` always attaches `error`; the guard is consistency only).

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect (low) | Confirmed at runtime: `String(Object.create(null))` throws `TypeError: Cannot convert object to primitive value`, and `console.error` on the same value does **not**, so the escape happens exactly at the coercion inside the `async` worker listener → worker-side `unhandledrejection`, no `game_error`, latch stays set. Fix: new `private static describeThrow(error)` in `GameRunner.ts` — `try { String(error) } catch { "non-coercible throw" }` — used for the non-`Error` branch of `errMsg`; the `Error` branch is unchanged. Test added to `tests/core/GameRunner.test.ts`: "a throw that String() cannot coerce is still reported (review R1)" — `throw Object.create(null)`, asserts `executeNextTick()` does not throw and the callback receives `{ errMsg: "non-coercible throw", stack: undefined }`. Fails on the pre-fix code (the TypeError escapes `executeNextTick`). | ✅ done |
| R2 | CORRECT | Defect (low, unreachable today) | `case "game_error"` guard is now `this.gameUpdateCallback && message.error`, matching the `game_update` sibling at `WorkerClient.ts:52`. No test added: no producer can send an `error`-less `game_error` (`Worker.worker.ts:27`), and the existing T2 `game_error` delivery test still covers the live path. | ✅ done |

## Accepted residuals (shared, do-not-re-litigate)

