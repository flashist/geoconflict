# Review — 0192

Task: ai-agents/tasks/done/0192-schedule-public-games-onto-ready-workers-with-bounded-create/brief.md
File(s) under review: src/server/Master.ts (0192 hunks only: `GameID` import; `PICK_GAME_ID_MAX_ATTEMPTS`, `CREATE_GAME_TIMEOUT_MS`, `GameIDPick`, `pickGameID`, `ScheduleDeps`, `liveScheduleDeps`, `noReadyWorkersLogged`, exported `schedulePublicGame` — `:11`, `:515-644`), tests/server/Master.test.ts (import hunk `:31-45`; `describe("0192 ready-worker placement + bounded create")` `:789-1155`). The uncommitted, already-closed 0056 / 0193 hunks in the same files are the base, not under review.
Status: closed-out

Round 1 — 2026-08-27. Reviewers run: fkit-reviewer own pass + Codex (`codex-cli 0.145.0`, `codex exec --sandbox read-only`, completed, 2 findings — both also raised by the own pass). Checks re-run by the reviewer this round: `npx jest tests/server/Master.test.ts` (42/42), `--randomize` ×3 (seeds 1142525705, -332716460, -1364049984 — all 42/42), `--detectOpenHandles` (clean), `npx tsc --noEmit -p tsconfig.json` (exit 0), `npx eslint` on the two files (exit 0). Plan conformance verified by reading: D1 (cap 1000 via `deps.maxAttempts ?? PICK_GAME_ID_MAX_ATTEMPTS` `:576`; exhausted → last unfiltered draw `:542-543` + `warn` `:598-608`), D2 (empty set → `null` `:538`, skip with nothing added `:578-587`, `error` once per episode via `noReadyWorkersLogged` `:579-585`, `info` on resume `:588-594`), D3 (`AbortController` + 5 s `:614-615`, `signal` `:628`, catch deletes the ID `:638` and rethrows `:640`, `clearTimeout` in `finally` `:642`). Healthy path: with every index ready the first draw hits (`:539-541`), then add → fetch as before; only `signal` is new. Everything that can throw (`config.workerPort`, `playlist.gameConfig()`) sits inside the `try`, so no path leaves the ID in `publicLobbyIDs` or the timer armed. `scheduleLobbies`' `Error scheduling public game` contract (`:167-171`) intact.

**Orphan count (worklog run 4, 5 vs "0057 baseline of 2"):** not a defect of this diff. The 0193 review's own pre-0192 live re-run (`ai-agents/tasks/done/0193-guard-fetchlobbies-against-overlapping-ticks/review.md:7`) already measured **5 orphans with the guard on** — the 0193 baseline is 5, and 0192's run 4 reproduces 5. The rise is 0193's (removal of the accidental 429 orphan guard), predicted there; 0192 leaves it unchanged. See R3.

## Reviewer findings
| #  | Round | Sev | file:line | Claim |
|----|-------|-----|-----------|-------|
| R1 | 1 | low | src/server/Master.ts:639 | Raised by both. The create-failure line — now the primary observable of the D3 timeout (abort, dead port, non-2xx) — is logged as text only, with no single-object meta (`gameID`, `workerIndex`, `timeoutMs`), unlike the three new lines at `:581-584`, `:590-593`, `:599-607`. Plan Step 2 sketched the line without meta ("text unchanged (greps)"), so this is plan-literal, not a defect; adding meta leaves the grep text unchanged and gives Uptrace a structured `gameID` on the failure path. Hygiene improvement; owner decides whether to add now or record as accepted. |
| R2 | 1 | nit | src/server/Master.ts:539-546 | Raised by both. `pickGameID` draws once before any cap check, so `maxAttempts <= 0` still spends one draw and reports `attempts: 1` (a first-draw hit even returns `onReadyIndex: true`). Unreachable via live deps (`deps.maxAttempts ?? 1000`, `:576`) and every test passes ≥ 3; the `Worker.ts:545-556` precedent has the same shape. Not a defect; either clamp (`Math.max(1, …)`) or record "cap ≥ 1 is the contract" as a residual. |
| R3 | 1 | low | ai-agents/tasks/backlog/0192-…/worklog.md:113-117, :196-198 | Docs only. The worklog states "Orphans went up, not down: 5 vs 0057's 2" and lists it under Deviations. The comparison is baseline-mismatched: the 0193 review already measured 5 orphans with the guard on, before 0192 (0193 `review.md:7`). The honest statement is "unchanged vs the 0193 baseline (5 / 5); 0057's 2 was the pre-0193 number". As written, the producer could read a regression that this diff did not cause. Amend the worklog wording; no code change. |

Phase 2 — 2026-08-27, reviewer verification of the owner's dispositions (R1 apply now; R2 residual; R3 reword + orphan residual; `Worker.ts` guard brief filed by the producer at close). Verified in the working tree: **R1** landed at `Master.ts:641-649` — message text byte-identical to HEAD's grep target (`Failed to schedule public game on worker ${workerPath}: ${formatError(error)}`), single-object meta `{ gameID, workerIndex, workerPath, timeoutMs }` with no nullable field (string / number / string / number); tests #11 and #12 assert it via `createFailureMeta()` on all three failure branches (abort `Master.test.ts:1122`, rejected fetch `:1140`, non-ok `:1155`). **R2** residual reads as ruled (entry below). **R3** worklog reworded to "unchanged vs the `0193` baseline (5 / 5)" in run 4 and Deviations, citing 0193 `review.md:7`. The owner-ruled **orphan residual was not in *Accepted residuals*** (only mentioned in the coder's R3 row) — added below by the reviewer, on the owner's ruling. `npx jest tests/server/Master.test.ts` re-run: 42/42. `Status: closed-out` confirmed.

## Coder response
Round 1 — 2026-08-27, Process-review worker under `/fkit-sprint-ship-loop` (owner rulings relayed by the driver via `AskUserQuestion`: R1 apply now, R2 residual, R3 worklog wording). Every finding verified against the working tree before acting; severities are the coder's own.

| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Neither — observability hygiene (plan-literal, not a defect; sev low: the text line already fires, only structured filtering was missing) | Verified: `Master.ts:639` (pre-fix) was the only new log line without meta. Added a single-object meta `{ gameID, workerIndex: config.workerIndex(gameID), workerPath, timeoutMs: CREATE_GAME_TIMEOUT_MS }` (`workerPath` is the local already computed at `:597` — free); message text byte-identical (`Failed to schedule public game on worker …`); no nullable field. Tests #11/#12 extended with one `toEqual(createFailureMeta(id))` assertion per failure branch (abort, rejected fetch, non-ok) via a small describe-local helper; suite structure untouched. Owner-ruled, not autonomous. | ✅ done |
| R2 | CORRECT | Frontier — `maxAttempts ≥ 1` is the function's contract (same shape as `Worker.ts:545-556`); sev nit: unreachable through `liveScheduleDeps` (`deps.maxAttempts ?? 1000`, `:576`) and every test passes ≥ 3 | none (owner ruled: record as residual, no clamp) | won't fix (frontier) |
| R3 | CORRECT | Docs only (worklog baseline mismatch; no code path involved) | Reworded `worklog.md` run 4 paragraph and the Deviations bullet to "unchanged vs the 0193 baseline (5 / 5); 0057's 2 was the pre-0193 number", citing `0193/review.md:7`. Run 4b (guard off → 1 orphan) evidence kept. Noted the owner-ruled separate `Worker.ts` guard brief (producer files at close). | ✅ done |

Convergence: three findings, all verified CORRECT, none a defect of this diff; nothing left `pending approval`; no regression or oscillation against prior rows (none exist). No new code path beyond the meta object.

## Accepted residuals (shared, do-not-re-litigate)
- Wedged-but-alive worker stays eligible (liveness, not health); private lobbies out of scope; quorum 18/20, 90 s — What: ADR-109 recorded, owner-accepted tradeoffs · Why (structural): ADR-109 "Residual risks" · Re-raise only if: a diff touches readiness semantics, or ADR-109's own re-raise conditions hold.
- `fetchLobbies` leaves its 5 s abort timer uncleared per poll — What: pre-existing, untouched (`Master.ts:446`); 0192's create timer is cleared in `finally` · Why (structural): 0193 ledger residual; out of 0192 scope · Re-raise only if: a diff changes `fetchLobbies`.
- Worker 20 req/s limiter counts the master — What: untouched · Why (structural): owner ruling (0193 ledger) · Re-raise only if: a diff changes the limiter.
- `pickGameID` draws once before checking the cap (R2) — What: an injected `maxAttempts` ≥ 1 is the contract; `maxAttempts ≤ 0` still spends one draw and reports `attempts: 1`; no clamp added · Why (structural): unreachable via live deps (`deps.maxAttempts ?? PICK_GAME_ID_MAX_ATTEMPTS`, `Master.ts:576`); mirrors the `Worker.ts:545-556` precedent; a clamp would add a branch nothing exercises. Owner ruling 2026-08-27 · Re-raise only if: a caller passes a computed/configurable `maxAttempts` that can reach 0, or the precedent in `Worker.ts` changes shape.
- Orphan public games on wedged-worker recovery (R3) — What: unchanged vs the 0193 baseline (5 / 5, worklog run 4; 0193 `review.md:7`); the 5 s create timeout bounds the master's hang but does not retract a request already in the stopped worker's socket buffer, so on `SIGCONT` the worker still creates games the master already dropped · Why (structural): not fixable in `Master.ts`; an orphan-proof fix is a `Worker.ts` check of `req.socket.destroyed` / `req.aborted` before `createGame` — a separate brief the producer files at close. Owner ruling 2026-08-27 · Re-raise only if: a diff touches the worker create path, or a measured count rises above the 0193 baseline.
