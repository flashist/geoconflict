# Review — 0003

Task: ai-agents/tasks/backlog/0003-migrate-done-cancelled-tasks-to-folders/brief.md
File(s) under review: working tree vs `HEAD` (`2d1135c`), uncommitted — 119 staged `R100` renames (118 briefs → `ai-agents/tasks/{done,cancelled}/<NNNN>-<slug>/brief.md` + `done/0110-stale-build-investigation/plan.md`); inserted `## ID` / `## Status` / `## Owner` blocks in the 118; reference rewrites in `ai-agents/sprints/`, `ai-agents/knowledge-base/`, `ai-agents/reviews/`, 11 backlog briefs, 4 done-folder briefs, `src/core/profile/{Citizenship,CreditContract,MatchQualification,PlayerProfile}.ts` (1 comment line each), `profile-backup.sh`, `.claude/skills/update-announcements/SKILL.md`
Status: closed-out

## Reviewer findings
| #  | Round | Sev    | file:line | Claim |
|----|-------|--------|-----------|-------|
| R1 | 1     | medium | `ai-agents/tasks/cancelled/0119-nations-balance/brief.md:13` | Inserted status `⛔ Cancelled (2026-04-18) — no-ship: implementation turned removed nation slots into regular Bots …` conflicts with the sprint plan's own record for the same task, `ai-agents/sprints/plan-sprint-4.md:56`: `⛔ Cancelled (2026-04-21) — created too many bugs; cancelled forever, though a similar task might return someday (owner-supplied reason, 2026-08-14)`. The brief names the sprint plans as the record to recover from; plan §3 sourced the date/reason from `knowledge-base/hvn-balance-pr70-no-ship-review.md` + the wiki page and never disclosed that the sprint-plan cell disagrees, so the D7 ruling was made without that conflict in view. Evidence: git rename into `cancelled/` is `1e857a0` (2026-04-18); the `04-21` date is the plan-edit commit `e7e1b12`; the owner-supplied reason was added in `0beb899` (2026-08-21). Raised by both reviewers. Disposition is the owner's — see the NEEDS-DECISION in the round-1 report. |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | **CORRECT** | Defect (severity as assigned by coder: **low** — two records for a closed task disagree; nothing reads the brief at runtime; but the brief/plan-must-agree convention makes it a defect, not a tradeoff). Verified: `plan-sprint-4.md:56` carries `⛔ Cancelled (2026-04-21) — created too many bugs; …` (reason added `0beb899` 2026-08-21; the `04-21` date is plan-edit commit `e7e1b12`); the brief entered `cancelled/` in `1e857a0` (2026-04-18); plan §3 sourced the string from `hvn-balance-pr70-no-ship-review.md` and did not disclose the conflict. | **Owner ruled option (C), 2026-08-25:** `cancelled/0119-nations-balance/brief.md:13` (the inserted `## Status` value, line index 12 of the recorded insert set) → `⛔ Cancelled (2026-04-18) — created too many bugs; cancelled forever, though a similar task might return someday` — date from the git rename `1e857a0`, reason = the owner's own words from `plan-sprint-4.md:56`. Sprint-plan cell **not touched** (0004's). Byte proof re-run: 118 briefs + 1 plan, 0 mismatches (the edit is inside the insert set, so body prose is provably untouched). Worklog D-11. ⚠️ **Left deliberately unresolved:** brief now says `04-18`, the plan cell still says `04-21` — the date reconciliation of that cell is 0004's, per the ruling. | ✅ done |

*Round-1 report items outside the findings table (X1 and the non-defect notes):* no code change; recorded
as such here and in worklog D-11. Handed to the producer via the worklog: the `0104`/`0107` possible
duplicate, `reviews/s4-profile-04i.md:4`, and plan §4's "10 vs 9" producer-owner count (10 was written
before D3 folded the plan doc; the build assigned 9).

## Accepted residuals (shared, do-not-re-litigate)
- **Owner rulings D1–D7 (plan.md, 2026-08-25) and the V10/D-8 fixes (worklog D-10)** — What: no-`--follow` close-date ID order; slug prefix stripping (`5d-b-` kept on the one collision); hf11a plan doc folded into `0110-stale-build-investigation/plan.md` with no ID; additions-only `✅ Done` on the 3 pre-existing `## Status` briefs (0107, 0128, 0139; 0139's `## Owner` lands before `## Priority`); cross-refs inside the 118 rewritten; HF-5 / HF-11e cancel dates from git rename commits `49f96bc` / `b6c871a`; condensed reason strings; 8 `../`→`../../` depth fixes in `sprints/done/plan-sprint-4c.md` + 4 extra rewrites (`geoconflict-overview.md:558,590,624`, `reviews/s4-profile-04e3.md:4`). Why (structural): each is a deliberate point on a settled tradeoff, ruled by the owner in the lead session. Re-raise only if: the owner reopens the specific ruling.
- **R1 disposition — `0119-nations-balance` cancel line, owner ruled option (C), 2026-08-25** — What: brief `## Status` = `⛔ Cancelled (2026-04-18) — created too many bugs; cancelled forever, though a similar task might return someday` — date from the git rename into `cancelled/` (`1e857a0`), reason = the owner's own words from `plan-sprint-4.md:56`; the sprint-plan cell (still `2026-04-21`) is deliberately untouched — its date reconciliation belongs to task 0004. Why (structural): the brief forbids invented reasons and the owner-supplied text is the authoritative one; `e7e1b12` dates the plan edit, not the cancellation; Status cells are out of 0003's scope by the brief. Rejected: (A) keep the no-ship summary, (B) copy the plan cell's `04-21` date verbatim. Reviewer round 2 verified the fix landed as ruled and nothing else moved. Re-raise only if: 0004 chooses `04-21` for the cell without also revisiting the brief, leaving the two records split.
- **Legacy filenames left on purpose** — What: 0002's `plan.md`/`worklog.md`/`review.md`, this task's `plan.md`/`worklog.md`, briefs `0051`/`0052` (they *describe* legacy names as sweep subjects), `reviews/s4-postgres-backup-routine-coder-handoff.md:6` (points at the review ledger, which keeps its name), and all of `ai-agents/wiki-vault/` (only `fkit-wiki` writes it; `log.md` is append-only). Why (structural): rewriting a record of legacy names falsifies it; the vault is a different role's write surface (0002 residual, owner-ratified 2026-08-10). Re-raise only if: the `@fkit-wiki` / 0052 follow-up or the 0051 re-brief is never queued.
