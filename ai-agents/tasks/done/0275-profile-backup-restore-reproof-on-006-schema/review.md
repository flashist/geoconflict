# Review — 0275

Task: ai-agents/tasks/backlog/0275-profile-backup-restore-reproof-on-006-schema/brief.md
File(s) under review: tests/testdata/profile-restore-drill/seed.sql, tests/testdata/profile-restore-drill/verify.sql, tests/testdata/profile-restore-drill/behaviour.sql, tests/testdata/profile-restore-drill/cleanup.sql, tests/profile-backup-dryrun.sh, ai-agents/knowledge-base/profile-backup-restore-runbook.md (§"Restore TEST drill"), ai-agents/tasks/backlog/0275-profile-backup-restore-reproof-on-006-schema/worklog.md — Part A only. Other uncommitted files (S2 / 0271) are out of scope.
Status: closed-out (validation-gated on Part B box results, recorded by the lead in worklog.md)

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1 | medium | ai-agents/knowledge-base/profile-backup-restore-runbook.md:212-224 | Failure branches after the identity lands on the box have no shred step: `decryption failed => STOP and escalate; delete nothing` (:220) sits before the only `shred -u` (:223), and `download failed` / `pg_restore failed` / a default-deny typo are not addressed at all. An aborted drill can leave the off-box age identity (the key to every backup) on the live box. "Delete nothing" should scope to backup objects/markers; the identity should still be shredded (its public key can be re-checked with `age-keygen -y` first, which prints nothing secret). [Codex X1; severity lowered high → medium: root-only box, file 0600.] |
| R2 | 1 | low | ai-agents/knowledge-base/profile-backup-restore-runbook.md:176-275 | No unwind path for the LIVE seed rows. Only the "mangled text" branch (:184) says to run cleanup.sql; every other STOP after step 1 (backup failed, restore failed, diff not IDENTICAL, behaviour line missing) leaves 68 synthetic rows in the live DB with no instruction to clean them before the 02:30 UTC cron or before the S2 deploy. A later re-run then fails, because seed.sql refuses when rows exist. |
| R3 | 1 | low | ai-agents/knowledge-base/profile-backup-restore-runbook.md:188-189, 227-229 | The box compare does not check that verify.sql RAN TO THE END. Both runs pipe to `tee` with no `${PIPESTATUS[0]}` check and no end-of-output marker. If both sides fail at the same statement (for example, the box schema differs from the repo), or the file is missing on both sides, two identically cut-short (or empty) files make `diff` print `IDENTICAL`. That is the "exit 0 proves nothing" failure the brief warns about. The harness is protected, because it checks psql's exit status (tests/profile-backup-dryrun.sh:152, :247); the runbook is not. |
| R4 | 1 | low | tests/testdata/profile-restore-drill/seed.sql:18-40 | The refuse-if-rows guard takes no lock: it runs `exists(...)` under READ COMMITTED, so a write committed between the guard and the inserts gets past the refusal. Blast radius is small. Nothing writes to the live DB in the D3 slot (0217 is not wired, S2 is not deployed). cleanup.sql deletes only drill rows, so a real row would survive untouched. The guard's main job, refusing once real players exist, still works. A `lock table … in share mode` over the 9 tables before the check would close the gap cheaply. [Codex X2; severity lowered medium → low.] |
| R5 | 1 | low | ai-agents/knowledge-base/profile-backup-restore-runbook.md:190 | Expected counts `8/9/24/6/4/6/5/3/3` have no table labels, and their order matches nothing the owner will see. verify.sql prints rows alphabetically (`order by 1` → cosmetic 4, identities 9, credits 24, messages 6, name_history 6, xp_grants 3, players 8, receipts 3, intents 5), and the worklog/harness use yet another order (`…4/5/3/6/3`). The values are right, but a correct output will look wrong. [Codex X3; PARTIALLY CORRECT — the values are correct, not wrong.] |
| R6 | 1 | low | ai-agents/knowledge-base/profile-backup-restore-runbook.md:160-275 | The runbook drill is now written as an end-to-end procedure (decision 8), but it leaves out the pre-drill captures its own pass criteria depend on. :265 requires `schema_migrations` "identical to the one before the drill", yet no step records that list; plan §5's P1 health, P2 zero counts, P3 sequences, P7 `/ready` before and P8 no leftover `restore-test` are also absent. The identity step (:212) lacks plan B4's "report present/0600 only, never its size". The 0218 §1 rules (never set CONFIRM_LIVE, never `env`/`set`/`cat backup.env`, redaction list) are also missing. Those rules were never in the runbook, so decision 8 lost nothing — but Part B's safety currently lives only in plan.md §5. |
| R7 | 1 | low | tests/testdata/profile-restore-drill/verify.sql:60-76 | The "schema shape" check does not cover column shape: types, defaults, and NOT NULL. On PG16, NOT NULL is not a pg_constraint row. A restored DB that lost a column default (e.g. `purchase_intents.id default gen_random_uuid()`, `moderation_status default 'approved'`) or a NOT NULL would still diff IDENTICAL, while the pass criteria (runbook :272-277) claim IDENTICAL schema shape. Unlikely with pg_dump/pg_restore; a digest over `information_schema.columns` (name, type, default, nullability) would close it. Behaviour check (c) does exercise one bigserial default. |
| R8 | 2 | low | ai-agents/knowledge-base/profile-backup-restore-runbook.md:379-380 | Factually wrong retention note: "the daily object … holds the synthetic drill rows until it expires (14 days) or tonight's 02:30 run replaces it". The 02:30 UTC run is on the NEXT UTC date and writes `profile-<next date>.dump.age` (`profile-backup.sh:134-136`), so it never replaces today's object; only `last-backup.json` is replaced. Drill rows stay in the backup set for the full 14 days (as D5 accepted). Docs only; Part B safety unaffected. |
| R9 | 2 | low | ai-agents/knowledge-base/profile-backup-restore-runbook.md:372-373 | Merged abort/normal ending dropped two of 0218's post-checks: `/health` (0218 E5/F2 checked `/health` and `/ready`) and an end-of-drill `docker compose ps`. Blast radius is minimal: `/ready` is DB-backed and returning 200 implies a live process, and the drill never touches the API. [Codex X1 for /health; severity kept low.] |

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect | Runbook step 4 restructured: identity copied with `chmod 600`, reported via `stat -c '%a'` only; optional `age-keygen -y` check that prints only a count (skip if the tool is missing); restore exit captured; **4b shreds unconditionally before the outcome is read**; each failure shape (default-deny, download, decryption, pg_restore) → abort path; "delete nothing" now scoped to backup objects and markers; abort path A1 re-checks the identity is gone. | Fixed (round 1) — owner ruling "Always delete the key" |
| R2 | CORRECT | Defect | New **Abort path** section (also the normal step 7): identity, throwaway + volume, `cleanup.sql` with exit check + counts line + `diff` of `schema_migrations:` against `before.txt` (`MIGRATIONS UNCHANGED`), `/ready`, paste back, remove files. Deadline stated: before the next 02:30 UTC backup and before any profile deploy. Every STOP after seeding points to it. Rehearsed A3 locally: exit=0, counts 0, `MIGRATIONS UNCHANGED`. | Fixed (round 1) |
| R3 | CORRECT (reproduced) | Defect | `verify.sql` now ends with `verify_end: complete`. Runbook steps 2 and 5 capture `${PIPESTATUS[0]}`; step 5 diffs only if both exits are 0 and both files end in the sentinel, else prints `INCOMPLETE`. Harness gains 2 sentinel checks. Local rehearsal: truncated-at-same-statement pair → plain `diff` identical, new check `INCOMPLETE` (also with exits forced 0); missing file → `INCOMPLETE`. Dry-run `RESULT: 55 passed, 0 failed`. `verify.sql` md5 now `7b86b0576eb97907992228f8e645f0cc`. | Fixed (round 1) |
| R4 | — | Frontier | None — accepted residual (owner ruling). | Accepted residual |
| R5 | PARTIALLY CORRECT (values right, labels/order missing) | Defect (docs) | Runbook step 2 lists counts by table name in verify.sql's alphabetical output order; worklog counts relabelled too. | Fixed (round 1) |
| R6 | CORRECT | Defect (procedure gap) | New **Before you start**: whole-drill rules from `0218` §1 (root, one shell, never CONFIRM_LIVE, never `env`/`set`/`cat backup.env`, redaction, identity "present, 0600" only, no in-place edits, PIPESTATUS, abort-path trigger + deadline) and read-only captures P1–P5 (health + `/ready`, UTC time/weekday + no `restore-test`, network + volume baseline, file copy + md5s, counts + `schema_migrations` + sequences → `before.txt`). P5 query rehearsed verbatim locally (exit=0). | Fixed (round 1) |
| R7 | — | Frontier | None — accepted residual (owner ruling). | Accepted residual |
| R8 | CORRECT | Defect (docs) | Verified: `profile-backup.sh:134-136` keys the object by the run's UTC date, so the 02:30 UTC run after a 03:15–23:30 drill writes the next date's key; only `last-backup.json` is replaced. Retention note rewritten: drill rows stay in the drill-date object for the full 14 days (D5); noted that a profile deploy later the same UTC date would overwrite it via its smoke backup. Prose only; no command changed. | Fixed (round 2) — owner ruling "Fix now" |
| R9 | CORRECT | Defect (minor, procedure) | Abort-path A4 now also runs `docker compose ps` and `curl … /health` (expect `health=200`, same loopback form as 0218's plan), alongside `/ready`. No other command in P1–A3 changed (owner running Part B from the runbook). | Fixed (round 2) — owner ruling "Fix now" |

## Accepted residuals (shared, do-not-re-litigate)
- Unlocked seed empty-check (R4, owner ruling 2026-09-15) — What: `seed.sql`'s refuse-if-rows guard checks `exists(...)` under READ COMMITTED with no table lock; a write committed between the guard and the inserts is not refused. · Why (structural): no writer reaches the live profile DB in the D3 slot (0217 not wired, S2 not deployed); `cleanup.sql` deletes only drill rows, so a racing real row survives intact; rejected alternative — a share-mode lock on the 9 tables (cheap, but guards a race with no writer). · Re-raise only if: a writer to the live profile DB exists during the drill slot (S2 deployed, or 0217 wired).
- Column shape not digested (R7, owner ruling 2026-09-15) — What: `verify.sql`'s schema-shape check digests constraint and index definitions only; column types, defaults and NOT NULL (not a `pg_constraint` row on PG16) are not compared. · Why (structural): pg_dump/pg_restore carry column DDL verbatim; row digests catch type drift, and behaviour check (c) exercises a bigserial default; rejected alternative — an `information_schema.columns` digest. · Re-raise only if: a restore shows a lost default/NOT NULL, or a migration adds app-relied column behaviour without a behaviour check.

## Owner dispositions (recorded by reviewer, 2026-09-15 — phase 2)
- R1 → owner "Always delete the key" (option A): every failure branch after the identity is copied starts by shredding it; "delete nothing" means backup objects/markers only. Coder fixes; reviewer re-verifies.
- R2, R3, R5 → owner "Fix before Part B" (option A): abort path always runs `cleanup.sql`; `verify.sql` end line + `PIPESTATUS` check on the box; labelled counts. Coder fixes; reviewer re-verifies.
- R6 → owner "Add it" (option A): "before you start" block with pre-drill state + 0218 safety rules. Coder fixes; reviewer re-verifies.
- R4, R7 → accepted residuals (above).
- Status stays `in-review` until the coder's round lands and R1/R2/R3/R5/R6 are re-verified (re-run `npm run test:scripts:docker` if any fixture changes).

## Round 2 re-verification (reviewer, 2026-09-15)
- R1 ✅ verified: step 4b shreds unconditionally before the restore outcome is read (with dd+rm fallback); every failure shape routes to the abort path; A1 re-checks. The recipient check (`grep -cxF "PROFILE_BACKUP_AGE_RECIPIENT=$(age-keygen -y …)"`) prints only a count and never prints a line of backup.env, so it fits the intent of "never `cat backup.env`" (don't print credentials). The value it matches is a public key. No false negative: setup-profile.sh writes the line with `printf '…=%q\n'`, which leaves an `age1[a-z0-9]` value unquoted, so `-x` matches.
- R2 ✅ verified: abort path A1–A5 covers identity, throwaway + volume, /tmp restore temps, cleanup exit + counts, schema_migrations diff, /ready, paste-back; deadline stated. Rehearsed locally on a runner-built 006 DB: P5 query exit 0; seed; cleanup; `MIGRATIONS UNCHANGED` (before.txt and after.txt line formats match). Dropped vs 0218: `/health` and an end `docker compose ps` → R9 (low).
- R3 ✅ verified: the sentinel is the last output line (`tail -1` = `verify_end: complete`, rehearsed). Step 5 diffs only when both exits are 0 and both sentinels are present, else `INCOMPLETE`. `PIPESTATUS` needs bash, the box's root shell, as proven in 0218.
- R5 ✅ verified: labels and order match verify.sql's `order by 1` output.
- R6 ✅ verified: "Before you start" carries 0218 §1 rules (root, never CONFIRM_LIVE, never env/set/cat backup.env, redaction, identity present/0600 only, no in-place edits, PIPESTATUS) + P1–P5 captures.
- Fixtures: only verify.sql changed (md5 `7b86b0576eb97907992228f8e645f0cc`); seed/behaviour/cleanup md5s unchanged from round 1. Dry-run re-run by reviewer: `RESULT: 55 passed, 0 failed`, exit 0.
- Codex round 2: ran (exit 0). X1 → R9. X2 (harness `diff` at tests/profile-backup-dryrun.sh:256 runs even when the sentinel checks fail) → not recorded: the run still ends ❌ with a non-zero RESULT, so it is never a silent pass; it is a nit (a misleading log line).

## Round 3 (reviewer, 2026-09-15)
- Owner ruling (AskUserQuestion, 2026-09-15): R8 + R9 → "Fix now". Coder fixed them in the runbook only; no fixture changed (md5s unchanged: seed `27d85346…`, verify `7b86b057…`, behaviour `0c6d7f83…`, cleanup `1ec92668…`).
- R8 ✅ verified by reading the text below abort path A5: the drill-date object keeps drill rows for the full 14 days; the 02:30 UTC run writes the next date's key and replaces only `last-backup.json`. The caveat is correct: a same-date profile deploy's smoke backup would overwrite the object.
- R9 ✅ verified by reading abort path A4: `docker compose ps`, `/health` and `/ready` (all loopback, as 0218 E5/F2).
- No Codex pass or dry-run this round (docs-only edit to two known lines, per coordinator). Converged: no open reviewer findings; R4/R7 stay accepted residuals. Closed out, validation-gated on Part B's box results.
