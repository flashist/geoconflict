# Review — 0241

Task: ai-agents/tasks/backlog/0241-profile-verify-first-weekly-backup-copy/brief.md
File(s) under review: ai-agents/tasks/backlog/0241-profile-verify-first-weekly-backup-copy/worklog.md (new); ai-agents/knowledge-base/profile-backup-restore-runbook.md (struck-not-deleted dated line, `:227-233`). No source diff — verification-only task. Context, not reviewed: `plan.md` (owner-approved), `profile-backup.sh` (the observed script, unchanged).
Status: closed-out

## Reviewer findings
| #  | Round | Sev  | file:line | Claim |
|----|-------|------|-----------|-------|
| R1 | 1 | low (defect — record overclaim; raised by both reviewers) | `worklog.md:133` (hand-off note), `profile-backup-restore-runbook.md:230` | "byte-identical to that day's daily object and to `last-backup.json`'s `size_bytes`" claims more than was observed. Evidence is `rclone lsl` (key, modtime, size) — 19312 == 19312 == 19312. No hash/ETag/download compare was done, so byte identity was **inferred** from `rclone copyto` being a server-side copy, not observed — the exact inference the brief's acceptance rule forbids. The plan's PASS row required only size equality, which IS met. Also a category slip: a size cannot be "byte-identical" to a `size_bytes` number. Recommended wording: "same size as that day's daily object and the marker's `size_bytes` (19312 B); byte identity not checked (no hash compared)". Verified: `worklog.md:60-66` bundle; `profile-backup.sh:171-176`. |
| R2 | 1 | nit | `profile-backup-restore-runbook.md:230` | Key written as `weekly/profile-2026-09-13.dump.age` — drops the `profiles/` prefix the runbook uses everywhere else (`:86`, `:116`, `:157` all `profiles/daily/...`) and the worklog/hand-off use (`profiles/weekly/...`). A reader copying it into `restore <s3-key>` gets the wrong key. |
| R3 | 1 | nit | `profile-backup-restore-runbook.md:227-229` | The strike covers two still-true facts ("It only triggers on a Sunday", "the current bucket was created after the last one") along with the one stale claim ("has never run"). The dated trailer restates the marker/exit-0 caveats but not the Sunday-only trigger. Narrow the strike to the stale clause, or restate Sunday-only in the trailer. |
| R4 | 1 | info — producer gate at close, NO coder action | `worklog.md:129-141` vs `brief.md` verification step 4 | Step 4 ("`0219` has been told the answer") is not yet met at review time: by owner ruling Q2 (plan approval record) the coder only drafts the note; the producer applies it to `0219`'s `## Depends on` block at close. Correct per ruling — recorded so the close does not skip it. Verdict is validation-gated on this. |

Disproven / not recorded as rows (so nobody chases them): none. Codex raised exactly one finding (= R1); its other five categories were explicit "no finding" and my pass agrees. Verified clean: object OBSERVED via bucket listing (`worklog.md:63-66`, not a log line); all seven plan Step 2 rows checked; `profile-backup.sh` claims (`:121` retention 56, `:171-177` Sunday branch, `:176` `|| log` swallow under `set -euo pipefail` at `:30`, `:188` marker writes daily key/size only, weekly never size-verified) all correct; sanitization clean in both files (only `<host>`, box paths already in the runbook, `PREFIX=profiles` public since `0218`); `0218` residual 1 explicitly NOT claimed closed (`worklog.md:106-109`); `0217` still in `backlog/`; plan blob hash `07e84109…` / 12894 B matches `plan.md`; no forbidden action visible (no `backup.sh` hand run claimed, `0219` brief untouched, wiki untouched, task files not moved).

Codex pass: `codex exec --sandbox read-only` (codex-cli 0.152.0), prompt `.fkit/tmp/adversarial-prompt.md`, output `.fkit/tmp/codex-0241-out.txt`, exit 0 — coverage full.

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect (record overclaim; severity low — a doc claim, no runtime effect) | Verified: the only evidence is `rclone lsl` size (`worklog.md:69-77`), no hash/ETag/download; "byte-identical" was inferred from `profile-backup.sh:174-176` being a server-side copy. Reworded both places to "the same size as … (19312 B each; byte identity **not checked** — no hash or ETag compared)": `worklog.md` hand-off note (`:131-134`) and runbook dated line (`:230-232`). Applied without per-fix approval under the sprint-loop standing approval (ADR-019 discipline; logged in `worklog.md` decision log). | ✅ done |
| R2 | CORRECT | Defect (nit — copy-unsafe key) | Verified: runbook keys elsewhere carry the prefix (`:86`, `:116`, `:157`); the dated line dropped it. Runbook `:230` key → `profiles/weekly/profile-2026-09-13.dump.age`. Applied under standing approval; logged. | ✅ done |
| R3 | CORRECT | Defect (nit — over-wide strike) | Verified: the `~~` spanned "It only triggers on a Sunday, and the current bucket was created after the last one" — both still true. Narrowed the strike to the one stale sentence ("has never run … `weekly/` was empty on 2026-09-11"); the Sunday-only / bucket-created sentence now stands unstruck ahead of the dated trailer. Chose narrowing over restating (the reviewer's two offered shapes) as the smaller edit — obvious-winner call, logged in the worklog decision log. | ✅ done |
| R4 | CORRECT | n/a — producer gate at close | none for the coder. Confirmed: hand-off note drafted verbatim at `worklog.md:129-141`; `0219`'s brief untouched (owner ruling Q2). The producer applies it to `0219`'s `## Depends on` block when closing `0241`. Flagged in the coder's return envelope so the driver carries it to the close. | ✅ done (acknowledged — no coder action; open on the producer) |

Round 1 convergence: all four are first-round, none re-litigates an accepted residual or an ADR; R1–R3 are record-wording defects, fixed; R4 is a gate outside the coder's hands. Nothing pending coder action. No source changed; `npm test` not run (no code). No commit.

## Accepted residuals (shared, do-not-re-litigate)
- `0218` residual 1 (schedule × data proven separately) — What: `0241` proves the weekly *path* on the schedule against an empty DB; it does not join schedule to data · Why (structural): `0217` has not landed, so no scheduled run can capture real data yet; carried by `0218` on an owner ruling · Re-raise only if: the worklog claims to close it (it does not — worklog "What this does NOT close").
- Small (~19 KB) weekly object = empty database — What: expected size, not a failure · Why: `0218` drill rows removed 2026-09-11, `0217` still in `backlog/` (checked 2026-09-13) · Re-raise only if: `0217` has landed before the observed run.
- Weekly modtime == daily modtime — What: `rclone copyto` server-side copy preserves the source modtime · Why: named in the plan's edge cases; the CRON record + script log tie the object to the run, not the modtime · Re-raise only if: the CRON/log tie is missing.
