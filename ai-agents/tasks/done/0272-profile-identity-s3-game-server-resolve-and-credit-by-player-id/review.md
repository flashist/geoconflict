# Review — 0272

Task: ai-agents/tasks/done/0272-profile-identity-s3-game-server-resolve-and-credit-by-player-id/brief.md
File(s) under review: src/core/profile/{CreditContract,InboxContract,MatchQualification}.ts · src/profile-server/{Routes (S3 internal-route hunks),PlayerIdentityRepository}.ts · src/server/{ProfileApiClient,Client,GameServer}.ts · setup-profile.sh (migration-failure message + comment) · tests/core/profile/{CreditContract,MatchQualification}.test.ts · tests/server/{ProfileApiClient,GameServerParticipation,GameServerProfileResolve,CitizenFlag,GameServerReconnect,GameServerWinner}.test.ts · tests/profile-server/Routes.test.ts · tests/integration/{Routes.it,GameServerProfileCredit.it}.test.ts
Status: closed-out

## Reviewer findings
| #  | Round | Sev    | file:line | Claim |
|----|-------|--------|-----------|-------|
| R1 | 1     | medium | src/server/GameServer.ts:1497 | New XP-loss path is invisible to ADR-101's empirical re-raise trigger. When a qualifying client's resolve fails at join AND again at credit time, `resolveThenCredit` drops the award and says so only at `debug` (decision D3). `ProfileApiClient.resolvePlayer` does warn `player resolve failed after retries`, but no warn line counts dropped awards. ADR-101 names `credit batch failed after retries; N award(s) dropped` as the only trace of lost XP and says trigger 2 counts the N. In an outage that spans join and match end, the unresolved players' awards are missing from that N, so the trigger undercounts exactly the outages it exists to measure. D3's reason (with the token blank, every resolve is null, so a warn would fire on every trigger) is valid, but it argues for telling "not configured" apart from "configured and failed", not for `debug` in both cases. Direction: warn with an award count when the client is configured; stay quiet when it is not. [raised by both — reviewer + codex X1] |
| R2 | 1     | low    | ai-agents/knowledge-base/architecture.md:485 | The route table still lists `POST /internal/v1/profile/upsert` (200/400/409/500), which this change removes, and does not list `POST /internal/v1/players/resolve`. `/internal/v1/credit` is also still described without its `(game_id, player_id)` key. A doc-only drift created by this change. ADR-103's historical `upsertProfileForClient` citation (`adr-103…md:41`) is an ADR body and is left alone. [reviewer] |

**Owner dispositions (AskUserQuestion, relayed by fkit-lead, 2026-09-15):**
- **R1 → defect, "Warn with a count" (option a).** When the profile client is configured and a resolve fails, warn with the dropped-award count, worded so ADR-101 trigger-2 counting includes it. Stay quiet when it is not configured. Open until the coder's round lands and the reviewer re-verifies it.
- **R2 → routed outside 0272, "Architect / next doc sync" (option a).** A doc follow-up for `architecture.md`, not a 0272 blocker. **Closed for this ledger** (routed, not fixed here).

**Round 2 re-verification (reviewer, 2026-09-15; own pass only, Codex not rerun for this small change):**
- **R1 → verified fixed.** `GameServer.resolveThenCredit` warns `player resolve failed at credit time; N award(s) dropped (no durable retry — ADR-101)` only when `profileApiClient.isConfigured()` is true. N counts distinct creditable identities, minus any a sibling connection resolved in the same batch. The line has no ids, and the old `debug` line is gone. The identity is read through `getCreditableYandexId`, so ADR-103 still holds. Reran `GameServerParticipation`, `ProfileApiClient` and `GameServerProfileResolve`: 67/67 green.
- **Leftover: over-count by one.** A sibling connection already credited in the synchronous batch is not excluded, so N can exceed the real drops by one. It never undercounts. It needs one account on two connections at once, one resolved at join and the other failing twice. Judged a **frontier-move**, not a defect: the error runs the safe way for a re-raise trigger, and excluding it would mean passing the ready clients' identities into the resolve path for a rare case. Accepted by the owner on 2026-09-15 (option a) and recorded under Accepted residuals.
- No new defects, so converged → `Status: closed-out`.

## Coder response
| #  | Verdict | Defect / Frontier | Action | Status |
|----|---------|-------------------|--------|--------|
| R1 | CORRECT | Defect (medium — confirmed: `resolveThenCredit` logged the drop at `debug` only, so ADR-101 trigger 2's `award(s) dropped` count missed it) | Owner ruling "Warn with a count". `GameServer.resolveThenCredit`: when `profileApiClient.isConfigured()`, warn `player resolve failed at credit time; N award(s) dropped (no durable retry — ADR-101)`. N = distinct creditable identities whose resolve failed, minus identities a sibling connection resolved in the same batch. No ids in the line. Not configured → no line (the old debug line removed). `ProfileApiClient.isConfigured()` made public (+1 unit test). Tests RED-first in `GameServerParticipation.test.ts` "dropped-award warn (review R1)" (5 cases; 2 red before the fix). Mutations: warn→debug, no configured guard, no sibling exclusion, count clients instead of identities — all red. Known over-count edge: a sibling connection credited in the SYNCHRONOUS batch (its id was already resolved) is not excluded — conservative for a re-raise trigger. | ✅ done (awaiting reviewer re-verify) |
| R2 | CORRECT | Defect (low, doc-only drift in `architecture.md:485`) | none in 0272 — routed to the architect / next doc sync per owner ruling | closeout (routed outside 0272) |

## Accepted residuals (shared, do-not-re-litigate)
- Dropped-award warn can over-count by one (R1 leftover) — What: `resolveThenCredit`'s `N award(s) dropped` warn counts distinct creditable identities whose resolve failed, minus identities a sibling connection resolved in the same batch. A sibling connection already credited in the synchronous batch is not excluded, so N can exceed the real drops by one. It never undercounts. · Why (structural): the error runs the safe way for ADR-101 trigger 2 (a re-raise signal), and it needs a rare case: one account on two connections at once, one resolved at join, the other failing twice. Rejected alternative: pass the credited connections' identities into the resolve path just to exclude that case. Owner accepted 2026-09-15 (AskUserQuestion, relayed by fkit-lead). · Re-raise only if: ADR-101 trigger-2 counting ever needs exact per-award accuracy, or one account on several connections at once becomes common.
