# Wiki Index — Geoconflict

> Master catalog of all wiki pages. One line per entry. Updated by LLM after every ingest.

## Features

- [[features/tutorial]] — Guided singleplayer bot match for first-time players; 7-step tooltip sequence, Yandex A/B gated
- [[features/reconnection]] — Crash reconnection: rejoin prompt when tab closes/crashes mid-match
- [[features/feedback-button]] — In-game feedback form on start screen and battle screen with automatic context
- [[features/announcements]] — Start-screen bell and popup for repo-authored player update messages with unread badge; task 0012 adds a citizens-only Personal inbox tab (built 2026-08-26, **not launched**)
- [[features/ai-players]] — AI Players in public lobbies: active, indistinguishable from humans in UI; **ADR-110 (2026-09-03) rules that one MAY be declared match winner**, with no in-modal tell — and 🔴 **that ruling carries a known expiry**, see [[decisions/adr-110-ai-winner-allowed]]

## Systems

- [[systems/project-brief]] — Product ground truth: what the game is, who it is for, how it earns, and the platform/legal/scope constraints every task works inside; 🔴 **its *Current focus* claim that "the profile host is live" was CORRECTED 2026-09-04 — that claim is UNVERIFIED and no longer claimable** (owner-ruled). ⚠️ **A same-day annotation reading "there is no profile host" / "two standing fleets, not three" OVERSTATED the owner's position and has been withdrawn: the profile VPS and the S3 bucket physically EXIST and are REUSED IN PLACE; their provisioning state is UNKNOWN AND UNVERIFIED**
- [[systems/architecture-overview]] — Evidence-first 2026-08-08 codebase survey: four tiers, tick model, deploy topology, ranked risks, and the documented-but-stale corrections; **extended 2026-08-28** with the §5 `windowOrigin` URL-join rule and the §9 table that tells the two look-alike local lobby traps apart
- [[systems/agent-conventions]] — The project's standing law: task status and owner vocabularies, status-report shape, evidence-before-assertion, one-skill-one-output, priority-vs-identity, dependency form, and the project-added task-ID allocation rule
- [[systems/glossary]] — 📌 **The vault's single source of truth for project vocabulary** (new 2026-09-03, owner-ruled; schema amended with a Glossary page type). The terms whose everyday meaning and code identifier diverge: **"Nation" is `PlayerType.FakeHuman`**, an **"AI player" runs the same `FakeHumanExecution` class as a Nation** and differs only in having a real `clientID`, and **`ColoredTeams.Bot` IS a real team** — in `teams()` and in win-check tile accounting — even though `isOnSameTeam()` returns false for it. Also the two team-assignment paths, the win-condition vocabulary, and the three routinely-confused player IDs. Carries the owner's stated model **beside** the code, labelled, with items 5–7 refuted. **Other pages link here; they do not restate definitions**
- [[systems/game-overview]] — Project overview: game types, maps, units, economy, combat, tick system; its Player Types / Team Types / disambiguation tables were **merged into [[systems/glossary]] 2026-09-03**, and two claims it carried were corrected against code — `HumansVsNations` is a `playerTeams` config value, **not a `GameMode`**, and Nations are present in public FFA and default private Team lobbies but **never** in the tutorial
- [[systems/producer-workflow]] — Producer role: scope, responsibilities, coordination boundaries, and release guardrails; carries the 2026-08-30 never-write-endpoints rule and the owner's **no-retro-scrub** ruling on the host IPs already committed
- [[systems/project-operations]] — Operational handbook: team roles, environment boundaries, sprint workflow, and roadmap constraints
- [[systems/game-loop]] — Deterministic turn replay from server turns into worker-executed core simulation
- [[systems/networking]] — Worker-routed WebSocket/HTTP flow with Zod-validated client/server messages; carries the post-outage master/worker coordination — `WorkerSupervisor`, the quorum gate, ready-worker ID sampling, the lobby-poll guard, and the departed-requester create guard
- [[systems/execution-pipeline]] — Client input to Intent to Execution to GameUpdate path
- [[systems/rendering]] — Layered client rendering, mixed canvas/Lit UI, and camera transform orchestration
- [[systems/flashist-init]] — Explicit Bootstrap.ts startup gate for analytics, Yandex SDK, player data, flags, language, degraded mode, and app loading; **records 2026-08-28** that `yandex-games_iframe.html` sets `flashist_isYandexPlatform = true` unconditionally, so an off-portal load still enters Yandex platform mode — behaviour there **unmeasured**
- [[systems/analytics]] — GameAnalytics player behaviour tracking: event conventions, bootstrap/session events, experiment funnels, and monetization measurement baselines; **updated 2026-09-02** — **zero citizenship events have ever fired anywhere** (hard-gated by `CITIZENSHIP_CARD_ENABLED: false`), the sixth funnel event `UI:Tap:CitizenshipLearnMore` is **dropped as obsolete**, and 🚨 **`Citizenship:Seen` may UNDER-count, which INFLATES every downstream conversion rate** — unproven, deliberately unfixed
- [[systems/telemetry]] — OTEL/Uptrace server/client observability, source maps, ClickHouse VPS guardrails, and investigation workflows; the winston-transport gotcha **corrected 2026-08-27** against measurement — single-object meta survives as attributes, only `null` values are dropped
- [[systems/configuration]] — GAME_ENV, /api/env, runtime public settings, and gameplay/server config selection
- [[systems/localization]] — LangSelector and translateText flow for bundled UI translations and English fallback
- [[systems/server-performance]] — Server-side lag candidates ranked by likelihood; `endTurn()` performance analysis
- [[systems/match-logging]] — What is recorded per match, where it goes, and what cannot be retrieved
- [[systems/clans]] — Name-tag clan grouping system: parsing, team assignment logic, gaps, and no-UI status
- [[systems/player-infrastructure]] — Pre-S4 identity/customization substrate: local-only persistence, join transport, dead inherited Stripe/Fuse monetization, and trust gaps; **corrected 2026-08-09** — the `flares` entitlement path is **live** and upstream-OpenFront-sourced, with ad suppression coupled to it (production liveness still unverified, task `0009`)
- [[systems/player-profile-store]] — Dedicated Sprint 4 profile API/Postgres backend and match-end XP crediting path for citizenship and future paid entitlements; now also the name-change loop (migration 004) and the `is_citizen` flag the game path reads. **Three separate config/exposure gaps keep it inert or leaky in production** — `0062`, `0195` (deploy script fixed 2026-09-01, but the routes still 503 because `0014` has not issued the key), and 0067's unauthenticated pending-name read. 🔴 **CORRECTED 2026-09-04 — HARDWARE EXISTS; ITS STATE IS UNKNOWN.** ⚠️ **This supersedes an earlier same-day entry reading "THERE IS NO PROFILE HOST", which overstated the owner's position and is withdrawn.** Two owner rulings, both live in session 2026-09-04 and **both standing**: *"We don't have ANY profile-related VPS yet, we would need to have a full-scale setup for it (whatever is needed)"*, then *"We don't need to cancel any billings, the VPS and S3 I created will be reused."* 🔴 **Reconciled: a profile VPS and an S3 bucket PHYSICALLY EXIST and are REUSED IN PLACE; provisioning state, what runs, and what the bucket holds are UNKNOWN AND UNVERIFIED — hardware existence and provisioning state are two different facts, and only the first is known.** ⛔ **The backend WAS built** — service code, image, `setup-profile.sh` (provisions a bare box and deploys the stack; idempotent, safe to re-run), `build-deploy-profile.sh`, the backup script with a scripted restore, and an operator runbook **all exist and are sound**. ⚠️ **Read every "on the real box" / "200/TLS verified" / "503s in production" phrase across this vault as UNVERIFIED — not disproven, and not claimable.** 🔴 **"Clean slate" means WIPE AND REBUILD ONTO THE EXISTING RESOURCES, not procure new ones** — tasks **`0213`–`0222` plus `0201`, all Sprint 4**, `0215` inspecting first. 🔴 **The `age`-key question is RE-OPENED** (the reused bucket still holds old encrypted objects, unreadable without the old private identity) — a live owner decision on `0222`. ⚠️ **Inference, not verified fact: match-end XP crediting has almost certainly NEVER worked in production** (`0062` exists because `PROFILE_INTERNAL_TOKEN` never reached the prod game server) — unmeasured, do not upgrade it. Grounding: `ai-agents/knowledge-base/reports/2026-09-04-profile-backend-clean-slate-survey.md`

## Decisions

### Project ADRs (this project's series — 101+)

> `ADR-001`–`ADR-099` are **fkit toolkit** ADRs and do not live in this repo. See [[decisions/adr-numbering-two-series]].

- [[decisions/adr-numbering-two-series]] — Owner ruling reserving 001–099 for the toolkit series; this project's ADRs start at 101. Also carries the immutability rule: `proposed` is a draft promoted in place, `accepted` is history, and the in-place amendment carve-out covers **clarifications only** — a reversal needs a superseding ADR
- [[decisions/adr-101-fail-soft-xp-crediting]] — Match-end XP crediting is fail-soft with bounded retries and no durable queue; outage XP loss is silent and unrecoverable
- [[decisions/adr-102-privilege-refresher-fails-open]] — Cosmetic entitlements fail open until `cosmetics.json` loads; **accepted 2026-08-09 while nothing is sold, expiring at the first paid entitlement of any kind** — trigger ruled three times in one day, all three kept visible
- [[decisions/adr-103-identity-trust-seam]] — Client-asserted Yandex IDs accepted for earned XP behind one trust seam; signed verification deferred until the Yandex secret key exists
- [[decisions/adr-104-archiving-disabled]] — Match archiving disabled behind one config switch until S3-backed, citizen-gated archival ships
- [[decisions/adr-105-compact-maps-out-of-rotation]] — Compact maps removed from public rotation until the map binaries are regenerated with correct shore bits
- [[decisions/adr-106-flags-suppressed]] — Real-country flags suppressed by parse-then-drop; flags reserved as a future paid non-country cosmetic
- [[decisions/adr-107-turn-interval-1-5x]] — The game runs at 1.5× upstream tick rate (66.7 ms); owner-supplied rationale (2026-08-09): two goals — quicker matches and a higher interstitial rate — with 1.5 chosen by playtesting and 2× rejected as too fast; neither goal measured
- [[decisions/adr-108-active-sprint-pointer]] — Direction for the next fkit update: the active sprint should be owner-set via an optional `.active-sprint` pointer, derived only as fallback, failing loud when stale. **Nothing in this repo changes** — the interim rule is to ask for status by name
- [[decisions/adr-109-worker-index-placement-contract]] — The worker index is a fixed placement contract computed independently by client, worker, nginx and master; to move a game, move its **ID** (rejection-sample it), never the index. Private-lobby exposure and wedged-but-alive workers are recorded, owner-accepted tradeoffs
- [[decisions/adr-110-ai-winner-allowed]] — 🔴 **CARRIES A KNOWN EXPIRY — never cite it as settled-forever.** Accepted 2026-09-03: an **AI player (`PlayerType.AiPlayer`, real `clientID`) MAY be declared winner**; the predicate stays `clientID() !== null` with **no `PlayerType.AiPlayer` exclusion**, as **one policy across FFA (`0206`) and Team (`0205`)** — a second explicit owner ruling. An AI winner is **credited nothing** (three independent gates in `selectMatchCredits`); what the award does is **unblock crediting for every real player** ⚠️ **CARRY THE QUALIFIER: only where a LIVING CLIENTFUL player exists to award to.** Where every clientful player is already eliminated, `players()` filters to `isAlive()`, no winner can be named and nothing is unblocked. 🔴 **Dropping this qualifier is exactly how ADR-110's false Consequences bullet came to be written — see [[decisions/adr-110-ai-winner-allowed]].** 🔴 **The owner accepted it knowing a durable, player-visible winner surface is PLANNED** ("None today, but planned") — so its strongest counter-argument was **overridden with eyes open, not refuted**, and it **MUST be re-examined before any leaderboard, match history, announcements feed, share card, or other surface naming a winner outside the end-of-match modal ships.** Nations and Bots stay excluded; the **all-Nations team** case was deliberately deferred to `0205`'s plan. 🔴 **CORRECTED 2026-09-04: this ADR now rules on a predicate that exists in NO SHIPPED CODE** — ~~`0206` shipped it 2026-09-03~~ **`0206` was REVERTED and never deployed; `0205` is unbuilt.** ⛔ **The decision is unaffected and NOT superseded** — a live policy awaiting its first implementation; its only in-code trace is one deliberately-kept comment. 🔴 **A false Consequences bullet was STRUCK** (*"Public FFA matches where every human dies now complete and credit XP"* — **false when written, and at acceptance**: T3's *"while AI players are still alive"* qualifier had been dropped). ⏳ **T1 carries a scheduled expiry** — `0211` removes the winner-gating it rests on, ⛔ **without** firing the re-raise trigger. ⚠️ **Re-raise pointer corrected `0206` → `0208`; that trigger is still UNFIRED**

### Product Strategy & Sprints
- [[decisions/product-strategy]] — Strategic logic: retention-first sequence, experiments policy, key analytics data
- [[decisions/sprint-1]] — Sprint 1 (done): analytics baseline, Sentry, mobile quick wins, ghost rate investigation
- [[decisions/sprint-2]] — Sprint 2 (done): tutorial, auto-spawn, auto-expansion, zoom-to-territory, announcements
- [[decisions/hotfix-post-sprint2]] — Post-Sprint 2 hotfix (done): experiment analytics, skip button, UI:Tap, HF-6/7/9
- [[decisions/sprint-3]] — Sprint 3 (done): server observability, stale-build fixes, map preload, and deferrals to Sprint 6
- [[decisions/sprint-4]] — Sprint 4 (mixed) — 🚢 **a production deploy landed 2026-08-29 (`362a2f9`), putting the whole outage track, `0066`, `0067`, `0068`, `0198`'s fix and `0063`'s config into production; citizenship stays dark BY DESIGN** (`PROFILE_INTERNAL_TOKEN` left blank, card flag off): profile T4/T5/T6/T8, citizenship XP UI, payments infrastructure (0019), degraded-mode UX (0049), the independent 0041/0042/0046 tasks, the 0054 card-hide flag, the 0055 outage half, and the 0066 licensing remediation done (all agent-closed, not owner-verified; **0066 deployed in `362a2f9` and its licensing gate DEMONSTRATED 2026-08-30**); 0017/0018 re-scoped local/mock-first 2026-08-23 and built+reviewed 2026-08-24 but open pending live tails — the go-live now hangs on 0065 (blocked by 0014, 0062 AND 0195; its "0066 DEPLOYED" flip-ON gate is now demonstrated, which unblocks nothing else); the 2026-08-22 outage track **closed 2026-08-28** (0057→0056→0192→0194, with 0193 alongside — all agent-closed, none owner-verified, none confirmed deployed); of the promoted config-drift tasks **`0060` and `0063` are now closed and `0062` remains open** (`0060` closed 2026-09-01, repo-only); **Phase 2 citizenship benefits `0067`/`0068` built 2026-08-28, both agent-closed and both awaiting deploy**, alongside four new briefs (`0195`–`0198`) of which `0198` was a **live production defect** — its fix deployed in `362a2f9` and the task **closed 2026-08-30 on local proof only**, its production check waived as unsatisfiable, re-homing its one open product question to `0199` on the Backlog board; a fifth brief `0200` (the `supertest` flake) was promoted in 2026-08-29. 🚢 **Four more tasks closed 2026-09-01 — `0196`, `0195`, `0200`, `0060` — all agent-closed, none owner-verified, and NONE live in production** (`0195`/`0060` repo-only pending deploys, `0200` shipped no code at all), filing `0201`/`0202` onto the Backlog board. 🔧 ~~This board has ZERO `🔄 In progress` rows~~ **— FALSE as of 2026-09-02.** 📌 **BOARD RECONCILED AND RE-COUNTED 2026-09-02: ~~47 done · 6 blocked · 4 backlog~~ → 48 done · 6 blocked · 3 backlog · 3 cancelled · 1 in progress — of 61** (re-counted after `0022` closed later the same day). The one in-progress row is **`0064`**, the config parity guard, started that day; **`0021` and `0022` closed** the same day — 🔧 **`0022`'s founding premise was REFUTED (PR #77's net effect on that path is zero), and risk 2 was split out to `0205`.** ✅ **Its findings are INGESTED as of 2026-09-03** — 🚨 **and they carry a LIVE production defect: an FFA match led by a bot or a Nation silently loses the whole match's match-end XP for every player, fixed only by `0206` — ~~unscheduled~~ **built and closed 2026-09-03 (agent-closed — not owner-verified); NOTHING DEPLOYED, so the defect is still live in production, and the fix does not cover the case where every clientful player is eliminated before the threshold.** See [[tasks/win-check-clientless-leader-guard]] and [[decisions/clientless-leader-win-policy]]. Three board-invisible briefs (`0020`, `0021`, `0022`) were appended here on owner Ruling A, four more (`0023`, `0024`, `0028`, `0043`) to the Backlog board. 🚨 **27 of the 49 done rows carry `(agent-closed — not owner-verified)` — no human verified that work, and `/fkit-status` collapses every `✅` variant to plain `done`, so the board reads greener than the evidence supports** (known, ADR-033, not a defect). Every other open row is `🚧 Blocked` or `🔲 Backlog`, which is **not** the same as the sprint being finished. 📌 **RE-COUNTED 2026-09-03 (closing lint): 62 rows — 49 done (27 agent-closed) · 6 blocked · 3 backlog · 3 cancelled · 1 in progress (`0064`)** (wiki count, agreeing with the producer's). ~~The one change is **`0206` PROMOTED ONTO THIS BOARD**, arriving as `🔲 Backlog`: scheduled is NOT started, and planning comes first.~~ 🚢 **`0206` was promoted, planned, built, reviewed AND CLOSED all on 2026-09-03** — and 🔴 **REVERTED 2026-09-04 on an owner ruling: its behaviour is NOT in the game and was NEVER DEPLOYED.** ⚠️ **Its row still reads `✅ Done`, correctly and deliberately — the WORK was done, the EFFECT was reverted.** ⛔ **`0206` was NOT buggy and did NOT cause the stall; the plan's PREMISE was disproved by measurement.** 🔴 **The XP loss is STILL OPEN AND STILL LIVE** — measured: a Nation at 100.0 % of the map, the match never ending, **no `handleWinner`, no `creditMatchXp`, no player stats.** It spawned four briefs (`0207`–`0210`) and, at its revert, a fifth. 📌 **RE-COUNTED 2026-09-04: 64 rows — 49 done (27 agent-closed) · 6 blocked · 5 backlog · 3 cancelled · 1 in progress**, the two new rows being **`0208`** (raised to `High`) and **`0211`**, both `🔲 Backlog` and neither started. 🔴 **ORDERED: `0208` deployed and collecting data BEFORE `0211` ships** — ✅ planning/building `0211` in parallel is fine, **only the ship is ordered**, and ⚠️ **neither task is `🚧 Blocked`**. 🔴 **2026-09-04: the board's claim that "the profile box is live" was CORRECTED — it is UNVERIFIED and no longer claimable** (owner ruling, given live in session). ⚠️ **A same-day annotation reading "THERE IS NO PROFILE HOST" OVERSTATED the owner's position and is withdrawn: the profile VPS and the S3 bucket physically EXIST and are REUSED IN PLACE, and their provisioning state is UNKNOWN AND UNVERIFIED.** ⛔ **The whole T4 profile track really shipped.** An owner-ruled **wipe-and-rebuild ONTO THE EXISTING RESOURCES — not a procurement** — is **scheduled into THIS sprint** as **`0213` (epic) → `0222`, plus `0201`** — ⚠️ **those rows are NOT yet reflected in this page's counts or its board table** (this pass corrected claims only; no ingest was run). ⚠️ **Capacity risk recorded once and not re-argued:** ~4–8 engineering days plus owner actions, on top of an already open live-verification tail — ✅ **reduced somewhat by the reframe: no procurement wait, and the `0216` reachability spike is runnable today**
- [[decisions/sprint-4b]] — Sprint 4b (done): interim public-match variety with compact maps, Duos/Trios/Quads, and weird-setting modifiers
- [[decisions/sprint-4c]] — Sprint 4c stabilization (closed; plan archived to `sprints/done/` 2026-08-24): quick wins done, source maps enabled, lobby/map fetch fixed, mobile WebGL deferred
- [[decisions/sprint-backlog]] — No-sprint backlog across both unsprinted boards: monitoring, mobile WebGL, worker init, bot anti-SAM nuke tactics, weird-mode cleanup, FuseTag/GutterAds fixes, the `0001`–`0011` briefs and cosmetics monetization dependency chain, plus the 2026-08 additions — heal tasks `0050`–`0053`, outage follow-ups `0058`/`0059`/`0061`/`0064`, the `0057`/`0062` promotions, the owner-ruled email-subscribe fold-in to `0048`, `0199` (the Yandex invite-portal product question, filed here 2026-08-28), the 2026-09-01 test-harness pair `0201`/`0202`, and the 2026-09-02 additions — `0023`, `0024` (which had **no `## Sprint` heading at all**), `0028` (hint videos: **MP4 not GIF, measured**; topic #6 unruled either way) and `0043` made board-visible, plus `0203`, the ten-item pre-arming gate for the config guard, and the 2026-09-02 win-condition pair `0205` (Teams Bot-team stall, ex-`0022` risk 2 — **filed as `0204` and renumbered on an owner ruling; `0204` has no task folder here**) and 🚨 **`0206`, the FFA award that is the ONLY fix for a live, silent, whole-match XP loss in production — ~~unscheduled, unstarted~~ ~~PROMOTED INTO SPRINT 4 on 2026-09-03; still `🔲 Backlog` and unstarted~~ PROMOTED, BUILT AND CLOSED 2026-09-03, its row here kept as `➡️ Moved`; 🔴 nothing deployed**. 📌 **`0207` appended 2026-09-03** (the `WinModal` participation comment, comment-only), **`0208`, `0209` and `0210` appended the same day** out of `0206`'s close — measure incidence (📌 **widening SETTLED: Part A multiplayer, Part B Singleplayer, not to be merged**), define `placement` semantics then fix the literal `1`, and Singleplayer reports nothing to the platform leaderboard (owner-ruled) — **all four `🔲 Backlog` and unscheduled**, and **`0205`'s premise was confirmed by simulation** the same day with its Low–Medium rank **weighed and held**. 📌 **`0201` is now `🔄 In progress` with its plan gate RULED — fold the shell harnesses into `npm test`; CI explicitly rejected — and the `0201`-then-`0202` order plus `0202`'s `Low` rank are owner-ruled: do not re-litigate either.** 🔧 **`0047`'s "After T4g (PR #125) merges" gate was stale and is CLEARED — nothing open blocks it**
- [[decisions/sprint-5]] — Sprint 5 (planned): coin economy, clans, cosmetics, map voting, replay
- [[decisions/sprint-6]] — Sprint 6 (planned): historical multiplayer maps, paid campaign packs, mobile warning; its "Sprint 5 cosmetics store" prerequisite was corrected 2026-08-09 — no such store exists
- [[decisions/cancelled-tasks]] — HF-5, feedback match history, HF-11e, tutorial action-pause, HvN balance, compact-map runtime fallback, and guest-first profile XP cancellations
- [[decisions/personal-data-152fz-compliance]] — Hash-based 152-ФЗ avoidance invalidated; notification/consent work deferred to backlog with accepted risk

### Legal & Operations
- [[decisions/fkit-transfer-blueprint]] — Extracting the two-model agent OS into a reusable kit: the generic/project seam, the routing manifest, and single-sourced skills that kill copy drift
- [[decisions/licensing-compliance]] — AGPL, CC BY-SA, source-access, and OpenFront asset/trademark constraints for GeoConflict; the 0025 audit (2026-08-23) found one violation (proprietary music) — remediation 0066 **deployed 2026-08-29 in `362a2f9`** and its live checks **run and passed 2026-08-30**, so the paid-IAP licensing gate is **DEMONSTRATED**; the **0025 audit task itself closed 2026-08-31** with H1 verified in production that day and **H3 the one finding still open, owned by `0073`** — and this clears that gate only; 0065 still waits on `0014`, `0062`, `0195`

### Bug Fixes & Investigations
- [[decisions/windoworigin-url-join-defect]] — 🔧 **Fix DEPLOYED 2026-08-29 (`362a2f9`); task 0198 CLOSED 2026-08-30 on local proof only, because its production proof is UNREACHABLE and was WAIVED** — the private-lobby buttons are `display: none` on the Yandex template (task `0198`): `windowOrigin` is `origin + pathname`, so concatenating a worker route onto it misses the route on the `/yandex-games_iframe.html` production entry point — a PUT/POST 404s, a GET gets the SPA fallback's 200 — and private-lobby start silently fails on Yandex Games. Review closed 2026-08-28 at `🚧 Blocked — awaiting deploy proof`, superseded by the 2026-08-30 waiver and close; six owner-confirmed residuals. Carries the durable rule, the non-root testing requirement, and the corrected Express-not-nginx substitution mechanism
- [[decisions/yandex-invite-portal-boundary]] — ❓ **Open, unruled** (task `0199`, Backlog / Unscheduled at producer rank, owner-confirmed placement): a private-lobby invite from the Yandex build lands the recipient **outside the portal iframe**. `0198` fixed the **path** on that line; this is the **host**. Records the code fact that `yandex-games_iframe.html` sets `flashist_isYandexPlatform = true` unconditionally, so an off-portal load still enters Yandex platform mode — **what that path does off-portal is NOT established and must be measured**
- [[decisions/config-parity-failure-class]] — the recurring class *a variable that never reaches production*: `0062`, `0063` and `0195`, the first outside `deploy.sh`; `0064` is the guard and must land after all three. **`0063` is fixed, deployed and fully evidenced (2026-08-30); `0062`'s fix shipped with the token deliberately BLANK**, so a presence check would pass on a variable doing nothing; **`0195`'s fix shipped 2026-09-01 but is NOT deployed**. ⚠️ **All three have now shipped a fix and none is demonstrated working in production.** 🚀 **`0064`, the guard, is STARTED 2026-09-02** — built, review round 2 CONVERGED, **shipping report-only (it exits zero and cannot fail a deploy)**, so nothing on the execution order is unstarted now. ⚠️ **Arming `--enforce` is now TEN gate items, not two**, split out as `0203` on the Backlog board; 🚨 **R1, owner-acknowledged: the client parity check PRINTS GREEN WHILE INCOMPLETE**, and ❓ one gate question — does Docker Compose `env_file` tolerate an `export` prefix — is **explicitly unresolved**
- [[decisions/clientless-leader-win-policy]] — 🚨 **A production defect, LIVE IN PRODUCTION and FIXED NOWHERE — not in production, not in the repo** (~~*"fixed only in the repo"*~~ 🔴 **struck 2026-09-04: `0206` was reverted, so nothing is fixed anywhere**)**:** when a bot or a Nation leads FFA at the win threshold, no `winner` message reaches the server, so `creditMatchXp` never runs and **the whole match's match-end XP is silently lost for every player**. Task `0022` shipped the **guard only** (the match no longer wedges; a human can still win later); ~~the **award that closes the XP loss is `0206`**~~ 🔴 **`0206` was REVERTED 2026-09-04 and NEVER DEPLOYED — the defect is fixed NOWHERE.** ⚠️ It was also **a no-op in the case that actually loses the XP** (`players()` filters to `isAlive()`), which measurement showed is **the** case, not a corner case. ~~The shipped award also ENDS public FFA at 80 %~~ 🔴 **struck — that never reached a player.** **The replacement is `0211`** (credit at elimination or match end, independent of any winner). 🔴 **And Team mode has the SAME defect** — `checkWinnerTeam()` shares the guard shape, unnoticed across `0022`/`0206`/`0205`. Also carries the **keep-them-apart table** for the two things conflated repeatedly that day — the `placement` rank **never leaves the browser**, while the `points` **do reach the Yandex platform** — and **ADR-110** (accepted 2026-09-03, one policy across both modes, 🔴 **with a known expiry**) and the 2026-09-03 **confirmation of `0205`'s premise by simulation**. Plus three owner rulings of 2026-09-02: the brief's own prescribed fix was **REJECTED — it would have regressed PR #77**; `0022` ships guard-only; and the award applies to **both branches (timer AND territory threshold), ruled once for `0205` and `0206` together** so FFA and Team stay on one policy — with a **hard tutorial re-check** riding on it. ⚠️ Reachability is structural reasoning, **not measured**
- [[decisions/autospawn-late-join-fix]] — Fix for auto-spawn failure when joining during catch-up (late join / reconnect)
- [[decisions/double-reload-fix]] — Fix for double page reload on browser refresh caused by orphaned `#refresh` history push
- [[decisions/archive-archival-strategy]] — Split archive work: disable noisy dead path now, defer S3-backed citizen archival until citizenship exists
- [[decisions/hvn-balance-pr70-no-ship]] — No-ship review for the cancelled Sprint 4 Humans vs Nations balance attempt
- [[decisions/registry-image-policy]] — Trusted vs untrusted image rules, registry visibility policy, and rollback/retention guidance
- [[decisions/stale-build-zombie-tabs]] — Investigation and fix for users persisting on old builds (HF-11a/b/c/d)
- [[decisions/vps-credential-leak-response]] — Incident postmortem: Docker build-context secret leak path, deploy hardening, and trusted recovery workflow
- [[decisions/profile-deploy-hardening-review-loop]] — Reset of the unbounded profile-deploy review loop into bounded T4 slices and fixed acceptance criteria
- [[decisions/profile-storage-strategy]] — Player profile DB storage: Option B (typed columns + jsonb overflow), `xp bigint`, `persistent_id text` — chosen in T5 before the first migration
- [[decisions/incident-2026-08-22-public-lobbies-outage]] — 2026-08-22 prod outage: worker 16 died, crash recovery has never worked (`worker.process.env` bug), the all-20 gate stalled scheduling for ~3.5 h; task split 0055→0057→0056 plus the config-drift findings 0060–0064

## Tasks

- [[tasks/session-start-sequence]] — Session start event sequence (Session:Start → Device:Type → Platform:OS → Player:New/Returning)
- [[tasks/build-number-tracking]] — HF-7 build segmentation: original GA custom-dimension approach and current native build-field tracking
- [[tasks/mobile-quick-wins]] — Task 3: retina off, 30fps cap, particles reduced on mobile
- [[tasks/spawn-ux]] — Tasks 4b + 4e: zoom-to-territory function and expanding ring spawn indicator
- [[tasks/stale-build-detection]] — HF-11b/c/d: `/api/version` endpoint, client polling, non-dismissible refresh modal
- [[tasks/ui-click-multiplayer]] — Investigation confirming `UI:ClickMultiplayer` fires on per-lobby JOIN attempts
- [[tasks/player-profile-store-investigation]] — Sprint 4 investigation recommending Postgres, now on a dedicated reg.ru profile/API VPS, and identifying the Yandex identity gap
- [[tasks/profile-schema-contract]] — Sprint 4 T1 shared PlayerProfile v1 contract, migration function, guest-profile factory, and tests
- [[tasks/yandex-identity-plumbing]] — Sprint 4 T3 client-to-server Yandex unique-ID plumbing for later profile crediting
- [[tasks/profile-server-skeleton]] — Sprint 4 T4a standalone profile-service `/health` skeleton, logger, and validated port
- [[tasks/profile-api-url-config]] — Sprint 4 T4b public profile API URL plumbing through runtime config and `/api/env`
- [[tasks/profile-docker-image]] — Sprint 4 T4c allowlist-copy, linux/amd64 Docker image for the profile service
- [[tasks/profile-vps-provisioning]] — Sprint 4 T4d dedicated reg.ru VPS provisioning, TLS/network boundary, and operator-verified RU residency. 🔴 **2026-09-04: NO PROVISIONED HOST EXISTS** — the provisioning **script** survives and is what the rebuild runs; also records the gaps it never covered (no log rotation, no image prune, no OS hardening, a `restart: on-failure` policy that misses a bare Docker daemon restart)
- [[tasks/profile-build-push-digest]] — Sprint 4 T4e1 local amd64 profile-image build, registry push, and fail-closed immutable digest resolution
- [[tasks/profile-onbox-stack-gate]] — Sprint 4 T4e2 on-box compose stack, health gate, systemd lifecycle, and digest rollback
- [[tasks/profile-deploy-wiring]] — Sprint 4 T4e3 SSH/SCP deploy wiring, secret staging, and end-to-end digest passthrough
- [[tasks/profile-image-secret-scan]] — Sprint 4 T4f built-image layer byte scan that blocks secret-bearing profile image pushes
- [[tasks/profile-server-bring-up-runbook]] — Sprint 4 T4i operator runbook for bringing the profile service live on a reg.ru VPS. 🔴 **2026-09-04: THE HOST IT BROUGHT UP NO LONGER STANDS** (owner-ruled) — the runbook itself is intact and is the asset the rebuild reuses. 🚨 **Carries a TRAP: its "leave `PROFILE_INTERNAL_TOKEN` blank, the box auto-generates it" step is now FALSE** and yields a silent **401 on every credit call**, with the XP **lost, not queued**; a stale `PROFILE_INTERNAL_ALLOW_IPS` is a second, independent **403** on the same path
- [[tasks/profile-deploy-hardening]] — Sprint 4 T4g argv-safety, deploy locking, atomic record, and wrong-host preflight hardening
- [[tasks/profile-game-server-deploy-env]] — Sprint 4 T4h propagation of `PROFILE_API_URL` through the game-server deploy environment
- [[tasks/profile-backend-db-api]] — Sprint 4 T5 Postgres-backed profile API, repository, migrations, readiness, and idempotent XP crediting
- [[tasks/profile-match-end-crediting]] — Sprint 4 T6 game-server match-end XP crediting into the profile API. 🔴 **2026-09-04: the reason it no-ops may be bigger than `0062` — the profile host EXISTS, but what it serves is UNVERIFIED** (owner-ruled; ⚠️ *a same-day "there is no profile host to credit" annotation overstated the position and is withdrawn*). ⚠️ **Inference, not verified fact: this path has almost certainly NEVER worked in production** — unmeasured, do not upgrade it
- [[tasks/postgres-backup-routine]] — Sprint 4 T8 encrypted daily off-box profile DB backups, restore runbook, and deploy-time smoke validation. 🔴 **2026-09-04: WHETHER ANY BACKUP IS RUNNING IS UNKNOWN** (owner-ruled; ⚠️ *a same-day "NO BACKUPS ARE RUNNING — there is no box" annotation overstated the position and is withdrawn — the box and bucket exist and are reused in place*); the machinery, restore path included, exists and fails closed at deploy. 🚨 **Three gaps now owned by `0218`:** restore never exercised against non-empty data (and the old drill's command line no longer works), **nothing reads the backup-freshness marker and there is no monitoring of any kind**, and 🔴 **the `age` private key has no recorded home** — the owner did not know what it was when asked, which the rebuild must design out **before the first backup runs**. 🔴 **The `age`-key question is RE-OPENED as a live owner decision:** the reused bucket still holds old encrypted objects, permanently unreadable without the old private identity — **purge, or keep pending a search?** — tracked in **`0222`** (renamed `0222-profile-cleanup-obsolete-secrets-and-old-bucket-objects`, rescoped from decommissioning to cleanup)
- [[tasks/personal-data-compliance-investigation]] — Sprint 4 152-ФЗ investigation whose hash-based conclusion was later overturned
- [[tasks/yandex-payments-investigation]] — Sprint 4 investigation recommending signed Yandex purchase verification and a session-cached catalog in `FlashistFacade`
- [[tasks/start-screen-redesign-investigation]] — Sprint 4 design investigation that locked the two-tab start screen and citizenship card placement
- [[tasks/start-screen-redesign-implementation]] — Sprint 4 implementation of the two-tab start screen, citizenship card shell, tab persistence, localization rename, and tab analytics
- [[tasks/citizenship-xp-progress-ui]] — Sprint 4 live citizenship card wiring: profile API read, XP progress, citizen state, and degraded zero-state handling
- [[tasks/citizenship-card-guest-cta-no-sdk]] — Sprint 4 citizenship-card fix hiding the Yandex login CTA outside a Yandex SDK context
- [[tasks/app-bootstrap-single-entry-point]] — Sprint 4 explicit client bootstrap refactor with bounded Yandex platform init, degraded mode, language-before-render, and app-chunk recovery
- [[tasks/legal-vat-investigation]] — External VAT/legal check that cleared the pre-launch payments gate
- [[tasks/global-announcements]] — Re-enabled repo-authored announcements bell and popup with unread badge
- [[tasks/feedback-modal-space-key]] — Sprint 4 fix allowing spaces in the in-match feedback modal and suppressing gameplay hotkeys while typing
- [[tasks/email-subscribe-modal]] — Start-screen and win-screen email opt-in modal backed by Telegram delivery
- [[tasks/map-preload]] — HF-13 background terrain preload on JOIN with preload-hit analytics
- [[tasks/ai-lobby-slot-bug]] — Sprint 4 fix preserving one human slot in mixed AI/public lobbies
- [[tasks/tutorial-no-nations]] — Tutorial config change removing nation-controlled opponents
- [[tasks/tutorial-build-menu-lock]] — Tooltip-5 guardrail that restricts building to City
- [[tasks/tutorial-reduce-bots]] — Tutorial config change lowering tutorial bot count from 400 to 100
- [[tasks/missions-difficulty-investigation]] — Sprint 4 investigation finding missions are generated, not authored; follow-ups exclude zero-nation maps, sort by prebuilt nation counts, and slow the Medium nation ramp
- [[tasks/solo-win-condition-fix]] — Solo-mode fix showing a distinct loss when an opponent reaches the win threshold; ✅ **CLEARED 2026-09-02** of `0022`'s suspected regression — PR #77's net effect on that path is zero
- [[tasks/win-check-clientless-leader-guard]] — Sprint 4 task `0022`, closed 2026-09-02 (agent-closed — not owner-verified). 🔧 **Its founding premise was REFUTED** — not a PR #77 regression; the `undefined` clientless winner is **original to the fork** (`feea527`). It survived because a **worse defect** sat underneath: a clientless leader winning FFA emits no `winner` message, so **`creditMatchXp` never runs and the match's entire XP is silently lost for every player** — and the predicate `clientID() === null` catches **Nations, not just bots**. Shipped **risks 1 and 3 only**: the guard at `WinCheckExecution.ts:69-77`, placed **before** `active = false` so a human can still win later, plus a `win_modal.bot_team` label fix. 🔴 **The XP loss is STILL LIVE AND FIXED NOWHERE** — ~~`0206` is the fix~~ **`0206` was REVERTED 2026-09-04, never deployed, and was a no-op in the case that loses the XP.** The fix is now `0211`. 🔴 **`checkWinnerTeam()` shares the guard shape, so Team mode loses its XP identically** — found at the revert. ⚠️ **Risk 1 has NO live reproduction** (owner-declined): synthetic tests only. Risk 2 split to `0205`
- [[tasks/teams-bot-team-win-stall]] — Task `0205`, the Teams half of the same defect class (risk 2, split out of `0022`): the Bot team leads, the guard returns above `active = false`, and the match is **unwinnable while it leads**. `🔲 Backlog`, unscheduled, **nobody building it** — owner-reviewed and deliberately left there. ✅ **PREMISE CONFIRMED 2026-09-03 by headless deterministic simulation** (real World map, 651,609 land tiles, 400 bots, `DefaultConfig`): bot team on top **12/12**, `setWinner` **0/12**, win check still `active` **12/12**; the guard is proven **CAUSAL** (same board, `gameType: Singleplayer` → `setWinner("Bot")`, 3/3); the 95 % route is crossed at **ticks 6180–9480 ≈ 7–10 min** on the **shipped public config**, so *"rare"* was **rare because humans play, not because 95 % is hard**; the defect is **passivity-dependent** (0–20 % active → stalls 3/3 each, 60–100 % → 0/3); `HumansVsNations` is the one public Team config exempt, and **only incidentally**. ⚠️ **Production frequency is UNMEASURED — a simulator result, not a field observation**, and the "active" players were `FakeHumanExecution` at Medium, better than a casual human, so **the real crossover is probably higher than 40 %**. Rank **Low–Medium weighed and HELD** against that evidence on an owner ruling. 🔴 **ADDED 2026-09-04: `checkWinnerTeam()` does not merely STALL — it LOSES THE MATCH'S XP, exactly as FFA does**, found at `0206`'s revert and unnoticed across `0022`/`0206`/`0205` (⚠️ reported, **not re-verified by symbol**). The owner ruled `0211` covers **Team mode as well as FFA**; ⛔ **that does NOT merge, replace or gate this task, whose status, scope and rank are UNCHANGED** — `0205` answers *who should win a stalled Team match?*, `0211` answers *do those players get their XP?*. ⚠️ **`0211` may make PART of this task's justification moot, but does not settle its question.** Policy/measure/branch-scope decided; **two questions stay open for plan time** — tie-breaking, and the deliberately deferred **all-Nations team**. 🔧 Corrects the brief's mechanism claim: Nations get their team from `assignTeams()`, **not** `maybeAssignTeam()`
- [[tasks/winmodal-participation-comment-correction]] — Task `0207`, filed 2026-09-03 to the Backlog board, unscheduled: `WinModal.buildPlayerParticipation`'s doc comment says AI players are skipped from participation; the skip is on `clientID === null`, which excludes **Bots and Nations** and **includes** AI players. **Comment-only, `src/client/` only, zero user impact today** — the server's frozen-start-roster gate makes the entries inert — **but a live trap** for whoever plans `0206`, because it contradicts ADR-110 on the exact predicate that ADR just ruled on. 📌 **`0206` was planned and BUILT the same day, before this was fixed, and did NOT trip on it.** 🔴 **`0206` was then REVERTED 2026-09-04 — which changes nothing here: this task never claimed `0206` had shipped, and its reason to exist is UNCHANGED and if anything STRONGER.** 🚩 **The trap now aims at `0211` and `0205`**, and the architect re-confirmed the defect independently on 2026-09-04. ⚠️ "Harmless today" rests entirely on that gate and is **not permanent**

- [[tasks/ffa-clientless-leader-fallback-award]] — Sprint 4 task `0206`. 🔴 **REVERTED 2026-09-04 ON AN OWNER RULING — ITS BEHAVIOUR IS NOT IN THE GAME AND WAS NEVER DEPLOYED.** ⚠️ **Its status is still `done`, correctly and deliberately: the WORK was done, the EFFECT was reverted — two different facts.** Promoted, planned, built, reviewed and closed all on 2026-09-03; **Codex "No findings."**, three low Claude findings dispositioned, **play-test gate PASSED** — ✅ **none of that is retracted.** ⛔ **`0206` was NOT buggy and did NOT cause the stall.** 🔴 **The plan's PREMISE was disproved by live measurement:** `players()` filters to `isAlive()` (`GameImpl.ts:421-423`), so with every human eliminated the award found nobody and took **the same early `return` as before `0206`** — **a Nation reached 100.0 % of the map and the match still did not end** (⚠️ observed in a **PRIVATE** instrumented FFA; 🔴 **the NATION case has been assumed twice and OBSERVED ZERO TIMES in a PUBLIC lobby — only a Bot was ever seen crossing the threshold — so treat it as a HYPOTHESIS there, never as established**). Its **only** live effect was crowning a survivor on ~0.5 % against a bot on 80.2 %, **which the owner reproduced and rejected.** ~~Public FFA now ends at 80 %~~ **struck — never reached a player.** ⚠️ **`WinCheckExecution.ts` is deliberately NOT byte-identical to its pre-`0206` state** — one ADR-110 comment was kept, the **only in-code trace of that ADR.** Spawned `0207`–`0210`, and at its revert `0211`
- [[tasks/measure-clientless-leader-and-solo-awards]] — Task `0208`. 🔄 **SCHEDULED INTO SPRINT 4 on 2026-09-04, reversing the 2026-09-03 *"file a brief, don't schedule"* ruling** (⛔ **that ruling was not wrong — it is SPENT**), and **re-ranked `Medium` → `High`**. 🔴 **Provenance SPLIT: the instruction to raise it is an OWNER RULING; the value `High` is the PRODUCER'S judgement** — ⛔ do not restate it as "the owner ranked it High". 🔴 **IT SHIPS BEFORE `0211`** — `0211` must not ship until this is **deployed and collecting data** (not merely merged or built); ✅ **`0211` may be planned and built in parallel**, ⚠️ **and NEITHER task is `🚧 Blocked`.** ✅ **Its Part A decay clock STOPPED** — `0206` never deployed, so the pre-fix denominator is measurable again; **`0211` shipping is what would destroy it.** 🔴 **Now load-bearing for THREE decisions:** ADR-110's re-raise trigger (still unfired), `0211`'s survivor scope, and `0205`'s rank. **Instrumentation only.** 📌 **Widened 2026-09-03 to TWO HALVES THAT MUST NOT BE MERGED** — **Part A** multiplayer clientless-leader incidence (`src/core/`; ⛔ **excludes AI players** — an AI win is a normal win under ADR-110), **Part B** Singleplayer platform-leaderboard award incidence (`src/client/`, via **both** `reportPlacements()` **and** the unguarded, farmable `reportParticipation()`). 🔴 **Part B counts `points`, never `placement`.** 🟢 **Part B has NO over-count problem** — `hasReportedParticipation`/`hasProcessedWin` are pre-existing production latches and Singleplayer runs one client, so **its denominator is matches, not client-matches**; ⛔ do not copy Part A's caveat onto it. ⚠️ **The folder name deliberately under-describes the task** — renaming would break inbound links. 🔴 **It does NOT gate `0210`**, and Part B is **a snapshot with an expiry date** — once `0210` ships, the counter reads zero forever by design
- [[tasks/credit-participation-xp-elimination-or-match-end]] — Task `0211`, **`0206`'s replacement**, filed by the revert on 2026-09-04 and **SCHEDULED INTO SPRINT 4 the same day**; status stays `🔲 Backlog` — ⚠️ **scheduled is not started, nobody is building it.** Rank **`Medium–High` — the producer's**, held three times that day; ⛔ **the owner ruled scheduling only and DECLINED "re-rank first".** **Credit participation XP at elimination OR match end, idempotent** — scoped by owner ruling to cover **SURVIVORS of a stalled match as well as eliminated players**, and **both FFA and Team mode**; ⛔ **Singleplayer explicitly OUT** — and ⚠️ **`0210`'s ruling was about platform LEADERBOARD POINTS, not profile XP; the two must NOT be read across.** ⛔ **NOT a revival of `0206`'s award** — it credits independently of any winner rather than crowning anyone. **XP holds at 10 flat, a deliberate hold.** 🔴 **It DELIBERATELY REVERSES the leaver rule** (`MatchQualification.ts:43-45`): an eliminated player who leaves is excluded today and would now be paid — **a knowing reversal that NARROWS, not deletes, the exclusion.** 🔴 **Its SHIP is ordered behind `0208`**; ✅ planning and building in parallel is fine, ⚠️ **it is NOT `🚧 Blocked`.** 📎 Design input: the architect's 2026-09-04 assessment — **idempotency already exists** (`(game_id, yandex_player_id)` primary key, XP increment gated on the insert, tested against real Postgres including the concurrent case) and 🔴 **the server does not learn a player was eliminated — the central design problem**
- [[tasks/placement-semantics-literal-one]] — Task `0209`, Backlog board, unscheduled, **`Low` — the producer's rank**. `const placement = +1;` is a unary plus on the literal `1`, so **every top-3 human is labelled placement 1**; `points` is correct and untouched. 🔴 **The framing it was filed with was OVERSTATED — the value is NOT sent to the Yandex platform**: `reportPlacement` passes only `params.points`, and `placement` reaches nothing but a `console.debug` under a TODO. **Today's harm is a wrong number in a browser debug log.** 📌 **Definition RULED 2026-09-03 — A, rank among humans** (B and C weighed and rejected); ⛔ **but the ruling settled the DEFINITION, not the EXPRESSION — `myIndex + 1` is still NOT prescribed**, because ties and eliminated/disconnected humans must be checked, not assumed. ⚠️ "Harmless today" is **not permanent** — acting on the TODO makes it a real platform write. 🔴 **UPDATED 2026-09-04: the frequency claim is STRUCK** — the extra class of matches `0206` would have made `reportPlacements()` fire in **does not exist**, because `0206` was reverted and never deployed. ✅ **The DEFECT is unchanged, pre-existing and still live**; only the blast radius reverts to the ordinary win path
- [[tasks/singleplayer-leaderboard-reporting-policy]] — Task `0210`, Backlog board, unscheduled, **`Medium` — the producer's rank, above `0209`** because **this one's wrong value DOES leave the client**. 🚨 **Live and farmable: in non-tutorial Singleplayer a human who LOSES to a bot is awarded 10 first-place platform points**, and every match *started* awards 1 more via the entirely unguarded `reportParticipation()`. **Pre-existing — NOT a `0206` regression**; `0206` correctly guards only its own new branch, while the older fall-through hands over the same points. 📌 **RULED 2026-09-03 — A: Singleplayer reports nothing** (B **doubly moot**, C rejected on farmability regardless of incidence). 🔴 **The guard must be ADDED, not repaired** — grep confirms **zero** game-type awareness anywhere in the client leaderboard path. ⚠️ **Nobody has ever run it live** — reproduce before fixing. 🔴 `0208` does **not** gate it. 🔴 **UPDATED 2026-09-04: `0206` was REVERTED, so its Singleplayer early-return and every line number in that file are GONE — locate by symbol, not line.** ✅ **This task's defect is COMPLETELY UNAFFECTED and the claim *"touched by neither `0022` nor `0206`"* is now true in a stronger sense.** ⚠️ **Its ruling is about platform LEADERBOARD POINTS — ⛔ must NOT be read across onto `0211`'s profile XP**
- [[tasks/telegram-link]] — Experiment-gated Telegram channel CTA on start/loading and game-end screens
- [[tasks/vk-link]] — Experiment-gated VK community CTA on start/loading and game-end screens
- [[tasks/nuke-trajectory-visibility]] — Sprint 4 visual polish making the nuke pre-launch targeting arc thicker
- [[tasks/teams-mode-max-teams]] — Sprint 4 server-side cap limiting regular public teams lobbies to 2, 3, or 4 teams
- [[tasks/sprint4b-mini-mode-investigation]] — Sprint 4b investigation finding compact nation-spawn risks and confirming Duos/Trios/Quads AI-fill compatibility
- [[tasks/sprint4b-duos-trios-quads]] — Sprint 4b public-only re-enable of Duos/Trios/Quads team-size modes with AI-fill coverage
- [[tasks/sprint4b-compact-map-rotation]] — Sprint 4b public compact-map modifier plus Mini/Мини lobby badge
- [[tasks/sprint4b-weird-setting-modifier]] — Sprint 4b weird-setting public modifiers and lobby badges
- [[tasks/monetization-analytics-spec]] — Sprint 4 analytics baseline for identity, match lifecycle, citizenship funnel, and ad-tier measurement
- [[tasks/analytics-p0-game-mode-segmentation]] — P0 analytics event pair segmenting fresh match starts into multiplayer versus solo
- [[tasks/analytics-p0-spawn-confirmation]] — P0 analytics event measuring server-confirmed spawn and time-to-spawn
- [[tasks/analytics-p0-match-duration]] — P0 analytics event measuring seconds from fresh match start to match end
- [[tasks/analytics-p0-player-days-played]] — P0 analytics event measuring cumulative unique local calendar days opened
- [[tasks/analytics-p0-yandex-login-status]] — P0 analytics event measuring Yandex logged-in, guest, and unknown session states
- [[tasks/analytics-p0-session-match-count]] — P0 analytics event measuring match starts per session via UUID-keyed localStorage, consumed before Session:Start
- [[tasks/cosmetics-serving]] — Sprint 4c fix restoring `/cosmetics.json` serving and deduplicating cosmetics fetch telemetry noise
- [[tasks/local-server-hash-guard]] — Sprint 4c guard preventing missing-turn hash messages from crashing local/singleplayer matches
- [[tasks/archive-endpoint-failures]] — Sprint 4c cleanup disabling broken archive writes through `archiveEnabled()`; S3-backed citizen archival deferred
- [[tasks/leaderboard-player-count]] — Sprint 4c quick win showing the human-like player count in the leaderboard's Players only label
- [[tasks/disable-compact-public-maps]] — Sprint 4c mitigation removing compact maps from public rotation while keeping opt-in compact paths
- [[tasks/s4c-enable-client-source-maps]] — Sprint 4c source-map upload pipeline for Uptrace client stack symbolication
- [[tasks/s4c-investigate-lobby-map-fetch]] — Sprint 4c fix for lobby polling noise and map manifest fetch failures
- [[tasks/incident-response-index]] — Security incident coordination page for the VPS credential leak response
- [[tasks/immediate-containment]] — Secret rotation, deploy freeze, and containment workflow for the leak response
- [[tasks/registry-image-audit]] — Historical image-trust audit plan; outcome was conservative pre-hardening image quarantine
- [[tasks/vps-access-hardening]] — Host-access review and SSH hardening checklist after leaked VPS credentials
- [[tasks/repo-build-context-hardening]] — `.dockerignore` plus allowlist Docker copies to block env-file leakage
- [[tasks/deployment-credential-hardening]] — SSH-key-first deploy contract and password-fallback demotion
- [[tasks/clean-redeploy-validation]] — Trusted post-hardening rebuild, redeploy, and validation workflow
- [[tasks/incident-postmortem-followups]] — Final postmortem capture and explicit security follow-up queue
- [[tasks/docker-secret-boundary-check]] — Automated guard against `.env*` or `COPY . .` regressions in Docker builds
- [[tasks/registry-image-policy-followup]] — Task that authored the registry visibility and image-retention policy
- [[tasks/investigate-clans-system]] — Investigation confirming clans parse/team-assign works; silent kick bug and no-UI gap documented
- [[tasks/compact-map-click-interaction]] — Investigation confirming compact boat-button failures come from lost `isShore` bits in `map4x.bin`, not click coordinates
- [[tasks/yandex-payments-implementation]] — Sprint 4 task 0019 payments infrastructure: catalog cache, profile-server HMAC verification endpoints, reconciliation; agent-closed, live verification deferred on catalog approval
- [[tasks/map-troops-labels]] — Sprint 4 task 0041 map label enhancement: troops `current / max` line plus red attacking-troops line in `NameLayer`; agent-closed, singleplayer-only live validation
- [[tasks/starting-gold-public-modifier]] — Sprint 4 task 0042 fifth weird sub-option granting real players 5M starting gold via the new `startGold` GameConfig field; agent-closed, live check owner-side
- [[tasks/feedback-remove-contact-field]] — Sprint 4 task 0046 152-ФЗ data minimization removing the feedback contact field end-to-end; agent-closed, post-deploy checks owner-side
- [[tasks/degraded-mode-ux-treatment]] — Sprint 4 task 0049 `isYandexDegraded()` citizenship-card connection-problem state clearing the earned/paid citizenship gate; agent-closed, healthy-SDK guest case unit-test-only
- [[tasks/hide-citizenship-card-flag]] — Sprint 4 task 0054 default-OFF `CITIZENSHIP_CARD_ENABLED` client flag hiding the citizenship card until 0017/0018 flip it ON at launch; agent-closed
- [[tasks/master-lobbies-worker-exit-diagnostics]] — Sprint 4 task 0055 outage-track fix: parseable empty lobbies body plus worker-exit `code`/`signal` logging in `Master.ts`, first-ever tests for that file; agent-closed, unpushed branch at close
- [[tasks/licensing-asset-audit]] — Sprint 4 task 0025, the production asset audit for proprietary/CDN assets: **1 confirmed violation (V1 music), 1 trademark item (A1 favicon), 3 hygiene items (H1–H3)**; everything else clean. **Closed 2026-08-31 (agent-closed — not owner-verified)** on an owner-ruled close, and it was the **last `🔄 In progress` row on the Sprint 4 board**. V1/A1 verified in production 2026-08-30, **H1 2026-08-31** (three live bundles grepped, 0 hits). 🚨 Carries the verification **method**: all seven purged paths returning `200` **is the pass** — byte-identity against a known-nonexistent control, never a 404. 📌 **H3 still open and OWNED by task `0073`.** ⚠️ Satisfies `0065`'s licensing prerequisite **and nothing else — `0014`, `0062`, `0195` remain; paid go-live NOT unblocked**
- [[tasks/licensing-remediation]] — Sprint 4 task 0066 licensing remediation from the 0025 audit: proprietary-music purge, original placeholder favicon, JWT fallback retarget; agent-closed 2026-08-24, **DEPLOYED in `362a2f9`**. ✅ **Live checks RAN AND PASSED 2026-08-30, so the licensing gate is DEMONSTRATED** — proven by byte-identity against a known-nonexistent control, **not** by a 404 (this server's `app.get("*")` never 404s, so seven `200`s are the pass). ⚠️ Clears one of 0065's four blockers; `0014`, `0062`, `0195` remain
- [[tasks/worker-routing-dead-worker-investigation]] — Sprint 4 task 0057 architect-led routing investigation: dead-index vs wedged-index severity, the placement contract behind ADR-109, and the confirmation (not revision) of the 18/20 quorum; agent-closed 2026-08-26
- [[tasks/worker-crash-recovery-and-quorum-gate]] — Sprint 4 task 0056 outage root-cause fix: `WorkerSupervisor`, restart cap 5/index/10 min with 1 s→30 s backoff, quorum `ceil(n × 9/10)`-or-90 s gate; agent-closed 2026-08-27, deployment unconfirmed
- [[tasks/schedule-public-games-onto-ready-workers]] — Sprint 4 task 0192 ADR-109's first application: `pickGameID` rejection-samples the game ID onto a ready index and a 5 s abort bounds the create call; agent-closed 2026-08-27, orphan residual since discharged by 0194
- [[tasks/fetchlobbies-in-flight-guard]] — Sprint 4 task 0193 single-poll-in-flight guard on the 100 ms lobby tick: error lines per stuck ID 50→1, lobby flapping 21%→0, self-inflicted 429s 3→0; agent-closed 2026-08-27
- [[tasks/citizenship-name-change]] — Sprint 4 task 0067, the first citizenship benefit: citizen-gated name-change request → operator moderation → apply, with self-service cancel; migration 004, three profile-server routes, shared Telegram notifier. Agent-closed 2026-08-28; **code DEPLOYED in `362a2f9` but nothing has run — the UI has still never been seen in a browser** (card flag off, profile writes no-op on a deliberately blank token); the pending unmoderated name is publicly readable and UNMITIGATED
- [[tasks/citizen-verified-icon]] — Sprint 4 task 0068: server-authored `isCitizen` on the frozen roster and lobby poll, placeholder `★` badge in four surfaces; the live 3-client desync check really ran (280 hash windows, 0 mismatches). Agent-closed 2026-08-28; **code DEPLOYED in `362a2f9`** — no citizens exist in production to badge, but R3's unauthenticated `isCitizen` payload now ships, and that acceptance is void the moment anything of value is gated on the flag
- [[tasks/worker-reject-departed-requester-create]] — Sprint 4 task 0194 departed-requester guard on `create_game` (bounded 10 ms settle wait, `503`): 0 orphans against a baseline of 5, closing the outage track; agent-closed 2026-08-28. ⚠️ Its plan deliberately supersedes its brief
- [[tasks/prod-api-env-https-apex]] — Sprint 4 task 0063: prod `/api/env` moved from `http` on a raw IP to `https` on the apex domain. ✅ **Fixed and DEPLOYED 2026-08-29 in release `362a2f9`**, measured live — the only config-parity instance actually fixed. ✅ **Evidence COMPLETED 2026-08-30** — all six deploy pendings now discharged (one owner-verified), the last via the Uptrace `openfront_host` attribute — ⚠️ **but four of the six were unevidenced at the moment the task closed**; the page carries a state-at-close vs final-state table so the ordering is not flattened. The task's headline user-impact claim was found **vacuous** (no auth service exists in this deployment)
- [[tasks/private-lobby-start-url]] — Sprint 4 task 0198: private-lobby Start silently failed on Yandex Games; the three `HostLobbyModal.ts` sites now build root-absolute worker paths, shipped in `362a2f9`. **Closed 2026-08-30 on LOCAL PROOF ONLY** — its production check was **waived as unsatisfiable, not unrun** (the private-lobby buttons are `display: none` on the Yandex template by the owner's own choice), so correctness in production is **inferred from the code, never observed**
- [[tasks/yandex-payments-secret-forwarding]] — Sprint 4 task `0195`, the **third** config-parity instance and the first outside `deploy.sh`: `build-deploy-profile.sh` never forwarded `YANDEX_PAYMENTS_SECRET`, so `/v1/payments/*` 503s ~~on the real box~~ (🔴 **2026-09-04: no real box exists** — owner-ruled; ⛔ **the code fix stands, only the production narrative is corrected**). Two edits shipped (staged-export block + `example.env.profile`), **closed 2026-09-01 as built + Deferred Live Tail (agent-closed — not owner-verified)**. 🚨 **Production is NOT fixed:** `0014` has not issued the key, so a profile deploy today lands the variable empty and the routes correctly keep 503ing. ⚠️ For `0065` this is a gate **satisfied, not removed** — the blocker count stays at three
- [[tasks/dependency-declaration-sweep]] — Sprint 4 task `0196`, the documentation-only sweep of the canonical `**Depends on:**` bullet into briefs that had none, so `dashboard.sh` stops rendering a **fabricated `ready`**. **Sprint 4's `none recorded` count 6 → 0**; agent-closed 2026-09-01. ⚠️ Scope was **30 briefs, not the stated 31**, and **7 board-visible, not 8** — the gaps were investigated, unreconcilable, and **reported rather than laundered; do not "correct" them back**. 📌 The "14" in the original framing was never a measurement
- [[tasks/supertest-profile-server-flake]] — Sprint 4 task `0200`, closed 2026-09-01 **with NO CODE FIX** — a recognition note in `CLAUDE.md` plus a findings report. Mechanism **confirmed for the timeout shape only** and **not a repository defect** (it reproduces in ~40 lines of plain Node); **every candidate fix measured and refuted**, `--runInBand` included. 🚨 **Two shapes stay UNEXPLAINED and must never be recorded as resolved** — the `401` on a route with no auth middleware, and the `socket hang up` sub-shape. 🔧 Disproved *"the only four suites using `supertest`"* (**seven**, incl. `tests/server/Master.test.ts` on the default `npm test` path) and *"in our own test code"*
- [[tasks/container-log-retention]] — Sprint 4 task `0060`: game-container log retention moved **into version control** at `update.sh:90-92`, plus `access_log off;` on the 1-second-poll `/api/public_lobbies` block. Agent-closed 2026-09-01, **nothing verified in production, seven Deferred Live Tail items unchecked**. 🔧 Refuted the brief's *"the config is not in this repo"* premise — it shipped as an ordinary two-file commit. ⚠️ **The two halves deploy differently**: the `update.sh` flags ride a plain `deploy.sh`, `access_log off;` needs an **image rebuild via `build-deploy.sh`**. The `100m × 10` value is **provisional**, and the pre-change baseline is **unverified**. ✅ **Review finding F6 — the only one that closed with NO disposition — was confirmed by owner ruling 2026-09-02: keep the deploy-path table.** It changes no status and clears no live-tail item
- [[tasks/analytics-p1-citizenship-funnel]] — Sprint 4 task `0021`, the shared citizenship funnel spec, **closed 2026-09-02 as documentation only — no code**. 🔧 **Its own founding premise was DISPROVED: the brief's "the first weeks of live data are lost and cannot be backfilled" is FALSE** — every citizenship event is hard-gated by `CITIZENSHIP_CARD_ENABLED: false`, so **zero have ever fired and no collection window ever opened**. 5 of 6 spec'd events were already shipped, tested and documented; the 6th, `UI:Tap:CitizenshipLearnMore`, is **dropped as obsolete — the Learn-more surface was never designed** (accepted cost: no "researched it but didn't buy" signal). 🚨 **`Citizenship:Seen` may UNDER-count on a slow first paint, INFLATING every conversion rate** — unproven, owner-ruled logged-not-fixed, and its follow-up brief is a **recorded deliberate non-filing**. Brief moved `backlog/` → `done/`
- [[tasks/test-suite-reliability-investigation]] — Sprint 4 task 0197, **renamed at close** because its own title was known-wrong: the jest-worker `SIGSEGV` is an **upstream V8 GC bug, not repository-fixable**; the jsdom/`canvas` and memory-pressure hypotheses were **refuted by experiment**; the integration-suite hang **does not exist** and `--forceExit` was removed. No mitigation bought — **a red run stays ambiguous**. The Node pin is explicitly NOT a fix. Split out: `0200` — 🔧 **and two claims this page carried about `0200` were disproved 2026-09-01 and are now struck in place**: the four profile-server suites were **not** the only `supertest` users (seven), and the flake is **not** in our own code (upstream, like the segfault)
