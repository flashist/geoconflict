# Wiki Index — Geoconflict

> Master catalog of all wiki pages. One line per entry. Updated by LLM after every ingest.

## Features

- [[features/tutorial]] — Guided singleplayer bot match for first-time players; 7-step tooltip sequence, Yandex A/B gated
- [[features/reconnection]] — Crash reconnection: rejoin prompt when tab closes/crashes mid-match
- [[features/feedback-button]] — In-game feedback form on start screen and battle screen with automatic context
- [[features/announcements]] — Start-screen bell and popup for repo-authored player update messages with unread badge; task 0012 adds a citizens-only Personal inbox tab (built 2026-08-26, **closed 2026-09-23 `(agent-closed — not owner-verified)` — still NOT launched, never seen in a browser**; see [[tasks/personal-inbox]])
- [[features/ai-players]] — AI Players in public lobbies: active, indistinguishable from humans in UI; **ADR-110 (2026-09-03) rules that one MAY be declared match winner**, with no in-modal tell — and 🔴 **that ruling carries a known expiry**, see [[decisions/adr-110-ai-winner-allowed]]

## Systems

- [[systems/project-brief]] — Product ground truth: what the game is, who it is for, how it earns, and the platform/legal/scope constraints every task works inside; 🔴 **its *Current focus* claim that "the profile host is live" was CORRECTED 2026-09-04 — that claim is UNVERIFIED and no longer claimable** (owner-ruled). ⚠️ **A same-day annotation reading "there is no profile host" / "two standing fleets, not three" OVERSTATED the owner's position and has been withdrawn: the profile VPS and the S3 bucket physically EXIST and are REUSED IN PLACE; their provisioning state is UNKNOWN AND UNVERIFIED**. ✅ **UPDATED 2026-09-10 — that UNKNOWN IS NOW ANSWERED and the "read every live claim as unverified" instruction is SPENT:** `0215` found the box **already live and healthy**, the **owner ruled ADOPT rather than wipe**, and `/health` + `/ready` both return **200** over a valid certificate (lead-verified). *(Routed to the vault by the clean-slate survey under ADR-005; the 2026-09-04 paragraph is kept unedited as history.)* ⛔ **This does NOT make the backend done — FOUR things must not be read into it:** ~~🔴 **the RESTORE PATH HAS NEVER BEEN TESTED**~~ ✅ **CORRECTED 2026-09-11 — A BACKUP RESTORES**, proven twice by `0218` against non-empty data, live database included; ⛔ **but the SCHEDULE and the DATA are proven SEPARATELY, NEVER TOGETHER** — every cron-produced object ever written is a dump of an **empty** DB and the only non-empty backup was **hand-run** · 🔴 **the GAME SERVER IS NOT WIRED to the box** (`0217` **open**) · 🔴 **the profile DB holds ZERO ROWS and nothing is deployed to players** · ⚠️ **`ufw` default-deny not re-verified, and RU residency rests on ONE geolocation provider** — registration evidence, not a site attestation. 📌 Owner-ruled remaining order **`0218` → `0219` → `0217`** 📌 **2026-09-23 (`PROJECT.md`): `0065` is the go-live only with no task condition — the owner accepted that paid citizenship may reach real players before any real purchase is proven** (test-buy → `0297`).
- [[systems/architecture-overview]] — Evidence-first 2026-08-08 codebase survey: four tiers, tick model, deploy topology, ranked risks, and the documented-but-stale corrections; **extended 2026-08-28** with the §5 `windowOrigin` URL-join rule and the §9 table that tells the two look-alike local lobby traps apart
- [[systems/agent-conventions]] — The project's standing law: task status and owner vocabularies, status-report shape, evidence-before-assertion, one-skill-one-output, priority-vs-identity, dependency form, and **three** project-added rules — task-ID allocation, and, both on 2026-09-07, the **task-attribute cross-reference sweep** (a ruling's reach extends past the brief and sprint plan to the prose copies of that attribute in *other* files) and **file:line citations** (declare the commit frame; re-derive by reading, **never shift arithmetically**; **no bare `:NNN`**). **Ten conventions** — ✅ **the tenth is now COMMITTED (`5913ac2`)**, written after `0227` invalidated four sibling briefs' citations in one afternoon and an arithmetic "fix" silently dropped the load-bearing line. 🔴 **EXTENDED 2026-09-08: SIX rules, not three, and FOUR failure modes, not three** — the new one is **wrong-from-birth-then-preserved-perfectly**: `0228` cited an `await` at `Main.ts:702` that was never an `await`, and a *careful* mechanical sweep re-derived it faithfully to `:711`, still wrong. ⛔ **Two mechanical passes carried the error intact; only a SEMANTIC pass caught it** — hence the new rules to **re-anchor on the code, not the old number**, and to **check a citation by CONTENT match**. 🔴 **EXTENDED AGAIN 2026-09-08 — the status vocabulary gained TWO ratified board-level exceptions, then had them WIDENED the same day**: `⬜ No sprint` (defined, worth doing, no sprint home) and `⏸ Parked` (not scheduled until a **named external condition** is met — a park with no stated condition is indistinguishable from an abandoned task). Both were ratified first for `sprint-backlog.md` **alone**, then widened to `backlog.md` too, because task `0001` retires the first board and the narrow wording left only **flattening to `🔲 Backlog`**, destroying the distinction just ruled worth keeping. 🚨 **The source frames the widening as a DELIBERATE RE-SCOPING of a same-day ruling, not as what was always meant — keep that framing.** ⛔ Both remain invalid in a brief's `## Status` and on `plan-sprint-N.md`, and **neither is rendered by `/fkit-status`** — degraded, **not** invisible (rows render; the summary line is useless). ⚠️ **The earlier "these markers are historical drift" note is now PARTLY superseded and struck** — narrowly: `⚠️ Urgent` and a bare `⬜` are still drift, and this is still no licence to invent values inline. 🔴 **EXTENDED 2026-09-10 — convention 10's failure mode 4 RECURRED on a different line of work (the profile backend), and the recurrence nearly DELETED TRUE EVIDENCE.** Three new named sub-shapes: **self-invalidation inside one commit** (one commit wrote a citation *and* inserted the banner that displaced it — so *"I read it before I wrote it"* is not enough); 🚨 **a wrong FILE is categorically worse than a wrong line** — a bare `:147-153` inside `0182` was mis-resolved by **three readers**, the claim was flagged **UNSOURCED**, **the owner RULED A RETRACTION**, and the producer refused and escalated after finding the real source, whereupon the owner **withdrew** it; and **concurrent uncommitted edits** — a commit hash is **not a sufficient frame** when several agents edit one file in a session. Two operative rules: **never a bare `:NNN`**, and 🚨 **before declaring a claim unsourced, SEARCH OUTSIDE THE CITED FILE** — "not at the cited location" ≠ "does not exist", and the remedies are opposite. ⛔ **NOT a carelessness problem** — the sweeping producer produced two instances itself. ⚠️ **FLAGGED: the convention doc and the `0239` brief row CONTRADICT each other on who caused the third recurrence; the file supports `0239` — the vault's `:175`/`:207` were CORRECT at their declared frame `589249c` and were displaced by the producer's later uncommitted edits. The vault cannot fix that document (ADR-005) — a human must** 📌 **2026-09-24: now TWELVE** — `sprint-status-vocabulary.md` (line-3 sprint banners) and `durable-citation-anchors.md` added 2026-09-23 (`16f44e8`); ⚠️ **neither is listed in the conventions `README.md` catalog**, which still lists ten.
- [[systems/glossary]] — 📌 **The vault's single source of truth for project vocabulary** (new 2026-09-03, owner-ruled; schema amended with a Glossary page type). The terms whose everyday meaning and code identifier diverge: **"Nation" is `PlayerType.FakeHuman`**, an **"AI player" runs the same `FakeHumanExecution` class as a Nation** and differs only in having a real `clientID`, and **`ColoredTeams.Bot` IS a real team** — in `teams()` and in win-check tile accounting — even though `isOnSameTeam()` returns false for it. Also the two team-assignment paths, the win-condition vocabulary, and the three routinely-confused player IDs. Carries the owner's stated model **beside** the code, labelled, with items 5–7 refuted. **Other pages link here; they do not restate definitions**
- [[systems/game-overview]] — Project overview: game types, maps, units, economy, combat, tick system; its Player Types / Team Types / disambiguation tables were **merged into [[systems/glossary]] 2026-09-03**, and two claims it carried were corrected against code — `HumansVsNations` is a `playerTeams` config value, **not a `GameMode`**, and Nations are present in public FFA and default private Team lobbies but **never** in the tutorial
- [[systems/producer-workflow]] — Producer role: scope, responsibilities, coordination boundaries, and release guardrails; carries the 2026-08-30 never-write-endpoints rule and the owner's **no-retro-scrub** ruling on the host IPs already committed
- [[systems/project-operations]] — Operational handbook: team roles, environment boundaries, sprint workflow, and roadmap constraints
- [[systems/game-loop]] — Deterministic turn replay from server turns into worker-executed core simulation. 🚨 **Added 2026-09-07: a throw inside `GameRunner.executeNextTick()` reaches NOTHING** — the worker drops the `ErrorUpdate` and the wire format cannot carry it, so the client's crash branch is **unreachable dead code** (task `0232`, open). ⚠️ **The consequence is REASONED FROM CODE, never observed** — freeze vs recover vs diverge is unknown
- [[systems/networking]] — Worker-routed WebSocket/HTTP flow with Zod-validated client/server messages; carries the post-outage master/worker coordination — `WorkerSupervisor`, the quorum gate, ready-worker ID sampling, the lobby-poll guard, and the departed-requester create guard
- [[systems/execution-pipeline]] — Client input to Intent to Execution to GameUpdate path
- [[systems/rendering]] — Layered client rendering, mixed canvas/Lit UI, and camera transform orchestration
- [[systems/flashist-init]] — Explicit Bootstrap.ts startup gate for analytics, Yandex SDK, player data, flags, language, degraded mode, and app loading; **records 2026-08-28** that `yandex-games_iframe.html` sets `flashist_isYandexPlatform = true` unconditionally, so an off-portal load still enters Yandex platform mode — behaviour there **unmeasured**
- [[systems/analytics]] — GameAnalytics player behaviour tracking: event conventions, bootstrap/session events, experiment funnels, and monetization measurement baselines; **updated 2026-09-02** — **zero citizenship events have ever fired anywhere** (hard-gated by `CITIZENSHIP_CARD_ENABLED: false`), the sixth funnel event `UI:Tap:CitizenshipLearnMore` is **dropped as obsolete**, and 🚨 **`Citizenship:Seen` may UNDER-count, which INFLATES every downstream conversion rate** — unproven, deliberately unfixed. 🔴 **EXTENDED 2026-09-07 with the 500-events-per-active-user-per-day limit, breached 4 Sep at 1,324.33/user (265 %)**: **two independent problems** — the **spike** (`Player` ~34×, `Experiment` ~45×, session-start categories, 🚨 **MECHANISM UNKNOWN AND UNEXPLAINED**, deferred as `0230`) and the **standing baseline** (`Performance` ≈ two-thirds of a normal day, cut 60 s → 300 s by `0224`). ⛔ **`0224` does not fix the breach and cannot** — a 4 Sep repeated today would still breach at ~235 %. ⛔ **"The banner is gone" is not evidence** — the metric self-resolved 5 Sep with zero code written. 🚨 **`DEPLOY_ENV` fails open to `prod`, live and unscheduled** (`0226`). ⚠️ **Cardinality is a SEPARATE metric and is TRENDING UP** (~103–105 → 118), and whether events are being dropped **could not be established**. 📌 **EXTENDED 2026-09-11 with two LIVE event families from `0208`** — `Match:WinCondition` (**21 reachable ids, not 56**) and `Match:Leaderboard:Award` (**5 of 6 reachable**), shipped in `6b30e22` / build `0.0.141`, plus the 4–10 Sep production read. 🔴 **Their two caveats are not optional: `Match:WinCondition`'s denominator is CLIENT-MATCHES not matches (absolute counts uninterpretable, ratios only, all rates LOWER BOUNDS), and `Match:Leaderboard:Award` counts ATTEMPTS including platform failures — never points banked.** ⛔ **Do not copy either caveat onto the other event** 🆕 **2026-09-24:** `Ad:Interstitial` (task `0020`, tier-free, real impressions only; in-Yandex check owed; tiers → `0299`, banners dropped) and the three `Citizenship:TenureGrant:*` events (task `0253`; **count by unique users**; `ClaimFailed` does not mean ungranted).
- [[systems/telemetry]] — OTEL/Uptrace server/client observability, source maps, ClickHouse VPS guardrails, and investigation workflows; the winston-transport gotcha **corrected 2026-08-27** against measurement — single-object meta survives as attributes, only `null` values are dropped
- [[systems/configuration]] — GAME_ENV, /api/env, runtime public settings, and gameplay/server config selection
- [[systems/localization]] — LangSelector and translateText flow for bundled UI translations and English fallback
- [[systems/server-performance]] — Server-side lag candidates ranked by likelihood; `endTurn()` performance analysis
- [[systems/match-logging]] — What is recorded per match, where it goes, and what cannot be retrieved. 🔴 **Corrected 2026-09-22: "archiving is off" is true of the WRITES only** — the client archive read still fires in production, benign, own-infrastructure, and only for lobby IDs that are not currently active (task `0292`)
- [[systems/clans]] — Name-tag clan grouping system: parsing, team assignment logic, gaps, and no-UI status
- [[systems/player-infrastructure]] — Pre-S4 identity/customization substrate: local-only persistence, join transport, dead inherited Stripe/Fuse monetization, and trust gaps; **corrected 2026-08-09** — the `flares` entitlement path is **live** and upstream-OpenFront-sourced, with ad suppression coupled to it (production liveness still unverified, task `0009`)
- [[systems/player-profile-store]] — Dedicated Sprint 4 profile API/Postgres backend and match-end XP crediting path for citizenship and future paid entitlements; also the name-change loop (migration 004) and the `is_citizen` flag the game path reads.
  **Current state (2026-09-07).** ✅ **The profile VPS and S3 bucket EXIST — confirmed twice** — and 🔴 **the box's setup and keys are being REDONE FROM SCRATCH** (owner, verbatim: *"I have the VPS, but I need to re-du the setup of it from the scratch, probably all the keys"*). ⇒ 🚨 **Nothing currently on the box may be assumed working, correct or trusted.** "Clean slate" means **wipe and rebuild ONTO the existing resources, not procure new ones** — `0213`–`0222` plus `0201`, all Sprint 4. ⛔ **The backend WAS built and is sound** — service code, image, `setup-profile.sh`, `build-deploy-profile.sh`, the backup script with a scripted restore, and an operator runbook all exist. **Three config/exposure gaps keep it inert or leaky:** `0062`, `0195` (script fixed 2026-09-01; ~~routes still 503 because `0014` has not issued the key~~ 🔴 **CORRECTED 2026-09-19 — a NON-EMPTY value, length 32, IS on the box and the routes are NOT 503ing; ⚠️ presence only — correctness unproven, no purchase exercised, ~~provenance unverified~~** ✅ **PROVENANCE ANSWERED 2026-09-20 — OWNER-ATTESTED, NOT repo-verified: the owner set the real Yandex per-game key themselves; ⛔ it still does NOT show the value is correct and it clears nothing**), and `0067`'s unauthenticated pending-name read. 🔴 **The `age`-key question is RE-OPENED** — a live owner decision on `0222`. ⚠️ **Inference, not verified fact: match-end XP crediting has almost certainly NEVER worked in production** — unmeasured, do not upgrade it. ⚠️ **Read every "on the real box" / "200/TLS verified" / "503s in production" phrase across this vault as UNVERIFIED** — not disproven, not claimable.
  📛 **Superseded positions — KEPT, in the order they were believed. This entry is worded defensively because BOTH of these were once written here as fact:** **(1)** *"the profile host is live"* — **wrong**, corrected 2026-09-04; **(2)** *"THERE IS NO PROFILE HOST"* — the **overcorrection**, which overstated the owner's position and was **withdrawn** the same day. ⛔ **Neither may be deleted and neither may be restored.** The three dated owner positions, all three kept with their standings, are on the page. Grounding: `ai-agents/knowledge-base/reports/2026-09-04-profile-backend-clean-slate-survey.md`
- [[systems/alert-delivery]] — 🆕 **Added 2026-09-18, closing a total vault gap — there was NO page for any of this.** How a monitoring alert reaches a human: Uptrace 2.0.2 **cannot post into a Telegram forum topic**, so alerts go through a **relay on the profile/admin box** ([[decisions/adr-114-admin-server-alert-relay]]). ✅ **PROVEN END TO END 2026-09-17** — a real metric alert fired, reached the owner, cleared itself, and the ✅ recovery arrived; `alert.status` = **`closed`** (was unverified, **now confirmed by observation**). ✅ **Guarded since 2026-09-18** by an **hourly liveness probe** drilled to a real page. 🚨 **THE TRAP: a `401`/`403`/`404` reply PERMANENTLY AND SILENTLY DISABLES the channel** (verified by disassembly) — and **the nginx allowlist answers 403 on a source-IP miss**, so **one address change kills alerting for good**, owner-accepted. ⇒ 🔑 **the relay is fail-closed on DELIVERY and NEVER on the HTTP STATUS**: a bad secret answers **200**, drops, and alarms elsewhere. ⛔ **Three things that do NOT cover this and each looks like it might:** `profile-checks.sh` (a value compared with itself) · `0283`'s daily digest (**actively misleading — a daily "the bot works" message while every alert is dead**; 🚨 **UPDATED 2026-09-19 — no longer hypothetical: that digest is DEPLOYED and two real messages have been watched arriving, so the false reassurance now physically EXISTS**, see [[tasks/name-change-daily-digest]]) · the probe itself, which catches the **CAUSE within ~24 h, not an already-disabled STATE** (`0285`). 🚩 **The payload is passed through VERBATIM and NEVER templated**, so **the measured value, threshold and window are NOT OBTAINABLE** — the monitor NAME is the only place a number can appear (owner-ruled one shared channel). 🚩 **The drill fixture written into `0274`'s plan CANNOT PASS** — an always-true rule never clears and deleting a monitor is not a recovery event, so it proves half the path and looks like it proved all of it. ⛔ **ONLY ONE alert rule exists**, and **A1 cannot be built until the first real player is created** — three metrics are absent from the picker because they have never been incremented. 📌 **UPDATED 2026-09-21 with where the deferred rules actually went, because "deferred" hid two different fates:** ➡️ **A4 has a task — `0288`, on the BACKLOG board** · 🔴 **A1, A2, A3 and A6 have NO TASK ON EITHER BOARD**, deferred for want of login traffic, so ⚠️ **nothing tracks them and no status tool will ever surface them — this is the gap most likely to be silently forgotten** · ➡️ **idle-period delivery (`0274`'s amendment A1 — 🚨 a DIFFERENT A1 from alert rule A1, never conflate them) has a task, `0289`, on the SPRINT 4 board.** ⇒ ⛔ **still ONE built rule (A5): filing a task is not building a rule**
- [[systems/client-game-teardown]] — 🆕 **Added 2026-09-07.** Who stops what when a client game ends: the `onGameEnd` seam `0227` added, the full site map, and the **bounded vs accumulating** distinction that drove every rank in the family. ✅ Fixed: `0225` (the one confirmed accumulating leak, evidenced with a negative control), `0227` sites B and C. 🚨 **Still open: `0228`, `0229`, `0231`, `0232`, `0233`** — and `0227`'s headline **site A is unreachable dead code, not fixed and not fixable from those files**. ⚠️ **`0231`'s accumulation and `0232`'s freeze are REASONED FROM CODE AND NEVER OBSERVED** — no browser session was watched, no count taken; ⛔ **no figure, rate or severity may be written for either.** 🔴 **`0231` REFRAMED 2026-09-08 (owner ruling): `stop()` runs on NO path, so the runner survives EVERY game, not just abandoned ones — the original title is kept, marked superseded.** 🚨 **THE REFRAME WIDENED THE REASONING ONLY AND ACQUIRED NO NEW EVIDENCE** — still never observed, step 1 is still to measure it, and a refutation is still a valid complete outcome; ⛔ **a bigger-sounding defect must not read as better-evidenced.** 🆕 **`0227`'s `onGameEnd` seam is GENERATION-GUARDED** (`if (joinGeneration !== this.monitorGeneration) return;`) — correct and deliberate, ⛔ **but a trap for `0232`'s acceptance test: crashing an already-superseded game correctly does nothing, which a naive test misreads as the seam failing.** ⛔ **NOTHING IN THIS FAMILY IS DEPLOYED** as of 2026-09-08
- [[systems/weekend-deploy-window]] — 🆕 **Added 2026-09-22, closing a total vault gap.** The ordering for the weekend deploy slot: **eleven tasks, four deploy commands — TWO profile deploys + one telemetry + one game**, steps **W0–W15**, each with the constraint that fixed its position. ⚠️ **The source runbook's own title says "four deploys" and was WRONG when written — it was five**, and is correct today only by accident of the W8 cut. 🚨 **`PROFILE_INTERNAL_ALLOW_IPS` SERVES TWO CALLERS — the game server AND the monitoring box's alert relay (mounted under `/internal/` SOLELY to inherit that allowlist). A source-IP miss answers 403, which PERMANENTLY AND SILENTLY DISABLES the notification channel, and the variable has NO on-box persistence ⇒ APPEND, NEVER REPLACE**, on every deploy in the window. 🚨 **Most of the runbook rests on a PREDICTION — `0286` step 8 has never run**, and it is the fix for the defect that *hangs* `npm run deploy:profile` (a hang, not a failure: `set -e` never fires); ⚠️ **even a clean run must never be written up as "the deploy can no longer hang"** — a dpkg conffile prompt is uncovered. 🚨 **W12 turns crediting on with ZERO prior production evidence AND no written way back: the absence of a rollback procedure is a DECISION, not an omission** (owner: *"Skip it — I know the boxes"*), and **the whole of the mitigation is a human watching at W13–W15**. ⚠️ **`0220` closes with ONE STEP DEFERRED, NOT "unverified"** — steps 1–2 run, the rotation is proven **against stubs** by harness **T13** under `npm test`, and ⛔ **stubbed coverage is explicitly not live-box evidence**; the live proof is task **`0294`**. ⛔ **`0065` step 3 and the `0054` flag flip are OUT** — the buy button sits behind `CITIZENSHIP_CARD_ENABLED`, a **compile-time `false` with no env and no remote override** (re-verified in `src/`), so it needs a source change **plus a second game deploy** — *a decision about launching citizenship, not a verification step.* 🕳️ **Four gaps flagged and deliberately NOT authored** (G1 ruled deliberate · G2 owned by `0295` but ⚠️ **an owner is not an answer** · G3 and G4 open). 🚨 **Accepted cost of the `0219` split: the TLS-certificate fuse stays UNWATCHED** — ⚠️ its two dates are **reported and never verified** 📅 **DATED 2026-09-23: Saturday 2026-09-26** (owner) — not yet run; the owner may postpone if `dev` is not ready. `0064` step 8 → `0298`, with a **new value-guard block** to capture at W12; ruling 3 **lifted** for `0203`; 🚨 **`0253`'s tenure claim route rides W3, so the claim-on-behalf risk opens at W3, not at the flip**; new W12 working-tree pre-check; new **W16** (close Sprint 4, then the owner starts Sprint 5); C1 **resolved** (`0065` = go-live only, test-buy → `0297`).
  ⏸️ **UPDATED 2026-09-23 (`b3ee5de`): THE WINDOW SLIPPED — owner confirmation 2026-09-22 — and is UNDATED; every step, W0 included, is undone; ⛔ do not infer a date.** Owner rulings the same day **closed G3** (the W14 watch: the owner watches **until the first credit lands** — event-based; stop needs **both** a credited row **and** zero error-level log lines; **silence is a FAILURE**) and **closed G4 by half** (a **manual W0 snapshot** of the newest backup object, deleted by hand only once the owner judges the window good; ⚠️ the freshness-marker half is **unanswered**; ⛔ not a rollback). **Both certs measured live**: profile **2026-11-20** (this fuse, confirmed), telemetry **2026-12-13** — a proposed swap of the two was refused. Runbook ruling letters namespaced **`RUNBOOK-A`–`G`**; the other three same-letter sequences left as is by owner ruling. `0062`'s D-steps are now **`0296` A1–A4**.


## Decisions

### Project ADRs (this project's series — 101+)

> `ADR-001`–`ADR-099` are **fkit toolkit** ADRs and do not live in this repo. See [[decisions/adr-numbering-two-series]].

- [[decisions/adr-numbering-two-series]] — Owner ruling reserving 001–099 for the toolkit series; this project's ADRs start at 101. Also carries the immutability rule: `proposed` is a draft promoted in place, `accepted` is history, and the in-place amendment carve-out covers **clarifications only** — a reversal needs a superseding ADR
- [[decisions/adr-101-fail-soft-xp-crediting]] — Match-end XP crediting is fail-soft with bounded retries and no durable queue; outage XP loss is ~~silent and~~ **unrecoverable**. 🔧 **WORDING CORRECTED 2026-09-10 — "silent" is REFUTED against the source and it MATTERED, because it propagated:** every failed credit batch logs **TWO WARN lines** (`src/server/ProfileApiClient.ts` — the `not retrying` warn inside the `status < 500 && status !== 429` guard, and the caller's `award(s) dropped` warn on the `response === null` branch; re-verified, file unchanged `589249c`→`00058df`). The looser wording is what was copied outward as *"nothing logs above `debug`"* into `0182`'s runbook and the clean-slate survey. ⛔ **THE LOSS ITSELF IS UNTOUCHED — awards are DROPPED, NEVER QUEUED**; and it still goes unnoticed because **nothing on that box reads the logs** (`0219`, open). ⚠️ **The BOUNDARY bullet's "silent" is a DIFFERENT, still-correct claim** — there the credit is never attempted, so there is genuinely nothing to log; do not sweep it 📌 **AMENDED 2026-09-11 — the `1 XP` rescale ([[decisions/adr-111-xp-economy-rescale]]) moves NONE of its three re-raise triggers**, each for its own reason (trigger 1 is CATEGORICAL — `10 XP` there is a **unit label for one match's earned progress**, not a magnitude claim; trigger 2 counts warn lines, not XP; trigger 3 turns on funding and an owner call). 🔧 **A 2026-09-11 LINT WARNING THAT CONCLUDED THE ARGUMENT GETS "STRONGER, NOT WEAKER" IS STRUCK — IT WAS WRONG** (architect-ruled 2026-09-11): it took the numerator without the denominator. **The award falls 10× AND the threshold falls 10×, by design, so a dropped credit costs 1 % of the way to citizenship before and after (1/100 = 10/1,000), and the recorded remedy — *"Affected players simply need one more match"* — is IDENTICAL. XP has no denominator other than the threshold; a unit whose only denominator moves with it has been RENAMED, not REVALUED.** ⇒ 🔴 **THE CORRECT READING IS UNCHANGED.** ✅ **THE SUPERSEDE GATE FIRED AND IS CLOSED, 2026-09-12 — ⛔ NO SUPERSEDING ADR; THE DECISION IS UNCHANGED.** `0211` picked **Mechanism A**, and the architect re-read all three named parts against it (routed there by owner ruling 7: *the coder implements, the architect records*): *"blast radius is one match"* **HOLDS** (it is about accumulation, and nothing is queued either way — ⚠️ **⛔ do NOT read it as *"an outage now costs less XP"*: each player is credited exactly once per match either way, so expected loss is UNCHANGED and only its GRANULARITY moves**) · the **3-attempt budget is UNCHANGED and now conservative rather than tight** (the new calls sit **off** the match-cleanup path that forced the bound) · **per-item pre-validation is BEHAVIOUR-CORRECT**, ⚠️ **and the tempting simplification is WRONG — multi-item batches do NOT disappear, the match-end batch survives**. 📌 **Re-raise Trigger 2 does not move, but its BASELINE does: one outage now produces MORE warn lines for the SAME lost XP — count the dropped awards, NOT the lines.** ⚠️ **FLAGGED: the clarification's own dated line *"NOTHING HAS SHIPPED … `winnerDeclarable` appears nowhere in `src/`"* declares frame `bb1674f` and was overtaken by `0211` LATER THE SAME DAY — its reasoning is unaffected, only that observation is spent; the vault cannot amend the canonical ADR (ADR-005)**
- [[decisions/adr-102-privilege-refresher-fails-open]] — Cosmetic entitlements fail open until `cosmetics.json` loads; **accepted 2026-08-09 while nothing is sold, expiring at the first paid entitlement of any kind** — trigger ruled three times in one day, all three kept visible
- [[decisions/adr-103-identity-trust-seam]] — Client-asserted Yandex IDs accepted for earned XP behind one trust seam; signed verification deferred until the Yandex secret key exists. 🔧 **CORRECTED 2026-09-11 — the vault mirror carried a figure the CANONICAL ADR DOES NOT CONTAIN.** The bound *"10 XP per (game, account)"* exists **only in this vault page**; the knowledge-base ADR states its abuse bound **qualitatively** (*"nothing to steal, only something to gift or to farm"*) and carries **no number**. Rewritten to name the **MECHANISM** — one credit per `(game_id, yandex_player_id)`, enforced by a database primary key, i.e. **whatever `XP_PER_MATCH` holds** — so it cannot go stale when `0211` ships. ⛔ **A 2026-09-11 lint warning claiming the `1 XP` ruling makes this ADR's risk argument "stronger" is STRUCK: farming economics are IDENTICAL (~100 forged matches either side) and THE RISK GRADE IS UNCHANGED, not lowered.** ⛔ **The canonical ADR takes NO change**
- [[decisions/adr-104-archiving-disabled]] — Match archiving disabled behind one config switch until S3-backed, citizen-gated archival ships 🔴 **SCOPE CORRECTED 2026-09-22 — THE *SERVER* LEG ONLY.** `archiveEnabled()` has exactly **two** consumers (`src/server/Archive.ts`, `src/client/LocalServer.ts`), both **writes**. The client archive **READ** — `checkArchivedGame()` in `src/client/JoinPrivateLobbyModal.ts` — is **not behind the flag and still fires in production**, so anyone reading this as "archiving is dark" is wrong. ⚠️ **BENIGN: no user impact, no failure observed** (the 404 is handled as `"not_found"`), and ⚠️ **it fires ONLY for lobby IDs that are NOT currently active** — an earlier *"fires on every entry"* framing was **over-stated**. ✅ **Destination measured 2026-09-22 on the LIVE PRODUCTION deployment: OUR OWN INFRASTRUCTURE, not a third party** ⇒ no 152-ФЗ question; 🔒 **classification only, no host, domain or URL recorded.** ⚠️ **Ceiling: one reading, two fields, prod only — `0009` finding 1 is NOT discharged.** Tracked as task **`0292`** (Backlog board; ⚠️ the owner ruled only *that it be filed separately* — board, rank and severity are the producer's). 📌 **`0030` must repoint this exact call** or its *"replays read back correctly"* check cannot pass
- [[decisions/adr-105-compact-maps-out-of-rotation]] — Compact maps removed from public rotation until the map binaries are regenerated with correct shore bits. 🔧 **Corrected 2026-09-08: the exit task `0026` is NOT on the Sprint 5 plan** (that plan's scope statement excludes it), and until that day it appeared in **no sprint file's table at all** — board-invisible. An owner ruling gave it a `⬜ No sprint` row on `sprint-backlog.md`, superseding a 2026-08-14 decision to leave it off. ⛔ **Visibility only — this ADR's expected exit is still UNSCHEDULED**
- [[decisions/adr-106-flags-suppressed]] — Real-country flags suppressed by parse-then-drop; flags reserved as a future paid non-country cosmetic
- [[decisions/adr-107-turn-interval-1-5x]] — The game runs at 1.5× upstream tick rate (66.7 ms); owner-supplied rationale (2026-08-09): two goals — quicker matches and a higher interstitial rate — with 1.5 chosen by playtesting and 2× rejected as too fast; neither goal measured
- [[decisions/adr-108-active-sprint-pointer]] — Direction for the next fkit update: the active sprint should be owner-set via an optional `.active-sprint` pointer, derived only as fallback, failing loud when stale. **Nothing in this repo changes** — the interim rule is to ask for status by name. 🔴 **THE PREDICTED FAILURE RECURRED 2026-09-07** — `select-active` returned Sprint 6 while the owner ruled *"The active sprint is the Sprint 4!"*. ✅ **Verified by reading the code: the pointer is NOT implemented in either resolver copy, so creating `.active-sprint` today would be INERT.** 🚨 **This documents around the risk; it does not resolve it** — `/fkit-status` and `/fkit-sprint-ship-loop` still answer for the wrong board on an empty argument. ~~**Whether to implement it locally or keep waiting on upstream is an OPEN owner decision**~~ ✅ **ANSWERED 2026-09-08 — amended IN PLACE, not superseded, and widened from the pointer to local patching of `fkit-status` generally: HOLD on local patching, ship task `0001` instead.** 🚨 **A prior claim that `/fkit-heal` would flag such a patch is FALSE and is corrected in the ADR — do not carry it.** Heal returns **zero verdict lines for any `.claude/` path**; the real cost is that `.claude/skills/fkit-*/` is **gitignored**, so a patch is unreviewable, absent on a fresh clone, invisible to every tool, and **silently overwritten by the next fkit update**. ⚠️ **Two INDEPENDENT blind spots, and neither fix closes the other**: the pointer fixes *wrong board chosen among candidates*; `0001` fixes *a board that is not a candidate at all* (file selection — the status skill names only `backlog.md`, singular, so **no documented command reaches `sprint-backlog.md`**). 🔴 **This ADR's own residual risk is UNCHANGED and still open — ask by name.** 🚨 **And `0001` fixes REACHABILITY, not RENDERING: after it ships, `backlog.md` will carry the two ratified status values the dashboard still does not render — the blind spot follows the rows** ✅ **2026-09-24: the wrong-board failure is gone in practice — by the line-3 status banners, not by this ADR's pointer;** `select-active` returns Sprint 4 (verified this sync). Whether the ADR is now superseded is an owner/architect call, not made.
- [[decisions/adr-109-worker-index-placement-contract]] — The worker index is a fixed placement contract computed independently by client, worker, nginx and master; to move a game, move its **ID** (rejection-sample it), never the index. Private-lobby exposure and wedged-but-alive workers are recorded, owner-accepted tradeoffs
- [[decisions/adr-110-ai-winner-allowed]] — 🔴 **CARRIES A KNOWN EXPIRY — never cite it as settled-forever.** Accepted 2026-09-03: an **AI player (`PlayerType.AiPlayer`, real `clientID`) MAY be declared winner**; the predicate stays `clientID() !== null` with **no `PlayerType.AiPlayer` exclusion**, as **one policy across FFA (`0206`) and Team (`0205`)** — a second explicit owner ruling. An AI winner is **credited nothing** (three independent gates in `selectMatchCredits`); what the award does is **unblock crediting for every real player** ⚠️ **CARRY THE QUALIFIER: only where a LIVING CLIENTFUL player exists to award to.** Where every clientful player is already eliminated, `players()` filters to `isAlive()`, no winner can be named and nothing is unblocked. 🔴 **Dropping this qualifier is exactly how ADR-110's false Consequences bullet came to be written — see [[decisions/adr-110-ai-winner-allowed]].** 🔴 **The owner accepted it knowing a durable, player-visible winner surface is PLANNED** ("None today, but planned") — so its strongest counter-argument was **overridden with eyes open, not refuted**, and it **MUST be re-examined before any leaderboard, match history, announcements feed, share card, or other surface naming a winner outside the end-of-match modal ships.** Nations and Bots stay excluded; the **all-Nations team** case was deliberately deferred to `0205`'s plan. 🔴 **CORRECTED 2026-09-04: this ADR now rules on a predicate that exists in NO SHIPPED CODE** — ~~`0206` shipped it 2026-09-03~~ **`0206` was REVERTED and never deployed; `0205` is unbuilt.** ⛔ **The decision is unaffected and NOT superseded** — a live policy awaiting its first implementation; its only in-code trace is one deliberately-kept comment. 🔴 **A false Consequences bullet was STRUCK** (*"Public FFA matches where every human dies now complete and credit XP"* — **false when written, and at acceptance**: T3's *"while AI players are still alive"* qualifier had been dropped). ⏳ **T1 carries a scheduled expiry** — `0211` removes the winner-gating it rests on, ⛔ **without** firing the re-raise trigger. ⚠️ **Re-raise pointer corrected `0206` → `0208`**; ~~that trigger is still UNFIRED~~ ✅ **UPDATED 2026-09-11 — THE MEASUREMENT NOW EXISTS**: `AiPlayer` **89 leader firings in 7 days**, FFA clientless **~1.6 %** (rare — the direction that weakens the strongest argument for `allow`, **FOR FFA ONLY**) vs Team **~53.2 %** (⛔ **emphatically not rare**). 🔴 **THE TRIGGER HAS FIRED ONLY IN THE SENSE THAT THE MEASUREMENT EXISTS** — ⛔ **record it as AN INPUT REQUIRING AN ARCHITECT'S READ, NOT as a conclusion about ADR-110; nothing about this ADR has changed and it is not superseded.** ⚠️ **Client-matches, lower bounds, and `0208` closed NOT fully verified**
- [[decisions/adr-111-xp-economy-rescale]] — 🔴 **NEW, accepted 2026-09-11** (owner ruling given 2026-09-10). **The XP economy is rescaled 10× DOWN — award `10` → `1`, citizenship threshold ÷ EXACTLY 10 (`1,000` → `100`) — both shipping inside `0211`.** ⛔ *"About 10×"* is **not** the ruling. 🔴 **THE LOAD-BEARING PART IS A STANDING DIRECTIONAL CONSTRAINT, not the numbers: players accept an award moving UP far more readily than DOWN, so `1` is a DELIBERATE FLOOR** — ⛔ **a future planner must not round it up, and must not propose moving it DOWN without reopening this ADR.** ✅ **Time-to-citizenship is DELIBERATELY UNCHANGED (~100 matches either side)** — that invariant is the mechanism by which ADR-101's and ADR-103's reasoning survives untouched. ⛔ **Scaling/tuning is a DELIBERATE HOLD, not an omission** — do not file it as an unfiled gap. ⚠️ **The architect's standing point is RECORDED AND NOT DISMISSED: `0211` moves the crediting trigger earlier, so a player who dies 30 s in is paid the same as one who plays to the end — true at `10`, equally true at `1`, and STILL OPEN as the minimum-participation-floor question.** 🚨 **Dated clarification 2026-09-11 (= `0211`'s ruling 8): when `0211` ships, any row already at ≥ 100 XP BECOMES A CITIZEN IMMEDIATELY, on a threshold they never met — roughly 10× easier than intended. The owner was offered a divide-by-10 migration, accepting the grants, or resequencing, and CHOSE TO ACCEPT THE GRANTS KNOWINGLY. ⛔ NO MIGRATION IS TO BE WRITTEN — a future implementer who spots it ESCALATES, does not migrate.** ✅ **SHIPPED 2026-09-12 inside `0211` — `src/core/profile/Citizenship.ts` now declares `100` / `1`, verified at `77fbc98`**, with the `en.json` / `ru.json` copy rescaled in the same change (step `4d` met). ⛔ **SHIPPED IS NOT DEPLOYED — no player has seen these values.** ✅ **AND RULING 8'S FREE-GRANT RISK COST NOTHING: the live DB was queried at close — `player_profiles` 0 rows, `max(xp)` 0** — ⚠️ **a POINT-IN-TIME reading, not a vindication of the risk**; it landed on an empty table only because the rescale shipped BEFORE `0217` wires crediting. ⛔ **The no-migration ruling is UNCHANGED and still binding.** ~~🔴 NOTHING HAS SHIPPED — still declares `1000` / `10`, verified 2026-09-11~~ 📌 **struck, true when written**

- [[decisions/adr-112-free-xp-grants]] — 🆕 **Accepted 2026-09-14, AMENDED 2026-09-15 — read the amendment, it wins wherever the two disagree.** Free XP grants are **capped, server-clamped, recorded once per account, and marked claimed only after a server ack**; the **tenure grant** for long-time players is the first. 🚨 **The server holds NO pre-launch history — the only tenure evidence is on the player's device**, so the server cannot verify a claim, only **bound the harm** of a false one. ⚠️ **What `daysPlayed` actually counts, measured: distinct local calendar days with an APP BOOT — a lobby visit with no match counts as a day — in the DEVICE's timezone, never pruned.** 🔴 **The redesign changed seven things at once:** login-then-claim (the profile always exists by then) · **no client date checks and no snapshot** · the claim is **always sent, even under 3 days** · the 90-day window is **removed** (the claim LOGIC is removed instead, ~60 days after release — `0268`) · one-claim-per-device **removed** in favour of **1 player = 1 check, server-enforced** · the ≥1-credited-match rule **removed** · and 🚩 **THE CHECK IS FINAL EVEN WHEN IT GRANTS 0 XP** — a 1–2-day player **never gets the grant later**, stored as an `xp_awarded = 0` row whose PK **is** *"the check is done"*. ⚠️ **"Claim on behalf of another account" STILL EXISTS** while identity is client-asserted (≤ 50 XP, once per player) **and it pre-empts that player's own final check** — accepted, on the reasoning that the surface disappears when `0268` lands. **Rule 2 now reads: the SERVER's record is the marker** — never local storage 📌 **2026-09-24: BUILT** by task `0253` (agent-closed, never run in production); 🚨 the claim-on-behalf risk starts **when the route reaches the profile box** (next profile deploy, before `0065`'s flip), not at the flip.
- [[decisions/adr-113-internal-player-id]] — 🆕 **Accepted 2026-09-15, owner-signed.** Profiles get **our own random UUID `player_id`**; platform logins live in `player_identities` keyed `(platform, platform_user_id)`; **`POST /v1/login`** is an idempotent race-safe find-or-create issuing a **stateless 24 h HMAC session token in v1** — owner: *"Token now, not later"*, **overruling the architect's recommendation to defer it on timing**. 🔑 **HARD RULE: no public route ever accepts a player id, and the internal id never reaches a client** — a proposal to do so is **a defect against this ADR, not a design question**. 🔓 **Stated honestly: while identity is client-asserted the token adds NO SECURITY** — login hands a token to anyone who asserts an id; what it buys is ids out of URLs and logs, and one place to verify later (`0267`). ⛔ **A `vfy:false` token must NEVER count as a proven owner.** Ships as **guarded migration `006`**, which **drops every old table**, is **deliberately NOT idempotent**, and **refuses if any affected table holds a row** — ⇒ ⛔ **if `0217` goes live first this becomes a DATA MIGRATION: re-plan, do not bypass the guard.** 🚨 **No per-IP rate limit on login (owner) — monitoring instead**, so **junk profiles are bounded only by detection and a switch: ~3–4 GB/day at 100 req/s against 48 G free ≈ 2 weeks of runway**, which is why **alerts must fire within minutes**. ⚠️ **CGNAT on Russian mobile carriers is why per-IP limits were rejected** — they would hit real players. **Account linking is RECORDED, deliberately NOT BUILT, and has NO TASK**; ⛔ **do not add a device-cookie `web` identity for guests before linking is designed — every guest who later logs in would become TWO players.** **Five slices, all before `0217`**
- [[decisions/adr-114-admin-server-alert-relay]] — 🆕 **Accepted 2026-09-17, owner-ruled placement, AMENDED the same day after the architect DISASSEMBLED the deployed Uptrace binary.** **The profile server is this project's ADMIN server**, and the Uptrace→Telegram **alert relay lives there**, behind the existing `/internal/` boundary. 🚩 **The box may be RENAMED later — "admin" is the ROLE, "profile" is only today's spelling; nothing here binds the word.** **The architect's first recommendation was the opposite (the game box) and is WITHDRAWN IN PLACE, with its seven-point objection table kept** — every objection was true of the *game* box and already solved on the admin box. 🚨 **Amendment 1 superseded Decision 3's "fail-closed" wording, which would have shipped a relay answering 401 to a bad secret:** ⇒ 🔑 **fail-closed on DELIVERY, NEVER on the HTTP STATUS** — the status code **is not a verdict we render, it is an instruction Uptrace obeys**, and a 401 buys only **32 retries over ~26 h re-persisting the wrong secret**. 🚨 **A5 — the secret exposure is WORSE than first recorded: Uptrace PERSISTS the full outbound JSON (secret included) and the first 100 bytes of our response, per attempt, into ClickHouse** ⇒ 🚫 **the relay must never echo ANY request content**, and **rotating the secret is NOT erasure**. ⚠️ **Ruling B: the allowlist STAYS and a source-IP miss silently and permanently kills the channel — owner-accepted, and PROSE IS NOT A MITIGATION**; the guard that closes it is `0284`. ⛔ **`0283`'s daily digest is ACTIVE FALSE REASSURANCE about this channel, and citing it as evidence that alerting works is the error A3 exists to prevent.** ⚠️ **A receiver on the egress proxy host is STRICTLY BETTER on resilience and stays the LIVE FALLBACK** — rejected on operability, not on merit

### Product Strategy & Sprints
- [[decisions/product-strategy]] — Strategic logic: retention-first sequence, experiments policy, key analytics data
- [[decisions/sprint-1]] — Sprint 1 (done): analytics baseline, Sentry, mobile quick wins, ghost rate investigation
- [[decisions/sprint-2]] — Sprint 2 (done): tutorial, auto-spawn, auto-expansion, zoom-to-territory, announcements
- [[decisions/hotfix-post-sprint2]] — Post-Sprint 2 hotfix (done): experiment analytics, skip button, UI:Tap, HF-6/7/9
- [[decisions/sprint-3]] — Sprint 3 (done): server observability, stale-build fixes, map preload, and deferrals to Sprint 6
- [[decisions/sprint-4]] — **THE ACTIVE SPRINT** (owner-ruled 2026-09-07, verbatim: *"The active sprint is the Sprint 4!"*). Citizenship + in-app-purchase foundation.
  🆕 **2026-09-24 (`6eeceeb`) — ZERO OPEN ROWS. Re-counted: 106 rows — 85 Done · 18 Moved · 3 Cancelled; 0 OPEN** (was 16). The **2026-09-23 rescope** (owner Q1–Q7) moved **12 after-deploy rows to Sprint 5**, split `0064` (step 8 + arming → `0298`), made `0065` the go-live only (test-buy → `0297`), and ruled **Sprint 4 closes AT THE DEPLOY** (W16, `/fkit-sprint-done` — not done). The four that stayed — `0064`, `0203`, `0253`, `0020` — **all closed 2026-09-24 `(agent-closed — not owner-verified)`, none proven in production.** Line-3 banner `🔄 In progress`; the *ask by name* warning is struck — the selector now returns this board (verified).
  **Current state (board re-counted 2026-09-08 at `HEAD` = `5913ac2`).** **82 rows — 53 Done · 17 Backlog · 7 Blocked · 1 In progress · 3 Cancelled · 1 Moved pointer (`0230`); 25 OPEN.** 🚨 **31 of the 53 done rows are `(agent-closed — not owner-verified)`** — no human checked that work, and `/fkit-status` collapses every `✅` variant to plain `done`, so **the board reads greener than the evidence supports** (known, ADR-033, not a defect). ⛔ **NOTHING from the 2026-09-07 work is DEPLOYED** — the owner deploys at the next weekend slot, and a `✅ Done` row means *built and closed*, **not** *in production*.
  **What actually shipped to production:** one deploy, `362a2f9` (2026-08-29), carrying the whole outage track, `0066`, `0067`, `0068`, `0198`'s fix and `0063`'s config. **Citizenship stays dark BY DESIGN** — `PROFILE_INTERNAL_TOKEN` deliberately blank, card flag off.
  **Board re-counted 2026-09-12 at `77fbc98`: 84 rows — 60 Done · 13 Backlog · 6 Blocked · 1 In progress · 3 Cancelled · 1 Moved; 20 OPEN** (was 82 / 25 open). 🚨 **38 status cells now carry `(agent-closed — not owner-verified)`** — ⛔ **the ratio got WORSE, not better, as the sprint shipped.** The single `🔄 In progress` row is **`0064`**, not `0211`.
  **Still open and load-bearing:** 🚨 **the clientless-leader XP loss is ✅ FIXED IN THE REPO by `0211` (shipped and closed 2026-09-12) but ⛔ STILL LIVE IN PRODUCTION — `0211` is NOT DEPLOYED.** ⚠️ **Read both halves; *"fixed"* alone is wrong.** It covers **FFA and Team**, via **Mechanism A** (a composite `winnerDeclarable`), and ⛔ **it fixes the XP LOSS, not the STALL** — the stall is `0242`, filed 2026-09-11 to the Backlog board. 🚨 **`0211` closed with an OUTSTANDING OWNER-SIDE ACTION — NO end-to-end proof that crediting works** (no harness for the `WinConditionCheck → Transport → server` seam; building one was offered and DECLINED in favour of a manual live check after deploy) **and THREE accepted residuals (R4, R5, R7).** 🔴 **And the cost is now SPENT: shipping it destroyed `0208`'s pre-fix denominator — the per-match stall rate will NEVER be known.** ⛔ **Not a gap; do not re-propose it.** ~~its ship is ordered behind `0208`~~ ~~cleared is NOT scheduled: still `🔲 Backlog`~~ ~~now reads `🔄 In progress` with no session owning it~~ 📌 **all struck, each true when written.** `0065`'s paid go-live is blocked on **three** conditions — `0014`, `0062`, `0195`. The profile **clean-slate rebuild** is `0213`–`0222` plus `0201`, and means **wipe and rebuild onto the EXISTING box and bucket, not procurement**.
  **Board re-counted 2026-09-22 at `6934226`: 106 rows — 78 Done · 14 Blocked · 4 Backlog · 1 In progress · 3 Cancelled · 6 Moved; 19 OPEN** (was 106 / 25 open at `7eebaf3` — the row count did not change; **six rows left the open pool**, five moved to [[decisions/sprint-5]] and one closed). 🔴 **FIVE SEPARATE OWNER RULINGS ON 2026-09-22 — do not merge them, none decided another's question:** a base **re-rank** moving `0272`/`0273`/`0266`/`0253` under `0217`; **RULING A**, the epic `0266` above its own child slices (⚠️ **a READING-ORDER correction that changes NO dependency** — the slice order still governs); **RULING B**, the three remaining append-ranks ratified `Low` (`0285` lowered from `Medium`, `0286` unchanged, `0289` lowered from `High` — ⛔ `0286` stays `🚧 Blocked`); **RULING C**, the four core labels ratified **as they stand** (⛔ not one value moved); **RULING D**, four post-deploy **checkups** moved to Sprint 5 (`0238`, `0285`, `0289`, `0061`); and **RULING E**, `0030` moved separately (⛔ **not a checkup, not RULING D**) with *"anything that depends on it"* **swept and resolved to NOTHING**. ⛔ **Across all five: no task folder moved, no mover was invoked, and no `## Status` token was touched.** 🚩 **Three rows were offered under D and the owner DECLINED each — `0065`, `0064`, `0203` stay on Sprint 4.** ✅ **`0014` CLOSED 2026-09-22** `(agent-closed — not owner-verified)` ⇒ 🔧 **`0065`'s blocker count drops THREE → TWO** (`0062`, `0195`); ⛔ **`0065` stays `🚧 Blocked`, its token did not move, and a dropped count is NOT movement toward go-live.** ✅ **RESOLVED 2026-09-22 (same day, BEFORE the synced commit): `0009` does NOT block `0030` and the blocker count STAYS TWO.** ~~`0009` declares it BLOCKS `0030`, which `0030`'s own two-item list does not name — a third blocker or a stale claim~~ ⇒ **it was the STALE CLAIM.** Architect verdict, **owner-accepted** live (⛔ not producer precedent): the claim traces to a **conditional** sentence in `architecture.md` §13 Q1 **flattened into "Blocks"**, and ⛔ **the determination was never run**; `0009` is right about **current** code but 🔄 **`0030` REPLACES the archive POST with an S3 write, so it removes that leg from `0009`'s scope — the arrow points the other way**; **the discriminator is that citizen-gating reads `is_citizen` from the PROFILE SERVER, not upstream flares** — had it keyed off the latter the dependency would be real. ⚠️ **ACCEPTED, NOT PROVEN — ~90 % technical, ~70 % on intent**, because ⛔ code cannot establish what "Blocks" meant to its author. **`0009` was edited only to downgrade it to *Related*** (scope/status/priority untouched). 🚩 **`architecture.md` §13 Q1 was deliberately NOT corrected — the owner DECLINED — so the seeding sentence can be re-read the same wrong way.** ~~Previous count~~ (was 104 / 25 open at `ceb5454`; **open unchanged**). 🚨 **96 `(agent-closed — not owner-verified)` mentions file-wide against 77 Done rows — 56 counted strictly in the status cell. Both bases are true; say which you mean. ⛔ The ratio keeps getting WORSE as the sprint ships.** ⚠️ **Cell-by-cell movement was NOT reconciled and is not asserted.** **Three verified changes:** ➕ **`0291` is a new row, closed the same day** (see [[tasks/citizenship-card-fail-closed-degraded-sdk]]) · ➕ **`0289` is a new row** (`🔲 Backlog`, rank **`High` — the producer's**), the idle-period alert-delivery drill split out of `0274`'s amendment A1 · 🔧 **`0014` flipped `🚧 Blocked` → `🔲 Backlog` — its EXTERNAL APPROVAL GATE IS VOID** (owner ruling 2026-09-19), leaving **two owner-side Yandex-console deliverables, neither externally gated**; ⛔ **`0014` is NOT done — it stopped waiting on Yandex, not on itself.** 🔴 **AND THE `0238` LAUNCH GATE THIS BOARD CARRIED IS DROPPED (2026-09-21, owner ruling): `0238` now OBSERVES the launch flip instead of preceding it and blocks nothing** — ⛔ its status token, board and rank UNCHANGED; **`0291` is the only remaining code precondition.** ⚠️ **Two `0238` findings survive the re-scope and must not be lost with the gate: a `./deploy.sh staging` box already ships a bundle whose `GAME_ENV === "prod"`, so a PRODUCTION game deploy is NOT required; and because `CITIZENSHIP_CARD_ENABLED` is `false` and `&&` short-circuits, the remote flag is NEVER READ on a stock build ⇒ the check would have PASSED VACUOUSLY unless that flag is flipped `true` in the build under test (⛔ staging only, NEVER committed).** 🚩 **One precondition stays OPEN and nothing in this repo can settle it — does a Yandex Games draft/dev version exist that can serve such a build? Without one the SDK serves NO flags at all.** ✅ **Payments-secret provenance ANSWERED 2026-09-20 — owner-attested, NOT repo-verified; `0065`'s blocker count stays THREE and is now owner-CONFIRMED.**
  📌 **UPDATED 2026-09-22, LATER THE SAME DAY (`0d39e4d`) — NO row was added, removed or re-statused after `6934226`; verified from the diff, not re-counted.** What landed is a **new ordering document** for eleven of this board's tasks: [[systems/weekend-deploy-window]] — **TWO profile deploys + one telemetry + one game**, written because five tasks each demanded a profile deploy in the same window under partly opposing conditions and **nothing ordered them**. ⚠️ **`0220` will close with a KNOWN, DELIBERATE GAP** (its §8 step 3 cut to task `0294`) — 🚨 **record it as ONE STEP DEFERRED, NOT "unverified"**, and **restate it at `0220`'s close**. ⛔ **`0065` step 3 and the `0054` flag flip are RULED OUT of the window** — the buy button is behind a **compile-time `false` with no env or remote override**, so it needs a source change **plus a second game deploy**; ⛔ **`0065` stays `🚧 Blocked`, no token moved, no mover invoked.** 🚨 **There is deliberately NO rollback procedure — a DECISION, not an omission — so the window's one step with ZERO prior production evidence has no written way back.**
  🆕 **2026-09-23 (`b3ee5de`) — `0062`, `0017`, `0012` CLOSED `✅ Done (agent-closed — not owner-verified)` on owner rulings, as built + reviewed — ⛔ NOTHING in them is proven in production;** their production checks moved to the new Sprint 5 task **`0296`**, and the local browser checks of `0012`/`0017` were **owner-WAIVED** (nobody sees the Personal tab in a browser until after launch). The flip is now **`0065` §6's alone**; `0065` waits on **`0296` + `0195`**; `0064`'s `0062` dependency removed. **Re-counted at `b3ee5de`: 106 rows — 81 Done · 11 Blocked · 4 Backlog · 1 In progress · 3 Cancelled · 6 Moved; 16 OPEN**; marker in **59 of 81** Done status cells. `0032`'s cert blocker is stale (telemetry cert live) — ⛔ it stays blocked on the window. See [[tasks/forward-profile-internal-token]], [[tasks/citizenship-earned]], [[tasks/personal-inbox]].
  📛 **Superseded positions — KEPT, in the order they were believed, because several of these are why later claims are worded so defensively:** **(1)** *"this board has ZERO `🔄 In progress` rows"* — false from 2026-09-02, when `0064` started. **(2)** The row-count chain **58 → 61 → 62 → 64 → 82**; every earlier figure is dated and struck, none deleted, and the 2026-09-04 count's own caveat (*"the `0213`–`0222` rows are not yet reflected here"*) is now **discharged**. **(3)** *"`0206` shipped the fix"* and *"public FFA now ends at 80 %"* — 🔴 **`0206` was REVERTED 2026-09-04 and NEVER DEPLOYED; that behaviour never reached a player.** ⛔ **`0206` was NOT buggy and did NOT cause the stall — its PREMISE was disproved by measurement.** Its row still reads `✅ Done`, correctly: **the WORK was done, the EFFECT was reverted.** **(4)** *"the profile box is live"* — **wrong**, corrected 2026-09-04. **(5)** *"THERE IS NO PROFILE HOST"* — the **overcorrection**, withdrawn the same day; the box exists, its setup and keys are being redone. **(6)** *"`0022` is a PR #77 regression"* — **premise refuted**; the clientless winner is original to the fork. **(7)** `0047`'s *"After T4g (PR #125) merges"* gate — **stale and cleared.** ⛔ **None of these may be deleted, and none may be restored as current.**
- [[decisions/sprint-4b]] — Sprint 4b (done): interim public-match variety with compact maps, Duos/Trios/Quads, and weird-setting modifiers
- [[decisions/sprint-4c]] — Sprint 4c stabilization (closed; plan archived to `sprints/done/` 2026-08-24): quick wins done, source maps enabled, lobby/map fetch fixed, mobile WebGL deferred
- [[decisions/sprint-backlog]] — No-sprint backlog across both unsprinted boards: monitoring, mobile WebGL, worker init, bot anti-SAM nuke tactics, weird-mode cleanup, FuseTag/GutterAds fixes, the `0001`–`0011` briefs and cosmetics monetization dependency chain, plus the 2026-08 additions — heal tasks `0050`–`0053`, outage follow-ups `0058`/`0059`/`0061`/`0064`, the `0057`/`0062` promotions, the owner-ruled email-subscribe fold-in to `0048`, `0199` (the Yandex invite-portal product question, filed here 2026-08-28), the 2026-09-01 test-harness pair `0201`/`0202`, and the 2026-09-02 additions — `0023`, `0024` (which had **no `## Sprint` heading at all**), `0028` (hint videos: **MP4 not GIF, measured**; topic #6 unruled either way) and `0043` made board-visible, plus `0203`, the ten-item pre-arming gate for the config guard, and the 2026-09-02 win-condition pair `0205` (Teams Bot-team stall, ex-`0022` risk 2 — **filed as `0204` and renumbered on an owner ruling; `0204` has no task folder here**) and 🚨 **`0206`, the FFA award that is the ONLY fix for a live, silent, whole-match XP loss in production — ~~unscheduled, unstarted~~ ~~PROMOTED INTO SPRINT 4 on 2026-09-03; still `🔲 Backlog` and unstarted~~ PROMOTED, BUILT AND CLOSED 2026-09-03, its row here kept as `➡️ Moved`; 🔴 nothing deployed**. 📌 **`0207` appended 2026-09-03** (the `WinModal` participation comment, comment-only), **`0208`, `0209` and `0210` appended the same day** out of `0206`'s close — measure incidence (📌 **widening SETTLED: Part A multiplayer, Part B Singleplayer, not to be merged**), define `placement` semantics then fix the literal `1`, and Singleplayer reports nothing to the platform leaderboard (owner-ruled) — **all four `🔲 Backlog` and unscheduled**, and **`0205`'s premise was confirmed by simulation** the same day with its Low–Medium rank **weighed and held**. 📌 **`0201` is now `🔄 In progress` with its plan gate RULED — fold the shell harnesses into `npm test`; CI explicitly rejected — and the `0201`-then-`0202` order plus `0202`'s `Low` rank are owner-ruled: do not re-litigate either.** 🔧 **`0047`'s "After T4g (PR #125) merges" gate was stale and is CLEARED — nothing open blocks it**. 📌 **UPDATED 2026-09-08 — the OLDER board (`sprint-backlog.md`) moved for the first time in a while: 25 rows, 23 `⬜ No sprint` + 2 `⏸ Parked`, ALL OPEN** (counted at `f7d28eb`; `backlog.md` re-counted **unchanged at 48 / 37**). Its two non-canonical statuses were **ratified**, two board-invisible briefs (`0026`, `0029`) gained rows — **superseding a 2026-08-14 decision not to add them, which is kept** — and Task 7 was briefed as **`0234`, deliberately an INVESTIGATION**: the source material was two lines plus an unvalidated whole-implementation effort estimate, **six build-shaping decisions are unmade**, and the brief says outright **not to write implementation briefs off it**. ✅ **`0234` verified as NOT a duplicate of `0161` or `0210`.** 🚨 **Two rows are RESIDUE — `Task 9` / `Task 9a` duplicate the briefed `0010` / `0011` on the other board, so the true unique blind spot is 23 rows, not 25, and consolidating them across during `0001` would CREATE duplicates.** ✅ **Seven briefs (`0012`, `0015`, `0016`, `0035`, `0036`, `0045`, `0047`) gained a `## Sprint` field, closing an earlier vault flag** — ⚠️ values **DERIVED, not assigned**, visibility-only, nothing scheduled or re-ranked. 📌 **UPDATED 2026-09-10 — a third brief landed on `backlog.md`: `0239`, measure-then-scope the bare `:NNN` citation population**, filed on an owner ruling whose **same breath said DO NOT START IT** (`Unscheduled` is the producer's label). 📊 **~3,450 full-path citations across 241 files (vault excluded), ~2,186 of them BARE `:NNN`, only 12 files carrying a declared frame, ~35 (~1 %) ever verified.** 🚨 **2,186 IS A HAZARD MEASUREMENT, NOT A DEFECT COUNT — how many are actually wrong has NEVER been measured, and measuring it is step 1.** ✅ Recommended gating first step: **sample 30–50 at random and measure the real error rate**, reporting **wrong-file** and **unresolvable** counts separately and first. 📌 The convention bans *new* bare refs, so the set should stop growing ⇒ ✅ **"do nothing, deliberately" is a live option, recorded as genuinely live rather than a strawman.** ⛔ Not folded into `0004`; ⛔ the vault is **out of its scope** (ADR-005). 📌 **UPDATED 2026-09-22 — the board re-counted at `6934226`: `backlog.md` 85 rows — 61 `🔲 Backlog` · 16 `➡️ Moved` · 4 `✅ Done` · 3 `🚧 Blocked` · 1 Cancelled; 64 OPEN** (was 83 / 62 at `7eebaf3`). **Two rows added 2026-09-22, both `🔲 Backlog`, neither brief paged** (backlog rule): **`0292`** — *the client archive READ bypasses `archiveEnabled()`; `0159`/ADR-104 darkened the SERVER leg only* — ⚠️ **the owner ruled ONLY that it be filed separately; board, rank and severity are the producer's**; 🟢 **BENIGN, no user impact, no failure observed**, fires **only for non-active lobby IDs**, and ✅ **the destination is OUR OWN INFRASTRUCTURE, measured on the live prod deployment (classification only, no host recorded)** so its escape condition *"if it resolves to upstream, re-raise the board"* **was checked and did NOT fire** — ⛔ but `0009` finding 1 is **not** discharged; and **`0293`** — *two silent board-record failures* — 🟢 **NOTHING IS BLOCKED and NO BOARD IS WRONG TODAY; what earns it a task is RECURRENCE, not damage**: (1) a sprint→sprint move updates **two** records when a Backlog-originated task has **three** — the moved-row here that nothing revisits — **measured blast radius 2 of the 5 tasks moved to Sprint 5 that day** (`0061`, `0238`, both since fixed), ⚠️ **the other three were spared only because they have no row here at all**, so ⛔ **"3 of 5 were fine" is NOT a low base rate**; and (2) **`dashboard.sh`'s move-target parse is GREEDY** — the leading `.*` takes the **last** marker in a Status cell, so a readable row reports the **stale** sprint with **no error**, and **an author cannot get both a readable row and a correct parse**. ⚠️ **The existing mitigation is a workaround enforced by nothing.** ⛔ **The fix is DELIBERATELY UNSETTLED** (three options for finding 1, none of which fixes finding 2) and 🚨 **it touches fkit's own skill files, which no ruling authorized anyone to edit.** 📌 **Related to `0050` but the owner was offered a merge and DECLINED** — fixing either leaves the other untouched. 🔧 **Also this window: `0061`'s and `0238`'s moved-rows here were RETARGETED Sprint 4 → Sprint 5** (they are finding 1's evidence, not a separate change), and `0248`/`0249`/`0250` took link-path fixes only as `0014` moved to `done/`. ~~Previous count~~ **Two rows added, both `🔲 Backlog`, neither brief paged** (backlog rule): **`0288`** — *alert rule **A4**, p95 of login latency > 750 ms for 10 min*, filed 2026-09-19 and ⚠️ **the ONE of `0274`'s six deferred alert rules that got a task at all** (A1/A2/A3/A6 are deferred for want of login traffic with **no task on either board**, and only **A5** is built) — and **`0290`** — *`setup.sh` calls `apt` where the other two deploy scripts call `apt-get`, and `apt` has no stable CLI for scripts*, filed 2026-09-20. ⚠️ **`0290` is RELATED TO BUT NOT THE SAME AS `0286`** (Sprint 4 — the missing `DEBIAN_FRONTEND=noninteractive`, **observed blocking a real deploy three times**); ⛔ **do not merge them on the strength of both being about `apt`** 📌 **UPDATED 2026-09-22, LATER THE SAME DAY — re-counted at `0d39e4d`: 86 rows — 62 `🔲 Backlog` · 16 `➡️ Moved` · 4 `✅ Done` · 3 `🚧 Blocked` · 1 Cancelled; 65 OPEN** (was 85 / 64 at `6934226`). **ONE row added, `🔲 Backlog`, brief not paged** (backlog rule): **`0294`** — *prove a rotated value overwrites the persisted one on the **LIVE** profile box, `0220` §8 step 3* — cut from the weekend deploy window on the owner's *"Skip it now, file it separately"*, so that **`0220` closes with a recorded, deliberate gap rather than an unnoticed one.** 🚨 **RECORD WHY THE STEP EXISTS — it nearly did not survive:** the owner's first instinct was *"rotate nothing, we don't know why the step exists"* and they **revised it once shown the reason** — if persist-or-reuse always prefers the box's stored value, **the day a key leaks and a new one is deployed the box silently keeps the old one**, and nothing says otherwise. ⛔ **Never write it up as "a redundant third deploy."** ⚠️ **ONE STEP DEFERRED, NOT "UNVERIFIED"** — harness **T13** already proves the rotation **against stubs** under `npm test`; ⛔ **stubbed coverage is explicitly NOT live-box evidence**, and ⛔ do not report the property as fully proven **or** as entirely unproven. 🚩 **The METHOD is deliberately unsettled** — all four candidates have a production consequence; 📌 rotating `TELEGRAM_PROXY_URL` (a **URL, not a secret**) was **OFFERED AND NOT TAKEN**, recorded only because it shows ✅ **the task needs NO real credential.** See [[systems/weekend-deploy-window]] 🆕 **2026-09-24 (`6eeceeb`): 87 rows — 63 Backlog · 16 Moved · 4 Done · 3 Blocked · 1 Cancelled; 66 OPEN** — one row added, **`0299`** (tiered ad-impression analytics, split from `0020`; waits on `0250` and the `0273` client deploy).
- [[decisions/sprint-5]] — Sprint 5 (planned): coin economy, clans, cosmetics, map voting, replay. 🆕 **A SIXTH ROW APPENDED LATER ON 2026-09-22 — 13 rows now (counted at `0d39e4d`), and ⛔ IT IS THE ONLY ONE THAT IS *NOT* A MOVE:** **`0295`** — *measure the game-prod egress IP and **APPEND** it to `PROFILE_INTERNAL_ALLOW_IPS` (`0217` Q4)* — filed here by name on the owner's *"Record as a task, add it to the Sprint 5, not the current Sprint 4."* ⛔ **Not RULING D, not RULING E; no row left Sprint 4 for it.** **Two deliverables:** measure by a **named, repeatable method**, and 🔒 **RECORD THE METHOD — ⛔ NEVER THE ADDRESS.** 🚩 **`fkit-lead` could NOT measure it — the prod host is not in any readable env file**, and ⚠️ **an earlier same-day measurement that *reportedly* matched was NEVER re-verified and NEVER written into a brief — ⛔ do not close `0295` by citing it.** 🚨 **BEING ON SPRINT 5 DOES NOT DEFER THE MEASUREMENT** — a separate ruling splits it: the **owner measures at W0 the day before the window**, the **task (choosing and recording the method) stays on Sprint 5**; ⛔ reading *"it's on Sprint 5"* as *"skip it this weekend"* would leave the first profile deploy on a **JUNE-DATED allowlist**, and a wrong entry is **403 on every credit call, indistinguishable from "working" — XP LOST, not queued.** See [[systems/weekend-deploy-window]]. ➡️ **FIVE REAL TASKS MOVED ONTO THIS BOARD EARLIER THE SAME DAY — 12 rows then, was 7, every one `🔲 Backlog`.** ⚠️ **TWO SEPARATE OWNER RULINGS, not one:** **RULING D** moved four **post-deploy checkups** — `0238`, `0285`, `0289`, `0061` — on the owner's *"Let's skip this type of chekups, I will take care of them after deploy … You can move the tasks connected to checkings to the next sprint"*; **RULING E** moved **`0030`** separately, ⛔ **not a checkup and not on D's authority**, with *"anything that depends on it"* **swept and resolved to NOTHING** so it moved alone. ⛔ **No task folder moved, no mover was invoked, and no `## Status` token or `## Priority` was touched — scheduling is not starting, and this board is still NOT the active sprint.** ⚠️ **None of the five belongs to this plan's original *Full F2P Loop* scope statement — deliberate, the owner put them here by name; appended, not inserted (ADR-035), and append position is not a priority signal.** ⚠️ **Two of this plan's own descriptions are now false: not every row derives from its task sections, and not every Brief cell reads `TBD`.** 🚨 **`0030` arrives STILL HARD-BLOCKED and its blockers did NOT come with it** — `0017` and `0018` stay on Sprint 4, so it sits a board *behind* its own prerequisites (**coherent, not drift** — ⛔ do not drag them here). 📐 **Its blocker 2 is now measured: the four S3 config slots are plumbed end to end but EMPTY and `archiveEnabled()` is a hard `false` ⇒ the plumbing is done, only the bucket is missing** — ⚠️ **read from the repository, NOT a deployed host.** ✅ **`0009` does NOT block `0030` — RESOLVED 2026-09-22, blocker count stays TWO** (architect verdict, owner-accepted; the "Blocks" claim was a **conditional** `architecture.md` sentence **flattened**, and `0030` *replaces* the archive leg rather than waiting on it). ⚠️ **Accepted, not proven — ~90 % technical / ~70 % intent.** 🚩 **The seeding sentence in `architecture.md` was deliberately left uncorrected by owner choice.** ✅ **On `0238`, the switchability property the owner named as the thing that matters is VERIFIED IN SOURCE 2026-09-22** — 🚨 ⛔ **NOT in production, and it holds on production builds ONLY** (`checkExperimentFlag()` returns `true` unconditionally under `GAME_ENV === "dev"`), so ⛔ **a local "the card appeared" reading proves nothing** and `0238` ⛔ **does not close as done if the check still cannot be run** — the record must say the switch is NOT KNOWN TO WORK. 📌 **Updated 2026-09-08: its 2026-08-14 reconciliation note is SUPERSEDED as to `0026` and `0029`** — both were board-invisible and an owner ruling gave them `⬜ No sprint` rows on `sprint-backlog.md`, overturning the decision not to add them. ⛔ **The old note is KEPT as the record of why they were parked, and NEITHER task was added to this plan — Sprint 5's scope statement still excludes both, which is exactly why the other board is their home** 🆕 **2026-09-24 (`6eeceeb`): 28 rows — 19 Backlog · 9 Blocked, all open** (was 14): 12 rows moved in by the Sprint 4 rescope, plus **`0298`** (parity guard's first real report-only run, then arm `--enforce`) and **`0297`** (owner-run paid-citizenship test-buy, placed at **row 1** by owner ruling). Banner `🔲 Backlog` — **not started**; the owner starts it after the deploy. 🚨 `0065` now has **no task condition** — paid citizenship may go live before any real purchase is proven (owner-accepted).
  🆕 **2026-09-23 — A SEVENTH ROW: `0296`, 14 rows now (counted at `b3ee5de`), a NEW task that RECEIVES the unrun production checks of the closed `0062`/`0017`/`0012` — ⛔ not a move.** Section **A** after the deploy window (token non-empty; A1–A6), section **B** after the flip owned by `0065` §6 (B1–B3; ⛔ B3 proves nothing while the flag is off). Carries `0062`'s forget-risk and the *NO GUARD* ruling at its top. **Unranked, not low.** No task page of its own until it closes — recorded on the board page.
- [[decisions/sprint-6]] — Sprint 6 (planned): historical multiplayer maps, paid campaign packs, mobile warning; its "Sprint 5 cosmetics store" prerequisite was corrected 2026-08-09 — no such store exists · 📌 2026-09-23: line-3 banner `🔲 Backlog`; its *"the selector wrongly returns this board"* warning is struck — no longer true (verified 2026-09-24)
- [[decisions/cancelled-tasks]] — HF-5, feedback match history, HF-11e, tutorial action-pause, HvN balance, compact-map runtime fallback, and guest-first profile XP cancellations
- [[decisions/personal-data-152fz-compliance]] — Hash-based 152-ФЗ avoidance invalidated; notification/consent work deferred to backlog with accepted risk

### Legal & Operations
- [[decisions/fkit-transfer-blueprint]] — Extracting the two-model agent OS into a reusable kit: the generic/project seam, the routing manifest, and single-sourced skills that kill copy drift
- [[decisions/licensing-compliance]] — AGPL, CC BY-SA, source-access, and OpenFront asset/trademark constraints for GeoConflict; the 0025 audit (2026-08-23) found one violation (proprietary music) — remediation 0066 **deployed 2026-08-29 in `362a2f9`** and its live checks **run and passed 2026-08-30**, so the paid-IAP licensing gate is **DEMONSTRATED**; the **0025 audit task itself closed 2026-08-31** with H1 verified in production that day and **H3 the one finding still open, owned by `0073`** — and this clears that gate only; 0065 still waits on `0014`, `0062`, `0195`

### Bug Fixes & Investigations
- [[decisions/windoworigin-url-join-defect]] — 🔧 **Fix DEPLOYED 2026-08-29 (`362a2f9`); task 0198 CLOSED 2026-08-30 on local proof only, because its production proof is UNREACHABLE and was WAIVED** — the private-lobby buttons are `display: none` on the Yandex template (task `0198`): `windowOrigin` is `origin + pathname`, so concatenating a worker route onto it misses the route on the `/yandex-games_iframe.html` production entry point — a PUT/POST 404s, a GET gets the SPA fallback's 200 — and private-lobby start silently fails on Yandex Games. Review closed 2026-08-28 at `🚧 Blocked — awaiting deploy proof`, superseded by the 2026-08-30 waiver and close; six owner-confirmed residuals. Carries the durable rule, the non-root testing requirement, and the corrected Express-not-nginx substitution mechanism
- [[decisions/yandex-invite-portal-boundary]] — ❓ **Open, unruled** (task `0199`, Backlog / Unscheduled at producer rank, owner-confirmed placement): a private-lobby invite from the Yandex build lands the recipient **outside the portal iframe**. `0198` fixed the **path** on that line; this is the **host**. Records the code fact that `yandex-games_iframe.html` sets `flashist_isYandexPlatform = true` unconditionally, so an off-portal load still enters Yandex platform mode — **what that path does off-portal is NOT established and must be measured**
- [[decisions/config-parity-failure-class]] — the recurring class *a variable that never reaches production*: `0062`, `0063` and `0195`, the first outside `deploy.sh`; `0064` is the guard and must land after all three. **`0063` is fixed, deployed and fully evidenced (2026-08-30); `0062`'s fix shipped with the token deliberately BLANK**, so a presence check would pass on a variable doing nothing; **`0195`'s fix shipped 2026-09-01 but is NOT deployed**. ⚠️ **All three have now shipped a fix and none is demonstrated working in production.** 🔧 **UPDATED 2026-09-19/20: `0195`'s variable HAS since reached the box** (non-empty, length 32; the prefix answers 404, not 503) and ✅ **its provenance is ANSWERED — OWNER-ATTESTED, not repo-verified.** ⛔ **Presence and origin only — correctness unproven, no purchase exercised.** 🔧 **And `0065`'s blocker count dropped THREE → TWO on 2026-09-22 when `0014` closed** — ⛔ `0065` stays `🚧 Blocked` and its token did not move. 🚀 **`0064`, the guard, is STARTED 2026-09-02** — built, review round 2 CONVERGED, **shipping report-only (it exits zero and cannot fail a deploy)**, so nothing on the execution order is unstarted now. ⚠️ **Arming `--enforce` is now TEN gate items, not two**, split out as `0203` on the Backlog board; 🚨 **R1, owner-acknowledged: the client parity check PRINTS GREEN WHILE INCOMPLETE**, and ❓ one gate question — does Docker Compose `env_file` tolerate an `export` prefix — is **explicitly unresolved** 🔄 **2026-09-23: `0062` CLOSED `(agent-closed — not owner-verified)` — ⛔ not a production fix; D1–D4 moved to `0296`; `0064`'s dependency on it removed.** 🛡️ **2026-09-24: the guard `0064` and its pre-arming gate `0203` are both CLOSED `(agent-closed — not owner-verified)` — ⛔ `--enforce` is wired nowhere and neither has run on a real deploy;** R1's client blind spot is fixed; a prod-only value guard now exists; arming + the first real run are `0298`. 🚩 The deliberately-blank `PROFILE_INTERNAL_TOKEN` state is **retired** (owner).
- [[decisions/clientless-leader-win-policy]] — 🚨 **A production defect: ✅ FIXED IN THE REPO 2026-09-12 by `0211`, ⛔ STILL LIVE IN PRODUCTION (not deployed).** ⚠️ **BOTH HALVES OR NOTHING — *"fixed"* alone is wrong, and so is *"fixed nowhere"*.** ⛔ **`0211` fixes the XP LOSS, not the STALL** (`0242`, Backlog). ~~*"fixed only in the repo"*~~ ~~🔴 **FIXED NOWHERE — `0206` was reverted, so nothing is fixed anywhere** (2026-09-04)~~ 📌 **both struck, each true when written.** The underlying defect: when a bot or a Nation leads FFA at the win threshold, no `winner` message reaches the server, so `creditMatchXp` never runs and **the whole match's match-end XP is silently lost for every player**. Task `0022` shipped the **guard only** (the match no longer wedges; a human can still win later); ~~the **award that closes the XP loss is `0206`**~~ 🔴 **`0206` was REVERTED 2026-09-04 and NEVER DEPLOYED — the defect is fixed NOWHERE.** ⚠️ It was also **a no-op in the case that actually loses the XP** (`players()` filters to `isAlive()`), which measurement showed is **the** case, not a corner case. ~~The shipped award also ENDS public FFA at 80 %~~ 🔴 **struck — that never reached a player.** **The replacement is `0211`** (credit at elimination or match end, independent of any winner). 🔴 **And Team mode has the SAME defect** — `checkWinnerTeam()` shares the guard shape, unnoticed across `0022`/`0206`/`0205`. Also carries the **keep-them-apart table** for the two things conflated repeatedly that day — the `placement` rank **never leaves the browser**, while the `points` **do reach the Yandex platform** — and **ADR-110** (accepted 2026-09-03, one policy across both modes, 🔴 **with a known expiry**) and the 2026-09-03 **confirmation of `0205`'s premise by simulation**. Plus three owner rulings of 2026-09-02: the brief's own prescribed fix was **REJECTED — it would have regressed PR #77**; `0022` ships guard-only; and the award applies to **both branches (timer AND territory threshold), ruled once for `0205` and `0206` together** so FFA and Team stay on one policy — with a **hard tutorial re-check** riding on it. ~~⚠️ Reachability is structural reasoning, **not measured**~~ ✅ **MEASURED 2026-09-11 (`0208`, 4–10 Sep 2026): FFA clientless-in-front ~1.6 %, Team ~53.2 % (~52.4 % stall-capable), overall ~29.1 %** — 🔴 **at the CLIENT-MATCH unit, as LOWER BOUNDS**, and ⛔ ***"52 % of Team matches stalled" is NOT a supported claim*** (a firing records who was first past the post, not how the match ended). 🔧 **The *"a Nation leading public FFA has been OBSERVED ZERO TIMES"* caveat is CORRECTED — `Nation` 50 firings, every FFA event in the window public.** 🔴 **The per-match stall rate will NEVER be known** (owner ruling, option B)
- [[decisions/autospawn-late-join-fix]] — Fix for auto-spawn failure when joining during catch-up (late join / reconnect)
- [[decisions/double-reload-fix]] — Fix for double page reload on browser refresh caused by orphaned `#refresh` history push
- [[decisions/archive-archival-strategy]] — Split archive work: disable noisy dead path now, defer S3-backed citizen archival until citizenship exists. 🔴 **Corrected 2026-09-22: phase 1 disabled the WRITES only — the client archive READ is still live in production** (benign, own infrastructure, active-lobby IDs excluded); task `0292`, and phase 2 (`0030`) inherits it
- [[decisions/hvn-balance-pr70-no-ship]] — No-ship review for the cancelled Sprint 4 Humans vs Nations balance attempt
- [[decisions/registry-image-policy]] — Trusted vs untrusted image rules, registry visibility policy, and rollback/retention guidance
- [[decisions/stale-build-zombie-tabs]] — Investigation and fix for users persisting on old builds (HF-11a/b/c/d)
- [[decisions/vps-credential-leak-response]] — Incident postmortem: Docker build-context secret leak path, deploy hardening, and trusted recovery workflow
- [[decisions/profile-deploy-hardening-review-loop]] — Reset of the unbounded profile-deploy review loop into bounded T4 slices and fixed acceptance criteria
- [[decisions/profile-storage-strategy]] — Player profile DB storage: Option B (typed columns + jsonb overflow), `xp bigint`, `persistent_id text` — chosen in T5 before the first migration
- [[decisions/codex-second-opinion-mandatory]] — Owner confirmation 2026-09-10 that **no code review closes without a Codex second opinion**, and that a Codex outage is a **LOUD degradation flag, not a quiet skip**. ⚠️ **No rule changed** — the three review skills already required it, and no convention or ADR was written (deliberately). 🚨 **The ruling's stated evidence did NOT survive the ledgers:** the *"three for three"* claim originated with the **lead**, was relayed to the owner before anyone checked it, and the real strength is **two Codex-only catches (one `high`)**, against one Claude-only finding and one Codex outage. **Do not quote a hit rate from it**
- [[decisions/incident-2026-08-22-public-lobbies-outage]] — 2026-08-22 prod outage: worker 16 died, crash recovery has never worked (`worker.process.env` bug), the all-20 gate stalled scheduling for ~3.5 h; task split 0055→0057→0056 plus the config-drift findings 0060–0064

## Tasks

- [[tasks/session-start-sequence]] — Session start event sequence (Session:Start → Device:Type → Platform:OS → Player:New/Returning)
- [[tasks/build-number-tracking]] — HF-7 build segmentation: original GA custom-dimension approach and current native build-field tracking
- [[tasks/mobile-quick-wins]] — Task 3: retina off, 30fps cap, particles reduced on mobile
- [[tasks/spawn-ux]] — Tasks 4b + 4e: zoom-to-territory function and expanding ring spawn indicator
- [[tasks/stale-build-detection]] — HF-11b/c/d: `/api/version` endpoint, client polling, non-dismissible refresh modal
- [[tasks/ui-click-multiplayer]] — Investigation confirming `UI:ClickMultiplayer` fires on per-lobby JOIN attempts
- [[tasks/player-profile-store-investigation]] — Sprint 4 investigation recommending Postgres, now on a dedicated reg.ru profile/API VPS, and identifying the Yandex identity gap
- [[tasks/profile-schema-contract]] — Sprint 4 T1 shared PlayerProfile v1 contract, migration function, guest-profile factory, and tests
- [[tasks/yandex-identity-plumbing]] — Sprint 4 T3 client-to-server Yandex unique-ID plumbing for later profile crediting
- [[tasks/profile-server-skeleton]] — Sprint 4 T4a standalone profile-service `/health` skeleton, logger, and validated port
- [[tasks/profile-api-url-config]] — Sprint 4 T4b public profile API URL plumbing through runtime config and `/api/env`
- [[tasks/profile-docker-image]] — Sprint 4 T4c allowlist-copy, linux/amd64 Docker image for the profile service
- [[tasks/profile-vps-provisioning]] — Sprint 4 T4d dedicated reg.ru VPS provisioning, TLS/network boundary, and operator-verified RU residency. ✅ **UPDATED 2026-09-10 — THE HOST IS PROVISIONED AND LIVE**, verified by `0215` re-running this script in place (swap, `vm.swappiness=10`, `ufw` 22/80/443, spec meets the floor, RU residency confirmed by geolocation). ⚠️ **`ufw` default-deny was NOT re-verified**, and 🔴 **the `/internal/` allowlist this slice declared is still the June egress IP — a silent 403 on every credit call if stale** (`0217` owns it). *(This corrects a 2026-09-04 index line reading "NO PROVISIONED HOST EXISTS", which the page body itself had already withdrawn as overstated.)* Also records the gaps it never covered (no log rotation, no image prune, no OS hardening, a `restart: on-failure` policy that misses a bare Docker daemon restart)
- [[tasks/profile-build-push-digest]] — Sprint 4 T4e1 local amd64 profile-image build, registry push, and fail-closed immutable digest resolution
- [[tasks/profile-onbox-stack-gate]] — Sprint 4 T4e2 on-box compose stack, health gate, systemd lifecycle, and digest rollback
- [[tasks/profile-deploy-wiring]] — Sprint 4 T4e3 SSH/SCP deploy wiring, secret staging, and end-to-end digest passthrough
- [[tasks/profile-image-secret-scan]] — Sprint 4 T4f built-image layer byte scan that blocks secret-bearing profile image pushes
- [[tasks/profile-server-bring-up-runbook]] — Sprint 4 T4i operator runbook for bringing the profile service live on a reg.ru VPS. ✅ **UPDATED 2026-09-10 — THE HOST IS SERVING AGAIN** (`0215` used this runbook, found the box live, and **adopted rather than wiped** it). 🚨 **THREE DRIFTS NOW CONFIRMED:** the `0182 brief.md:136-137` token-trap citation is **STALE and NEVER POINTED AT THAT TEXT AT ANY COMMIT** — commit `879b2f4` wrote the citation and inserted a correction banner above the target **in the same commit** ("self-invalidation inside one commit", a confirmed recurrence of failure mode 4 in `conventions/file-line-citations.md`); the `Restart=always` systemd unit silently undoes a `docker compose down -v`; and supplying the token by environment leaves a **stale persisted `.internal_token`** a blank-valued future deploy would re-adopt. **The real targets are now cited BY CONTENT, not by line — search `0182/brief.md` for `STOP — CORRECTION` (the banner), `~~*"Optional — leave blank` (the struck sentence), `#     "Optional — leave blank` (the same claim in the `.env.profile.secret` block) and `PROFILE_INTERNAL_TOKEN=` (the corrected value line). 🔴 That file is UNCOMMITTED and moved FOUR TIMES on 2026-09-10; two successive line-number sets went stale before they could be written, so this page stopped citing it by line. See `schema.md` § *Citing source files*.** *(This corrects an earlier index line citing `:175`/`:207`, now withdrawn, and a 2026-09-04 one reading "THE HOST IT BROUGHT UP NO LONGER STANDS".)* 🚨 **Carries a TRAP: its "leave `PROFILE_INTERNAL_TOKEN` blank, the box auto-generates it" step is now FALSE** and yields a **401 on every credit call**, with the XP **lost, not queued** — ⚠️ **but NOT silent: the 401 is WARN-logged twice** — `src/server/ProfileApiClient.ts`, the `not retrying` warn inside the `status < 500 && status !== 429` guard, plus the caller's `award(s) dropped` warn on the `response === null` branch *(cited by content, not by line: this entry previously carried `:265-267` and a **bare `:147-149`** — the exact form convention 10 bans, on the entry describing that very defect)*; a stale `PROFILE_INTERNAL_ALLOW_IPS` is a second, independent **403** on the same path — 🔧 **and that 403 is NOT "swallowed quietly" either (corrected 2026-09-10): it is also a non-5xx non-429 4xx, so it takes the SAME two-WARN path.** ⛔ **Neither correction shrinks the trap** — awards are still **dropped, never queued**, and **nothing on that box reads the logs** (`0219`, open). 🔧 **ATTRIBUTION CORRECTED 2026-09-10: the withdrawn `:175`/`:207` citations were NOT a vault error** — both were **correct at their declared frame `589249c`** (`:175` the struck sentence, `:207` the `.env.profile.secret` copy) and were displaced only by the producer's later **uncommitted** sweep edits (post-sweep: `:185` / `:241`). ⚠️ **`conventions/file-line-citations.md` still records this as the wiki reproducing the defect and CONTRADICTS the `0239` brief row; the file supports `0239` — flagged for a human, the vault cannot edit it (ADR-005)**
- [[tasks/profile-deploy-hardening]] — Sprint 4 T4g argv-safety, deploy locking, atomic record, and wrong-host preflight hardening
- [[tasks/profile-game-server-deploy-env]] — Sprint 4 T4h propagation of `PROFILE_API_URL` through the game-server deploy environment
- [[tasks/profile-backend-db-api]] — Sprint 4 T5 Postgres-backed profile API, repository, migrations, readiness, and idempotent XP crediting
- [[tasks/profile-match-end-crediting]] — Sprint 4 T6 game-server match-end XP crediting into the profile API. 🔴 **2026-09-04: the reason it no-ops may be bigger than `0062` — the profile host EXISTS, but what it serves is UNVERIFIED** (owner-ruled; ⚠️ *a same-day "there is no profile host to credit" annotation overstated the position and is withdrawn*). ⚠️ **Inference, not verified fact: this path has almost certainly NEVER worked in production** — unmeasured, do not upgrade it 🔄 **2026-09-23: `0062`'s D3 is now `0296` A3 — still the acceptance criterion, still never run.**
- [[tasks/postgres-backup-routine]] — Sprint 4 T8 encrypted daily off-box profile DB backups, restore runbook, and deploy-time smoke validation. 🔴 **2026-09-04: WHETHER ANY BACKUP IS RUNNING IS UNKNOWN** (owner-ruled; ⚠️ *a same-day "NO BACKUPS ARE RUNNING — there is no box" annotation overstated the position and is withdrawn — the box and bucket exist and are reused in place*); the machinery, restore path included, exists and fails closed at deploy. 🚨 **Three gaps were owned by `0218`; ✅ the first is DISCHARGED 2026-09-11:** ~~restore never exercised against non-empty data (and the old drill's command line no longer works)~~ — **a backup RESTORES, proven twice**, and the "command line no longer works" half was **REFUTED BY EXECUTION**; ⛔ **the SCHEDULE and the DATA remain proven SEPARATELY, NEVER TOGETHER**; still open: **nothing reads the backup-freshness marker and there is no monitoring of any kind**, and 🔴 **the `age` private key has no recorded home** — the owner did not know what it was when asked, which the rebuild must design out **before the first backup runs**. ~~🔴 **The `age`-key question is RE-OPENED as a live owner decision:** the reused bucket still holds old encrypted objects, permanently unreadable without the old private identity — **purge, or keep pending a search?** — tracked in **`0222`** (renamed `0222-profile-cleanup-obsolete-secrets-and-old-bucket-objects`, rescoped from decommissioning to cleanup).~~ ✅ **CLOSED 2026-09-10 — struck, not deleted:** the old bucket was abandoned and the owner had **already deleted it**, and 🚨 **the premise was RETRACTED — the bucket was EMPTY and always had been, so those objects NEVER EXISTED** (⛔ objects only; the lost old `age` private identity is unchanged). 🔒 **And the old access key's revocation is CLOSED BY OWNER DECISION 2026-09-11 — deliberately NOT done, NOT outstanding work; ⛔ it was NOT satisfied, the key was never revoked and its scope was never established.** See [[tasks/profile-cleanup-obsolete-secrets]]. 🚨 **ADDED 2026-09-10 — THIS PAGE'S 0-ROWS DRILL CLAIM CAME ONE STEP FROM BEING DELETED AS FABRICATED, AND IT WAS TRUE THE WHOLE TIME.** `0182` supported it with a **bare `:147-153`** written inside `0182` itself; **three readers (producer, lead, owner)** resolved it to `0182`'s own brief, where that range is an unrelated section header; the claim was flagged **UNSOURCED** and **the owner RULED A RETRACTION.** ✅ **The producer searched outside the cited file BEFORE executing, found the real source — `ai-agents/knowledge-base/profile-backup-restore-runbook.md`, the `Recorded RTO` paragraph, unchanged in every commit checked — refused, and escalated; the owner then WITHDREW the ruling. Nothing was deleted.** ⛔ **The gate NEVER weakened** — it now rests on **two** properly-cited reasons instead of one half-cited one, and ~~🔴 **the restore path is still UNPROVEN with `0218` OPEN**~~ ✅ **`0218` CLOSED 2026-09-11 — see [[tasks/profile-durability-restore-drill]]; the gate was DISCHARGED, not weakened**. ⚠️ **Two different 0-rows readings must NOT be conflated** — the 2026-07-01 drill's empty DB, and `0215`'s separate 2026-09-08/09 re-read at execution time; **neither is a restore**
- [[tasks/personal-data-compliance-investigation]] — Sprint 4 152-ФЗ investigation whose hash-based conclusion was later overturned
- [[tasks/yandex-payments-investigation]] — Sprint 4 investigation recommending signed Yandex purchase verification and a session-cached catalog in `FlashistFacade`
- [[tasks/start-screen-redesign-investigation]] — Sprint 4 design investigation that locked the two-tab start screen and citizenship card placement
- [[tasks/start-screen-redesign-implementation]] — Sprint 4 implementation of the two-tab start screen, citizenship card shell, tab persistence, localization rename, and tab analytics
- [[tasks/citizenship-xp-progress-ui]] — Sprint 4 live citizenship card wiring: profile API read, XP progress, citizen state, and degraded zero-state handling
- [[tasks/citizenship-card-guest-cta-no-sdk]] — Sprint 4 citizenship-card fix hiding the Yandex login CTA outside a Yandex SDK context
- [[tasks/app-bootstrap-single-entry-point]] — Sprint 4 explicit client bootstrap refactor with bounded Yandex platform init, degraded mode, language-before-render, and app-chunk recovery
- [[tasks/legal-vat-investigation]] — External VAT/legal check that cleared the pre-launch payments gate
- [[tasks/global-announcements]] — Re-enabled repo-authored announcements bell and popup with unread badge
- [[tasks/feedback-modal-space-key]] — Sprint 4 fix allowing spaces in the in-match feedback modal and suppressing gameplay hotkeys while typing
- [[tasks/email-subscribe-modal]] — Start-screen and win-screen email opt-in modal backed by Telegram delivery
- [[tasks/map-preload]] — HF-13 background terrain preload on JOIN with preload-hit analytics
- [[tasks/ai-lobby-slot-bug]] — Sprint 4 fix preserving one human slot in mixed AI/public lobbies
- [[tasks/tutorial-no-nations]] — Tutorial config change removing nation-controlled opponents
- [[tasks/tutorial-build-menu-lock]] — Tooltip-5 guardrail that restricts building to City
- [[tasks/tutorial-reduce-bots]] — Tutorial config change lowering tutorial bot count from 400 to 100
- [[tasks/tutorial-abandonment-platform-segmentation]] — Task `0212`, **`🔲 Backlog`, unscheduled, NOT STARTED** — the owner authorised **filing** the investigation 2026-09-04, which is not a scheduling decision, and 🔴 **the `Medium–High` rank is the PRODUCER'S, not an owner ruling.** Segment the tutorial's **9.8 % completion / ~87 % abandonment** by platform/browser/device **before proposing any fix** — the question is **segmentation, not the aggregate rate**. 🚨 **The headline is very likely OVERSTATED for a STRUCTURAL reason:** the `localStorage` gate is set on completion and skip but **never on abandonment**, so abandoners re-enter every session while completers contribute one start — **the denominator counts attempts, the numerator is capped at one per browser profile.** ✅ The deflator already exists in the data (`Tutorial:Started` carries the lifetime attempt count). ⚠️ **Phase 1 is dashboard measurement and is NOT runnable from the repo**; ⚠️ **its "no code needed" premise is NOT established** — the custom dimensions are set but **never declared**, and the SDK may reject them (unverified). Also: `Tutorial:Completed` **fuses two different meanings**, the `TooltipShown` roll-up that would localise the drop-off to tooltip 1–2 is **UNVERIFIED**, the event reference's experiment-gating claim looks **stale** and **decides the denominator**, `mobile` is a **coarse input-modality proxy** (touch laptops count), `Tutorial:Started` fires **before** the match so 9.8 % is a **LOWER bound**, and the dashboard showed a persistent **"Demo mode"** banner — figures are **owner-confirmed, NOT unconditionally verified**. ⛔ **A mobile fix may be OUT OF SCOPE** — deep mobile work is parked until mobile DAU > 1,500
- [[tasks/missions-difficulty-investigation]] — Sprint 4 investigation finding missions are generated, not authored; follow-ups exclude zero-nation maps, sort by prebuilt nation counts, and slow the Medium nation ramp
- [[tasks/solo-win-condition-fix]] — Solo-mode fix showing a distinct loss when an opponent reaches the win threshold; ✅ **CLEARED 2026-09-02** of `0022`'s suspected regression — PR #77's net effect on that path is zero
- [[tasks/win-check-clientless-leader-guard]] — Sprint 4 task `0022`, closed 2026-09-02 (agent-closed — not owner-verified). 🔧 **Its founding premise was REFUTED** — not a PR #77 regression; the `undefined` clientless winner is **original to the fork** (`feea527`). It survived because a **worse defect** sat underneath: a clientless leader winning FFA emits no `winner` message, so **`creditMatchXp` never runs and the match's entire XP is silently lost for every player** — and the predicate `clientID() === null` catches **Nations, not just bots**. Shipped **risks 1 and 3 only**: the guard at `WinCheckExecution.ts:69-77`, placed **before** `active = false` so a human can still win later, plus a `win_modal.bot_team` label fix. 🔴 **The XP loss is STILL LIVE AND FIXED NOWHERE** — ~~`0206` is the fix~~ **`0206` was REVERTED 2026-09-04, never deployed, and was a no-op in the case that loses the XP.** The fix is now `0211`. 🔴 **`checkWinnerTeam()` shares the guard shape, so Team mode loses its XP identically** — found at the revert. ⚠️ **Risk 1 has NO live reproduction** (owner-declined): synthetic tests only. Risk 2 split to `0205`
- [[tasks/teams-bot-team-win-stall]] — Task `0205`, the Teams half of the same defect class (risk 2, split out of `0022`): the Bot team leads, the guard returns above `active = false`, and the match is **unwinnable while it leads**. `🔲 Backlog`, unscheduled, **nobody building it** — owner-reviewed and deliberately left there. ✅ **PREMISE CONFIRMED 2026-09-03 by headless deterministic simulation** (real World map, 651,609 land tiles, 400 bots, `DefaultConfig`): bot team on top **12/12**, `setWinner` **0/12**, win check still `active` **12/12**; the guard is proven **CAUSAL** (same board, `gameType: Singleplayer` → `setWinner("Bot")`, 3/3); the 95 % route is crossed at **ticks 6180–9480 ≈ 7–10 min** on the **shipped public config**, so *"rare"* was **rare because humans play, not because 95 % is hard**; the defect is **passivity-dependent** (0–20 % active → stalls 3/3 each, 60–100 % → 0/3); `HumansVsNations` is the one public Team config exempt, and **only incidentally**. ~~⚠️ **Production frequency is UNMEASURED — a simulator result, not a field observation**~~ ✅ **UPDATED 2026-09-11 — there IS now a field figure. The defensible sentence, VERBATIM:** *"in 52 % of measured Team-mode client-matches that reached the win condition, the leader at that moment was the all-bot team, and no winner could be declared at that moment."* (52.4 % = 1,950 / 3,719, 4–10 Sep 2026.) ⛔ ***"52 % of Team matches stalled" is AN UNSUPPORTED CLAIM*** — client-match unit, a **LOWER BOUND**, latched at the **first crossing**; 🔴 **the per-match stall rate will NEVER be known** (owner ruling, option B) and ⛔ **that is not a gap for anyone to close.** ~~⚠️ **Nobody re-ranked this task on it and no ruling was sought — status, scope and rank unchanged.**~~ 🔴 **FALSE SINCE 2026-09-11 — struck, not deleted: THE OWNER RULED THAT `0205` BE RE-RANKED**, because its rank was held down by the now-disproved *"frequency is unmeasured"* premise. ~~🟡 **The producer PROPOSES `Medium`, awaiting the owner's sign-off; ⛔ the rank IN FORCE is still `Low–Medium`.**~~ ✅ **SIGNED OFF THE SAME DAY — struck, not deleted: the rank IN FORCE is `Medium`.** ⛔ **THREE AUTHORSHIP LAYERS, NEVER FLATTENED:** the owner ruled **THAT** it be re-ranked · the producer proposed the **VALUE `Medium`** · the owner **SIGNED `Medium` OFF** — ⛔ never *"the owner ranked it `Medium`"*, never *"the producer set it to `Medium`"*; neither is true alone. ⚠️ **Sign-off weakens no caveat above** — client-match unit, lower bound, latched at first crossing, per-match rate permanently unknowable. ⛔ **Status, scope, folder and board ARE unchanged — RANK ONLY.** And the "active" players were `FakeHumanExecution` at Medium, better than a casual human, so **the real crossover is probably higher than 40 %**. Rank **Low–Medium weighed and HELD** against that evidence on an owner ruling **of 2026-09-03** — 📌 **and RE-OPENED by the owner on 2026-09-11, see above; `Low–Medium` is still what is in force, but it is no longer "held".** 🔴 **ADDED 2026-09-04: `checkWinnerTeam()` does not merely STALL — it LOSES THE MATCH'S XP, exactly as FFA does**, found at `0206`'s revert and unnoticed across `0022`/`0206`/`0205` (⚠️ reported, **not re-verified by symbol**). The owner ruled `0211` covers **Team mode as well as FFA**; ⛔ **that does NOT merge, replace or gate this task, whose status and scope are UNCHANGED** — `0205` answers *who should win a stalled Team match?*, `0211` answers *do those players get their XP?*. ⚠️ **`0211` may make PART of this task's justification moot, but does not settle its question** — and 🔴 **`0211` is now RULED TO BUILD (2026-09-11)**, so once it ships only the **UX harm** is left carrying this task's case. Policy/measure/branch-scope decided; **two questions stay open for plan time** — tie-breaking, and the deliberately deferred **all-Nations team**. 🔧 Corrects the brief's mechanism claim: Nations get their team from `assignTeams()`, **not** `maybeAssignTeam()`
- [[tasks/winmodal-participation-comment-correction]] — Task `0207`, filed 2026-09-03 to the Backlog board, unscheduled: `WinModal.buildPlayerParticipation`'s doc comment says AI players are skipped from participation; the skip is on `clientID === null`, which excludes **Bots and Nations** and **includes** AI players. **Comment-only, `src/client/` only, zero user impact today** — the server's frozen-start-roster gate makes the entries inert — **but a live trap** for whoever plans `0206`, because it contradicts ADR-110 on the exact predicate that ADR just ruled on. 📌 **`0206` was planned and BUILT the same day, before this was fixed, and did NOT trip on it.** 🔴 **`0206` was then REVERTED 2026-09-04 — which changes nothing here: this task never claimed `0206` had shipped, and its reason to exist is UNCHANGED and if anything STRONGER.** 🚩 **The trap now aims at `0211` and `0205`**, and the architect re-confirmed the defect independently on 2026-09-04. ⚠️ "Harmless today" rests entirely on that gate and is **not permanent**

- [[tasks/ffa-clientless-leader-fallback-award]] — Sprint 4 task `0206`. 🔴 **REVERTED 2026-09-04 ON AN OWNER RULING — ITS BEHAVIOUR IS NOT IN THE GAME AND WAS NEVER DEPLOYED.** ⚠️ **Its status is still `done`, correctly and deliberately: the WORK was done, the EFFECT was reverted — two different facts.** Promoted, planned, built, reviewed and closed all on 2026-09-03; **Codex "No findings."**, three low Claude findings dispositioned, **play-test gate PASSED** — ✅ **none of that is retracted.** ⛔ **`0206` was NOT buggy and did NOT cause the stall.** 🔴 **The plan's PREMISE was disproved by live measurement:** `players()` filters to `isAlive()` (`GameImpl.ts:421-423`), so with every human eliminated the award found nobody and took **the same early `return` as before `0206`** — **a Nation reached 100.0 % of the map and the match still did not end** (⚠️ observed in a **PRIVATE** instrumented FFA; 🔴 **the NATION case has been assumed twice and OBSERVED ZERO TIMES in a PUBLIC lobby — only a Bot was ever seen crossing the threshold — so treat it as a HYPOTHESIS there, never as established**). Its **only** live effect was crowning a survivor on ~0.5 % against a bot on 80.2 %, **which the owner reproduced and rejected.** ~~Public FFA now ends at 80 %~~ **struck — never reached a player.** ⚠️ **`WinCheckExecution.ts` is deliberately NOT byte-identical to its pre-`0206` state** — one ADR-110 comment was kept, the **only in-code trace of that ADR.** Spawned `0207`–`0210`, and at its revert `0211`
- [[tasks/measure-clientless-leader-and-solo-awards]] — Task `0208`, **instrumentation only**.
  ✅ **CLOSED 2026-09-11 `(agent-closed — not owner-verified)` on an owner ruling — BOTH DELIVERABLE NUMBERS READ** (4–10 Sep 2026, full days, one session): **Part A 6.86K client-matches** — FFA clientless **~1.6 %** · Team **~53.2 %** (of which **~52.4 %** is the stall-capable all-bot team) · overall **~29.1 %** · `AiPlayer` **89** · `Threshold` **100 %** and **the timer branch fired ZERO times** · **zero private-lobby events**; **Part B 79.11K award ATTEMPTS** (`Solo` 65.45K · `SoloTutorial` 13.66K; `Participation` 62.29K · `PlacementWon` 14.28K · `PlacementLost` 2.53K), cross-tab ⇒ **`0210`'s non-tutorial MATCH count 49.64K ≈ 7.1K/day**. 🚨 **THE CAVEATS ARE NOT OPTIONAL AND TRAVEL WITH EVERY FIGURE:** 🔴 **the unit is CLIENT-MATCHES and ATTEMPTS — never matches, never points banked; absolute counts are uninterpretable, only ratios, and all rates are LOWER BOUNDS** · ⛔ ***"52 % of Team matches stalled" is NOT a supported claim*** — a firing records who was **first past the post**, not how the match ended; **the defensible sentence travels VERBATIM**: *"in 52 % of measured Team-mode client-matches that reached the win condition, the leader at that moment was the all-bot team, and no winner could be declared at that moment"* · 🔴 **the per-match stall rate WILL NEVER BE KNOWN** (owner ruling, option B, 2026-09-11: accept the directional lower bound, **build no server-side counter**) and **the pre-fix denominator disappears permanently the moment `0211` ships — a deliberate, accepted, irreversible loss** · ⚠️ **`Timer: 0` is EXPECTED** (public lobbies hardcode `maxTimerValue` undefined) and ⛔ **must NOT be read as "matches never run out of time"** — **three termination paths emit nothing and are genuinely unmeasured** (the 3-hour `maxGameDuration` kill, the all-clients-left end of a stalled match, and a match where no leader survives to cross). ⛔ **NOT A CLEAN CLOSE: `V16` and `V17` close UNTESTED**, argued from source only — **the owner was offered the test and CHOSE TO CLOSE** — so ⛔ **`0208` is closed but NOT fully verified and no page may imply otherwise**; ✅ **`V18` DID pass, by observation** (mid-match reload → straight to the main menu). 📌 **Also durable:** the non-tutorial cross-tab **proved a forbidden shortcut wrong** — the blended 82.7 % `Solo` share applied to 62.29K gives **~51.5K against an actual 49.64K**, because the share runs **79.7 % / 93 % / 100 %** across the three award kinds ⇒ ⛔ **never multiply a blended marginal share into a sub-population unless it is known uniform**; and **`PlacementLost` was identical in both columns, CONFIRMING IN PRODUCTION DATA** the code-derived prediction that `…:PlacementLost:SoloTutorial` cannot currently fire. 📌 **Consequences:** `0211`'s **ship gate is CLEARED** (~~⚠️ its status, `🔲 Backlog`, is unchanged — nobody on it~~ 🔴 **and the owner then ruled it BE BUILT: now `🔄 In progress`, ⚠️ still nobody on it**) · `0210` has its incidence figure but its **status, scope and rank are unchanged** · **ADR-110's re-raise trigger has fired only in the sense that the measurement EXISTS** — ⛔ **an INPUT REQUIRING AN ARCHITECT'S READ, NOT a conclusion about ADR-110.**
  📛 **Superseded — kept:** ~~🚧 **Blocked — BUILT, COMMITTED (`6b30e22`) AND DEPLOYED in build `0.0.141`; the DELIVERABLE NUMBER is still not read.**~~ *(2026-09-07, true when written)* ✅ **It is EMITTING in production** — 177 `Match:WinCondition` events over ~2 h on 2026-09-05. 🚨 **This task is one of the FEW Sprint 4 items that IS deployed — do not sweep it into the sprint-wide "nothing is deployed".** ~~⛔ **BUILT AND DEPLOYED IS NOT DONE:** the deliverable is **the clientless-leader share**, and there is none; ⚠️ **the midday sample proves the instrumentation and NOTHING about incidence.** 🔴 **`0211`'s ship gate is therefore STILL NOT CLEAR**~~ 📌 **SPENT 2026-09-11 — struck, not deleted; TRUE WHEN WRITTEN. The full-day read was taken and the gate is CLEARED.** `0211` may be planned and built in parallel, and ⚠️ **neither task is `🚧 Blocked` on the other**. 🔴 **Load-bearing for THREE decisions:** ADR-110's re-raise trigger (~~still unfired~~ **the measurement now exists — an input, not a verdict**), `0211`'s survivor scope, `0205`'s rank. ✅ **Its Part A decay clock was STOPPED long enough for the read to be taken** — `0206` never deployed; **`0211` shipping is what destroys the pre-fix denominator, permanently.** 📌 **TWO HALVES THAT MUST NOT BE MERGED** — **Part A** multiplayer clientless-leader incidence (`src/core/`; ⛔ **excludes AI players**), **Part B** Singleplayer platform-leaderboard award incidence (`src/client/`, via **both** `reportPlacements()` **and** the unguarded, farmable `reportParticipation()`). 🔴 **Part B counts `points`, never `placement`**, and 🟢 **has NO over-count problem** — ⛔ do not copy Part A's caveat onto it. ⚠️ **The folder name deliberately under-describes the task** — renaming would break inbound links. 🔴 **It does NOT gate `0210`**; Part B is **a snapshot with an expiry date** — once `0210` ships the counter reads zero forever by design.
  📛 **Superseded positions — KEPT, in the order they were believed:** **(1)** *"File a brief, don't schedule"* (2026-09-03) — ⛔ **not wrong, SPENT**; reversed 2026-09-04 when the task was scheduled into Sprint 4 and re-ranked `Medium` → `High`. **(2)** *"NOT DEPLOYED, NO DATA, UNCOMMITTED"* — **FALSE**, corrected 2026-09-05. **(3)** *"Nothing built yet"* with `Status: backlog` — **stale**, corrected 2026-09-07. **(4)** *"the owner ranked it High"* — 🔴 **provenance SPLIT: the instruction to RAISE it is an OWNER RULING; the value `High` is the PRODUCER'S judgement.** ⛔ Do not restate it as the owner's. **(5)** *"this task's `WinCheckExecution` latch caused the 2026-09-06 GameAnalytics spike"* — **the first hypothesis, REFUTED by the per-category breakdown; `0208` is exonerated.**
- [[tasks/credit-participation-xp-elimination-or-match-end]] — Task `0211`, **`0206`'s replacement**, filed by the revert on 2026-09-04, scheduled into Sprint 4 the same day, and ✅ **SHIPPED AND CLOSED 2026-09-12 `(agent-closed — not owner-verified)`** by the sprint ship-loop. **What shipped:** threshold `1000` → `100` and award `10` → `1` (**both language files rescaled in the same change**), **Mechanism A** via a composite `winnerDeclarable`, **FFA and Team** in scope, **Singleplayer suppressed**, and the **leaver rule narrowed** so a player eliminated who then closes the tab **is paid at the moment of death**. 🚨 **CLOSES CARRYING AN OUTSTANDING OWNER-SIDE ACTION: there is NO end-to-end proof that crediting works** — `getCreditableYandexId()` returns `null` in every local run and the `WinConditionCheck → ClientGameRunner → Transport → server` seam **has no harness in this repo**, so all crediting evidence is unit/server-handler level with an **injected** Yandex id. **Building that harness first was OFFERED and DECLINED**; the owner chose a **manual live check after deploy.** ⛔ **Do NOT record this as verified, and ⛔ shipped is NOT deployed.** **Three accepted residuals, all owner-declined or owner-accepted:** **R4** (no latch on zero-credit reports, no WS rate limit — ⚠️ **and the R3 fix slightly WIDENS that amplification: a retained uncredited claim now re-posts on RECONNECT too**) · **R5** (`spawnedClients` records spawn *intents*, not successful spawns) · **R7** (a `maxGameDuration`-capped match whose win condition never fired credits nobody — **verified NON-REGRESSIVE**; 🔴 **`0211` fixes the XP LOSS, not the STALL** — that is `0242`). ✅ **Closed by evidence, not residuals:** **R6** zero-impact **by measurement** (live DB at close: `player_profiles` **0 rows**, `max(xp)` **0** ⇒ ADR-111 ruling 8's free grants **cost nothing** — ⚠️ a **point-in-time** reading, true only because the rescale shipped **before** `0217` wires crediting; ⛔ **still no migration, ESCALATE instead**) · **R2** (`migrations/001` `xp_awarded default 10`) a correct observation **ruled out of scope**, **not ruled blind**. 🔴 **THE IRREVERSIBLE COST IS NOW SPENT: shipping this destroyed `0208`'s pre-fix denominator — the per-match stall rate will NEVER be known.** ⛔ **Do not write it up as a gap.** **Verification run first-hand by the lead:** `npm test` **116 suites / 1232 tests, 0 failing**; `npx tsc --noEmit` clean; coder also ran `npm run test:integration` against real Postgres, **5 suites / 70 tests, 0 failing**. **Run NOT degraded** — Codex ran (`codex-cli 0.152.0`, exit 0, 4 findings) alongside the reviewer's own pass; converged in one round. 📛 **Superseded, kept:** ~~`🔲 Backlog` — scheduled is not started~~ · ~~`🔄 In progress` (2026-09-11 owner ruling to BUILD; no session owned it, no plan approved, no code written)~~ · ~~reset to `🔲 Backlog` 2026-09-12 so the ship-loop's selection was LAWFUL, then `🔄 In progress` again by the loop's own step 2~~ — **each true when written; none deleted**
- [[tasks/placement-semantics-literal-one]] — Task `0209`, Backlog board, unscheduled, **`Low` — the producer's rank**. `const placement = +1;` is a unary plus on the literal `1`, so **every top-3 human is labelled placement 1**; `points` is correct and untouched. 🔴 **The framing it was filed with was OVERSTATED — the value is NOT sent to the Yandex platform**: `reportPlacement` passes only `params.points`, and `placement` reaches nothing but a `console.debug` under a TODO. **Today's harm is a wrong number in a browser debug log.** 📌 **Definition RULED 2026-09-03 — A, rank among humans** (B and C weighed and rejected); ⛔ **but the ruling settled the DEFINITION, not the EXPRESSION — `myIndex + 1` is still NOT prescribed**, because ties and eliminated/disconnected humans must be checked, not assumed. ⚠️ "Harmless today" is **not permanent** — acting on the TODO makes it a real platform write. 🔴 **UPDATED 2026-09-04: the frequency claim is STRUCK** — the extra class of matches `0206` would have made `reportPlacements()` fire in **does not exist**, because `0206` was reverted and never deployed. ✅ **The DEFECT is unchanged, pre-existing and still live**; only the blast radius reverts to the ordinary win path
- [[tasks/singleplayer-leaderboard-reporting-policy]] — Task `0210`, Backlog board, unscheduled, **`Medium` — the producer's rank, above `0209`** because **this one's wrong value DOES leave the client**. 🚨 **Live and farmable: in non-tutorial Singleplayer a human who LOSES to a bot is awarded 10 first-place platform points**, and every match *started* awards 1 more via the entirely unguarded `reportParticipation()`. **Pre-existing — NOT a `0206` regression**; `0206` correctly guards only its own new branch, while the older fall-through hands over the same points. 📌 **RULED 2026-09-03 — A: Singleplayer reports nothing** (B **doubly moot**, C rejected on farmability regardless of incidence). 🔴 **The guard must be ADDED, not repaired** — grep confirms **zero** game-type awareness anywhere in the client leaderboard path. ⚠️ **Nobody has ever run it live** — reproduce before fixing. 🔴 `0208` does **not** gate it. ✅ **ITS INCIDENCE FIGURE NOW EXISTS (2026-09-11, `0208` Part B): 49.64K non-tutorial `Solo` `Participation` award ATTEMPTS over 4–10 Sep 2026 ≈ 7.1K/day — the non-tutorial MATCH count its scope wanted** (79.11K attempts across both leaves). ⛔ **ATTEMPTS, platform failures included — never points confirmed banked.** ⛔ **Its status, scope and rank are UNCHANGED by that** — the ruling was never conditioned on incidence. 🔴 **UPDATED 2026-09-04: `0206` was REVERTED, so its Singleplayer early-return and every line number in that file are GONE — locate by symbol, not line.** ✅ **This task's defect is COMPLETELY UNAFFECTED and the claim *"touched by neither `0022` nor `0206`"* is now true in a stronger sense.** ⚠️ **Its ruling is about platform LEADERBOARD POINTS — ⛔ must NOT be read across onto `0211`'s profile XP**
- [[tasks/telegram-link]] — Experiment-gated Telegram channel CTA on start/loading and game-end screens
- [[tasks/vk-link]] — Experiment-gated VK community CTA on start/loading and game-end screens
- [[tasks/nuke-trajectory-visibility]] — Sprint 4 visual polish making the nuke pre-launch targeting arc thicker
- [[tasks/teams-mode-max-teams]] — Sprint 4 server-side cap limiting regular public teams lobbies to 2, 3, or 4 teams
- [[tasks/sprint4b-mini-mode-investigation]] — Sprint 4b investigation finding compact nation-spawn risks and confirming Duos/Trios/Quads AI-fill compatibility
- [[tasks/sprint4b-duos-trios-quads]] — Sprint 4b public-only re-enable of Duos/Trios/Quads team-size modes with AI-fill coverage
- [[tasks/sprint4b-compact-map-rotation]] — Sprint 4b public compact-map modifier plus Mini/Мини lobby badge
- [[tasks/sprint4b-weird-setting-modifier]] — Sprint 4b weird-setting public modifiers and lobby badges
- [[tasks/monetization-analytics-spec]] — Sprint 4 analytics baseline for identity, match lifecycle, citizenship funnel, and ad-tier measurement
- [[tasks/analytics-p0-game-mode-segmentation]] — P0 analytics event pair segmenting fresh match starts into multiplayer versus solo
- [[tasks/analytics-p0-spawn-confirmation]] — P0 analytics event measuring server-confirmed spawn and time-to-spawn
- [[tasks/analytics-p0-match-duration]] — P0 analytics event measuring seconds from fresh match start to match end
- [[tasks/analytics-p0-player-days-played]] — P0 analytics event measuring cumulative unique local calendar days opened
- [[tasks/analytics-p0-yandex-login-status]] — P0 analytics event measuring Yandex logged-in, guest, and unknown session states
- [[tasks/analytics-p0-session-match-count]] — P0 analytics event measuring match starts per session via UUID-keyed localStorage, consumed before Session:Start
- [[tasks/cosmetics-serving]] — Sprint 4c fix restoring `/cosmetics.json` serving and deduplicating cosmetics fetch telemetry noise
- [[tasks/local-server-hash-guard]] — Sprint 4c guard preventing missing-turn hash messages from crashing local/singleplayer matches
- [[tasks/archive-endpoint-failures]] — Sprint 4c cleanup disabling broken archive **writes** through `archiveEnabled()`; S3-backed citizen archival deferred. 🔴 **WRITES ONLY — corrected 2026-09-22:** the switch has exactly two consumers, both writes; the client archive **read** in `JoinPrivateLobbyModal.ts` was never gated and **still fires in production**. ⚠️ **Benign** (404 handled as `"not_found"`, no user impact), fires **only for lobby IDs that are not currently active**, and ✅ **the destination is OUR OWN infrastructure, measured on the live prod deployment** — classification only, no host recorded. Tracked as `0292`
- [[tasks/leaderboard-player-count]] — Sprint 4c quick win showing the human-like player count in the leaderboard's Players only label
- [[tasks/disable-compact-public-maps]] — Sprint 4c mitigation removing compact maps from public rotation while keeping opt-in compact paths
- [[tasks/s4c-enable-client-source-maps]] — Sprint 4c source-map upload pipeline for Uptrace client stack symbolication
- [[tasks/s4c-investigate-lobby-map-fetch]] — Sprint 4c fix for lobby polling noise and map manifest fetch failures
- [[tasks/incident-response-index]] — Security incident coordination page for the VPS credential leak response
- [[tasks/immediate-containment]] — Secret rotation, deploy freeze, and containment workflow for the leak response
- [[tasks/registry-image-audit]] — Historical image-trust audit plan; outcome was conservative pre-hardening image quarantine
- [[tasks/vps-access-hardening]] — Host-access review and SSH hardening checklist after leaked VPS credentials
- [[tasks/repo-build-context-hardening]] — `.dockerignore` plus allowlist Docker copies to block env-file leakage
- [[tasks/deployment-credential-hardening]] — SSH-key-first deploy contract and password-fallback demotion
- [[tasks/clean-redeploy-validation]] — Trusted post-hardening rebuild, redeploy, and validation workflow
- [[tasks/incident-postmortem-followups]] — Final postmortem capture and explicit security follow-up queue
- [[tasks/docker-secret-boundary-check]] — Automated guard against `.env*` or `COPY . .` regressions in Docker builds
- [[tasks/registry-image-policy-followup]] — Task that authored the registry visibility and image-retention policy
- [[tasks/investigate-clans-system]] — Investigation confirming clans parse/team-assign works; silent kick bug and no-UI gap documented
- [[tasks/compact-map-click-interaction]] — Investigation confirming compact boat-button failures come from lost `isShore` bits in `map4x.bin`, not click coordinates
- [[tasks/yandex-payments-implementation]] — Sprint 4 task 0019 payments infrastructure: catalog cache, profile-server HMAC verification endpoints, reconciliation; agent-closed, live verification deferred. 🔴 **The routes are NOT 503ing on the box** (corrected 2026-09-19: a non-empty secret of length 32 is present, the prefix answers 404 on a missing sub-path, `payments endpoints disabled` logged 0 times) and ✅ **the PROVENANCE is ANSWERED 2026-09-20 — OWNER-ATTESTED, not repo-verified: the owner set the real Yandex per-game key themselves.** ⛔ **That settles presence and origin and NOTHING MORE — correctness is unproven, there is no signed payload, and NO real purchase has ever been exercised; a page saying "payments work" would be as wrong as one saying they 503.** ⛔ The fail-closed code is unchanged
- [[tasks/map-troops-labels]] — Sprint 4 task 0041 map label enhancement: troops `current / max` line plus red attacking-troops line in `NameLayer`; agent-closed, singleplayer-only live validation
- [[tasks/starting-gold-public-modifier]] — Sprint 4 task 0042 fifth weird sub-option granting real players 5M starting gold via the new `startGold` GameConfig field; agent-closed, live check owner-side
- [[tasks/feedback-remove-contact-field]] — Sprint 4 task 0046 152-ФЗ data minimization removing the feedback contact field end-to-end; agent-closed, post-deploy checks owner-side
- [[tasks/degraded-mode-ux-treatment]] — Sprint 4 task 0049 `isYandexDegraded()` citizenship-card connection-problem state clearing the earned/paid citizenship gate; agent-closed, healthy-SDK guest case unit-test-only
- [[tasks/hide-citizenship-card-flag]] — Sprint 4 task 0054 default-OFF `CITIZENSHIP_CARD_ENABLED` client flag hiding the citizenship card until 0017/0018 flip it ON at launch; agent-closed 🔄 **2026-09-23: the flip is now owned ONLY by `0065` §6** (removed from `0017`).
- [[tasks/master-lobbies-worker-exit-diagnostics]] — Sprint 4 task 0055 outage-track fix: parseable empty lobbies body plus worker-exit `code`/`signal` logging in `Master.ts`, first-ever tests for that file; agent-closed, unpushed branch at close
- [[tasks/licensing-asset-audit]] — Sprint 4 task 0025, the production asset audit for proprietary/CDN assets: **1 confirmed violation (V1 music), 1 trademark item (A1 favicon), 3 hygiene items (H1–H3)**; everything else clean. **Closed 2026-08-31 (agent-closed — not owner-verified)** on an owner-ruled close, and it was the **last `🔄 In progress` row on the Sprint 4 board**. V1/A1 verified in production 2026-08-30, **H1 2026-08-31** (three live bundles grepped, 0 hits). 🚨 Carries the verification **method**: all seven purged paths returning `200` **is the pass** — byte-identity against a known-nonexistent control, never a 404. 📌 **H3 still open and OWNED by task `0073`.** ⚠️ Satisfies `0065`'s licensing prerequisite **and nothing else — `0014`, `0062`, `0195` remain; paid go-live NOT unblocked**
- [[tasks/licensing-remediation]] — Sprint 4 task 0066 licensing remediation from the 0025 audit: proprietary-music purge, original placeholder favicon, JWT fallback retarget; agent-closed 2026-08-24, **DEPLOYED in `362a2f9`**. ✅ **Live checks RAN AND PASSED 2026-08-30, so the licensing gate is DEMONSTRATED** — proven by byte-identity against a known-nonexistent control, **not** by a 404 (this server's `app.get("*")` never 404s, so seven `200`s are the pass). ⚠️ Clears one of 0065's four blockers; `0014`, `0062`, `0195` remain
- [[tasks/worker-routing-dead-worker-investigation]] — Sprint 4 task 0057 architect-led routing investigation: dead-index vs wedged-index severity, the placement contract behind ADR-109, and the confirmation (not revision) of the 18/20 quorum; agent-closed 2026-08-26
- [[tasks/worker-crash-recovery-and-quorum-gate]] — Sprint 4 task 0056 outage root-cause fix: `WorkerSupervisor`, restart cap 5/index/10 min with 1 s→30 s backoff, quorum `ceil(n × 9/10)`-or-90 s gate; agent-closed 2026-08-27, deployment unconfirmed
- [[tasks/schedule-public-games-onto-ready-workers]] — Sprint 4 task 0192 ADR-109's first application: `pickGameID` rejection-samples the game ID onto a ready index and a 5 s abort bounds the create call; agent-closed 2026-08-27, orphan residual since discharged by 0194
- [[tasks/fetchlobbies-in-flight-guard]] — Sprint 4 task 0193 single-poll-in-flight guard on the 100 ms lobby tick: error lines per stuck ID 50→1, lobby flapping 21%→0, self-inflicted 429s 3→0; agent-closed 2026-08-27
- [[tasks/citizenship-name-change]] — Sprint 4 task 0067, the first citizenship benefit: citizen-gated name-change request → operator moderation → apply, with self-service cancel; migration 004, three profile-server routes, shared Telegram notifier. Agent-closed 2026-08-28; **code DEPLOYED in `362a2f9` but nothing has run — the UI has still never been seen in a browser** (card flag off, profile writes no-op on a deliberately blank token); the pending unmoderated name is publicly readable and UNMITIGATED. 🔧 **Its `0195` note is corrected 2026-09-19/20: payments are NOT 503ing on the box and the secret's provenance is owner-attested** — ⛔ still not a claim that payments work
- [[tasks/citizen-verified-icon]] — Sprint 4 task 0068: server-authored `isCitizen` on the frozen roster and lobby poll, placeholder `★` badge in four surfaces; the live 3-client desync check really ran (280 hash windows, 0 mismatches). Agent-closed 2026-08-28; **code DEPLOYED in `362a2f9`** — no citizens exist in production to badge, but R3's unauthenticated `isCitizen` payload now ships, and that acceptance is void the moment anything of value is gated on the flag
- [[tasks/worker-reject-departed-requester-create]] — Sprint 4 task 0194 departed-requester guard on `create_game` (bounded 10 ms settle wait, `503`): 0 orphans against a baseline of 5, closing the outage track; agent-closed 2026-08-28. ⚠️ Its plan deliberately supersedes its brief
- [[tasks/prod-api-env-https-apex]] — Sprint 4 task 0063: prod `/api/env` moved from `http` on a raw IP to `https` on the apex domain. ✅ **Fixed and DEPLOYED 2026-08-29 in release `362a2f9`**, measured live — the only config-parity instance actually fixed. ✅ **Evidence COMPLETED 2026-08-30** — all six deploy pendings now discharged (one owner-verified), the last via the Uptrace `openfront_host` attribute — ⚠️ **but four of the six were unevidenced at the moment the task closed**; the page carries a state-at-close vs final-state table so the ordering is not flattened. The task's headline user-impact claim was found **vacuous** (no auth service exists in this deployment)
- [[tasks/private-lobby-start-url]] — Sprint 4 task 0198: private-lobby Start silently failed on Yandex Games; the three `HostLobbyModal.ts` sites now build root-absolute worker paths, shipped in `362a2f9`. **Closed 2026-08-30 on LOCAL PROOF ONLY** — its production check was **waived as unsatisfiable, not unrun** (the private-lobby buttons are `display: none` on the Yandex template by the owner's own choice), so correctness in production is **inferred from the code, never observed**
- [[tasks/yandex-payments-secret-forwarding]] — Sprint 4 task `0195`, the **third** config-parity instance and the first outside `deploy.sh`: `build-deploy-profile.sh` never forwarded `YANDEX_PAYMENTS_SECRET`, so `/v1/payments/*` 503s ~~on the real box~~ (🔴 **2026-09-04: no real box exists** — owner-ruled; ⛔ **the code fix stands, only the production narrative is corrected**). Two edits shipped (staged-export block + `example.env.profile`), **closed 2026-09-01 as built + Deferred Live Tail (agent-closed — not owner-verified)**. 🚨 **Production is NOT fixed:** ~~`0014` has not issued the key, so a profile deploy today lands the variable empty and the routes correctly keep 503ing~~ 🔴 **CORRECTED 2026-09-19 — a NON-EMPTY value (length 32, content never read) IS present on the box and the prefix answers 404, not 503, on a missing sub-path** (`payments endpoints disabled` logged **0 times**). 🚨 **Presence is ALL that settles — the value's correctness is unproven, no real purchase was exercised, and ~~⚠️ its provenance is UNVERIFIED (NOT evidence `0014` issued the key)~~.** ✅ **PROVENANCE ANSWERED 2026-09-20 — struck, not deleted; the placeholder worry was the right question and it was PUT TO THE OWNER. OWNER-ATTESTED: the owner set the real Yandex per-game key themselves** (*"that's the real Yandex key, I set it."*, given live via `AskUserQuestion`) ⇒ the key **was issued** and **the owner performed the profile-box redeploy with it populated.** 🚨 **OWNER-ATTESTED, *NOT* REPO-VERIFIED — a deploy leaves NO artifact in git, so nothing in this repository can confirm it; same standing as the 2026-09-12 key-issuance ruling.** ⛔ **It settles *"is it there"* and NOTHING MORE — correctness still unproven, still no signed payload and no real purchase, so a page saying "payments work" would be as wrong as one saying they 503.** 🔒 **Length only, always — the value has never been read into any log, file or transcript.** ✅ For `0065` this is a gate **satisfied, not removed** — **the blocker count stays at THREE and is now OWNER-CONFIRMED, not a producer's conservative choice**: the alternative (clear it, two blockers) was explicitly offered and the owner answered *"Stay open on correctness — 3 blockers."* ⛔ **The count did not change; its STANDING did. Do not re-litigate it.** ⛔ **Not changed by the wiki**, and its old 503 rationale stays stale. 📌 **Also owner-ruled 2026-09-20: live-tail items D1 and D2 stay UNTICKED** — the 2026-09-19 probe covers them *in substance* but was **INDIRECT** (it proved the `paymentsEnabled` middleware passes, not that a signed payload works); **D3 is unaffected and stays owner-gated** 📌 **2026-09-23: no longer gates `0065`; its open value-correctness question moved to `0297` §1 — NOT answered.**
- [[tasks/dependency-declaration-sweep]] — Sprint 4 task `0196`, the documentation-only sweep of the canonical `**Depends on:**` bullet into briefs that had none, so `dashboard.sh` stops rendering a **fabricated `ready`**. **Sprint 4's `none recorded` count 6 → 0**; agent-closed 2026-09-01. ⚠️ Scope was **30 briefs, not the stated 31**, and **7 board-visible, not 8** — the gaps were investigated, unreconcilable, and **reported rather than laundered; do not "correct" them back**. 📌 The "14" in the original framing was never a measurement
- [[tasks/supertest-profile-server-flake]] — Sprint 4 task `0200`, closed 2026-09-01 **with NO CODE FIX** — a recognition note in `CLAUDE.md` plus a findings report. Mechanism **confirmed for the timeout shape only** and **not a repository defect** (it reproduces in ~40 lines of plain Node); **every candidate fix measured and refuted**, `--runInBand` included. 🚨 **Two shapes stay UNEXPLAINED and must never be recorded as resolved** — the `401` on a route with no auth middleware, and the `socket hang up` sub-shape. 🔧 Disproved *"the only four suites using `supertest`"* (**seven**, incl. `tests/server/Master.test.ts` on the default `npm test` path) and *"in our own test code"*
- [[tasks/container-log-retention]] — Sprint 4 task `0060`: game-container log retention moved **into version control** at `update.sh:90-92`, plus `access_log off;` on the 1-second-poll `/api/public_lobbies` block. Agent-closed 2026-09-01, **nothing verified in production, seven Deferred Live Tail items unchecked**. 🔧 Refuted the brief's *"the config is not in this repo"* premise — it shipped as an ordinary two-file commit. ⚠️ **The two halves deploy differently**: the `update.sh` flags ride a plain `deploy.sh`, `access_log off;` needs an **image rebuild via `build-deploy.sh`**. The `100m × 10` value is **provisional**, and the pre-change baseline is **unverified**. ✅ **Review finding F6 — the only one that closed with NO disposition — was confirmed by owner ruling 2026-09-02: keep the deploy-path table.** It changes no status and clears no live-tail item
- [[tasks/analytics-p1-citizenship-funnel]] — Sprint 4 task `0021`, the shared citizenship funnel spec, **closed 2026-09-02 as documentation only — no code**. 🔧 **Its own founding premise was DISPROVED: the brief's "the first weeks of live data are lost and cannot be backfilled" is FALSE** — every citizenship event is hard-gated by `CITIZENSHIP_CARD_ENABLED: false`, so **zero have ever fired and no collection window ever opened**. 5 of 6 spec'd events were already shipped, tested and documented; the 6th, `UI:Tap:CitizenshipLearnMore`, is **dropped as obsolete — the Learn-more surface was never designed** (accepted cost: no "researched it but didn't buy" signal). 🚨 **`Citizenship:Seen` may UNDER-count on a slow first paint, INFLATING every conversion rate** — unproven, owner-ruled logged-not-fixed, and its follow-up brief is a **recorded deliberate non-filing**. Brief moved `backlog/` → `done/`
- [[tasks/test-suite-reliability-investigation]] — Sprint 4 task 0197, **renamed at close** because its own title was known-wrong: the jest-worker `SIGSEGV` is an **upstream V8 GC bug, not repository-fixable**; the jsdom/`canvas` and memory-pressure hypotheses were **refuted by experiment**; the integration-suite hang **does not exist** and `--forceExit` was removed. No mitigation bought — **a red run stays ambiguous**. The Node pin is explicitly NOT a fix. Split out: `0200` — 🔧 **and two claims this page carried about `0200` were disproved 2026-09-01 and are now struck in place**: the four profile-server suites were **not** the only `supertest` users (seven), and the flake is **not** in our own code (upstream, like the segfault)
- [[tasks/gameanalytics-per-user-event-limit]] — Sprint 4 task `0224`, **closed 2026-09-07 `✅ Done (agent-closed — not owner-verified)` on an owner ruling to *"close now with an honest note"*** — the owner approved the **decision to close** and **verified nothing in production**. Shipped: `Performance` sampling 60 s → 300 s (commit `35afc64`), a hidden-tab accuracy fix, and the reference-doc update. 🚨 **Criteria 3 and 4 are UNMET and UNMEETABLE** — `0225` cuts the same `Performance` column in the same build, so any post-deploy drop is the **joint** effect and ⛔ **nobody may attribute it to `0224` alone.** 🚨 **THE CHANGE DOES NOT ADDRESS THE 3–4 SEP BREACH AND CANNOT** — that breach was **session-start** categories (`Player` ~34×, `Experiment` ~45×) while `Performance` barely moved (~1.6×); **a 4 Sep repeated today would still breach at ~235 % of the limit.** ⛔ **"The banner is gone" is not evidence** — the metric self-resolved 5 Sep at 162.79 with zero code written. 🟢 **`0208` is EXONERATED** — `Match` is flat across the spike. ⏸️ **Still deferred:** the reduction target (never set), `DEPLOY_ENV` fail-open (now `0226`, **live and unscheduled**), cardinality (**trending up**), full event enumeration. ⚠️ **Two dashboard figures do not reconcile and neither is called wrong** — including the "7-day mean 581.97" that its own rows average to **378.86**. ⛔ **NOT DEPLOYED**
- [[tasks/orphaned-performance-monitors-lobby-rejoin]] — Sprint 4 task `0225`, **closed 2026-09-07 `✅ Done (agent-closed — not owner-verified)`** — the owner ruled the *decisions*, not the result, and did not run it. **The family's one genuinely ACCUMULATING leak**: `handleJoinLobby()` stopped the game without stopping its `PerformanceMonitor`, then `onStart` overwrote the only handle that could — one permanently unstoppable monitor per mid-game join. Fixed in `Main.ts` `+18/−6` (commit `702a8ea`) via `stopPerformanceMonitor()` / `restartPerformanceMonitor()`. ✅ **The strongest evidence in the chain — a dev-console observation WITH a negative control on the un-fixed build.** ⚠️ **And its limits stay:** runtime evidence covers **one of six** touched sites, cannot separate `:678` from `:757` (4 ms apart), and three sites are **static-only**. ⚠️ **Codex review coverage was full but ONE FINDING DEEP**; the reviewer did **not** re-run `npm test`. **Magnitude unquantified — do not write a number for it.** Spawned `0227`, `0228`, `0229`. ⛔ **NOT DEPLOYED**
- [[tasks/crashed-game-teardown-seam]] — Sprint 4 task `0227`, **closed 2026-09-07 `✅ Done (agent-closed — not owner-verified)`** after three review rounds and three Codex passes. Added the missing `onGameEnd` seam from `ClientGameRunner` back to `Main` (commit `c910452`), plus a **generation token** so a superseded game's late teardown cannot stop the current game's monitor. 🚨 **ITS HEADLINE SITE A WAS NOT FIXED AND CANNOT BE** — the worker's `gameUpdate` drops every `ErrorUpdate`, so the client's crash branch is **unreachable dead code**; **criterion 1 cannot be met for site A and criterion 2's site-A observation is void.** ✅ **The `stop()` seam stays by owner ruling — dormant-but-correct**, live the moment the worker drop is fixed. Sites **B** (worker-init failure) and **C** (un-`catch`ed `createClientGame` rejection) **are** fixed — B's earlier exclusion rested on **false reasoning**, caught by Codex. 🔴 **NOT a leak and NOT orphaned monitors** — bounded, non-accumulating; that language belongs to `0225`. **Promoted to Sprint 4 by an owner judgement that REVERSED their own earlier same-day ruling — deliberately, because the input changed; the severity class did not.** Spawned `0232`, `0233`. ⛔ **NOT DEPLOYED**
- [[tasks/profile-box-adopt-and-reprovision]] — Sprint 4 task `0215`, **closed 2026-09-10 `✅ Done (agent-closed — not owner-verified)`**. 🚨 **THE BRIEF'S PREMISE WAS REVERSED:** it said *inspect, THEN wipe*; a read-only inventory found the box **already live and healthy** and the **owner ruled ADOPT — NO WIPE**. The single destructive act was destroying the Postgres data volume to rotate `POSTGRES_PASSWORD` at `initdb` (owner-ruled, all four tables re-verified at **0 rows at execution time**). Every regenerable secret regenerated — new `age` keypair with custody **PROVEN by decrypting a copy retrieved from storage**, a **brand-new clean bucket** plus scoped key, an **explicitly generated** `PROFILE_INTERNAL_TOKEN`, a new `POSTGRES_PASSWORD` via fresh `initdb`, `PROFILE_ID_PEPPER` deleted. The Let's Encrypt certificate was **PRESERVED byte-identical** — that is what adopting bought. Lead-verified: `/health` 200, `/ready` 200 (proving the API authenticated to Postgres with the new password on a fresh volume), and one encrypted 19,330-byte object in the new bucket. ~~🔴 **THE RESTORE PATH HAS NEVER BEEN TESTED — "backups working" means ENCRYPT-AND-UPLOAD ONLY** (`0218`, open)~~ ✅ **DISCHARGED 2026-09-11 by `0218` — a backup RESTORES, proven twice against non-empty data, including into the LIVE database in place; ⛔ the SCHEDULE and the DATA are still proven SEPARATELY, NEVER TOGETHER, and this task's `age` second-copy weakness was CARRIED, not closed**. ⚠️ B4's historical half stays UNKNOWN; `ufw` default-deny was not re-verified; RU residency rests on **one** geolocation provider and is registration evidence, not a physical-site attestation
- [[tasks/profile-le-certificate-renewal-proof]] — Sprint 4 task `0216`, **NARROWED by owner ruling then closed 2026-09-10 `✅ Done (agent-closed — not owner-verified)`** — from a broad RU-network spike down to *"can this box obtain a certificate"*. `certbot renew --dry-run` completed a **full ACME HTTP-01 challenge against LE STAGING**, pre/post nginx hooks exercised, **zero production rate-limit spent**. 🚨 **THIS PROVED CAPABILITY, NOT MONITORING** — **nothing reads the renewal log** (`0219`, open), so a later break fails **silently, twice a day, until the certificate expires 2026-11-20**. Intermittency and latency were **permanently dropped by the narrowing** and are not restored by the close
- [[tasks/profile-durability-restore-drill]] — Sprint 4 task `0218` (profile epic P3), **closed 2026-09-11 `✅ Done (agent-closed — not owner-verified)` WITH EIGHT RESIDUALS — not a clean sweep**. 🔴 **The OWNER personally executed every command on the box; no agent touched it** — the marker reflects who performed the CLOSE, not that nobody looked. ✅ **THE VAULT'S LARGEST STANDING CLAIM IS NOW FALSE: "the restore path has never been tested" is SUPERSEDED. A BACKUP RESTORES** — proven **twice** against **non-empty** data (76 rows), into a throwaway database **and, for the first time ever, into the LIVE database in place** via the confirmed-live branch; both `IDENTICAL` on row counts, per-table content digests, **both** sequences, constraint/index shape and **three behavioural checks**. The nightly **schedule fires** on three independent signals, five consecutive days. ⛔ **DO NOT SHORTEN THIS TO "BACKUPS WORK": the SCHEDULE and the DATA are proven SEPARATELY, NEVER TOGETHER** — every cron-produced object that has ever existed is a dump of an **EMPTY** database; the only non-empty backup was **HAND-RUN**. ⛔ **The measured RTOs (0.374 s / 0.435 s) are on ~21 KB and DO NOT EXTRAPOLATE — never quote them as the project's recovery time.** ⛔ **The weekly-copy path has NEVER run** against this bucket (task `0241`, first attempt Sunday 2026-09-13). ⚠️ **Backup history is thin and was misread once already** — two objects before the drill, only ONE cron-produced. ⚠️ **The `age` second-copy residual is CARRIED, not closed** (both copies are cloud stores that may share a phone-number recovery path; owner-accepted, no remediation). 🔧 **Two real runbook defects found by executing it, both fixed:** a **docker network name that does not exist on the box**, and a live-recovery example that **embedded the real database password in a URL** — the password-free local-socket target is now the documented form. ⛔ A claim in `0218`'s own brief — that the documented command line no longer works — was **REFUTED BY EXECUTION**
- [[tasks/profile-cleanup-obsolete-secrets]] — Sprint 4 task `0222` (profile epic, Cleanup), **closed 2026-09-10 `✅ Done (agent-closed — not owner-verified)`, over a documented objection**. ✅ Its old-bucket question is **ANSWERED AND CLOSED**: the owner abandoned and had **already deleted** the old bucket, and 🚨 **the premise was RETRACTED — the bucket was EMPTY and always had been, so the "permanently unreadable objects" it was about NEVER EXISTED** (⛔ objects only; the lost old `age` private identity is unchanged, and that is luck, not a control). 🔒 **STANDING 2026-09-11 — the old storage access key will DELIBERATELY NOT be revoked: CLOSED BY OWNER DECISION, not outstanding work, not a task, not to be re-raised** (owner, verbatim: *"Forget about the old S3 keys, mark this task as cancelled"*; ⚠️ **there was no open task to cancel** — `0222` was already Done and **stays Done**, no file moved, no mover skill invoked). ⛔ **What that does NOT change — never soften it into "revoked", "resolved" or "no longer a risk":** the key was **NEVER revoked at the provider**, **nobody ever established its scope** — **inert** if bucket-scoped to the deleted bucket, **reaching the NEW backup bucket** if account-wide — and an objection on exactly that point was **OVERRULED TWICE** (2026-09-10, 2026-09-11). ⚠️ **A deliberate decision not to act is not the same as the risk not existing:** check **the key's policy at the provider** first if it ever matters — nothing in this repository can answer it. ➡️ The repo-side purge was **carved out to `0240`** (`PROFILE_ID_PEPPER` / obsolete variables, **open**); ⛔ **the access-key residual is NOT in `0240` and must not be added to it**
- [[tasks/uptrace-alert-delivery-to-telegram]] — task `0277`, **done (agent-closed — not owner-verified), closed 2026-09-17 on an OWNER RULING** (offered close-now / hold-for-the-drill / owner-verify-first; the owner chose close-now knowing two residuals could only be proven by a real alert firing and clearing). **The blocker it closed, plainly: the monitoring stack could not deliver ANY alert notification — no channel of any kind — so `0274`'s six rules would have fired correctly and reached NOBODY.** Shipped **branch B, the webhook relay**, chosen by a local proof the brief made the decider, with a pre-approved no-second-round-trip switch. 🎯 **LIVE PRODUCTION EVIDENCE, owner-observed by screenshot** — ⚠️ **but state its scope exactly: it proves the transport, auth, format and rendering of a SYNTHETIC TEST MESSAGE, not a real alert** (the real one came later the same day, in the drill). **Scope grew by owner ruling: this task also carries the `0061` connection fix for ALL THREE consumers — one fix, three consumers** — and 🔴 **the player-feedback half is FIXED IN THE TREE AND UNSHIPPED until a game deploy, so do NOT read "0061 is fixed" off this task.** 🚩 **Residuals 1–11 live in its review ledger — do not re-derive them**; the unsafe one is **5**, best-effort un-marking (a crash between the 202 and a delivery failure **loses the alert**). ⛔ **It shipped a proven pipeline with NOTHING FEEDING IT** — that is `0274`'s work, not a gap here
- [[tasks/alert-path-liveness-probe]] — task `0284`, **done (agent-closed — not owner-verified), closed 2026-09-18**, guarding the one failure mode that **turns monitoring off for good and says nothing**: a `401`/`403`/`404` from a webhook endpoint **disables the Uptrace channel permanently**, and the nginx allowlist answers 403 on a source-IP miss ⇒ **three ordinary operational events — an address change, a typo, a dropped deploy variable — each end alerting forever.** ✅ **THE HARD CLOSE GATE WAS DISCHARGED, NOT WAIVED** — a drill ran, **owner-executed**, and passed: marker moved aside → `FAIL` → **the external dead-man's switch PAGED THE OWNER BY EMAIL**, owner-confirmed by screenshot → restored → auto-resolved. 🚩 **THE CENTRAL ASSUMPTION WAS PROVEN — AND NOT BY THE DRILL: the nginx access log had ALREADY recorded the probe and four real monitoring-stack calls arriving from the SAME source address.** 📌 **How it was found matters as much as what it says — a read-only log query answered what a destructive drill was scheduled to answer.** ⚠️ **The drill was REDUCED by owner ruling** (do not deliberately disable the channel) and **the lead reduced it further and said so**; ⛔ **the missing-marker branch ran, NOT the stale-marker branch**, and **removing the address from the allowlist was never exercised — that stays binary-disassembly evidence, not observation.** ⚠️ **The marker is agent-closed AND the owner personally executed every deploy and drill step — both are true, quoting either alone misleads, and the marker is NOT upgraded.** 🚩 **EIGHT residuals survive**, loudest: **it catches the CAUSE, not an already-disabled STATE** (`0285`) · it does not check the **channel config's own copy** of the secret · it proves **nothing about Telegram delivery** · ⚠️ **an hourly probe may actively MASK an idle-path defect** it cannot see
- [[tasks/name-change-daily-digest]] — 🆕 task `0283`, **done (agent-closed — not owner-verified), closed 2026-09-18.** One Telegram message a day into the **Name Changes** topic saying how many players wait for a name review — **sent even at zero, by owner ruling (2026-09-17), because the daily arrival IS the heartbeat and its ABSENCE is the signal.** ⛔ **Never add a "skip when empty" condition; two tests and a harness assertion exist to turn that red.** Built as a **host cron line `0 4 * * *` UTC (07:00 MSK, owner-chosen)** running a one-shot Node process in the existing container — ✅ **the design question the brief deliberately left open was DECIDED in the plan** (neither an in-process timer nor a Telegram send bolted onto `profile-checks.sh`). 🔑 **No HTTP route was added, deliberately** — a 401/403/404 from the relay route permanently disables the Uptrace channel. It writes a **success-only freshness marker** read by `profile-checks.sh` **check 12**, and the deploy sends one extra digest **on purpose** so a morning deploy cannot produce a false page. 🎯 **OBSERVED DELIVERING IN PRODUCTION 2026-09-19 — the first real messages this feature has ever produced** (every earlier send was a mocked seam): two owner-watched messages, the deploy-time send and cron, **both reading `Waiting for review: 0`**, in the **owner-confirmed Name Changes topic**, cron landing at **07:00 MSK to the minute.** ⛔ **THAT EVIDENCE DOES NOT UPGRADE THE MARKER** — only the owner may upgrade a landed `✅ Done`. 🚨 **AND IT PROVES NOTHING ABOUT UPTRACE ALERT DELIVERY — reading it that way is the error ADR-114's A3 exists to prevent:** the digest never touches the monitoring stack, never crosses the `/internal/` allowlist, never arrives from the monitoring box's egress address, **so a 403 could have permanently disabled alerting while both messages arrived perfectly.** ⛔ **It also does NOT discharge `0274` amendment A1** (delivery after an idle period) — the ~9.5 h gap is **weak evidence toward it, not the test it asks for.** ⏳ **Still open: the second day's SINGLE message** (one firing is not a schedule), **check 12's first run**, and **any non-zero count — never rendered for real.** 🚩 **Check 12 firing does NOT mean Telegram is broken** — three faults age that marker, and the **Postgres one wears a Telegram costume**
- [[tasks/setup-profile-heredoc-root-command-execution]] — 🆕 task `0282`, **done (agent-closed — not owner-verified), closed 2026-09-18.** `setup-profile.sh` wrote the compose file from an **UNQUOTED here-document**, so **backticks inside its COMMENTS ran as commands, as root, on every profile deploy** — 🚨 **pre-existing, introduced by NO Sprint 4 task**, found by the lead in the owner's 2026-09-17 deploy output (four commands the box actually ran: `` `docker compose stop` ``, `` `always` ``, `` `node` ``, `` `docker stop` ``). **Functionally harmless today — comments only** — ⛔ **but NOT cosmetic, and it must not be filed as such: the `` `docker compose stop` `` substitution failed ONLY because that working directory held no compose file; in a directory that did, a COMMENT would have stopped the production stack mid-deploy**, and any future backtick added there runs arbitrary root commands silently. Fixed by quoting the delimiter, re-supplying the four intended values behind a **fail-closed guard**, de-backticking five comment lines, and **five new harness assertions locking the delimiter quoted** (8 `0282` assertions green). ✅ **The acceptance criterion was the rendered-compose DIFF, not a nicety: exactly 5 lines, all YAML comments; comment-stripped renders IDENTICAL** — any change to a `services:`/`image:`/`environment:`/`healthcheck:`/`ports:`/`volumes:`/`logging:`/`restart:` value would have meant a lost intended expansion. **Codex second opinion RAN — full coverage, no degradation.** ⚠️ **Not verified on a box** — it rides `0217`'s mandatory redeploy, and the on-box step is the owner's and outstanding. **Four owner-accepted residuals**, and a **separate pre-existing finding (R4) was owner-ruled into its own task, `0287`** — ⛔ **whose severity the two reviewers DISAGREE on (Codex `HIGH` vs `fkit-reviewer` `LOW`) and which is unresolved ON PURPOSE**
- [[tasks/profile-identity-s1-database-rekeying]] — task `0270` (S1 of epic `0266`), **done (agent-closed — not owner-verified), deployed 2026-09-15.** Migration **`006`**: guard first, then `players` (random UUID PK), `player_identities`, `player_xp_grants`, and **every child table re-keyed** — `001`–`004` stay **byte-identical** and the untracked, never-deployed `005` is **deleted**. **Done then because the box had 001–004 and ZERO rows** — ⛔ **after `0217` the same change is a data migration.** Also **absorbed `0253`'s uncommitted code** in the same files (serialize, never concurrent) and moved the integration suites onto **one runner-based setup**, which is why that run is now **destructive by design**. ⚠️ **One unexplained `NameChange.it.test.ts` failure on the first build run — consistent with the known supertest flake, NOT proven.** Box-verified read-only: migrations `001–004 + 006`, all tables **0 rows**, old `player_profiles` gone, `/ready` 200, a profile GET for an unknown id → **404, not 500, and created no rows.** ⛔ **No real player traffic verified — there is none until go-live**
- [[tasks/profile-identity-s5-monitoring-and-creation-switch]] — 🆕 task `0274` (S5), **done (agent-closed — not owner-verified), closed 2026-09-19 on an OWNER RULING** (offered close-now / owner-verifies-first / leave-open; the owner chose close-now). Profile-server OTEL metrics (`geoconflict.profile.*`, own minimal setup — never `src/server/Logger.ts`), the `PROFILE_LOGIN_CREATE_ENABLED` creation switch (`503 creation_paused`), a junk-cleanup runbook, and a `profile-checks.sh` disk / growth backstop. 🎯 **Nine live metric series arriving since 2026-09-17 are the FIRST AND ONLY evidence the `Server.ts` wiring works — no green gate ever was.** ✅ **Three owner steps discharged 2026-09-19:** creation-switch drill (OFF ⇒ `503 creation_paused` with 0 rows; ON ⇒ `200 created:true`; row cleaned via the runbook) · the **per-request** name-change operator notification **observed arriving** (it discards its result with `void`, so observation is the only evidence that can EVER exist) · the `profile · overview` dashboard built. 🔴 **THE CLOSE DOES NOT MEAN MONITORING IS DONE — six named gaps:** ⛔ **ONE of six alert rules exists (A5 alone)**; A1/A2/A3/A6 **deferred for want of login traffic with NO task of their own**, A4 ➡️ `0288`, idle-period delivery (amendment A1 — a DIFFERENT A1) ➡️ `0289`, both `🔲 Backlog` — 🔧 **BOARD CORRECTED 2026-09-21: they are NOT on the same board. `0288` is on the BACKLOG board (rank `—`); `0289` is on the SPRINT 4 board (rank `High`, the PRODUCER'S). Verified against both board files at `7eebaf3`** — ⚠️ **worth the correction because they are reached by different status commands, and `0288` sitting on the older board is exactly how an item goes unnoticed** · one dashboard panel unbuilt · two unexplained board observations not chased · `session_rejected`'s picker presence UNCHECKED · 🚨 **the dashboard, monitors and channels are UI-ONLY STATE — `setup-telemetry.sh` seeds none of them, so a monitoring-box rebuild deletes the lot and nothing in the repo recreates it** · R11 and the `session.rejected` baseline stand, and ⛔ **do not arm any alert on `players_total` until it has been observed**
- [[tasks/profile-identity-s2-login-and-session-token]] — task `0271` (S2), **done (agent-closed — not owner-verified), PROVEN LIVE on the box 2026-09-17.** `POST /v1/login` plus the `v1.<payload>.<HMAC>` 24 h Bearer token, with **`resolveCaller` as the single caller-resolution point** for every public route, a **find-only** legacy fallback that never creates, and a **fail-closed** empty-secret path (`503`, boot warn naming the variable, never its value). ⚠️ **The owner collapsed two planned profile deploys into ONE, which shipped FIVE tasks together** (`0271`, `0272`'s server side, `0273`'s legacy-fallback removal, `0274`'s monitoring, `0276`) — ⛔ **do not read any of them as independently deployed.** 🚩 **Why the live check mattered: signing a token and VERIFYING one are separate paths — a token issued but not accepted would have presented EXACTLY as the box did before the run**, and would have surfaced only once the client shipped. Now settled live: 200 with a Bearer token · **401 on one altered character** · **401 on a lowercase `bearer` prefix** (residual R3, closed) · `created=false` on a second login · **zero ids and zero tokens in container logs**. 🔓 **Security gained today: NONE — anyone who asserts an id gets a token**, and **no per-IP limiter** is owner-accepted
- [[tasks/profile-backup-restore-reproof-006]] — task `0275`, **done (agent-closed — not owner-verified).** 🎉 **`IDENTICAL` — the restore path is PROVEN END TO END on the `006` schema with NON-EMPTY data (2026-09-16):** all 10 tables, both sequences, and the constraint- and index-**DEFINITION** digests matched; all eight behavioural lines printed; teardown clean. 🚨 **This CLOSES the standing *"THE RESTORE PATH HAS NEVER BEEN TESTED"* alarm carried in `0218` and in project memory**, and `0218`'s brief was annotated in place (struck, not deleted). ⛔ **Four things this close does NOT say, none of which may be softened:** the marker is agent-closed **AND** the **owner personally executed every writing command** (both true) · 🚨 **`real 0m1.252s` is NOT a usable RTO — it is a 24 KB dump**, and that caveat survives **unchanged** · one comparison file was **reconstructed, not produced by the drill's own redirect** · the drill's synthetic rows **sit inside that day's backup object for its full 14-day retention**, owner-accepted. ⛔ **Still open: a CRON-produced backup of REAL data** — **the schedule and the data remain proven SEPARATELY, never together.** Key custody went to its own task (`0281`)
- [[tasks/profile-weekly-backup-copy-verified]] — task `0241`, **done (agent-closed — not owner-verified).** ✅ **The first-ever weekly backup copy against the current bucket was OBSERVED**, 2026-09-13, **from the untouched scheduled run** — ⛔ the backup script was **not** run by hand. Four signals agree (object key, three matching byte counts, marker exit status and timestamps, the cron record, and the Sunday branch in the log with no warning). 🔴 **Not busywork — it GATED `0219`**: *a monitor built on a broken producer is worse than no monitor, because it manufactures confidence.* ⚠️ **Two things a reader must know or they will misread it: that run captured an EMPTY database (a small object is CORRECT here), and this proves the weekly COPY path and NOTHING about restoring real data.** **Authority split: the owner ruled only that it be TRACKED — the `Low` rank is the producer's, and `Low` is a rank, not an assessment of consequence**
- [[tasks/internal-path-case-variant-allowlist-bypass]] — task `0276`, **done (agent-closed — not owner-verified), proven on the box 2026-09-17.** **nginx prefix `location` matching is case-sensitive; Express 4 routing is case-insensitive by default** ⇒ `/INTERNAL/v1/…` **skipped the allowlist entirely and reached `internalAuth`**. ⚠️ **Severity: a LOST LAYER, not an open door** — the token still gates and fails closed; what was lost is the allowlist keeping the whole internet away from the token check. **Both layers fixed** (`~* ^/internal/` regex plus app-level case sensitivity), with **seven new harness assertions** including one guarding `allow`-before-`deny all`, since **the reverse order would have locked the game server out**. ⛔ **Two findings that outlive the ledger: the app-side fix ONLY works ABOVE the timing middleware** — Express 4 builds its router lazily and reads app settings at the first route registration, so a late `app.set` is **silently ignored** and moving it fails **16 tests byte-for-byte identically to DELETING it** — and **there are no sub-routers today, but adding an `express.Router()` later REOPENS the hole**, because a Router does not inherit the app's setting. 🚩 **The gap that held it open was the `allow` half: a 401 (not 403) from the game box proves the allowlist still lets the game server through.** ⚠️ **Two nginx versions of evidence — the BOX probes are what prove deployed behaviour.** Residual: **a bare `/internal` with no trailing slash is not matched** — latent, no route there today
- [[tasks/telemetry-cert-expired-renewal-cron]] — task `0257`, **done (agent-closed — not owner-verified).** 🚨 **The telemetry certificate EXPIRED 2026-09-04 and client telemetry was DARK FOR TEN DAYS** (error groups **226 → 19 → 2**; nothing from the then-current prod build had ever appeared). **Root cause: the renewal could never have worked** — certbot persisted `authenticator = standalone`, so every `renew` tried to bind port 80, **which nginx permanently owns**, and the `--post-hook` reloaded nginx *after* the failure. **It had failed every run since the first attempt window and would have failed forever**, while **nothing read the renewal log** (that gap is `0258`, still open). **A second broken renewer — the distro `certbot.timer` — was enabled here** though the profile box disables it on purpose. 🔴 **Fix shape OWNER-RULED: mirror the profile box's nginx pre/post hooks, NOT `--webroot` — the boxes converge**, accepting a few seconds of ingest gap every ~60 days. ⚠️ **Rank owner-ruled High** (unusually — most Sprint 4 ranks are the producer's). ⚠️ **Only the CLIENT lane was measured; three other lanes pointing at the same endpoint were REASONED, not observed.** ⚠️ **The LE production limit of 5 duplicate certs/week means a botched `--force-renewal` can lock renewal out for a week — every rehearsal used staging**
- [[tasks/uptrace-retention-not-applied]] — task `0259`, **done (agent-closed — not owner-verified)** — an investigation, **no code in scope**. 🔴 **Verdict: retention IS running daily — at a FIXED ~14 DAYS for every data class, not the configured 7 d / 90 d. Uptrace 2.0.2 without a licence IGNORES the project TTL fields** ⇒ 🚨 **the repo's retention mechanism is INERT AS WRITTEN, and the promised 90-day metrics are deleted at ~14 days.** ⚠️ **Evidence classes kept apart: the MECHANISM is measured, the vendor LICENCE GATE is INFERRED.** **All four original hypotheses were REFUTED**, and three of the refutations are what a reader would otherwise guess wrong: **the daily `retention check` cron is NOT what deletes data** (Uptrace's own in-process worker does, at 00:00 UTC, by `DROP PARTITION`) · **the telemetry tables carry NO table-level TTL at all**, so lazy part expiry is out · **the project TTL row is intact and in the right unit — it is read and ignored.** 📌 **Owner ruling: the ~14 days is ACCEPTED for now**, follow-up `0263` asks whether it is a hard edition cap; ⚠️ **the repo still claims 7 d and 90 d where a reader will believe it — a known, accepted, UNFILED gap**
- [[tasks/client-source-map-upload-verification]] — task `0260`, **done (agent-closed — not owner-verified). STOPPED at the plan's stop-gate; no code written.** 🔴 **The deployed Uptrace 2.0.2 has NO source-map upload endpoint — every upload `0164` has EVER sent was answered `405`**, byte-identical to a nonexistent path, while a route 2.0.2 *does* register returns a real `400` through the same nginx ⇒ **the 405 is Uptrace's own router.** **No listing route exists either.** ⇒ **client stack traces are NOT symbolicated and never have been**; the dead upload and its two inputs were **removed**, and **revisit is `0264`**. ⛔ **Unchanged and deliberate: maps are still `hidden-source-map`, still deleted from the image, and the master still 404s `.map` requests.** 🚩 **The lesson, not a coding error: the upload was best-effort by design, so "the build was green" was NEVER evidence it ran.** ⚠️ **Its end-to-end verification step is MOOT, not passed — nothing resolves frames today**
- [[tasks/citizenship-kill-switch-coverage]] — Sprint 4 task `0236`, **closed 2026-09-10 `✅ Done (agent-closed — not owner-verified)`**. Two gating layers already existed; **three ungated surfaces** now route through **one shared helper**, and the ★ badge is gated inside `renderCitizenBadge()` (owner: *"Hide it too — kill means kill"*) — one edit, four surfaces. 🚨 **NO RUNTIME VERIFICATION EXISTS AND NONE WAS FAKED** — evidence is unit tests plus code reading; `client.isCitizen` cannot become `true` locally (`0217` unwired, zero citizen rows). 🔴 **Leaves a LAUNCH GATE, `0238`:** layer 2 must be validated in a **real staging/prod build** before `CITIZENSHIP_CARD_ENABLED` is flipped — **neither `0236` nor `0217` discharges it**. Records two owner rulings that **pull in opposite directions** (kill-means-kill for the badge, keep-the-fail-open for the card) and are deliberately **not** reconciled. 🔴 **UPDATED 2026-09-21 — BOTH OF THOSE ARE NOW SUPERSEDED BY ONE OWNER RULING. (a) THE `0238` LAUNCH GATE IS DROPPED** — `0238` falls to *"flip the flag at launch, confirm, write it down"* and **blocks nothing**; ⛔ its status token, board and rank are UNCHANGED (a re-scope, not a schedule), and **three things it struck are kept as history**: the gate framing (the owner's own 2026-09-10 ruling), the 2026-09-20 ruling to split `0238` three ways, and the architect's split recommendation. **(b) THE TENSION IS RESOLVED AND RULING 1 IS WITHDRAWN** — the card's fail-open was **removed** by `0291`, so *kill means kill* now applies uniformly across all four surfaces. 🚩 **WHY, and it is the durable lesson: the 2026-09-10 acceptance was granted while `CITIZENSHIP_CARD_ENABLED` was `false` and the carve-out was INERT — flipping layer 1 at launch would have made dead code live, so the acceptance never covered the case it was about to meet.** ⚠️ **The accepted cost travels with the withdrawal: degraded-SDK players now lose the card entirely instead of seeing a "couldn't connect" state — the fail-open's whole purpose. ⛔ Do not restore it to "fix" that**
- [[tasks/yandex-catalog-registration]] — 🆕 task `0014`, the Yandex Games catalog registration, **CLOSED 2026-09-22 on the owner's verbatim "Close it"**, `(agent-closed — not owner-verified)`. 🚨 **THE "NOT OWNER-VERIFIED" GAP IS NOT CLERICAL HERE: EVERY deliverable this task tracked is OWNER-ATTESTED AND NOT REPO-VERIFIABLE** — nothing in this repository can see the Yandex console, and it never could. ⛔ **Do not summarise this close as "verified".** **Four owner rulings on 2026-09-22 emptied it:** (a) the **test-purchase Yandex login is ADDED**; (b) the **cosmetics catalogue item is DELIBERATELY NOT to be registered** (*"We don't need it if it's Sprint 5."*) — ⛔ **not "unknown", not outstanding work**, and 🚨 **this OVERRODE the 2026-09-19 note that had been careful to say the instruction was UNCHANGED** (sequential, not contradictory); (c) the **price unit is `99` YAN, NOT 99 ₽** — ⛔ **a deliberate REVENUE decision, not a clerical choice**: per the owner 99 ₽ would net *around* 40–49 ₽ after Yandex's cuts, *"which is too small"* ⇒ **a later reader proposing "simplify to 99 ₽" is proposing a revenue cut the owner has already rejected** (⚠️ the net figure is **the owner's own estimate as stated**, not verified here); (d) the **`citizenship_ui` experiment flag is SET**. ⚠️ **(a) and (d) are the OWNER CORRECTING THEIR OWN 2026-09-12 REPORTS**, not this project discovering the record was wrong — the struck *"NOT DONE"* text is kept throughout. ⛔ **"Set in the console" is NOT "observed taking effect in a build", and that observation is IMPOSSIBLE TODAY** — `CITIZENSHIP_CARD_ENABLED` is `false` and `&&` short-circuits, so the remote flag is never read on any build, production included; that is `0238`'s job at launch. 🚩 **RESIDUAL, asserted NEITHER way: the flag's NAME and VALUE were never confirmed against the code** (the code requires exactly `citizenship_ui` / exactly `enabled`) — ⛔ **a one-character mismatch leaves the kill switch silently OFF at launch with nothing warning anyone.** 🔒 **The product ID is likewise fixed at exactly `citizenship` by code, in two independent places — a typo there is a paid purchase that grants nothing.** ✅ **The list conflict this brief carried DISSOLVED rather than being settled** — ruling (d) emptied the deliverables table, so ⛔ **the question of which list governs is still UNANSWERED and this close is NOT precedent for picking one.** 🔧 **It drops `0065`'s blocker count THREE → TWO** (`0062`, `0195`) — ⛔ **`0065` stays `🚧 Blocked`, no mover was invoked on it, and SATISFIED ≠ EXERCISED: no purchase of any kind has ever been made.** 📌 **The owner confirmed they WILL run that test purchase after the game deploy; 🚩 a lead-session claim that they refused test purchases was a MISATTRIBUTION and was never true.** ⛔ **And the cosmetics SKU still cannot be sold by any code path** — `PAYMENT_PRODUCT_IDS` has exactly one entry, so selling it needs its own entry **and** its own grant branch.
- [[tasks/citizenship-card-fail-closed-degraded-sdk]] — 🆕 task `0291`, **filed, built, reviewed and closed all on 2026-09-21, `(agent-closed — not owner-verified)` — no human has verified it.** The citizenship card gated on `isYandexDegraded()` **OR-ed with** the flag read, so **a degraded-SDK session rendered the card EVEN WITH THE `citizenship_ui` FLAG OFF** — the other three gated surfaces (★ badge, inbox, payments-reconciliation `POST`) all failed **closed**; the card was the odd one out. The carve-out is removed and **all four surfaces now fail closed**. **AUTHORITY: one OWNER RULING given live via `AskUserQuestion` on 2026-09-21** (*"we are overfocused on the enable/disable flags … it should be a boolean thing, that's it"*), relayed by `fkit-lead` to a spawned producer with no owner channel; ⛔ **not producer precedent.** 🔴 **It WITHDREW the 2026-09-10 acceptance of that fail-open**, because that acceptance was made **while the flag was `false` and the code was INERT**. 🚩 **LOUDEST RESIDUAL — NO LIVE/BROWSER VERIFICATION: nobody has observed the card failing to render in a REAL degraded production session**; the evidence is `npm test` **138 suites / 1870 tests** passing, a **red-before / green-after** unit check, and Codex coverage **FULL** — ⛔ **unit-level in jsdom, never a browser.** That observation is **deliberately handed onward to `0238`'s launch check.** 🚩 **The degraded STATE is real and observed, though: an owner-run devtools probe on a signed-in production session returned `isYandexDegraded: true`** — ⛔ **which proves the state occurs, NOT that the new behaviour was seen there.** ⛔ **THIS LAUNCHES NOTHING** — `CITIZENSHIP_CARD_ENABLED` is unchanged and still `false`. ⚠️ **ACCEPTED COST, ruled on and not to be re-litigated: degraded-SDK players lose the card entirely instead of seeing a "couldn't connect" state.** ⚠️ **Scope of the switch, so nobody over-trusts it in an incident: it hides FOUR CLIENT SURFACES and does NOT stop the server crediting XP ⇒ not a legal or moderation takedown mechanism.** 🔴 **`0291` is now the ONLY thing preceding the `CITIZENSHIP_CARD_ENABLED` flip; `0238` is NOT.** ✅ **THAT FLAG IS RESOLVED 2026-09-22 — the brief was CORRECTED on an owner ruling (*"fix it"*), the false `## Status` paragraph struck (struck, not deleted).** ⛔ **The owner's ruling was never in question — only the record's description of what was done.** **Each part's basis, named separately:** ✅ **the END STATE is repo-verified** — `0238`'s fail-open acceptance was **already struck** at the 2026-09-21 re-scope, *before* the ruling about it was sought, so **there was no stale line left to leave**; 📎 **the ORDERING is NOT, and 🚩 GIT CANNOT ESTABLISH IT** (0 `0291` refs at `45040b0`, 4 at `HEAD`, all inside the one squashed `Sprint push`) — it rests on **the re-scoping producer's own hand-back report, given BEFORE the owner was asked anything**, i.e. ⚠️ **AGENT TESTIMONY, not a repo artefact and NOT proof**; and **`0238` WAS edited by the close — hrefs only, four lines**, ⛔ status, board and rank untouched. 🔎 **ROOT CAUSE: `fkit-lead` put a STALE question to the owner** about work already done; the good-faith *"leave it"* answer propagated into three places describing a state that no longer existed. ⛔ **The owner did NOT change their mind and NO agent defied a ruling.** ⚠️ **The bare-`:NNN` hazard is unchanged in kind — the corrected text still carries coordinates, now with a "verify by content" caveat beside them.** 📌 **ALSO: `0238` MOVED TO SPRINT 5 on 2026-09-22**, so the live/browser observation this task handed onward now lives a board later, after the deploy. ~~The original flag, kept as the record: the brief CONTRADICTED ITSELF about the `0238` hand-off — its `## Status` said `0238` was NOT edited and still recorded the fail-open as accepted, its `## Notes` said that bullet is struck and points here; verified at `7eebaf3`, the `## Notes` version was right and the `## Status` version FALSE~~ — `0238` *was* edited in the same commit, its bullet *is* struck, and it names `0291` four times. **Failure mode 4 (self-invalidation inside one commit), arriving with a bare-style `:497` coordinate of exactly the form convention 10 bans; ~~the vault cannot fix a brief (ADR-005) — a human must~~ ✅ **a human DID, 2026-09-22****.
- [[tasks/forward-profile-internal-token]] — 🆕 task `0062`, `PROFILE_INTERNAL_TOKEN` never forwarded by `deploy.sh` so the profile client silently no-oped in production. **CLOSED 2026-09-23 `(agent-closed — not owner-verified)` on an owner ruling, as built + reviewed** (one `deploy.sh` line + partial-config warnings, 2026-08-24; **D5 passed LOCALLY 2026-09-23**). ⛔ **D1–D4 have NEVER run with a real value** — D2 on 2026-09-04 was **inconclusive** (owner-blanked token); they moved to **`0296` A1–A4**. The **deploy-time forget-risk** stands, and 🔴 **NO GUARD WILL BE BUILT** (owner, 2026-09-04).
- [[tasks/citizenship-earned]] — 🆕 task `0017`, earned citizenship at the XP threshold, granted inside the credit transaction. **CLOSED 2026-09-23 `(agent-closed — not owner-verified)`, built + reviewed on the LOCAL stack only** — ⛔ no production grant has ever happened. Verification step 3 (inbox in a browser) **never ran — owner-WAIVED**; the live tail moved to **`0296`** (A2–A3, A5, A6, B1); the flip was **removed** and is `0065` §6's alone. ⚠️ Threshold figures: repository carries the `0211` rescale (100 / 1 XP), production does not.
- [[tasks/personal-inbox]] — 🆕 task `0012`, the citizens-only Personal tab (8d-B) in the announcements popup. **CLOSED 2026-09-23 `(agent-closed — not owner-verified)`, built + reviewed 2026-08-26 — NOT launched and NEVER seen in a browser** (browser leg **owner-WAIVED**). Live tail → **`0296`** (A2–A3; B2/B3 only after the flip). Filled both citizenship inbox seams and **wrapped their call sites** (discharges `0017` R1). 🚩 Its residual D5's re-raise condition is partly met by the new plan itself — flagged.
- [[tasks/deploy-time-config-parity-guard]] — 🆕 task `0064`, the deploy-time config parity guard: Phase 1 **names** parity for game / profile / browser pipelines, Phase 2 a prod-only **value** guard (non-empty; `https`, no bare IP for exactly four URL variables). **CLOSED 2026-09-24 `(agent-closed — not owner-verified)` on Phase 2** — ⛔ **`--enforce` wired nowhere, never run on a real deploy**; step 8 + arming split to `0298` (Sprint 5). The *deliberately blank token* exception is **retired** by owner ruling.
- [[tasks/config-parity-guard-pre-arming-gate]] — 🆕 task `0203`, the items that must land before the parity guard's `--enforce` is wired (R1 client blind spot, R12 drop-detector by inversion, R4a/R4b stops, R13, item 11 + R19, R21, per-pipeline tagging). **CLOSED 2026-09-24 `(agent-closed — not owner-verified)`** — ⛔ **it arms nothing**; `0298` consumes the tags. Accepted: the call-argument destructuring shape stays silent (owner).
- [[tasks/tenure-xp-grant]] — 🆕 task `0253`, the one-time tenure XP grant (1 XP/day, cap 50, min 3, server-clamped, **final even at 0 XP**, once per player). **CLOSED 2026-09-24 `(agent-closed — not owner-verified)` — never run in a browser or production.** 🚨 **The route answers as soon as it is on the profile box (W3 if the 2026-09-26 window runs), so the owner-accepted claim-on-behalf risk opens then, not at the flip**; `0268` removes it.
- [[tasks/analytics-p1-ad-impression-baseline]] — 🆕 task `0020`, shipped as **one tier-free event, `Ad:Interstitial`**, on real impressions only. **CLOSED 2026-09-24 `(agent-closed — not owner-verified)`** — 🚩 **in-Yandex check OWED** (owner, after the deploy); tiered events → `0299`; banners dropped. 🚩 **Its premise was disproved: no tier suppresses interstitials today** (`0248`). First full `npm test` had one `socket hang up` (likely the supertest flake, not proven; passed on re-run).
