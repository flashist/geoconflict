build/
node_modules/
out/
static/
coverage/
TODO.txt
resources/images/.DS_Store
resources/.DS_Store
.env*
# age private identity for profile backups (T8) — the only key that can decrypt backups.
# Belongs in a password manager / off-box, never in git. Guard against accidental commit.
*identity*.txt
profile-backup-identity.txt
.DS_Store
.clinic/
.idea/
CLAUDE.local.md
/.playwright-mcp
*.obsidian/
karpathy-vault/.obsidian/
.claude/wiki-pending.txt
.claude/wiki-watermark
.claude/scheduled_tasks.lock
.playwright-cli/
/output/playwright

# fkit-managed local state (intake, tmp; re-created by fkit init)
.fkit/

# fkit-managed agents (refreshed by fkit-claude-init.sh)
.claude/agents/fkit-*.md

# fkit-managed skills (refreshed by fkit-claude-init.sh)
.claude/skills/fkit-*/
